---
id: T-XX-000
parent: null
title: <一句话任务名>
story: ../../docs/PRD-v2-Agent/story-NN-<slug>.md
arch: ../../docs/技术架构-v2/0X-LX-<slug>.md
priority: P0
milestone: M0
depends_on: []
status: todo
decisions: []
verify:
---

<!--
模板说明：
- `arch_link` 字段供账本渲染，取值形如 `"[0X §n](../../docs/技术架构-v2/0X-LX-<slug>.md)"`；
  本模板不预置该字段（占位链接会被`verify_docs.py`判断链），建任务时按实际文件补。
- `story` / `arch` 是裸路径（非 Markdown 链接），不受断链校验影响。
- `gate: skip` 仅用于长跑/运营类任务（不阻塞里程碑收口，理由写进备注）；功能交付任务不得使用。
-->

# <T-XX-000 · 任务标题>

## 目标
<这个任务交付什么，一句话>

## 验收标准（Given-When-Then，从 Story 抄）
- GWT-1：
- GWT-2：

## 接口面
<④ 对齐时实填（`doing` 起不可留占位），见工作流.md 第 ④ 步>
- 输入（消费的前置接口）：<上游任务 id + 具体 API/落盘位置>；上游未交付的写「待 \`T-<id>\` 交付」
- 输出（本任务交付的公共 API / 落盘位置）：

## 可关闭的遗留
<④ 对齐时实填：扫 [遗留问题](../遗留问题/README.md) 各层册的未闭区，逐条列「归属＝本任务」或
「解封条件＝本任务 \`done\`」的条目（册内 id + 处置：本批关闭 / 转移 / 修正归属）；无命中写 `无`。
见工作流.md「遗留消费」节——命中项计入本任务验收>

## 假设与前提
<④ 对齐时实填；每条 `A<n>` 含 前提内容 / 若错的影响 / 验证方式；确认无假设写 `无（<原因>）`>

## 涉及契约（链接到具体小节）
- [../../docs/技术架构-v2/01-平台共享契约.md](../../docs/技术架构-v2/01-平台共享契约.md) §<n> <名称>

## 参考
- Story（What）：运行 `render_ledger.py bootstrap` 生成的既有任务文件里可查本层 Story 链接写法

## 备注
<实现细节不写这里，留给代码/commit/执行日志>
