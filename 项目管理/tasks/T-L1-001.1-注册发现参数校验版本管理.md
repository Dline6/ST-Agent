---
id: T-L1-001.1
parent: T-L1-001
title: Skill注册发现+参数校验+版本管理
story: ../../docs/PRD-v2-Agent/story-02-skills-runtime.md
arch: ../../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md
arch_link: "[03 §1.1·§1.4](../../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md)"
priority: P0
milestone: M0
depends_on: [T-SC-001, T-L0-001, T-L0-003]
status: done
decisions: []
verify: pytest 28/28（tests/l1/test_registry.py）· 全量 343/343 · strict PASS（107 文件 / 868 链接 / 0 断链）· squash c30a00f（PR #9）· 日志 [T-L1-001.1]
---

# T-L1-001.1 · Skill 注册发现 + 参数校验 + 版本管理

## 目标
实现 03 §1.1（注册与发现）+ §1.4（版本管理）：SkillDescriptor 注册表经 `Store` 的 `config` 分区持久化，注册时过 §6 中性命名校验与 §10 权限语法校验，参数超范围拦截，官方 Pack 约 12 个 Skill 种子自动可用，版本 `follow-latest/locked` + 主版本变更标「待检查」。同时定义 `skill_id` 版本后缀拼接规则（兑现 T-SC-001 遗留①）。

## 验收标准（Given-When-Then）
- GWT-1（Pack 自动可用）：Given 首次启动，When 注册表初始化，Then 官方 Pack 约 12 个 Skill（公共认知 6 + 主动服务 6）自动可用，无需额外下载。
- GWT-5（更新可选）：Given 官方发布新版本，When 查询更新，Then 显示变更日志 + 影响范围，可更新 / 跳过 / 锁定旧版；主版本变更时引用方自动标「待检查」，确认后才升级。
- GWT-7（参数拦截）：Given 参数超出 ParameterSpec 范围，When 注册/更新参数默认值或调用传参，Then 校验拦截 + 说明理由，不允许保存。
- GWT-R（注册不变量）：Given 任一 SkillDescriptor，When 注册，Then `skill_id` 符合 `sk_<名>_v<主>.<次>` 拼接规则且全局唯一，重复注册显式拒绝不静默覆盖。

## 接口面
> 追补于 2026-09-26：本任务完成早于接口面机制落地，据已合入代码与执行日志回溯填写（非 ④ 对齐时填写）。
- 输入（消费的前置接口）：T-SC-001.4（`SkillDescriptor` / `ParameterSpec` / `Provenance` / `validate_permissions`）；T-SC-001.3 的 `NeutralityGuard.check_name`（经回调注入）；T-L0-001 `Store`（`config` 分区）
- 输出（本任务交付的公共 API / 落盘位置）：
  - `st_agent.l1.skills`：`SkillRegistry.register` / `.publish_version` / `.set_parameters` / `.confirm_update` / `.skip_update` / `.get` / `.get_latest` / `.list_versions` / `.list_all` / `.pending_updates` / `.validate_call_params`、`ensure_official_pack`、`validate_param_values`、`UpdateInfo`
  - ID 口径：`skill_id_for` / `parse_skill_id` / `base_of` / `check_skill_id`（兑现 T-SC-001 遗留①的版本后缀拼接规则）
  - 落盘：描述体 `config/skill-registry/<skill_id>.json`、待检查标记 `config/skill-update/<base>.json`
  - 消费方：T-L1-001.2（执行流水线）、T-L1-001.3（取 `permissions`）、T-L1-001.4 / .5、T-L1-002（tool → Skill 映射）

## 假设与前提
- A1 `skill_id` 形态取 `sk_<注册名>_v<主>.<次>`（注册名小写 kebab，版本取 SkillDescriptor 版本两位，主版本变更即新 ID，老 ID 保留供锁定）/ 若错：注册与引用面返工 / 验证：GWT-R 注册测试（对齐已确认）
- A2 注册本体放 `config` 分区 `skill-registry/<skill_id>.json`，更新历史与待检查标记同分区 `skill-update/` 下 / 若错：存储布局返工 / 验证：落盘分区断言
- A3 命名中性校验复用 `NeutralityGuard.check_name` 回调注入（契约包既定依赖倒置），权限语法复用 `validate_permissions` / 若错：校验链返工 / 验证：非法命名/权限注册拒绝测试

## 涉及契约
- [01-平台共享契约](../../docs/技术架构-v2/01-平台共享契约.md) §1 标识体系（`skill_id` 形态）/ §2 SkillDescriptor / §6 中性化校验 / §9 版本化 / §10 权限模型
- [03-L1 §1.1·§1.4](../../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 注册与发现 / 版本管理

## 参考
- Story（What）：[runtime](../../docs/PRD-v2-Agent/story-02-skills-runtime.md)
- 遗留兑现：T-SC-001 遗留①（`skill_id` 版本后缀拼接规则由本任务定义）

## 备注
实现细节不写此处，留给代码 / commit / 执行日志。
