---
title: 执行日志
type: log
mode: append-only
---

# 执行日志（append-only · 每次任务做了什么）

> 规则：任务 `status→done` 前必在此追加一条（宪法第 10 条）。标题格式固定 `## [T-<id>] <ISO时间> — <短标题> · <status>`，便于 `grep "^## \[T-"`。每条必含 **验证** 行与 **假设复核** 行（对齐时任务文件 `## 假设与前提` 的 A<n> 逐条结论：成立 / 已推翻→联动未决或新任务编号）。遗留若产生新任务，同步登记 [任务账本.md](任务账本.md) 并建 `tasks/` 文件。超 500 条按年归档至 `执行日志/`。
>
> 下方为**格式示例（非真实条目，可删）**，展示一条合格记录长什么样：

```markdown
## [T-L2-003.1] 2026-09-24T22:30+08:00 — 契约类型定义 · done

- **目标**：为 Memory Graph 节点写入定义 shared contract 类型
- **对齐**：已与人确认——复用 stock_id、契约置于 shared/（假设 A1–A2 见任务文件 `## 假设与前提`）
- **输入**：story-03 §验收 GWT-1..5；01-平台共享契约 §1 / §4 / §5
- **关键动作**：编码 shared/contracts/memory-node.ts；补测试 memory-node.spec.ts
- **变更文件**：memory-node.ts (+124) · memory-node.spec.ts (+201)
- **验证**：pytest 22/22 · 断链 0 · commit `a3f2b9c`
- **假设复核**：A1 成立（复用 stock_id 无冲突）· A2 成立（shared/ 契约无循环依赖）
- **决策**：契约类型放 shared/ 不放 L2 私有（见 决策日志 D-007）
- **遗留**：GWT-4 边界值需真实 L2 存储接入 → 新增 T-L2-003.2.1
```

---

<!-- 真实条目从此行下方按时间顺序追加 -->

## [sc] 2026-09-24T23:43+08:00 — 仓库初始化与脚手架落地 · done

- **目标**：按 [Git协作规范.md](Git协作规范.md) 建立 Git 仓库——`git init` + `.gitignore` + pre-commit 卡点 + 首次提交，同时补齐规范要求的根 `README.md`
- **对齐**：已与人确认三问——执行全套脚手架（含 README / pre-commit）；规范中的 `AGENTS.md` 统一改为 `CLAUDE.md`（不新建 AGENTS.md，本仓库实际入口即此名）；Git 身份用 `qianq` + 占位邮箱 `qianq@localhost`，正式远端前由人替换
- **输入**：Git协作规范 §1–§2 §5.1 §8；工程宪法第 9 / 10 / 11 条；决策日志 D-002（脚手架留待正式启动时执行 → 本次兑现）
- **关键动作**：`git init -b main` 并配仓库级 config（pull.rebase / autostash / log.date iso8601）；写入 `.gitignore`（数据主权红线）· `.pre-commit-config.yaml`（`verify_docs.py --strict`）· 根 `README.md`（面向人类总览）；把 6 处 `AGENTS.md` 引用改为 `CLAUDE.md`（含 verify_docs 的 `OPS_DOCS` 种子清单）
- **变更文件**：新增 .gitignore / .pre-commit-config.yaml / README.md · 修订 CLAUDE.md · 项目管理/{Git协作规范,README,决策日志,AI-Coding项目管理方案}.md · tools/verify_docs.py
- **验证**：`verify_docs.py --strict` PASS ✅（96 文件 / 803 链接 / **0 断链**，修复了 `项目管理/README.md` 与 `决策日志.md` 两处指向 `../AGENTS.md` 的死链）· 账本 SYNC（活跃 33 / 就绪 1，无任务状态改动故未 render）· `git log --grep "Task: sc"` 命中 `010649c`（106 files / +8101）· 敏感文件扫描 0 命中
- **决策**：入口文件命名统一为 `CLAUDE.md`、脚手架一次性落地（见 决策日志 D-003）
- **遗留**：① 未建远端（`git remote add origin` 待人给仓库地址；§3.3 要求 solo 也建 remote 做备份）；② 未 `pre-commit install`（环境无 pre-commit 包，需 `pip install pre-commit --break-system-packages` 后于本地安装）；③ `user.email` 为占位值，推送前须替换真实邮箱（仓库级配置，勿用 `--amend` 改写历史以外的方式绕过）；④ 下一就绪任务仍为 `T-SC-001`，开工走 ④ 对齐

## [sc] 2026-09-24T23:55+08:00 — Git 身份落定与规范 §8 勘误 · done

- **目标**：把仓库提交身份从占位值换为负责人真实身份；处理上一条遗留 ③ 与 §8 勘误
- **对齐**：人已给出身份 `Dline6 <dline6awsd@163.com>` 并指示删除 §8 中 `commit.cleanup verbose` 一行（本机 Git 2.34 实测非合法值，接受 `strip / whitespace / verbatim / default`）
- **关键动作**：`git config user.name/user.email` 仓库级更新；`--unset commit.cleanup` 撤销先前设的 default；`Git协作规范.md` §8 删除该行
- **变更文件**：项目管理/Git协作规范.md (-1) · 项目管理/执行日志.md (+本条)
- **验证**：`git var GIT_COMMITTER_IDENT` 输出 `Dline6 <dline6awsd@163.com>` · `verify_docs.py --strict` 复检
- **遗留**：既有两条 commit（`010649c` / `5bf9bd7`）作者仍为占位 `qianq <qianq@localhost>`；无远端，如需统一作者历史可 rebase 重写，但会改变 hash、连带更新本日志与 D-003 的引用——默认保留不改

## [T-SC-001] 2026-09-25T11:30+08:00 — 平台共享契约核心类型落地 · done

- **目标**：把 01-平台共享契约的可代码化类型实现为全平台唯一可 import 的契约真相源（`src/st_agent/contracts/`），L0–L6 只 import 不另立
- **对齐**：已与人确认三问——技术栈 Python + pydantic v2（本机已装 2.12.5）；本轮一次完成全部 5 个子任务；pre-commit 顺手装上启用（兑现既有遗留②）；其余 4 个子任务依赖序 .1→.2/.3/.4/.5，各自独立 commit + 日志
- **输入**：任务文件 5 条 GWT + 拆分预判；01-平台共享契约 §1–§11；10-platform-capabilities 契约 1–9；数据库设计 §05（as_of/水位口径）；story-02/04/06（中性词表、参数体系）；09 §5（provenance）
- **关键动作**：
  ③ 拆分——按预判建 5 个叶子任务；与预判差异：§2/§3（SkillDescriptor/LensOpinion）原清单未覆盖并入 .4，§11 事件信封并入 .5；`render_ledger.py` 就绪集修正为只含叶子任务（工作流规定）
  ⑤ 实现——`.1` 12 类 ID frozen 值对象（id_kind 判别防串用；stock_id 走 BaoStock 交易所格式、拒绝本地随机生成）+ ResultEnvelope 六 status 构造期不变量；`.2` Trace append-only（frozen + tuple + append_step 唯一写入口 + 封链语义 + append_degraded 降级留痕 + sha256 摘要）；`.3` NeutralityGuard 三强制执行点（RulePack 可换、API 形状无 bypass 开关；词表 = PRD 明文禁止项 + 人名结构规则）；`.4` SkillDescriptor（imported 强制完整 provenance；name 中性校验回调注入）/ LensOpinion（stance 含 insufficient-data 失败隔离）/ ConfigEntry 七字段 / SemVer 主次兼容判定 / §10 权限语法校验；`.5` DataAnchor（snapshot_id + as_of + 六数据域，时区强制）/ StalenessVerdict / PlatformEvent（10 事件名，构造期强制 trace_id/change_id 关联）
- **变更文件**：src/st_agent/contracts/{__init__,errors,identifiers,result_envelope,trace,neutrality,capability_types,registry_types,time_events}.py（9 文件）· tests/contracts/ 4 个测试文件 · pyproject.toml · tools/render_ledger.py（就绪集叶子过滤）· tasks/T-SC-001{,.1–.5}*.md（6 文件）
- **验证**：pytest **107/107** 通过（.1→37 · .2→46 · .3→68 · .4→95 · .5→107 递增口径）· `verify_docs.py --strict` PASS（101 文件 / 830+ 链接 / 0 断链）· 账本 render 后 SYNC · 就绪集正确推进至 T-L0-001 · 父任务 T-SC-001 由 children 派生 done · pre-commit 钩子实战拦截 2 次账本未同步的提交后放行 · commits：拆分 `d4ba69e` + c8e936f / df70513 / 744467b / e66e767 / 49c3e15
- **决策**：实现选型 pydantic v2 frozen 模型（值对象不可变 + 构造期校验，免手写校验层）；序列化格式定为 JSON 兼容（契约文档不锁定方言，本选型记此不另立 D-NNN）；中性化词表注入采用依赖倒置（契约包不依赖具体词表实现）
- **遗留**：① 三个 API 待 L0/L1 首个消费方回收反馈——`PlatformId.of()` 的前缀正则对 `skill_id` 含版本后缀形态（`sk_xxx_v2`）需在 T-L1-001 注册流程中定义拼接规则；`EvidenceRef.kind` 与 LensOpinion.evidence_refs（裸字符串）双轨，T-L4-002 时统一；`DataAnchor.is_stale` 通用超龄判定与 sync_state 自检 SQL 的整合在 T-L0-005。② pre-commit 已装但 `verify_docs` 每次全量扫描 101 文件约 1–2s，可接受；文件数过 300 时考虑缓存。③ 远端仍未建（遗留①未变），本分支仅本地，PR 流程按规范待远端就绪后补走。

## [sc] 2026-09-25T12:10+08:00 — 远端建立与 T-SC-001 PR 合入收口 · done

- **目标**：兑现上条遗留③——建 GitHub 远端、推送、按规范 §5.2 gate 走 PR squash 合入，完成 T-SC-001 全链路
- **对齐**：人在 `.env`（不入库，.gitignore 红线）配好 `GITHUB_PAT`（scope=repo）与 `GIT_REMOTE_URL` 后指示继续走完整流程
- **关键动作**：API 验证 PAT 与空仓库 `Dline6/ST-Agent` → `git remote add origin` → 推 `main` + `feat/T-SC-001-contract-types` → 建 [PR #1](https://github.com/Dline6/ST-Agent/pull/1) → §5.2 gate（strict PASS / pytest 107 / 就绪集 T-L0-001）→ squash 合入 `acb774f` → 删远端+本地任务分支 → main 与 origin 同步
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：`git ls-remote` 仅剩 `refs/heads/main @ acb774f` · main 上 strict PASS + pytest 107/107 · `git status` 与 origin/main 零偏移 · squash 提交含 `Task: T-SC-001` trailer（`git log --grep` 可回溯）
- **遗留**：① 遗留兑现，`.env` 凭据流转仅经本机 git/curl，未落 `.git/config`（推送用 stdin 临时凭据）；② PAT 建议后续换 fine-grained 最小权限（当前 classic `repo`）；③ 下一就绪任务 `T-L0-001 存储分区落盘加密`，开工走 ④ 对齐

## [T-L0-001] 2026-09-25T14:21+08:00 — 存储分区落盘加密与损坏恢复 · done

- **目标**：实现 02-L0 §2 存储子系统——七分区注册与目录布局、全分区落盘加密、分区级完整性校验与损坏恢复
- **对齐**：已与人确认——④ 对齐结论：PBKDF2（600_000 迭代）+ AES-256-GCM 每分区独立密钥；`secrets` 独立 salt 隔离；每分区加密清单（SHA-256）；每分区一个目录、对写/清空独立；`data_cache` 按 opaque blob 存 SQLite；损坏定位分区级 + 冷启动重建（备份恢复在 T-L0-006）
- **输入**：story-05 验收段 8 条（本任务覆盖分区/加密/损坏 3 组 + 零接触铁律）；02 §2.1–§2.3；01 §5（失败显式化——损坏走 StorageCorruptionError 显式载体）
- **关键动作**：`partition.py`（七分区注册 + 防路径注入）· `crypto.py`（PBKDF2 双 salt + AES-256-GCM + verifier 锚）· `manifest.py`（加密清单 + SHA-256 校验）· `store.py`（Store 门面：create/open/put/get/delete/export/wipe/reset_partition/storage_report）· `tests/l0/test_storage.py`（37 用例对 5 条 GWT）；中途清掉 `verify_backup` 占位与 `keep_files` 死分支（T-L0-006 在分区级校验和口径上定义备份格式）
- **变更文件**：src/st_agent/l0/{__init__,storage/{__init__,crypto,errors,manifest,partition,store}}.py（7 文件）· tests/l0/test_storage.py · pyproject.toml（+cryptography>=42.0）· tasks/T-L0-001（5 条 GWT + doing→done）
- **验证**：pytest **144/144** 通过（contracts 107 + l0 37）· `verify_docs.py --strict` PASS（102 文件 / 831 链接 / 0 断链）· 盘上无明文抽查通过 · SQLite blob 往返通过 · commits：开工 `e4e042a` + 实现 `d01b686`
- **决策**：口令派生取 PBKDF2-HMAC-SHA256 / 600_000 迭代（OWASP 2023，02 §2.2 不锁定算法，安全评审可调）；`secrets` 用独立 salt 重走一遍 PBKDF2 而非 HKDF 子密钥——隔离强度等同主密钥重设；清单文件名固定 `manifest.bin` 且禁用户占用
- **遗留**：① T-L0-002（凭据掩码+使用记录）· T-L0-004（出网审计）· T-L0-005（data_cache 库表）· T-L0-006（备份/恢复/清空）均依赖本任务容器；② KDF 迭代数是性能敏感参数——600k 在本机约 0.3s/次，T-L0-002 登录态缓存会话密钥时复用同一口径

## [sc] 2026-09-25T14:45+08:00 — T-L0-001 PR 合入收口 · done

- **目标**：T-L0-001 走 PR squash 合入 main，完成全链路
- **对齐**：沿用 T-SC-001 既定流程（规范 §5.2 gate + squash 合入即删分支）
- **关键动作**：推 `feat/T-L0-001-storage-encryption` → 建 [PR #2](https://github.com/Dline6/ST-Agent/pull/2)（建 PR 时标题中文经 API 乱码，已 PATCH 修复）→ gate（strict PASS / pytest 144 / 就绪集 T-L0-002·T-L0-004·T-L0-006·T-L2-001）→ squash 合入 `28c61f4` → 删远端+本地任务分支 → main 与 origin 同步
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：`git ls-remote` 仅剩 `refs/heads/main @ 28c61f4` · main 上 strict PASS + pytest 144/144 · `git status` 干净
- **遗留**：下一就绪队首 `T-L0-002 密钥与凭据子系统`，开工走 ④ 对齐

## [T-L0-002] 2026-09-25T15:30+08:00 — 密钥与凭据子系统（掩码显示 + 使用记录） · done

- **目标**：实现 02-L0 §3——敏感凭据加密存 `secrets` 分区、默认掩码可见、取用留痕可查、支持吊销重设、凭据明文零泄漏
- **对齐**：已与人确认（④ 对齐结论：`CredentialVault` 经 `Store` 操作 `secrets` 分区、每凭据一文件；掩码尾部最多 4 位、短凭据全掩码；使用记录存 `execution_log` 分区；失败走 01 §5 显式错误；不改 01 契约）——人回「对」后置 `doing` 开工
- **输入**：story-05 验收段 9 条（本任务覆盖 Key 配置/使用记录 2 组 + 掩码组件 + 零接触铁律）；02 §3 全节；01 §5（失败显式化）/ §8（时间带时区）；T-L0-001 的 `Store` 门面（`secrets` 独立派生密钥、`execution_log` 分区容器）
- **关键动作**：`errors.py`（四错误类型，失败显式化）· `masking.py`（掩码单规则：≤4 全掩、>4 留尾 4）· `models.py`（`CredentialRecord` 内部含明文但 repr/str 强制掩码 + `CredentialView` 对外无 value 字段 + `CredentialUsageRecord` 时刻/发起方/目的/数据量/结果）· `vault.py`（`CredentialVault`：add/rotate/revoke/get_view/list/use/record_usage/query_usage/export_safe，唯一明文出口 `use()` 同步记 `ok` 记录；标识即分区内安全路径段）· `tests/l0/test_secrets.py`（28 用例对 5 条 GWT，含全盘 rglob 无明文扫描）
- **变更文件**：src/st_agent/l0/secrets/{__init__,errors,masking,models,vault}.py（5 文件）· tests/l0/test_secrets.py · tasks/T-L0-002（GWT 补全 + doing→done + frontmatter 补 parent/arch 字段）
- **验证**：pytest **172/172** 通过（contracts 107 + l0 存储 37 + 凭据 28）· `verify_docs.py --strict` 待收工 commit 前复检 · 全盘明文扫描通过 · commits：`7bebee1`（gitignore 收窄）+ `19c582e`（实现）
- **决策**：无新选型（掩码 last4 沿用 Stripe 式可辨识惯例；种类 `kind` 只做形状校验不做白名单，留扩展口；使用记录留存周期沿用 T-L0-006 口径，本任务不定默认值）——均记于对齐结论，不另立 D-NNN
- **遗留**：① 下一就绪 `T-L0-004 出网审计网关`（凭据的网络调用审计点在彼处兑现）· `T-L0-006` 备份时须用 `export_all_safe` 口径（含掩码视图+使用记录，禁明文）；② 本任务未动 01 契约与 02 架构文档，无需 ⑦ 同步

## [T-L0-003] 2026-09-25T16:30+08:00 — LLM 端点抽象（能力协商/流式/多端点/零中转/用量） · done

- **目标**：实现 02-L0 §4——多端点配置并存、调用前能力协商、统一流式协议、失败走 ResultEnvelope、用量本地可见、云端 Key 直连零中转零留存
- **对齐**：已与人确认（④ 对齐结论：EndpointRegistry 经 Store 读写 config 分区；Key 唯一走 CredentialVault.use()；用量记 execution_log 只含计数；用量明细归 execution_log、流式以同步迭代器起步；不改 01 契约）——人回「对」后置 `doing` 开工
- **输入**：story-05 验收段（Key 配置/网络活动/离线标注 3 组 + 零接触铁律）；02 §4 全节 + §6/§7（网关只声明 offline_level，本体在 T-L0-004）；01 §5（失败显式化）/ §8（时间带时区）；T-L0-001 的 Store 门面（config/execution_log 分区容器）+ T-L0-002 的 CredentialVault.use()
- **关键动作**：`errors.py`（配置面三异常；执行面一律走 ResultEnvelope 不抛）· `models.py`（EndpointCapability 供给档位 + CapabilityRequirement 需求档位 + LlmEndpoint 云端必凭据引用/本地禁凭据 + LlmUsageRecord 只记计数禁内容字段 + StreamEvent chunk/done/error 三态互斥 + estimate_tokens 每 4 字符 1 token）· `registry.py`（EndpointRegistry：register/replace/update_priority/remove/get/list/negotiate/pick，配置落 config 分区 llm-endpoint/ 前缀）· `client.py`（LlmClient.invoke：协商 → 取 Key → 流式 → 用量；超上下文/空 prompt/缺归属直接 validation_failed；无传输即 unavailable；取消/超时显式 error；用量成败必记，failed 带 log_ref；Key 内存引用即时释放）· `tests/l0/test_llm.py`（33 用例对 5 条 GWT，含全盘 rglob 无 prompt/response 明文扫描）
- **变更文件**：src/st_agent/l0/llm/{__init__,errors,models,registry,client}.py（5 文件）· tests/l0/test_llm.py · tasks/T-L0-003（GWT 补全 + doing→done + frontmatter 补 parent/arch/verify 字段）
- **验证**：pytest **205/205** 通过（contracts 107 + l0 存储 37 + 凭据 28 + LLM 33）· `verify_docs.py --strict` 待收工 commit 前复检 · 全盘 prompt/response 明文扫描通过 · commits：`07053a2`（实现）
- **决策**：无新选型（token 估算取每 4 字符 1 token 的本地展示口径，提供方账单以其实测为准；传输层以 callable 注入，T-L0-004 网关接入时替换；流式同步迭代器起步，异步形态留 L1 需要时再加）——均记于对齐结论，不另立 D-NNN
- **遗留**：① 实际网络发送待 T-L0-004 出网审计网关统一接入（当前无传输即 unavailable，测试经 transport 注入）；② T-L1-001 Skill 调用 LLM 时复用 LlmClient.invoke + pick 口径；③ 本任务未动 01 契约与 02 架构文档，无需 ⑦ 同步

## [sc] 2026-09-25T17:00+08:00 — T-L0-002 与 T-L0-003 PR 合入收口 · done

- **目标**：网络恢复后补走两个任务的推送 + PR squash 合入全链路，完成收口
- **对齐**：沿用 T-SC-001/T-L0-001 既定流程（规范 §5.2 gate + squash 合入即删分支）；本轮由人指示「尝试推送」后执行
- **关键动作**：推 `feat/T-L0-002-credential-vault` → 建 [PR #3](https://github.com/Dline6/ST-Agent/pull/3) → gate（strict PASS / pytest 205 / SYNC）→ squash 合入 `67941e7` → 删远端+本地分支（`-D`，squash 后未完全合并）→ main 同步；`feat/T-L0-003-llm-endpoint` 变基 `--onto main 46298eb`（剔除已合入的 T-L0-002 部分）→ gate（strict PASS / pytest 205 / SYNC）→ 建 [PR #4](https://github.com/Dline6/ST-Agent/pull/4) → squash 合入 `4a44cba` → 删分支 → main 同步
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：`git ls-remote` 仅剩 `refs/heads/main @ 4a44cba`（+ PR refs 残留）· main 上 `git status` 干净 · 推送凭据经 `.env` GITHUB_PAT 临时注入（env 变量 + credential helper），未落 `.git/config`、未进命令行历史
- **遗留**：① PAT 仍为 classic `repo` scope，建议后续换 fine-grained 最小权限（既有遗留未变）；② 下一就绪队首 `T-L0-004 出网审计网关 + 离线能力分级`，开工走 ④ 对齐

## [T-L0-004] 2026-09-25T15:40+08:00 — 出网审计网关 + 离线能力分级 · done

- **目标**：实现 02-L0 §6+§7——一切出网经统一网关执行并留痕（NetworkEvent），离线三级清单（full/degraded/none），LLM 真实发送经网关适配器接入（兑现 T-L0-003 遗留）
- **对齐**：已与人确认（人回「对」）——④ 对齐结论：审计记 `execution_log/net/` 前缀；补发执行归 L5，本任务仅提供待补发查询；暂不做 socket 级强制拦截，强制性靠唯一出口 + 测试约束；远程 MCP / 官方索引审计点留待 T-L1-002
- **输入**：story-05 验收段（网络活动透明化 / 离线模式指示 2 组）；02 §6–§7 全节；01 §5（失败显式化）/ §11（NetworkRequestLogged 事件）；T-L0-001 的 Store 门面 + T-L0-003 的 LlmClient transport 签名
- **关键动作**：`errors.py`（配置面三异常 + `Egress*` 执行异常体系）· `models.py`（NetworkEvent 六字段 frozen + purpose 内容词黑名单 + CapabilityRecord 复用契约 OfflineLevel + OfflineReport 三类清单）· `gateway.py`（EgressGateway：execute/stream/query/export_report/pending_reconnect/能力登记/llm_transport 适配器）· `tests/l0/test_net.py`（33 用例对 5 条 GWT）；中途修复 `_check_request` 探针缺 `bytes_in`、`detail` 误扫黑名单、适配器缺 `Egress*→Transport*` 翻译三处缺陷
- **变更文件**：src/st_agent/l0/net/{__init__,errors,models,gateway}.py（4 文件）· tests/l0/test_net.py · tasks/T-L0-004（GWT 补全 + doing→done）
- **验证**：pytest **238/238** 通过（既有 205 + 网关 33）· `verify_docs.py --strict` PASS（102 文件 / 834 链接 / 0 断链）· 全盘 prompt/response/key 明文扫描通过 · commits：`45bfd20`（doing+GWT）+ `2d9292c`（实现）
- **决策**：两处选型记此不另立 D-NNN——① `detail` 字段不扫内容词黑名单（发包异常原文可含 value 等子串，零泄漏靠网关签名只收字节数保证）；② `llm_transport` 内把 `Egress*` 译为 `Transport*` 体系（LlmClient 只认后者）
- **遗留**：① 远程 MCP / 官方索引浏览的审计调用点待 T-L1-002 接入；② 补发执行归 L5（本任务仅 `pending_reconnect` 查询口）；③ 下一就绪 `T-L0-006` / `T-L1-001` / `T-L2-001`

## [sc] 2026-09-25T15:55+08:00 — T-L0-004 PR 合入收口 · done

- **目标**：T-L0-004 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）
- **关键动作**：推 `feat/T-L0-004-egress-gateway` → 建 [PR #5](https://github.com/Dline6/ST-Agent/pull/5)（建 PR 时标题中文经 API 乱码，已 PATCH 修复）→ gate（strict PASS / pytest 238 / SYNC）→ squash 合入 `d478789` → 删远端+本地分支 → main 与 origin 同步
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：`git ls-remote` 远端 main @ `d478789` · main 上 strict PASS + pytest 238/238 · `git status` 干净
- **遗留**：下一就绪 `T-L0-006` / `T-L1-001` / `T-L2-001`，开工走 ④ 对齐

## [T-L0-005] 2026-09-25T16:30+08:00 — 数据源缓存 + BaoStock 首个源落地 · done

- **目标**：实现 02-L0 §5——`data_cache` 分区内 SQLite 单库（schema.sql 全量表 + 视图）+ `sync_state`/`data_source` 水位元数据 + 新鲜度自检 + 抓取经网关 `data_fetch`
- **对齐**：已与人确认（人回「对，按此开工」）——④ 对齐结论：`MarketDb` 经 `Store` 读写 `data_cache/market.db` 加密 blob（工作拷贝 + 写回）；fetcher 可注入（`baostock` 包不内置，测试不碰真网）；清洗 / 复权 / 完整性按 DB 文档；快照锚定 `sync_state` 水位组合；分钟线默认禁用；开关状态不落库（调用方传入）
- **输入**：story-05 验收段（首次联网建缓存 / 离线可查 / 数据源开关 3 组）；02 §5 全节 + §6（抓取唯一出口）；01 §5（失败显式化）/ §8（`dataset_snapshot_id`/`as_of` 口径）；数据库设计族 00/01–06 + schema.sql；T-L0-001 的 `Store` 门面 + T-L0-004 的 `EgressGateway`
- **关键动作**：`errors.py`（配置面 `MarketValidationError` + 抓取器 `Fetch*` 异常体系）· `cleaning.py`（空串→NULL、`—` 负号、`或`多值取首值、分钟线时间归一）· `db.py`（`MarketDb`：`init_db`/`transact` 写回/`query` 只读白名单/`freshness`/`as_of`/`snapshot_id`）· `tasks.py`（20 任务注册 + 水位语义 + `window_for` 窗口推导）· `fetch.py`（`Fetcher` 协议 + 06 全量映射：K 线三表/复权因子/分红/`mortgate` 拼写修正/财务六接口合并 `pub_date` 取最大/快报预告/板块快照/宏观五表）· `sync.py`（`BaoStockSync`：`setup` 种子/`run_task` 前置→窗口→网关抓取→入库→水位→校验/`run_all` 初始化顺序/`freshness_verdict`→`StalenessVerdict`）· 网关加按次 `sender` 注入（T-L0-004 GWT-1 预留形态，审计仍唯一）；`schema.sql` 包内副本与文档族同内容；`tests/l0/test_market.py`（39 用例对 5 条 GWT，Fake 注入不碰真网）
- **变更文件**：src/st_agent/l0/market/{__init__,errors,cleaning,db,tasks,fetch,sync}.py + schema.sql（8 文件）· src/st_agent/l0/net/gateway.py（按次 sender 注入）· tests/l0/test_market.py · tasks/T-L0-005（GWT 补全 + doing→done）
- **验证**：pytest **277/277** 通过（既有 238 + 市场 39）· `verify_docs.py --strict` 待收工 commit 前复检 · 盘上库 blob 无建表明文抽查通过 · 审计无行内容（「浦发银行」只在加密 blob）通过 · commits：待收工
- **决策**：两处选型记此不另立 D-NNN——① 网关按次 `sender` 注入而非为同步另立出口（02 §6 唯一出口纪律；构造时 sender 缺省语义不变）；② `MarketDb.transact` 提交后无条件写回（单文件 blob 语义下「提交即落盘」，回滚由下次同步幂等 UPSERT 自愈）
- **遗留**：① T-L0-001 遗留①部分兑现（`data_cache` 库表已落地，`is_stale` 通用判定仍由上层按 `freshness_verdict` 消费）；② T-L1-004 官方 Skill Pack 的行情/财务查询面经 `MarketDb.query` 进入；③ `baostock` 真实抓取器封装待管道联调时注入（协议已定）；④ 下一就绪 `T-L0-006` / `T-L1-001` / `T-L2-001`

## [sc] 2026-09-25T17:40+08:00 — T-L0-005 PR 合入收口 · done

- **目标**：T-L0-005 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；本轮由人指示「推送分支走 PR 合入」后执行；中途 git 协议 443 不通，API 通道走通
- **关键动作**：推 `feat/T-L0-005-baostock-cache` → 建 [PR #6](https://github.com/Dline6/ST-Agent/pull/6)（`gh` 缺失改 API 建 PR；标题中文经 API 乱码，已 PATCH 修复——与 PR #2/#5 同一已知问题）→ gate（strict PASS / pytest 277 / SYNC）→ squash 合入 `3a55f23`（API 确认 merged True）→ 删远端分支（API 422 即已无残留）→ 本地 `git fetch` 不通，改 `merge --ff-only bf0933e` 快进本地 main（内容与 squash 同源 13 文件；`ahead 1` 系 hash 差异，网络恢复后 pull 自动收敛）→ 删本地任务分支 → 登记本条
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：API 查 PR state=closed/merged=True · 远端 main @ `3a55f23` · 本地 main 上 strict PASS + pytest 277/277 + SYNC · `git branch --list` 仅剩 main
- **遗留**：① 本地 main `ahead 1`（`bf0933e` vs 远端 squash `3a55f23` 内容同源），网络恢复后 `git pull --ff-only` 收敛；② PAT 仍为 classic `repo` scope（既有遗留未变）；③ 下一就绪队首 `T-L0-006 备份/恢复/清空`，开工走 ④ 对齐

## [T-L0-005.1] 2026-09-25T18:30+08:00 — BaoStock 真实抓取器接入与首次真实入库 · done

- **目标**：兑现 T-L0-005 遗留③——按 `Fetcher` 协议封装真实 `baostock` 抓取器（`login → 批量 query → logout`），跑通首次真实入库（元数据 + 单证券行情），抓取经网关审计
- **对齐**：任务由人指示「帮我真实入库」后立项（T-L0-005.1，依赖 T-L0-005）；GWT 三条（登录拉取 / 行情样本 / 失败可解释）立任务时已定，人回「请开始」「继续」后开工
- **输入**：`Fetcher` 协议（`fetch.py` 19 方法签名）；BaoStock-API 手册 04/07/11/12（K 线/财务/宏观/板块参数）；T-L0-005 的 `BaoStockSync`/`MarketDb`/`EgressGateway` 按次 sender 注入点
- **关键动作**：`live.py`（`BaoStockFetcher`：上下文管理登录登出 + `_drain` 结果集 + `error_code` 非 0 → `FetchError` + K 线固定 `adjustflag=3` + 准备金 `yearType=0` + 缺包构造即 `FetchUnavailableError`）；`sync.py` 修未知任务裸抛（`get_task` 包进 `validation_failed`，GWT-3 缺口）；`__init__.py` 出口 `BaoStockFetcher`；`pyproject.toml` 加 `baostock>=0.9.4`；`test_market.py` 补 `TestLiveFetcherOffline`（未知任务/缺包/协议对齐 3 用例，不碰真网）；真实入库在 `tmp/live-market`（`.gitignore` 覆盖，不入库）跑通后清理
- **变更文件**：src/st_agent/l0/market/live.py（新增）· src/st_agent/l0/market/{__init__,sync}.py · pyproject.toml · tests/l0/test_market.py（+3）· tasks/T-L0-005.1（新建 + doing→done）
- **验证**：pytest **280/280** 通过 · 真实入库：`security` 8981 行 + `trade_calendar` 8733 交易日 + `sh.600000` 6515 根日线（水位 `2026-09-24`，`as_of kline` 同值）+ 网关 4 条 `data_fetch`/`baostock` 审计 · `verify_docs.py --strict` 待收工 commit 前复检 · commits：待收工
- **决策**：无新选型（`baostock` 标联网可选依赖，缺失走 `unavailable`；真实库只落 `tmp/` 不入库；`pandas` 不引入，逐行 `get_row_data` 取数）——均记于任务备注，不另立 D-NNN
- **遗留**：① `freshness_verdict("kline").stale=True` 预期内（真实库仅 1 只证券，未跑全量，`bs_k_period` 等任务未执行）；② 全量首次同步（全市场 1990 年起）体量大，需单独排期跑 `run_all`；③ 下一就绪 `T-L0-006` / `T-L1-001` / `T-L2-001`

## [sc] 2026-09-25T19:00+08:00 — T-L0-005.1 PR 合入收口 · done

- **目标**：T-L0-005.1 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；本轮由人指示「继续」后执行；git 协议已恢复，全程走通无绕路
- **关键动作**：推 `feat/T-L0-005-1-live-fetcher` → 建 [PR #7](https://github.com/Dline6/ST-Agent/pull/7)（API 建 PR + PATCH 中文标题，同一已知乱码问题）→ gate（strict PASS / pytest 280 / SYNC）→ squash 合入 `03bb048` → `git push --delete` 删远端分支 → `checkout main + pull --ff-only` 干净快进 → `-D` 删本地分支 → 登记本条
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：`git ls-remote` 仅剩 `refs/heads/main @ 03bb048` · main 上 strict PASS + pytest 280/280 + SYNC · `git branch --list` 仅剩 main · 与 origin/main 零偏移
- **遗留**：① 下一就绪队首 `T-L0-006 备份/恢复/清空`，开工走 ④ 对齐；② 全量首次真实同步（`run_all` 全市场）体量大，待排期

## [sc] 2026-09-25T19:30+08:00 — 任务假设记录机制落地 · done

- **目标**：补上"每个任务必有、可校验、可复核"的假设留痕机制（此前假设只散落在执行日志对齐散句里）
- **对齐**：人指示「请开始」（④ 对齐结论：任务文件正文节而非 frontmatter；历史 done 不追补；todo 对齐时填、verify 只卡 doing/blocked/done）——假设见本条 A1–A2
- **关键动作**：工作流 ④/⑥/任务文件字段三处细化（`三块→四块` + 对齐落两处 + 收工含假设复核）；执行日志规则 + 格式示例加 `假设复核` 行；`render_ledger.py` bootstrap 模板加节；`verify_docs.py` 加第 [5] 项假设完整检查（doing/blocked/done 叶子必有实填节，`--strict` 下判失败；机制前 12 个 done 豁免）
- **变更文件**：项目管理/{工作流,执行日志}.md · tools/{render_ledger,verify_docs}.py
- **验证**：`verify_docs.py --strict` PASS ✅（103 文件 / 842 链接 / 0 断链 / 假设 0 缺失）· 合成测试：缺节 doing 任务被 [5] 检出，实填放行 · 账本 SYNC（未改派生逻辑）· 测试残留文件已清理
- **假设复核**：A1 成立（正文节方案：单行 frontmatter 解析器不动，verify 用正文正则即可）· A2 成立（12 个历史 done 列入 `GRANDFATHERED_NO_ASSUME`，不追补）
- **遗留**：下一就绪任务开工即按新机制跑 ④ 对齐（假设编号进任务文件）

## [T-L0-006] 2026-09-25T20:10+08:00 — 备份 / 恢复 / 完全清空 + 数据留存策略 · done

- **目标**：实现 02-L0 §8–§9——一键备份单加密归档、任意设备导入恢复、三次确认完全清空、对话历史与执行日志留存可配；闭环 T-L0-001 的损坏恢复（有备份可恢复）
- **对齐**：人回「对，按此开工」+ 备份范围「仅四分区」+ 脏工作区「先提交再开工」（④ 对齐结论：范围严格按 02 §8.1 四分区 + 可选 data_cache，secrets/execution_log 永不进归档；归档独立 salt 走 PBKDF2；恢复覆盖写；清空三令牌；留存默认 365/90 天；假设 A1–A7 见任务文件）
- **输入**：story-05 验收段（备份/恢复/清空 3 组 + 留存可配 + 损坏联动）；02 §8–§9 全节；01 §7（留存旋钮 ConfigEntry）/ §8（时间带时区）；T-L0-001 的 Store 门面（分区导出/覆盖/清空 + 分区级校验和口径）
- **关键动作**：`errors.py`（五错误类型：密码错/损坏/版本/留存/确认不足，失败显式化）· `models.py`（BackupManifest 归档清单 + RestoreReport/WipeReport + RetentionPolicy + retention_config_entries 01 §7 双通道条目）· `backup.py`（create_backup 四分区导出 + 独立 salt 加密归档 / _open_archive 密码→版本→分区表→逐文件校验 / restore_backup 覆盖写 + 损坏目标内部解锁联动 / wipe_all 三令牌删整目录 / get/set/apply_retention 留存读写清）· `store.py` 加 `sealed_mtime` 公共口径（留存年龄走落盘密文 mtime）· `tests/l0/test_backup.py`（35 用例对 5 条 GWT，含全盘密码明文扫描 + 无网络 import 断言）
- **变更文件**：src/st_agent/l0/backup/{__init__,errors,models,backup}.py（4 文件，684 行）· src/st_agent/l0/storage/store.py（+sealed_mtime）· tests/l0/test_backup.py（35 用例）· tasks/T-L0-006（GWT 补全 + doing→done）
- **验证**：pytest **315/315** 通过（既有 280 + 备份 35）· `verify_docs.py --strict` 待收工 commit 前复检 · 盘上归档无明文抽查通过 · 全盘密码明文扫描通过 · commits：待收工
- **假设复核**：A1 成立（四分区口径：恢复报告分区表断言通过）· A2 成立（独立 salt 归档加密：无明文 + 错密码拒绝通过）· A3 成立（v1 自描述：篡改/版本不兼容拒绝且不写脏通过）· A4 成立（覆盖写 + 损坏目标联动：三类恢复测试通过）· A5 成立（三令牌清空：6 组确认不足拒绝 + 无残留通过）· A6 成立（留存 365/90 默认显式 + 边界当天不删次日删通过）· A7 成立（零接触：无网络 import + 全盘密码扫描通过）
- **决策**：无新选型（归档 KDF 口径沿用 T-L0-001 PBKDF2/600k；iterations 进归档外层元数据供前向兼容；ConfigEntry change_policy 取需确认+记历史+可回滚）——均记于对齐结论，不另立 D-NNN
- **遗留**：① 下一就绪 `T-L1-001` / `T-L2-001`；② 全量首次真实同步（`run_all`）体量大，待排期（既有遗留未变）

## [sc] 2026-09-25T20:30+08:00 — T-L0-006 PR 合入收口 · done

- **目标**：T-L0-006 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；中文标题经 API PATCH 乱码为已知问题（PR #2/#5/#7 同症），本次英文标题保 gate
- **关键动作**：脏工作区先独立提交 `f5e01fb`（机制落地）→ 切 `feat/T-L0-006-backup-retention` → 建 [PR #8](https://github.com/Dline6/ST-Agent/pull/8) → gate（strict PASS / pytest 315 / SYNC）→ squash 合入 `f5c3667` → 删远端+本地分支 → 本地 main 与远端 squash 分叉（`ahead 2` 系 hash 差异），`reset --hard origin/main` 收敛（工作区干净、内容无丢失已验证）→ 登记本条
- **变更文件**：项目管理/执行日志.md（+本条）· tasks/T-L0-006（verify 补 squash hash）
- **验证**：`git ls-remote` 仅剩 `refs/heads/main @ f5c3667` · main 上 pytest 315/315（待复检）· `git branch --list` 仅剩 main · 与 origin/main 零偏移
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① 下一就绪 `T-L1-001` / `T-L2-001`，开工走 ④ 对齐；② 全量首次真实同步（`run_all`）待排期

## [T-L1-001.1] 2026-09-25T21:00+08:00 — Skill 注册发现 + 参数校验 + 版本管理 · done

- **目标**：实现 03-L1 §1.1 + §1.4——SkillDescriptor 注册表经 `Store` 的 `config` 分区持久化，官方 Pack 12 Skill 种子自动可用，参数超范围拦截，版本 `follow-latest/locked` + 主版本待检查；定义 `skill_id` 版本后缀拼接规则（兑现 T-SC-001 遗留①）
- **对齐**：人回「对，按此开工」——④ 对齐结论：拆 4 叶子（.1 注册/.2 流水线/.3 沙箱/.4 复用）；skill_id 取 `sk_<名>_v<主>.<次>`；注册放 config 分区、运行日志放 execution_log；沙箱进程内 enforcement；执行器同步 callable；freshness 复用 L0 口径；空/不可用走 empty/unavailable；Skill 无状态（假设 A1–A7 见子任务文件）
- **输入**：story-02 验收段 9 条（本任务覆盖 GWT-1/5/7 + 注册不变量 GWT-R）；03 §1.1/§1.4；01 §1/§2/§6/§9/§10；T-SC-001 遗留①；T-L0-001 Store 门面 + T-L0-003 EndpointRegistry 布局先例
- **关键动作**：`ids.py`（A1 拼接规则 + parse/base/skill_id_for）· `errors.py`（三异常，失败显式化）· `pack.py`（官方 12 Skill 种子 + 幂等播种）· `registry.py`（register/publish_version/set_parameters/confirm_update/skip_update/get/get_latest/list + validate_call_params，主版本无 changelog 回滚）· `tests/l1/test_registry.py`（28 用例对 4 条 GWT）
- **变更文件**：src/st_agent/l1/{__init__,skills/{__init__,errors,ids,pack,registry}}.py（6 文件）· tests/l1/test_registry.py · tasks/T-L1-001{,.1–.4}*.md（5 文件）
- **验证**：pytest **343/343** 通过（既有 315 + 注册 28）· `verify_docs.py --strict` PASS（107 文件 / 868 链接 / 0 断链 / 假设 0 缺失）· 账本 SYNC（就绪 .2/.3/T-L2-001）· 盘上描述体无明文抽查通过 · commits：待收工
- **假设复核**：A1 成立（拼接规则注册测试通过）· A2 成立（config 前缀落盘断言通过）· A3 成立（中性命名/权限非法拒绝测试通过）
- **决策**：无新选型（KDF/分区口径沿用 L0；命名校验复用 NeutralityGuard 依赖倒置；主版本无日志发布回滚防脏标记）——均记于对齐结论，不另立 D-NNN
- **遗留**：① 下一就绪 `T-L1-001.2`（流水线）/ `T-L1-001.3`（沙箱）/ `T-L2-001`（记忆图谱）；② 全量首次真实同步（`run_all`）待排期（既有遗留未变）

## [sc] 2026-09-25T21:20+08:00 — T-L1-001.1 PR 合入收口 · done

- **目标**：T-L1-001.1 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；中文标题经 API PATCH 乱码为已知问题（PR #2/#5/#7/#8 同症），本次英文标题保 gate（同 PR #8 先例）
- **关键动作**：推 `feat/T-L1-001-1-skill-registry` → 建 [PR #9](https://github.com/Dline6/ST-Agent/pull/9)（API 建 PR + PATCH 英文标题）→ gate（strict PASS / pytest 343 / SYNC）→ squash 合入 `c30a00f` → 删远端分支（API DELETE）→ `checkout main + pull --ff-only` 干净快进 → `-D` 删本地分支 → 补任务 verify squash hash → 登记本条
- **变更文件**：项目管理/执行日志.md（+本条 + 上条 T-L1-001.1）· tasks/T-L1-001.1（verify 补 squash hash）
- **验证**：远端 main @ `c30a00f` · main 上 strict PASS + pytest 343/343 · `git branch --list` 仅剩 main · 与 origin/main 零偏移
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① 下一就绪 `T-L1-001.2`（流水线）/ `T-L1-001.3`（沙箱）/ `T-L2-001`（记忆图谱），开工走 ④ 对齐；② 全量首次真实同步（`run_all`）待排期

## [T-L1-001.2] 2026-09-25T22:10+08:00 — 执行流水线 + 依赖 DAG + 权限 + SkillRun 留痕 · done

- **目标**：实现 03-L1 §1.2 执行流水线——解析 → 参数确认 → 依赖 DAG 解析 → 权限检查 → 执行与留痕（ResultEnvelope + SkillRun 挂 trace_id）
- **对齐**：人回「对，按此开工」——④ 对齐结论：同步 callable 注册（A4，LLM 经 LlmClient.invoke、出网经 EgressGateway）；SkillRun 落 `execution_log/skill-run/*.json` + Trace 追加 skill_run 步（A5）；权限逐项核对、未批准即 validation_failed（A6）
- **输入**：story-02 验收段 9 条（本任务覆盖 GWT-2/3/4/6）；03 §1.2；01 §4（Trace）/ §5（ResultEnvelope）/ §10（权限）；T-L1-001.1 的 SkillRegistry（validate_call_params + 描述体 dependencies）
- **关键动作**：`errors.py`（RunnerValidationError/CycleDetectedError，执行面失败走信封不抛）· `models.py`（SkillRun frozen + RUN_PREFIX）· `runner.py`（SkillRunner：match bigram 匹配/confirm_card 参数确认卡/run 递归执行——循环即拒/缺失即拒/上游非 ok 即 dependency_failed/未批准即拦截/无执行器·裸异常·非法返回即 failed 带 log_ref；每次执行 SkillRun 落盘 + TraceStep 追加）· `tests/l1/test_pipeline.py`（24 用例对 4 条 GWT，不碰真网）
- **变更文件**：src/st_agent/l1/runner/{__init__,errors,models,runner}.py（4 文件）· tests/l1/test_pipeline.py · tasks/T-L1-001.2（doing→done）
- **验证**：pytest **367/367** 通过（既有 343 + 流水线 24）· `verify_docs.py --strict` PASS · 账本 SYNC（就绪 .1.4 待 .2 done 派生/.3/T-L2-001）· commits：待收工
- **假设复核**：A4 成立（同步 callable + 上下文注入 LLM/网关测试通过）· A5 成立（SkillRun 落盘 + Trace 回放断言通过）· A6 成立（未授权拒绝/已批准放行测试通过）
- **决策**：无新选型（成环点内层 validation_failed、入口按纪律透出 dependency_failed；GWT-4 下游措辞含「依赖失败」字样）——均记于此不另立 D-NNN
- **遗留**：① 下一就绪 `T-L1-001.4`（输出复用，依赖本任务 + T-L0-005）/ `T-L1-001.3`（沙箱）/ `T-L2-001`（记忆图谱）；② 全量首次真实同步（`run_all`）待排期（既有遗留未变）

## [sc] 2026-09-25T22:40+08:00 — T-L1-001.2 PR 合入收口 · done

- **目标**：T-L1-001.2 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；中文标题经 API PATCH 乱码为已知问题（PR #2/#5/#7/#8 同症），本次英文标题保 gate（同 PR #8 先例）
- **关键动作**：中途出口到 GitHub TCP 443 整体不通（git 协议 + API 双不通，DNS 正常；SSH 无本机私钥亦不可行）→ 退避重试后 HTTPS 恢复 → 推 `feat/T-L1-001.2-pipeline` → 建 [PR #10](https://github.com/Dline6/ST-Agent/pull/10)（API 建 PR + PATCH 英文标题）→ gate（strict PASS / pytest 367 / SYNC）→ squash 合入 `9113029` → 删远端分支（API DELETE，分支 404 确认；`refs/pull/N/head` 为 GitHub 只读保留非残留）→ `checkout main + pull --ff-only` → `fetch --prune` 清过期跟踪 → `-D` 删本地分支 → 登记本条
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：远端 main @ `9113029` · main 上 strict PASS + pytest 367/367 · `git branch -a` 仅剩 main + origin/main · 与 origin/main 零偏移
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① 下一就绪 `T-L1-001.4`（输出复用）/ `T-L1-001.3`（沙箱）/ `T-L2-001`（记忆图谱），开工走 ④ 对齐；② 全量首次真实同步（`run_all`）待排期

## [T-L1-001.3] 2026-09-26T09:55+08:00 — 执行沙箱越界拦截 · done

- **目标**：实现 03 §1.5 执行沙箱——文件访问限声明 `local_read` 范围、网络限 `net_access` 模式、命令执行需 `exec_command` 审批；越界即拦截 + `BehaviorViolation` 事件（01 §11）+ 警示「这个 Skill 行为异常」+ 提供禁用选项
- **对齐**：人回「可以开工了」——④ 对齐结论：进程内声明式 enforcement（A3）；事件载荷最小口径（A7）；禁用取注册表级最小口径（A8）；本任务只交付子系统、不接 runner 注入（A9）；假设编号见任务文件 `## 假设与前提`
- **输入**：story-02 验收段（边缘情况「行为越界拦截」+ 沙箱诉求）；03 §1.5；01 §10（权限声明模型）/ §11（`BehaviorViolation` 事件）；T-L1-001.1 的 SkillRegistry（取 `SkillDescriptor.permissions`）
- **关键动作**：`errors.py`（`SandboxValidationError` + 流式越界异常 `SandboxViolationError`）· `scopes.py`（路径归一化 / 逃逸判定 / `local_read` 前缀与 glob 匹配；`net_access` 主机 glob，含 `*.x` 兼匹配裸域）· `models.py`（`GuardVerdict` 拦截结论 + `ViolationRecord` 留痕 + 前缀常量；A7 最小载荷）· `sandbox.py`（`SkillSandbox` 门面：session / disable / enable / is_disabled / violations；`SandboxSession` 三类出口核对——`check_file`·`read_file`、`check_net`·`guard_gateway`、`check_command`·`run_command`；`SandboxedGateway` 受限出网出口：越界不触碰底层 sender，只暴露 execute/stream/online 无绕过入口）；中途修复 `_scope_of`——`parse_permission` 返回的 scope 含 §10 记法的 `<>` 包裹，剥括号后范围匹配才成立（原实现全判越界）
- **变更文件**：src/st_agent/l1/sandbox/{__init__,errors,scopes,models,sandbox}.py（5 文件）· src/st_agent/l1/__init__.py（docstring 指向 .3）· tests/l1/test_sandbox.py（30 用例）· tasks/T-L1-001.3（doing→done + verify + A8/A9）
- **验证**：pytest **397/397** 通过（既有 367 + 沙箱 30）· `verify_docs.py --strict` PASS · 沙箱 5 模块无 `requests`/`urllib`/`socket`/`subprocess` 等 import（零出网零执行）· 越界留痕落盘抽验不含资源串（A7）· 默认拒绝断言：越界时 reader/runner/sender 均未被调用 · commits：`cdf60dd`（实现 + 收口登记）
- **假设复核**：A3 成立（进程内声明式 enforcement：三类出口统一核对，越界测试通过）· A7 成立（事件载荷仅 `skill_id`+`violation`，磁盘留痕无路径/主机串）· A8 成立（`disable` 后三类出口全拒 + 落 config 可重载 + enable 复原）· A9 成立（未动 runner，runner 既有测试零回归）
- **决策**：D-004（权限作用域匹配口径跨任务统一——剥 `<>` 包裹 + `*.x` 兼匹配裸域 `x`；不改 01 §10）
- **遗留**：① 沙箱**未接 runner 注入**（A9 既定范围）——`SkillRunner` 仍把原始 llm/gateway 交给执行器；接线已立项为 **T-L1-001.5**（禁用拦截 + 受限 ctx 句柄 + LLM 出网路径），待开工；② `llm_transport` 内部经网关自身 `stream` 出网，不接线则该 LLM 路径不受沙箱约束（与①同源，一并由 .5 兑现）；③ 核查 .5 所需 `EndpointRegistry`/`CredentialVault`/`LlmClient` 装配时发现**运行时组合根全账本无人承载**（`src/` 无装配层、docs 零提及），按铁律 10 登记未决 **Q-001** → 决议 **A**（`provider_hosts` 暂归 L1，见 D-005）+ 新立 **T-L1-006** 组合根任务；④ 下一就绪 `T-L1-001.4`（输出复用）/ `T-L1-001.5`（接线）/ `T-L2-001`（记忆图谱）

## [sc] 2026-09-26T10:10+08:00 — T-L1-001.3 PR 合入收口 · done

- **目标**：T-L1-001.3 走 PR squash 合入 main，完成全链路
- **对齐**：人回「现在提交并开 PR」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；中文标题经 API PATCH 乱码为已知问题（PR #2/#5/#7/#8/#9/#10 同症），本次英文标题保 gate
- **关键动作**：本任务 PR 内含三件事——沙箱实现、T-L1-001.3 收口留痕、以及**核查中发现的两个新结构**（未决 Q-001 决议与 T-L1-006 组合根任务）；`pre-commit` 钩子按「账本须与 `tasks/` 同步」拒绝拆分提交（代码提交时账本被 stash 即判 STALE），改为单笔 `cdf60dd` 承载——钩子正常工作，非绕过
- **变更文件**：项目管理/执行日志.md（+本条 + 上条验证行补 hash）· tasks/T-L1-001.3（verify 补 PR + squash hash）
- **验证**：远端 main @ `fa05d8b`（PR #11，API 查 merged=True）· main 上 `verify_docs --strict` PASS + pytest 397/397 · `git branch -a` 仅剩 main + origin/main（`fetch --prune` 清过期跟踪）· 与 origin/main 零偏移 · 远端 PR 无 CI 配置（check-runs total=0），gate 以本地三项为准
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① `ccd_pr.bind_pr` 绑定 PR #11 失败（本机 `gh` 未登录，app 读不到），故 CI 与 PR 状态经 API 直查；如需 app 内 PR 面板/自动修复，需先 `gh auth login`；② 下一就绪 `T-L1-001.4`（输出复用）/ `T-L1-001.5`（沙箱接线）/ `T-L2-001`（记忆图谱），开工走 ④ 对齐；③ 全量首次真实同步（`run_all`）待排期（既有遗留未变）

## [sc] 2026-09-26T11:09+08:00 — 开发流程加固（任务接口面 · 里程碑集成关卡 · CI 机器门禁） · done

- **目标**：兑现流程隐患调查（同会话）的落地批次 ①–③——给任务加输入/输出契约（`## 接口面`）、给每个里程碑加集成关卡任务（`T-INT-*`）、把 PR gate 从人工自觉改为 CI 机器强制；顺带修 render_ledger 回写丢字段与打包配置两处工具缺陷
- **对齐**：人指示「请按照顺序帮我落地」——三批顺序（① 流程文档+工具 → ② 立项 → ③ CI+测试）与四项决策点采纳本会话调查报告的建议（接口面入任务正文而非另立登记表 · INT 用独立层前缀 · T-L1-006 由 M1 挪 M0 · CI 建在 GitHub Actions）；假设 A1–A3 见本条
- **输入**：本会话调查结论（任务无 I/O 契约 · 无集成任务 + 七项其他隐患）；工程宪法第 7/9/10/11 条；[工作流.md](工作流.md) / [tasks/_任务模板.md](tasks/_任务模板.md) / tools/{render_ledger,verify_docs}.py；Git协作规范 §5.2；00-架构总览 §5 端到端数据流；01 §2 `input_schema`/`output_schema`
- **关键动作**：
  ① 流程与工具——任务正文四块→五块（新增 `## 接口面`：输入＝上游任务 id + 具体 API/落盘位置，输出＝本任务交付的公共 API 与落盘位置），工作流 ④/⑤ 步、任务字段说明、任务模板、bootstrap 模板、方案文档、根 `CLAUDE.md` 同步；新增「集成关卡（里程碑收口）」节 + `gate: skip` 例外；`render_ledger.serialize` 改为已知字段定序 + 未知字段原序追加 + 空值键保留（原实现重写任务文件时静默丢字段）；`ID_RE`/`LAYERS` 支持 `INT` 层；`verify_docs` 新增检查 6（接口面实填，豁免 16 个机制前 done 任务）与检查 7（每里程碑必有 `T-INT-*` 且其依赖覆盖本里程碑全部叶子任务，`gate: skip` 除外）
  ② 立项——新增 `T-INT-001..005`（M0–M4 各一，`depends_on` 覆盖本里程碑全部叶子任务并与上一关卡串联；INT-001 已给出 5 条可执行 GWT，锚定 00 §5 端到端数据流）、`T-L1-001.6`（执行输出契约校验——让 01 §2 的 `input_schema`/`output_schema` 从装饰字段变执行期真校验，供 T-L1-003 连线校验复用）、`T-L0-007`（全量首次同步与限流实测，`gate: skip`，兑现 T-L0-005.1 遗留②）；`T-L1-006` 由 M1 挪 M0（组合根是 M0「最小闭环」的组成部分）
  ③ 门禁与测试——`.github/workflows/ci.yml`（push/PR → `verify_docs --strict` + `pytest`，`pip install -e .` 顺带验证打包）；Git协作规范 §5.2 拆为「机器强制 / 人工自查」两部分；`tests/test_layering.py`（铁律 7 层依赖方向机器校验）；`tests/test_tools.py`（render_ledger 回写保真 + INT 层识别 + 接口面/集成关卡检查与 `gate: skip` 例外）；`pyproject.toml` 修正打包配置（src 布局 `where = ["src"]`、`include = ["st_agent*"]`）、baostock 转 `[market]` 可选 extra、注册 `live` 标记并默认排除
- **变更文件**：项目管理/{工作流,README,AI-Coding项目管理方案,Git协作规范,执行日志,决策日志,任务账本}.md · 项目管理/tools/{render_ledger,verify_docs}.py · 项目管理/tasks/{_任务模板,T-INT-001,T-INT-002,T-INT-003,T-INT-004,T-INT-005,T-L0-007,T-L1-001.6,T-L1-006}.md · CLAUDE.md · pyproject.toml · .github/workflows/ci.yml · tests/{test_layering,test_tools}.py
- **验证**：pytest **467/467** 通过（既有 397 + 新增 70）· `verify_docs.py --strict` PASS（116 文件 / 967 链接 / **0 断链** / 检查 5·6·7 全 0）· 账本 SYNC（活跃 52 / 归档 0 / 就绪 5 = T-L1-001.4 / T-L1-001.5 / T-L1-001.6 / T-L2-001 / T-L0-007）· 打包实测：`pip wheel .` 产物含 `st_agent/…`（修前为 `src/st_agent/…`，装后 `import st_agent` 必失败）· render 幂等复跑账本零 diff · 集成关卡依赖覆盖经检查 7 逐里程碑核验（M0 22 / M1 9 / M2 4 / M3 3 / M4 5 个叶子任务）
- **假设复核**：A1 成立（接口面放任务正文：检查 6 落地即 PASS，未引入第二真相源）· A2 成立（INT 独立层前缀：render 正确分组、就绪集排序 INT 天然最后、检查 7 通过）· A3 成立（`gate: skip`：T-L0-007 不计入 T-INT-002 覆盖，其余任务仍受覆盖约束，检查 7 无告警）
- **决策**：D-006（开发流程加固：接口面载体 / 集成关卡形态 / 门禁方式三项选型 + 两项工具修正）
- **遗留**：① 两项新机制**实效待首批任务检验**——T-L1-001.4/.5/.6 与 T-INT-001 是首批按新机制走 ④ 的任务，若字段粒度不当（如「输出」写成了实现细节）再迭代工作流与模板；② 历史 16 个 done 任务未追补接口面（沿用 2026-09-25 假设机制的豁免先例，见 `verify_docs.GRANDFATHERED_NO_IFACE`）；③ CI 需远端仓库启用 Actions 才生效（本机 `gh` 未登录，无法代验），建议同时在分支保护勾选「Require status checks to pass」；④ ruff 已配 `[tool.ruff]` 但从未在任何 gate 执行、存量代码未 lint，未纳入本次 CI（避免首跑即红）；⑤ PAT 仍为 classic `repo` scope（既有遗留未变）；⑥ T-INT-002..005 的 GWT 待各自 ④ 对齐时按 00 §5 对应步骤补全

## [sc] 2026-09-26T11:19+08:00 — 追补历史任务接口面（兑现上条遗留②） · done

- **目标**：为接口面机制落地前已 done 的 15 个叶子任务回填 `## 接口面`，并清空 `verify_docs` 的豁免清单（兑现上一条 `[sc] 2026-09-26T11:09` 的遗留②）
- **对齐**：人指示「帮我补一下已经完成的任务的接口面」——明确覆盖历史 done 任务。与 2026-09-25 假设机制「不追补」的处置**刻意不同**：接口面是跨任务交接点，历史缺失会持续影响下游任务开工，且 15 个任务的文件都还在活跃区（未 archive）
- **输入**：15 个 done 任务文件；已合入代码的公开面（各模块 `__all__` / 门面类方法 / 前缀常量 / 落盘分区）；执行日志各任务的「关键动作」与「变更文件」
- **关键动作**：按任务模板节序把 `## 接口面` 插到 `## 验收标准` 之后（脚本一次性、跑完即删不入库）；内容分「输入（消费的上游任务 id + 具体 API/落盘位置）」与「输出（本任务交付的公共 API / 落盘位置）」，逐条取自代码实测而非复述 GWT——如 `CredentialVault.use` 落 `secrets/cred/<id>.json` + 使用记录 `execution_log/cred-usage/**`、`EgressGateway.llm_transport` 审计落 `execution_log/net/**`、`SkillRunner` 落 `execution_log/skill-run/<skill_run_id>.json`；节首加一行「追补于 2026-09-26（非 ④ 对齐时填写）」标记；`verify_docs.GRANDFATHERED_NO_IFACE` 清空
- **变更文件**：项目管理/tasks/T-SC-001.1 · .2 · .3 · .4 · .5 · T-L0-001 ~ T-L0-006 · T-L0-005.1 · T-L1-001.1 · .2 · .3（15 文件各 +`## 接口面` 节）· 项目管理/tools/verify_docs.py（豁免清单清空）· 项目管理/执行日志.md（+本条）
- **验证**：pytest **467/467** 通过 · `verify_docs.py --strict` PASS（116 文件 / 974 链接 / **0 断链** / **检查 6 在零豁免下 0 缺失**）· 15 个文件的节序与任务模板一致（`## 验收标准` → `## 接口面` → `## 假设与前提`）· 回填内容逐条对照代码公开面（`__all__` / 门面方法名 / 前缀常量 / `store.put` 分区实参）
- **假设复核**：无（纯文档回填，未改代码行为、未改任何接口）
- **决策**：无新选型（回填粒度、节序与「输入 / 输出」两行式沿用 [工作流.md](工作流.md) 与任务模板的既有约定）
- **遗留**：① `## 假设与前提` 节的历史豁免（12 个 done，见 `GRANDFATHERED_NO_ASSUME`）**保持不追补**——本条的「对齐」行已写明与接口面处置不同的理由；② 父任务 `T-SC-001` 未回填（check 按父链跳过，其子任务已各自回填）；③ 下一就绪 `T-L1-001.4` / `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T11:27+08:00 — 行尾归一化 + 工具写文件强制 LF · done

- **目标**：修掉「行尾不一致」这一工具缺陷——上一条接口面回填实际踩到：6 个任务文件被整文件 diff 淹没（真实改动仅 8–9 行，diff 却是 55 增 45 删）
- **对齐**：人指示「归一化行尾并修工具写文件」——分两笔：先修工具（防止再生）、再归一化存量
- **输入**：上条回填的 diff 证据（`git show 03767bd --numstat`）· `git ls-files --eol` 索引态统计 · `render_ledger` / `verify_docs` 的四个写文件调用点
- **关键动作**：① 工具——`render_ledger` 新增唯一写入口 `write_file()`（`newline="\n"` 强制 LF），bootstrap 建任务文件 / 派生父状态回写 / render 写账本三处与 `verify_docs --fix` 一处全部改经它（此前四处皆为文本模式 `open(..., "w")`，在 Windows 上产出 CRLF——33 个 CRLF 任务文件即由此而来）；② 存量——`git add --renormalize .` 只归一化索引，工作区不动（`core.autocrlf=true` 保持检出 CRLF）
- **变更文件**：项目管理/tools/{render_ledger,verify_docs}.py · tests/test_tools.py（+2 用例）· 27 个 tasks/*.md（纯行尾）· 项目管理/执行日志.md（+本条）
- **验证**：索引中存 CRLF 的任务文件数 **33 → 27（上条随提交归一化 6）→ 0**——`git cat-file blob` 逐文件实测 551016f / 03767bd / 6fb6699 / 85464fc 四个提交点 · `git diff --cached --ignore-all-space` 为空（归一化确为纯行尾，内容零差异）· 端到端复验：`render` 后账本 diff 恰为 1 行（时间戳行）· pytest **469/469**（新增 2）· `verify_docs --strict` PASS · 归一化后 `git status` 干净
- **假设复核**：A1 成立（写入口唯一化即可根治——测试 2 例分别覆盖 write_file 与「派生父状态回写」这条历史 CRLF 来源路径）· A2 成立（renormalize 只动索引、不改工作区内容：`--ignore-all-space` 为空且工作区状态干净）
- **决策**：无新选型（LF 定为规范行尾，与仓库其余 198 个 i/lf 文件一致；未引入 `.gitattributes`，见遗留①）
- **遗留**：① **未加 `.gitattributes`**——当前保证依赖各机器的 `core.autocrlf`（本机 true）。若在 `autocrlf=false` 的机器上用会写 CRLF 的编辑器改任务文件，仍可能漂移；彻底解法是 `* text=auto eol=lf` + 一次 renormalize，属独立决定，待定；② 下一就绪 `T-L1-001.4` / `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T11:46+08:00 — PR #12 合入收口（流程加固三批 + 工具修复） · done

- **目标**：把本会话三批流程加固（接口面 / 集成关卡 / CI 门禁）与两处工具修复走 PR squash 合入 main，完成全链路
- **对齐**：人指示「推送分支并开 PR」→「我去加 workflow scope」→「合入 PR #12」——沿用既定流程（§5.2 gate + squash 合入即删分支）
- **关键动作**：
  ① **推送受挫一次**——首次 push 被 GitHub 拒：「refusing to allow a Personal Access Token to create or update workflow `.github/workflows/ci.yml` without `workflow` scope」；只读侦察确认 token 有效（`ghp_` 40 位、classic、scope 仅 `repo`、账号 Dline6）且仓库 admin 权限、Actions 已启用，故确认为纯 scope 缺项 → 人补 `workflow` 后推送成功并保留原 token 值
  ② **PR 开展**——[PR #12](https://github.com/Dline6/ST-Agent/pull/12)（5 提交 / 65 文件 / +2060−1143）；标题与正文经 Python 构造 UTF-8 payload（`ensure_ascii=False` + `charset=utf-8`）提交，**回读中文完好**——推翻此前「API 中文乱码」的判断：那是 Windows 控制台 GBK 拼 curl `-d` 时的客户端编码损坏，不是 API 限制
  ③ **CI 首次生效并双跑全绿**——`push` 与 `pull_request` 两个 run 均 completed/success（ubuntu-latest + Python 3.12）；这是项目**第一次在非 Windows 环境执行** 全量自检与测试，中文文件名路径与 `pythonpath = ["src", "项目管理/tools"]` 配置均无问题
  ④ **合入与清理**——API squash 合入 `3a9687eb`（提交体含 `Task: sc` trailer 与署名行）→ 删远端分支（HTTP 204）→ `fetch --prune` + `checkout main` + `merge --ff-only origin/main` 快进 → `-D` 删本地分支
- **变更文件**：项目管理/执行日志.md（+本条）；本轮无源码改动（内容全部来自本会话前三条 `[sc]` 条目）
- **验证**：PR `mergeable_state: clean` + `check-runs` 2/2 success · 远端 `refs/heads/main @ 3a9687eb` 且 `git branch -a` 仅剩 `main` 与 `origin/main` · main 上 pytest **469/469** · `verify_docs.py --strict` PASS（116 文件 / 975 链接 / **0 断链** / 检查 5·6·7 全 0 / 就绪集 T-L0-007·T-L1-001.4·T-L1-001.5·T-L1-001.6·T-L2-001）· squash 提交体中文字面回读完好
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① **分支保护仍未开**（API 查 `main` protection = 404）——但注意副作用：若勾「Require status checks: CI」且勾了「Include administrators」，本仓库既有的「收口日志直推 main」惯例（`docs(项目管理): 登记 …` 类提交）会被拦，需同时配 bypass 名单或取消 Include administrators；② `ccd_pr.bind_pr` 仍失败（本机无 `gh`、未 `gh auth login`），代价是 app 内 PR 面板与「CI 失败自动唤醒修复」不可用，PR 状态只能经 API 直查；③ PAT 仍为 classic `repo`+`workflow`（既有遗留未变，仍建议换 fine-grained）；④ 下一就绪 `T-L1-001.4` / `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [T-L1-001.4] 2026-09-26T12:01+08:00 — 输出复用 + 新鲜度查询 · done

- **目标**：实现 03 §1.2 步骤 6（输出登记）+ §1.3（输出复用）——`ok`/`empty` 输出登记为可引用结果、后续经 `skill_run_id` 复用；复用时给 freshness 判定（判定只给结论，是否重算由上层自决）；freshness 复用 L0 同步状态口径，不另建第二套
- **对齐**：人回「按实际代码接线，同时更改任务文件」——④ 对齐结论：**按实测代码接线**（`freshness_verdict(domain)` 在 `BaoStockSync` 而非任务文件原写的 `MarketDb`；`MarketDb` 只提供 `as_of`/`snapshot_id`/`freshness` 行），并据此改任务文件「目标 / 涉及契约」两处；登记取索引化（载荷单一来源于 SkillRun，不复制）；`ok`+`empty` 均登记；`domain` 由复用查询方给出；假设 A1–A9 见任务文件 `## 假设与前提`
- **输入**：story-02 验收段（边缘情况「数据源不可用」+ 空状态，2 条 = GWT-8/9）；03 §1.2 步骤 6 / §1.3；01 §5（信封分支）/ §8（`as_of` 与最后更新）/ §11；数据库设计 §05（陈旧判据）；T-L1-001.2 的 `SkillRunner`/`SkillRun`；T-L0-005 的 `BaoStockSync.freshness_verdict`；T-L0-001 的 `Store`
- **关键动作**：新子系统 `l1/reuse/`——`errors.py`（`ReuseError`/`ReuseValidationError`/`OutputNotRegisteredError`）· `models.py`（`OUTPUT_PREFIX` + `REUSABLE_STATUSES=("ok","empty")` + `ReusableOutput` 索引五字段 + `ReuseView`＝索引+原信封+判定）· `freshness.py`（`FreshnessOracle` Protocol；`MarketFreshnessOracle` 对 L0 做**鸭子类型**适配、不合口径对象构造期即拒；`age_based_verdict` 超龄兜底 + `DEFAULT_MAX_AGE=24h`；`unavailable_envelope` 让「最后更新时间」只能取自判定）· `registry.py`（`OutputRegistry`：`register`/`is_registered`/`get`/`load_envelope`/`freshness`/`reuse`；`_verdict_for` 判定口径——有 oracle+domain 时以 L0 判定为基并补判「引用 `as_of` 早于该域当前截止」）；`runner.py` 接线（新增可选参 `outputs`/`freshness`；`SkillContext` 增 `freshness` 句柄；`finish` 内对 `ok`/`empty` 自动登记，登记失败转 `failed` 带 `log_ref`）；`tests/l1/test_reuse.py`（28 用例对 3 条 GWT + A1–A4/A8/A9）
- **变更文件**：src/st_agent/l1/reuse/{__init__,errors,models,freshness,registry}.py（5 新文件）· src/st_agent/l1/runner/runner.py · src/st_agent/l1/__init__.py（docstring 指向 .4）· tests/l1/test_reuse.py · tasks/T-L1-001.4（doing→done）· 项目管理/{任务账本,执行日志,决策日志}.md（父任务 `T-L1-001` 派生状态回到 `todo`——.4 done 后仍有 .5/.6 未开工，与开工时 `doing` 相抵，净零 diff）
- **验证**：pytest **502/502** 通过（既有 474 + 复用 28）· `verify_docs.py --strict` PASS（116 文件 / 979 链接 / 0 断链 / 检查 5·6·7 全 0）· 账本 SYNC · A1 实测：`skill-output/<id>.json` 键集恰为 5 个（无 payload），载荷只在 `skill-run/<id>.json` · A3 实测：`outputs=None` 时 `skill-output/` 为空且 `ctx.freshness is None` · A4 实测：注入必抛 `register` → 信封 `failed` 且 `log_ref` 非空 · commits：`a3dbedf`（实现 + 收口登记）· squash `da2363e`（PR #13）
- **假设复核**：A1 成立（索引五键实测 + 载荷单源）· A2 成立（ok/empty 登记、四类失败分支登记即拒）· A3 成立（None 时零行为变化，回归用例通过）· A4 成立（登记失败显式化，不静默）· A5 成立（适配器委托 L0；无 `freshness_verdict` 的对象构造期拒绝）· A6 成立（empty/unavailable 构造期不变量未被绕过，reason/最后更新时间均齐备）· A7 成立（登记只存定位 + 状态 + 时间锚点，无用户态字段）· A8 成立（`domain` 为查询参：`reuse(domain=…)` 与 `reuse()` 分别走 L0 与超龄两条路径）· A9 成立（L0 为基 + 快照落伍补判；引用落伍时 `stale=True` 且时间戳取自 L0；无 oracle/domain 时 24h 超龄）
- **决策**：D-007（输出复用登记形态与新鲜度判定口径——索引化 / ok+empty / L0 为基 + 快照落伍补判 / 超龄兜底须标明）
- **遗留**：① **`OutputRegistry` 尚无上层装配**——`src/` 无组合根（Q-001 的既有缺口），本任务只交付 L1 子系统 + runner 接线，不建组合根；`outputs`/`freshness` 的端到端注入待 **T-L1-006**（组合根）承担；② **与 T-L1-001.6 的落点顺序**——`.6` 要在 runner 里加 `output_schema` 校验，其要求「不合契约的载荷不得按 `ok` 登记进输出复用」，故 `.6` 必须在校验失败时改走失败信封**再**进 `finish`，不得在 `finish` 之后补拦（当前实现把登记放在 `finish` 内、以最终信封为准，天然满足该序）；③ 下一就绪 `T-L1-001.5`（沙箱接线）/ `T-L1-001.6`（输出契约校验）/ `T-L2-001`（记忆图谱）/ `T-L0-007`（全量同步，P1）

## [sc] 2026-09-26T12:16+08:00 — T-L1-001.4 PR 合入收口 · done

- **目标**：`T-L1-001.4` 走 PR squash 合入 main，完成全链路
- **对齐**：人指示「推送分支并开 PR」→「我安装了github app，请继续尝试」→「我安装了github CLI，请帮我完成后续任务」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）
- **关键动作**：
  ① **app 内 PR 绑定失败并定位根因**——`ccd_pr.bind_pr` 三次与 `<pr-created>` 标记一次均未绑定；排查确认**本机从未安装 GitHub CLI**（`Program Files/GitHub CLI`、`AppData/Local/GitHub CLI`、choco/scoop/WinGet 四处皆无，`gh` 不在 PATH），而 app 读 PR 依赖它。人装 `gh` 2.101.0 后绑定仍失败——因 **app 进程 PATH 是启动时快照**；改用绝对路径 `/c/Program Files/GitHub CLI/gh.exe`、以 git 已存的同一 github.com token 非交互登录（`gh auth login --with-token`，账号 Dline6，scopes 含 `repo`/`workflow`）后，`gh` 本身可用，绑定预计需**重启 app** 才生效（收工时未重启，故本条仍走 `gh`/API 直查口径）
  ② **PR 开展**——[PR #13](https://github.com/Dline6/ST-Agent/pull/13)（1 commit / 12 文件 / +954−14）；中文标题与正文经 Python 构造 UTF-8 payload（`ensure_ascii=False` + `charset=utf-8`）提交，**逐字符回读校验 `title` 一致**（`len=34`）——再次确认「控制台乱码」是 Windows GBK 显示问题而非 API 限制
  ③ **CI 双跑全绿**——push 与 pull_request 两个 run 均 completed/success（ubuntu-latest + Python 3.12）：[36216771308](https://github.com/Dline6/ST-Agent/actions/runs/36216771308) / [36216786116](https://github.com/Dline6/ST-Agent/actions/runs/36216786116)；`gh pr checks 13` 两条 `pass`，`mergeStateStatus: CLEAN`
  ④ **合入与清理**——`gh pr merge 13 --squash --delete-branch`（显式 `--subject` / `--body-file`，保 `Task:` trailer 与署名行）→ squash `da2363e` → `git fetch --prune`（远端任务分支已由 gh 删除）→ 本地 `main` 已快进至 `da2363e`、与 `origin/main` 零偏移
- **变更文件**：项目管理/执行日志.md（+本条 + 上条验证行补 hash）· tasks/T-L1-001.4（verify 补 PR + squash hash）
- **验证**：PR `state: MERGED`（`mergedAt 2026-09-26T04:14:35Z`）· main 上 pytest **502/502** · `verify_docs.py --strict` PASS · `git ls-remote --heads origin` 仅 `main @ da2363e` · 本地 `git branch -a` 无任务分支 · squash 提交体含 `Task: T-L1-001.4` trailer 与署名行
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① **app 内 PR 面板与「CI 失败自动唤醒修复」仍不可用**——根因已定位：本机缺 `gh`（已补齐并登录）**且 app 进程 PATH 未刷新**；重启 Claude 桌面版后 `bind_pr` 预计可成，未重启前 PR 状态只能经 `gh`/API 直查；② **分支保护仍未开**（`main` protection 404，既有遗留未变）——CI 属建议性不拦合入，本仓库「收口日志直推 main」惯例亦依赖这一点；③ 下一就绪 `T-L1-001.5`（沙箱接线）/ `T-L1-001.6`（输出契约校验）/ `T-L2-001`（记忆图谱）均 P0，`T-L0-007`（全量同步）P1，开工走 ④ 对齐

## [T-L0-007.1] 2026-09-26T12:34+08:00 — 脚本化全量同步与续跑语义 · done

- **目标**：兑现 T-L0-007 拆分后可离线验证的部分——把全市场首次全量同步从一次性手工操作变为可重复流程（长跑脚本 + JSON 摘要），并以 Fake 注入兑现续跑语义（中断后续跑不重复写、水位单调不回退、失败不静默）
- **对齐**：人回「对」——④ 对齐结论：`T-L0-007` 拆为 `.1`（脚本化 + 续跑语义，本批交付）/ `.2`（真实全市场长跑，人择时）；两子均 `gate: skip` 沿用父的运营类定性，避免 T-INT-002 覆盖涟漪；脚本落 `项目管理/tools/`（不引入包内 CLI）；为 `run_all` 加公开 `RUN_ORDER` 别名供脚本逐任务计时；假设 A1–A5 见任务文件 `## 假设与前提`
- **输入**：T-L0-005 `BaoStockSync.setup/run_task/run_all/freshness_verdict` + `MarketDb` + `SYNC_TASKS`/`window_for`；T-L0-005.1 `BaoStockFetcher`；T-L0-004 `EgressGateway.execute(kind="data_fetch", …)`；T-L0-001 `Store`；02 §5/§6 + 数据库设计族 05
- **关键动作**：`sync.py` 把 `_RUN_ORDER` 提为公开 `RUN_ORDER`（含 `__all__` 与 `market/__init__` 出口），`run_all` 两处引用同步；新增 `项目管理/tools/run_full_sync.py`（`summarize_run` 逐任务计时 + 参数块；CLI `--root/--out/--init/--watchlist/--enable-minute/--timeout-ms`，主口令只经 `ST_AGENT_PASSPHRASE`，不进 argv 与摘要）；新增 `tests/l0/test_full_sync.py`（13 用例：FlakyFetcher 中断→续跑不重复写、RecordingFetcher 验窗口起点随水位前进、失败记 `last_error` 且 `freshness_verdict` 点名、审计条数对位、摘要形状与口令零泄漏、脚本序 == `run_all` 序）；新增 `tests/live/test_full_sync_live.py`（`@pytest.mark.live` + `ST_AGENT_LIVE_FULL=1` 双重开关，默认排除）
- **变更文件**：src/st_agent/l0/market/{sync,__init__}.py · 项目管理/tools/run_full_sync.py（新）· tests/l0/test_full_sync.py（新）· tests/live/test_full_sync_live.py（新）· 项目管理/tasks/{T-L0-007（转父）,T-L0-007.1（新）,T-L0-007.2（新）,T-L0-005（接口面 +`RUN_ORDER` 行）}.md · 项目管理/{任务账本,执行日志}.md
- **验证**：pytest **515/515** 通过（既有 502 + 续跑语义 13；live **1 deselected**）· `verify_docs.py --strict` PASS（118 文件 / 1018 链接 / **0 断链** / 检查 5·6·7 全 0）· 账本 SYNC（活跃 54 / 就绪集 T-L1-001.5·T-L1-001.6·T-L2-001）· 实测并修正一处命名冲突：`tests/l0/test_full_sync.py` 与 `tests/live/test_full_sync.py` 在无 `__init__.py` 下触发 pytest import mismatch，live 侧改名 `test_full_sync_live.py` 后全绿 · commits：`0d59fa9`（分支实现）· PR #14 · squash `1b53f7a`
- **假设复核**：A1 成立（Fake 中断→续跑用例：已入库行不重复、缺失行补齐、失败不推进水位、窗口起点 = 水位次日，无需新增退避逻辑）· A2 成立（`RUN_ORDER` 公开别名落地，`run_all` 引用同源，`T-L0-005` 接口面已同步该行）· A3 成立（脚本落 `项目管理/tools/`，`pyproject` 的 `pythonpath` 已含该目录，用例可 `import run_full_sync`）· A4 成立（脚本默认 `--root` 指向 `tmp/live-market`；本批未跑真实同步，无真实库产生）· A5 成立（live 用例默认 `1 deselected`，不进 CI；另加元用例断言 `tests/live/` 下的用例必带 `live` 标记）
- **决策**：无新选型（拆分沿用 T-L0-005 → 005.1 先例；`RUN_ORDER` 公开化属接口面补充非选型；脚本落点与摘要形状按对齐结论落地）——记此不另立 D-NNN
- **遗留**：① **真实全市场长跑未执行**——归 [T-L0-007.2](tasks/T-L0-007.2-真实全量长跑与限流续跑实测.md)（`gate: skip`，人择时；本批只交付脚本与续跑语义）· ② 下一就绪 `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T12:44+08:00 — L0 遗留处置（两处小尾巴定案）+ 遗留登记册落地 · done

- **目标**：处置 L0 遗留清单中「L0 自身能闭」的项，并把**未解决**的遗留单独立册（`项目管理/遗留问题/`），与 [阻塞与未决.md](阻塞与未决.md)（Q-NNN 规格空白台账）分工
- **对齐**：人指示「先解决能解决的遗留问题，再新建一个文件夹把还没有解决的遗留问题单独形成文件单独存放进去」；经提问定范围「闭小尾巴 + 开工 T-L0-007」与形态「`项目管理/遗留问题/` · 每层一文件」
- **输入**：L0 全量遗留盘点（各任务 done 时的执行日志「遗留」行 + 未决 Q-001 / 决策 D-005 + T-L0-001 悬空假设 A6）；`export_all_safe` 与 `is_stale` 的源码 docstring
- **关键动作**：
  ① **修正盘点初稿的判断**——初稿把 `export_all_safe`（`src/st_agent/l0/secrets/vault.py:217`）与 `is_stale`（`src/st_agent/contracts/time_events.py:66`）记为「死代码小尾巴」，读码后推翻：前者 docstring 已写明消费方是**分享物导出**（备份路径改由「secrets 永不进归档」满足禁明文），后者 docstring 写明「L0 应以 `sync_state` 自检 SQL 为准，本方法仅用于无 `sync_state` 可查的内存路径」且 L0 确已如此落地——两者都不是欠账，而是**归属已定、归属方尚未开工**（与「远程 MCP 审计点」「Skill 复用 LLM 口径」同类）
  ② **处置定案**：`export_all_safe` 归属 **T-ECO-001**（分享物类型/格式 + 导出流程），保留不删；`is_stale` 记为**已由 T-L0-005 的设计关闭**（通用超龄判定保留给内存路径；L1 复用层的 `age_based_verdict` 为其兜底对位实现）
  ③ **立册**：新建 `项目管理/遗留问题/`——`README.md`（登记规则 + 与 [阻塞与未决.md](阻塞与未决.md) 的分工：那册收 Q-NNN 规格空白，本册收任务收工时的转移/待办）+ [`L0-遗留问题.md`](遗留问题/L0-遗留问题.md)（未解决 9 项，逐条列归属任务 / 阻塞根因 / 解封条件）
- **变更文件**：项目管理/遗留问题/README.md（新）· 项目管理/遗留问题/L0-遗留问题.md（新）· 项目管理/执行日志.md（+本条）
- **验证**：`verify_docs.py --strict` PASS（新目录 2 文件纳入断链校验，仍 **0 断链**）· 登记册 9 项与执行日志原始「遗留」行逐条可回溯（册内附来源任务与行锚点）· L0 源码零改动（本批只动文档）
- **假设复核**：无（纯文档处置，未改代码行为、未改任何接口）
- **决策**：无新选型（「保留既有公共 API 而非删除」属保守处置非选型；登记册形态按人指定落地）——记此不另立 D-NNN
- **遗留**：① T-ECO-001 若最终不走「分享物携带凭据安全视图」，`export_all_safe` 需再评估存废（已记入 [`L0-遗留问题.md`](遗留问题/L0-遗留问题.md) 的观察项）· ② 遗留登记册机制**实效待后续层检验**（当前仅 L0 一册，L1/L2… 收工遇转移项时同构加文件）· ③ 下一就绪 `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T12:57+08:00 — T-L0-007.1 PR 合入收口 · done

- **目标**：把本会话的 L0 批次（脚本化全量同步 + 遗留处置 + 遗留登记册）走 PR squash 合入 main，完成全链路
- **对齐**：人指示「按规范走分支 + PR 合入」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；分支 `feat/T-L0-007-full-sync`（分支 type 取主导变更 `feat`，T-ID 绑 `T-L0-007`）
- **关键动作**：
  ① **提交**——单笔 `0d59fa9` 承载本批三件事（T-L0-007.1 实现 + 两处遗留处置 + 遗留登记册）；`pre-commit` 钩子（`verify_docs.py --strict`）在 commit 前自动跑并 Passed，未绕过
  ② **PR 开展**——[PR #14](https://github.com/Dline6/ST-Agent/pull/14)（1 commit / 13 文件 / +770−15）；经 `gh api --input`（Python 构造 UTF-8 JSON 载荷）建 PR，**逐字符回读 `title` 一致**（`len=36`）——再次确认中文走 JSON 载荷无编码问题
  ③ **CI 双跑全绿**——push 与 pull_request 两个 run 均 pass（[36218453320](https://github.com/Dline6/ST-Agent/actions/runs/36218453320) 1m18s / [36218464804](https://github.com/Dline6/ST-Agent/actions/runs/36218464804) 1m4s）；`mergeable: MERGEABLE`
  ④ **合入与清理**——`gh pr merge 14 --squash --delete-branch`（显式 `--subject` / `--body-file`，保 `Task: T-L0-007.1` trailer 与署名行）→ squash `1b53f7a` → gh 顺带切回 main 并删本地分支 → `fetch --prune` 清远端跟踪 → main 与 origin/main 零偏移
- **变更文件**：项目管理/执行日志.md（+本条 + 上条验证行补 hash）· 项目管理/tasks/T-L0-007.1（verify 补 PR + squash hash）
- **验证**：PR `state: MERGED`（`mergedAt 2026-09-26T04:42:15Z`）· 本地 `main` = `origin/main` = `1b53f7a` · `git ls-remote --heads origin` 仅 `main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · pre-commit 自检 Passed
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型
- **遗留**：① **本仓库禁用 GitHub auto-merge**（`set_auto_merge` 报 "Auto merge is not allowed for this repository"）——故合入只能「等 CI 绿后手动 squash」；若想让 CI 绿即自动落地，需在仓库设置开启 Allow auto-merge · ② **app 内 CI 监视未开**（`auto_fix` off）——CI 失败不会自动唤醒；如需要可开 · ③ 下一就绪 `T-L1-001.5`（沙箱接线）/ `T-L1-001.6`（输出契约校验）/ `T-L2-001`（记忆图谱）均 P0，`T-L0-007.2`（真实全量长跑）P1 待人择时，开工走 ④ 对齐

## [T-L0-007.2] 2026-09-26T13:05+08:00 — 真实链路冒烟（小样本） · doing（冒烟通过，全量待择时）

- **目标**：先用小样本把「Store → MarketDb → EgressGateway → BaoStockFetcher → BaoStockSync → 落盘/审计/新鲜度」整条真实链路走通一次，证明管道可用（全市场全量长跑仍待择时）
- **对齐**：人指示「随便生成一个主口令，然后再做一个简单的小样本真实冒烟测试，能证明链路能跑通就行」——范围＝小样本冒烟（非全量）；交付＝可复跑的 live 冒烟用例 + 真实基线证据；假设 A1–A4 见任务文件 `## 假设与前提`
- **输入**：T-L0-005 `BaoStockSync.setup/run_task/freshness_verdict/dataset_snapshot` + `MarketDb`；T-L0-005.1 `BaoStockFetcher`；T-L0-004 `EgressGateway`；T-L0-001 `Store`；02 §5/§6 + 数据库设计族 05
- **关键动作**：
  ① **口令**——生成 32 位随机口令写入 `.env` 的 `ST_AGENT_PASSPHRASE`（该文件已 gitignore；口令值不进会话、不进 argv）——仅服务开发/冒烟存储，产品路径仍由用户登录时提供
  ② **新增 live 冒烟用例** `tests/live/test_smoke_sync.py`——真实 `Store.create`（PBKDF2 600k + 七分区）→ 真实 `login` → `setup` → 日历 → 证券主档 → 全量快照（取日历内最近交易日）→ 3 只真实 A 股（浦发/平安/工商）日线全历史；断言数据面非空、抓取全经网关审计、`freshness_verdict` 口径可查、**全盘明文 0 命中**
  ③ **首跑失败并据实修正**——`run_task` 直调未先 `setup()` → 显式 `unavailable`（「本地市场数据库尚未建立」），失败显式化如期工作；补 `setup()` 后第二跑通过——该点记入任务假设 A4
- **变更文件**：tests/live/test_smoke_sync.py（新）· 项目管理/tasks/T-L0-007.2（doing + GWT-0 + 接口面 + 假设）· 项目管理/遗留问题/L0-遗留问题.md（C1 更新）· .env（+`ST_AGENT_PASSPHRASE`，未入库）
- **验证**：`python -m pytest -m live -s tests/live/test_smoke_sync.py` → **1 passed in 185.52s**；实测＝密钥派生+建库 0.21s · setup 0.18s · 日历 34.5s/13066 行 · 主档 35.3s/8981 行 · 全量快照 20.4s（交易日 `2026-09-24`）· 日线 90.4s/**3 只 / 20018 行**（`1990-12-19` 起全历史）· 网关 `data_fetch` 审计 **6 条** · 快照串 `bs_all_stock:2026-09-24@…|bs_calendar:-@…|bs_k_daily:2026-09-24@…|bs_security_basic:-@…` · **全盘明文扫描 0 命中**（24 条目）
- **假设复核**：A1 成立（3 码 × 全历史未见限流；全量风险见遗留①）· A2 成立（数据只落 pytest 临时目录加密分区，明文 0 命中、`git status` 无真实库）· A3 成立（冒烟覆盖 setup / 日历 / 主档 / 全量快照 / 日线五类调用 + 网关审计）· A4 成立（首跑踩到「直调须先 setup」并据报错修正，第二跑通过）
- **决策**：无新选型（口令生成与存放位置、冒烟范围均按人指示）——记此不另立 D-NNN
- **遗留**：① **全市场全量长跑仍未执行**，且侦察实测其成本 ≈ **85 小时**（`k_daily` 50h + `k_period` 23h + 其余 12h；8981 码 × 全历史），远超本任务备注原写的「数小时」；同批侦察另发现三处结构性障碍——**码分片会被全局水位跳历史**（分片 2 的窗口起点 = 分片 1 的水位次日）、**任务内中断水位不推进**（重跑全码重拉，40h 进度即白跑）、`live.py` 抛错时**丢失 `error_code`** 致限流无法识别 → 全量长跑须先补「分批 + 退避」能力（建议新立 `T-L0-008`，待定）· ② **冒烟基线**：单码全历史日线 ≈ 18–30s、主档 35s、日历 34s，可作排期估算锚点 · ③ 下一就绪 `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T13:14+08:00 — T-L0-007.2 冒烟 PR 合入收口 · done

- **目标**：把小样本真实链路冒烟批次（冒烟用例 + 任务/登记册留痕）走 PR squash 合入 main，完成全链路
- **对齐**：人指示「走分支 + PR 合入」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；分支 `test/T-L0-007.2-real-smoke`（分支 type 取主导变更 `test`）
- **关键动作**：单笔 `4bc14a5`（`pre-commit` 自检 Passed，未绕过）→ [PR #15](https://github.com/Dline6/ST-Agent/pull/15)（1 commit / 6 文件 / +158−9）经 `gh api --input` 的 UTF-8 JSON 载荷创建、**标题逐字符回读一致**（`len=25`）→ CI 双跑全绿（[36219908913](https://github.com/Dline6/ST-Agent/actions/runs/36219908913) 1m20s / [36219918559](https://github.com/Dline6/ST-Agent/actions/runs/36219918559) 1m18s）→ `gh pr merge 15 --squash --delete-branch`（显式 `--subject` / `--body-file`，保 `Task: T-L0-007.2` trailer 与署名行）→ squash `a80a252` → gh 顺带切回 main 并删本地分支 → `fetch --prune` 清远端跟踪
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：PR `state: MERGED`（`mergedAt 2026-09-26T05:11:10Z`）· 本地 `main` = `origin/main` = `a80a252` · `git ls-remote --heads origin` 仅 `main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · `pre-commit` 自检 Passed
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型
- **遗留**：① **`T-L0-007.2` 仍为 `doing`**——冒烟（GWT-0）已通过，GWT-1/2/3 的全市场长跑未跑（≈85h），故不置 `done`；收口不写任务 `verify`（该字段 `done` 时填）· ② **`T-L0-008`（分批 + 退避）待立项**（登记册 C2；待人定）· ③ 下一就绪 `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T13:36+08:00 — 遗留册接入工作流（开工查 · 收工销）+ 检查 8 机器兜底 · done

- **目标**：把「开工前先看当前任务能解决哪些遗留、能解决就解决并销账」从册规里的一句口头约定，变成 [工作流.md](工作流.md) 的显式步骤 + 任务文件的一个节 + `verify_docs.py` 的一道门禁——让遗留条目**必然被翻开**，而不是靠自觉
- **对齐**：人指示「帮我在工作流中加一条规则，看看开始任务前先查看当前任务能解决哪些遗留问题，如果能解决，解决后更新遗留问题」——三问定案：强度＝**工作流 + 模板节 + 机器门禁**· 遗留关闭**计入本任务验收** · 兜底＝**机器检查 8**（非冷启动人工盘点）
- **输入**：工作流七步现状（全篇无「遗留」字样）· [遗留问题/README.md](遗留问题/README.md) 分工表与规则 1–5 · [遗留问题/L0-遗留问题.md](遗留问题/L0-遗留问题.md) 未闭 9 项 · `verify_docs.py` 检查 5/6/7 的既有形态（`_check_task_section` 复用点）· `tests/test_tools.py` 的 `fake_tasks` 夹具
- **关键动作**：
  ① **工作流.md**：新增「遗留消费（开工查 · 收工销）」节——四步＝**开工查**（筛「归属＝本任务」或「解封条件＝本任务 `done`」的条目）/ **能闭则闭**（闭不了的**就地修正册内归属或解封条件并写明理由**，判据仍为「谁来写这段代码不清楚 → 进未决；清楚但没轮到 → 留本册」）/ **收工销账**（移出未闭区 → 册尾「已闭（备查）」留一行 + 执行日志写关闭留痕）/ **机器兜底**（检查 8）
  ② **四处联动改**：④ 复述清单加「**遗留面**」一条；对齐结论「任务文件两节」→「**三节**」（加 `## 可关闭的遗留`）；调度规则第 3 步收工顺序插入「开工查遗留册」与「销账」；正文「五块」→「**六块**」并新增该节格式段（含「该节本身不加门禁」的理由）
  ③ **`tasks/_任务模板.md`**：加 `## 可关闭的遗留` 节（含 `无` 的写法与册链接）
  ④ **[遗留问题/README.md](遗留问题/README.md)**：加规则 6「开工消费 · 收工销账」，并把分工表「处置」列补成 `不改任务状态；归属任务开工时消费 → 收工时销账（规则 6）`
  ⑤ **`verify_docs.py`**：新增检查 8 `check_legacy_settlement()`——扫 `遗留问题/*.md` 的**未闭区**（`## 已闭` 之前）表格行，取「归属」「解封条件」两列点名的任务 id，**若已全部 `done` → 命中**（该销的账没销）。三条不误报约定：只认这两列（「来源」列的已 `done` 任务不算，那是留痕不是归属）· 册内点到尚不存在的 id（如「待立项」`T-L0-008`）不判 · 「已闭（备查）」区不判。警告级，`--strict` 判失败（CI 与 pre-commit 都跑 `--strict`，故实为门禁）
  ⑥ **`tests/test_tools.py`**：+3 用例（归属已 `done` → 命中 / 归属未轮到 → 不判 / 已闭区与不存在 id → 不判），沿用既有 `fake_tasks` 夹具 + `monkeypatch` 换 `LEGACY_DIR`
- **变更文件**：项目管理/工作流.md · 项目管理/tasks/_任务模板.md · 项目管理/遗留问题/README.md · 项目管理/tools/verify_docs.py · tests/test_tools.py · 项目管理/{执行日志,决策日志}.md · （`tasks/*.md` frontmatter 零改动——本次不改任务状态，账本仍 SYNC）
- **验证**：pytest **518/518** 通过（既有 515 + 检查 8 的 3；live 2 deselected）· `verify_docs.py --strict` PASS（120 文件 / 1057 链接 / **0 断链** / 检查 4·5·6·7·8 全 0 / 就绪集不变）· 检查 8 **反向探针**（独立脚本注入合成册，跑完即删）：归属＝已 `done` 任务 → 命中 1 条；归属＝`todo` → 不判；id 不存在（待立项）→ 不判；落在「已闭（备查）」区 → 不判——**证明门禁会开火且只对该开火的开火**。探针曾抓到一处真 bug 并已修：id 正则原写成 `T-[A-Za-z]+-\d{3}`，**层号里的数字（`L0`/`L1`）匹配不上**，致检查恒为 0（假绿）；改为复用 `rl.ID_RE` 后命中 · commits：`13c4ab0`（分支实现）· PR [#16](https://github.com/Dline6/ST-Agent/pull/16) · squash `424592d`
- **假设复核**：无（纯流程 + 工具改动：未改 `docs/` 架构与 01 契约、未改 L0/L1 代码行为——不触发工作流第 ⑦ 步）
- **决策**：D-008（遗留册接入工作流：开工查 · 收工销 · 检查 8 机器兜底）
- **遗留**：① **任务侧 `## 可关闭的遗留` 节未加机器检查**（只做册侧检查 8）——机制落地前已有 20+ 个 `done`/`doing` 任务未带该节，加检查即需历史豁免清单；若后续要加，先出豁免清单再落 ② **新规则即刻生效于下一就绪任务**：`T-L1-001.5` 的遗留面命中册内 **A2**（Skill 调 LLM 复用 `LlmClient.invoke` + `pick` 口径；归属 `T-L1-001`，解封条件含 `.5` / `.6`）——该任务 ④ 对齐时须点名，`.5` / `.6` 全 `done` 后方可销账 ③ 下一就绪 `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐（先跑「开工查遗留册」）

## [sc] 2026-09-26T13:56+08:00 — 遗留册接入工作流 PR 合入收口 · done

- **目标**：把遗留册接入工作流这批（工作流规则 + 模板节 + 检查 8 + 用例 + 留痕）走 PR squash 合入 main，完成全链路
- **对齐**：人指示「走分支 + PR 合入」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；分支 `docs/sc-legacy-intake`（本批无任务号，按规范 §3.2 的 T-ID 段用 `sc` 占位；type 取主导变更 `docs`）
- **关键动作**：单笔 `13c4ab0`（`pre-commit` 自检 Passed，未绕过）→ [PR #16](https://github.com/Dline6/ST-Agent/pull/16)（1 commit / 7 文件 / +135−5）经 `gh api --input` 的 UTF-8 JSON 载荷创建、**标题逐字符回读一致**（`len=49`，含 `（开工查 · 收工销）` 全角括号与间隔号）→ CI 双跑全绿（[36222022822](https://github.com/Dline6/ST-Agent/actions/runs/36222022822) 1m18s / [36222082350](https://github.com/Dline6/ST-Agent/actions/runs/36222082350) 1m20s；`mergeable: MERGEABLE`）→ 经 `gh api -X PUT …/pulls/16/merge`（`merge_method: squash` + 显式 `commit_title` / `commit_message`，保 `Task: sc` trailer 与署名行）→ squash `424592d` → 经 API 删远端分支 → 本地 `checkout main` + `fetch --prune` + `merge --ff-only` 快进至 `424592d`
- **分支清理的额外一步**：本次 PR 经 API 合入（非 `gh pr merge`），**本地分支不会被自动删除**；`git branch -d` 因 squash 与分支无祖先关系而拒绝删除，故先以 `git diff --quiet main docs/sc-legacy-intake` **证明二者树完全一致**（分支无独有提交）再 `-D`
- **变更文件**：项目管理/执行日志.md（+本条 + 上条验证行补 hash）
- **验证**：PR `state: MERGED`（合入 API 返回 `merged: true`）· 本地 `main` = `origin/main` = `424592d` · `git ls-remote --heads origin` 仅 `main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · main 上 pytest **518/518** · `verify_docs.py --strict` PASS（检查 8 = 0）· squash 提交体含 `Task: sc` trailer 与署名行（标题 49 字符以 UTF-8 解码核对一致）
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型
- **遗留**：① **`ccd_pr.bind_pr` 本次成功**——本机 `gh` 已装且 app 进程 PATH 已刷新（此前执行日志记为「待重启复验」），PR #16 已绑定本会话，app 内 PR 面板与「CI 失败自动唤醒」通道可用（`auto_fix` 仍 off，未改）· ② 本仓库仍禁用 GitHub auto-merge（既有结论未变，故合入仍为「等 CI 绿后手动 squash」）· ③ 下一就绪 `T-L1-001.5`（其遗留面命中册内 A2）/ `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐（先跑「开工查遗留册」）

## [T-L1-001.5] 2026-09-26T14:11+08:00 — 沙箱接线 SkillRunner 注入 · done

- **目标**：兑现 T-L1-001.3 遗留①②——把执行沙箱真正接进 `SkillRunner` 的一次执行（禁用 Skill 直接拦截、执行器拿到的 `ctx.gateway` / `ctx.llm` 为受限出口、经 `ctx.sandbox` 暴露受限会话供文件/命令出口核对），并补齐 LLM 出网路径（`SandboxedGateway.llm_transport` 结构性 API + `provider → host` 映射按 01 §7 条目形态落配置）
- **对齐**：人回「对」——④ 对齐结论：交付面 = GWT-W1–W4 + 回归；沙箱经 `SkillRunner(sandbox=…)` 可选参注入（`None` 时逐字节回归）；LLM 侧取行为性代理 `session.guard_llm(client)`（A3，因 .2 注入的 `LlmClient` 其 transport 构造期已绑真网关、runner 无法重建）；`provider → host` 映射按 [D-005](决策日志.md) 暂归 L1 沙箱；假设 A1–A6 见任务文件 `## 假设与前提`；遗留面命中 L0 册 A2（本批**不销账**）
- **输入**：T-L1-001.2 `SkillRunner.__init__` / `_execute` / `SkillContext` + `RUN_PREFIX`；T-L1-001.3 `SkillSandbox.session` / `SandboxSession` 三类出口 / `SandboxedGateway` / `GuardVerdict`；T-L0-004 `EgressGateway.execute`/`stream`/`online` + `llm_transport`；T-L0-003 `LlmClient.invoke` / `LlmEndpoint.provider` / `EndpointRegistry.get`；T-L0-001 `Store`；01 §5/§7/§10/§11 + 03 §1.2/§1.5
- **关键动作**：
  ① **映射子系统**：新增 `st_agent.l1.sandbox.provider_hosts.ProviderHostRegistry`（`register` / `update` / `resolve` / `get` / `list`），按 01 §7 `ConfigEntry` 七字段形态落 `config` 分区 `llm-provider-host/<provider>.json`（A6 / D-005）；`SkillSandbox` 增可选注入 `endpoints` / `provider_hosts`；提供方标识复刻端点标识「安全路径段」口径（防目录穿越），主机拒空白与 `/`
  ② **三类出口接线**：`_execute` 在描述体取到后即时查 `is_disabled` → 直接 `validation_failed`（**不解析依赖、不调执行器**）；执行前开 `sandbox.session(descriptor, approved, trace_id=…)`，`ctx.gateway` → `session.guard_gateway(...)`、`ctx.sandbox` → 会话本体
  ③ **LLM 出口（A3 行为性代理）**：`SandboxSession.guard_llm(client)` → `GuardedLlmClient`，在 `invoke` 边界按「端点 provider 的目标主机」核对 `net_access`，越界产单条 `error` 事件且**不调用底层 client**；主机无法解析（未配端点/提供方映射，或该提供方未登记）**fail-closed** 拒绝（沙箱是 09 导入第三方 Skill 的拦截基础，取保守侧）
  ④ **A4 结构性 API**：`SandboxedGateway.llm_transport(provider_hosts)`——签名与 L0 同款，核对后经受限 `stream` 发出；越界 / 未登记主机一律抛 `SandboxViolationError` 且不触碰底层 sender；runner 路径**不依赖**它
  ⑤ **配套**：`errors.py` 增 `ProviderHostError` / `ProviderHostExistsError` / `ProviderHostNotFoundError`；`sandbox/__init__.py` 出口同步
- **变更文件**：src/st_agent/l1/sandbox/provider_hosts.py（新）· src/st_agent/l1/sandbox/{sandbox,errors,__init__}.py · src/st_agent/l1/runner/runner.py · tests/l1/test_runner_sandbox.py（新，26 用例）· 项目管理/tasks/T-L1-001.5（doing→done，补 `## 接口面` / `## 可关闭的遗留` / `verify`）· 项目管理/{任务账本,执行日志}.md
- **验证**：pytest **545/545** 通过（既有 518 + 新 26 + `test_layering` 因新增 `provider_hosts.py` 多出 1 条参数化；live 2 deselected）· `verify_docs.py --strict` PASS（120 文件 / 1058 链接 / 0 断链 / 检查 5·6·7·8 全 0）· 账本 SYNC（就绪集 `T-L1-001.6` / `T-L2-001`）· 实测：禁用后执行器**零调用**且信封 `validation_failed`；`ctx.gateway` 出网 `evil.example.com` 被拦、spy sender 零调用、落盘留痕内**不含 `evil`**（A7）；`ctx.sandbox.read_file` 越界时 reader 零调用；provider 主机越界时 `RecordingLlm.calls == []`（不触碰底层）且 `invoke` 只产 `error` 事件；映射经 fresh `ProviderHostRegistry` 读回为七字段 `ConfigEntry`
- **假设复核**：A1 成立（`sandbox=None` 时 `ctx.sandbox is None` 且 `ctx.gateway` / `ctx.llm` 与传入对象同一）· A2 成立（禁用拦截在描述体取到后、执行器调用前；依赖被禁用时下游显式见 `dependency_failed`）· A3 成立（行为性代理落地，越界底零调用）· A4 成立（结构性 API 交付且 runner 不依赖）· A5 成立（`SkillContext` 增 `sandbox` 字段，01 契约未动）· A6 成立（映射暂归 L1 沙箱并按 01 §7 形态落盘；**未迁移**，留 `T-L1-006` 复核）
- **决策**：无新选型——映射归属与 LLM 出口形态由 [D-005](决策日志.md) 与假设 A3/A6 覆盖，沙箱注入沿用 `.4` 的「可选参 + None 即回归」先例。记此不另立 D-NNN
- **遗留**：① **`provider → host` 最终归属待复核**——本批按 D-005 A 案暂归 L1 沙箱，复核与可能的迁移归 `T-L1-006`；该转移项已由 [L0-遗留问题.md](遗留问题/L0-遗留问题.md) **B1**（解封条件含「复核 `provider_hosts` 是否迁回 L0」）与 D-005 承载，故**不新建条目、册内归属与解封条件不变** · ② **L0 册 A2 未销账**（解封条件＝父 `T-L1-001` 含 `.6` 全 `done`；本批已兑现其在 `.5` 侧的实现面）——待 `.6` 收工后销账 · ③ **旁证：`verify_docs.py` 检查 2 会写盘**——其 `rl.compute_status(tasks)` 对真实 `tasks/` 派生父状态并回写（本批即因此使 `T-L1-001` 被改写为 `doing`；值正确且文档明令父状态禁手写，故无害，但「跑自检会改真相源」值得知悉，未在本批处置）· ④ 下一就绪 `T-L1-001.6`（执行输出契约校验）/ `T-L2-001`（记忆图谱）均 P0，开工走 ④ 对齐（先跑「开工查遗留册」）

## [sc] 2026-09-26T14:19+08:00 — T-L1-001.5 PR 合入收口 · done

- **目标**：把沙箱接线批次（沙箱↔runner 接线 + `provider → host` 映射 + 用例 + 留痕）走 PR squash 合入 main，完成全链路
- **对齐**：人指示「走分支 + commit + PR 合入」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；分支 `feat/T-L1-001.5-sandbox-runner-wiring`（分支 type 取主导变更 `feat`，T-ID 绑 `T-L1-001.5`）
- **关键动作**：
  ① **提交**——单笔 `203987a`（9 文件 / +921−15；`pre-commit` 的「文档完整性自检」Passed，未绕过）
  ② **PR 开展**——[PR #17](https://github.com/Dline6/ST-Agent/pull/17) 经 `gh pr create` 创建（**标题逐字符回读一致**：`len=54`，含全角括号与 `→`），故无需再显式 `bind_pr`——`ccd_pr.get_status` 即报 `bound: true`，app 内 PR 面板通道正常
  ③ **CI 双跑全绿**——push 与 pull_request 两个 run 均 pass（[36223221936](https://github.com/Dline6/ST-Agent/actions/runs/36223221936) 1m22s / [36223232997](https://github.com/Dline6/ST-Agent/actions/runs/36223232997) 1m30s）
  ④ **合入与清理**——`gh pr merge 17 --squash --delete-branch`（显式 `--subject` / `--body-file`，保 `Task: T-L1-001.5` trailer 与署名行）→ squash `83ee6da` → gh 顺带切回 main 并删本地分支 → `fetch --prune` 清远端跟踪
- **变更文件**：项目管理/执行日志.md（+本条 + 上条 verify 补 hash）· 项目管理/tasks/T-L1-001.5（verify 补 PR + squash hash）
- **验证**：PR `state: MERGED`（`mergedAt 2026-09-26T06:18:09Z`）· 本地 `main` = `origin/main` = `83ee6da` · `git ls-remote --heads origin` 仅 `main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · main 上 pytest **545/545**（2 deselected）· `verify_docs.py --strict` PASS（120 文件 / 1060 链接 / 0 断链 / 检查 5–8 全 0 / 账本 SYNC）· squash 提交体含 `Task: T-L1-001.5` trailer 与 `Co-Authored-By` 署名行
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型
- **遗留**：① 本仓库仍禁用 GitHub auto-merge（既有结论未变），故合入仍为「等 CI 绿后手动 squash」· ② app 内 CI 监视仍为 off（`auto_fix` false）——PR #17 本次已绑定，但 CI 失败不会自动唤醒；如需可开 · ③ 下一就绪 `T-L1-001.6`（执行输出契约校验）/ `T-L2-001`（记忆图谱）均 P0，开工走 ④ 对齐（先跑「开工查遗留册」；`.6` 收工时须一并销 L0 册 A2）

## [T-L1-001.6] 2026-09-26T14:35+08:00 — 执行输出契约校验 · done

- **目标**：兑现 01 §2 的 `input_schema` / `output_schema` 执行期真校验——`SkillRunner` 在「执行成功 → 输出登记」之间按描述体 `output_schema` 核验 `ok` 载荷，不合契约即显式失败且**不登记**为可复用结果；同一校验件交付为可复用件，供 `T-L1-003` / `T-L1-004` / `T-ECO-002` 判「上游输出 → 下游输入」匹配，避免各层各造一套口径
- **对齐**：人回「对」，三处岔口均取推荐——**方言＝最小 JSON Schema 子集**（`type`/`properties`/`required`/`items`）· **01 §2 加方言澄清段**（触发工作流第 ⑦ 步）· **违规分支取 `failed` + `log_ref`**；假设 A1–A5 见任务文件 `## 假设与前提`；遗留面命中 L0 册 A2（解封条件＝`T-L1-001` 含 `.5`/`.6` 全 `done`，`.6` 为最后一个叶子，本批**关闭并销账**）
- **输入**：T-L1-001.1 `SkillRegistry.get` / `SkillDescriptor.input_schema`·`output_schema`（`capability_types.py:108`）+ 官方 Pack 12 个描述体形态（`pack.py`，均 `{"type": "object", "properties": {…}}` 无 `required`）；T-L1-001.2 `SkillRunner._execute` 执行器返回点与 `finish()` 登记点 + `REUSABLE_STATUSES`（`reuse/models.py`）；01 §2/§5；03 §1.2 步骤 5/6 + §3.1 连线校验
- **关键动作**：
  ① **第 ⑦ 步顺序**：先改 [01 §2](../docs/技术架构-v2/01-平台共享契约.md)（新增「方言澄清（2026-09-26 定案）」段 + `input_schema` 行说明改写）→ 同步 [03 §1.2](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 步骤 5/6（核验点 + 「未过核验不登记」）→ 最后改代码
  ② **可复用校验件**：新增 `src/st_agent/contracts/schema_check.py`——`check_payload`（递归下钻，`$.a[0].b` 定位 + 人可读说明）、`check_link`（下游 `required` 每项须由上游 `properties` 声明且类型兼容 + 两侧同名属性类型兼容；`integer → number` 放行）、`PayloadCheck` / `LinkCheck` / `SchemaViolation`；宽松面＝空 schema、未进 `required` 的可选属性缺省、子集外关键字（`oneOf`/`$ref`/`format`）、未知 `type` 取值——均不解析不报错
  ③ **runner 接线**：`_execute` 在执行器返回且类型合法后、`finish`（登记）**之前**插入核验——`status == "ok"` 时 `check_payload(descriptor.output_schema, envelope.data)`，不合契约 → `ResultEnvelope.failed(reason=违规点, log_ref=本次 skill-run 路径)`；该分支不在 `REUSABLE_STATUSES` 内，**天然不登记**（GWT-C1 下半，无需额外判断）
  ④ **导出**：`contracts/__init__.py` 增 7 个出口名（`check_payload` / `check_link` / `PayloadCheck` / `LinkCheck` / `SchemaViolation` / `SCHEMA_TYPES` / `DIALECT_KEYWORDS`），消费方只经 `st_agent.contracts`
- **变更文件**：docs/技术架构-v2/{01-平台共享契约,03-L1-能力底座-Skills与MCP}.md · src/st_agent/contracts/{schema_check,__init__}.py · src/st_agent/l1/runner/runner.py · tests/contracts/test_schema_check.py（新，33 用例）· tests/l1/test_output_contract.py（新，12 用例）· 项目管理/tasks/T-L1-001.6（doing→done，补 `## 接口面` / `## 可关闭的遗留` / `## 假设与前提` / `verify`）· 项目管理/决策日志.md（D-009）· 项目管理/遗留问题/L0-遗留问题.md（销 A2）· 项目管理/{任务账本,执行日志}.md
- **验证**：pytest **591/591** 通过（既有 545 + 新 45 + `test_layering` 因新增 `schema_check.py` 多出 1 条参数化；live 2 deselected）· `verify_docs.py --strict` PASS（120 文件 / 0 断链 / 检查 2 就绪集 `T-L2-001` · 检查 5·6·7·8 全 0）· 账本 SYNC · **实测**：缺必填（`{"other": 1}` 对 `required: [risk_level]`）→ 信封 `failed`、`reason` 含 `risk_level`、`log_ref` == `skill-run/<run_id>.json`，且 `OutputRegistry.get(run_id)` 抛 `OutputNotRegisteredError`（不登记）；类型不符（`{"risk_level": 3}`）→ `reason` 含 `string`；合规载荷 → `ok` 且登记可复用；`status=empty` 且 schema 声明 `required` → 仍 `empty`（不误伤）；`output_schema` 为空 → 任意载荷（含 `[1, {"a": 2}]`）照常 `ok` + 登记；违规上游 → 下游 `dependency_failed`；官方 Pack 描述体 + 既有执行器输出全绿（零迁移）
- **假设复核**：**A1 成立**（最小子集覆盖 Pack 全部 12 描述体，回归 591 全绿，零迁移）· **A2 成立**（`empty` 与空 schema 两路均不校验，实测见上）· **A3 成立**（`failed` + `log_ref` 为构造期不变量，实测 `log_ref` 指向本次 `skill-run` 记录）· **A4 成立**（消费方只经 `st_agent.contracts`，`test_layering` 通过）· **A5 成立**（`bool` 对 `number`/`integer` 反例用例通过）
- **决策**：[D-009](决策日志.md)（方言＝最小 JSON Schema 子集 + 校验件单一口径 + 01 §2 文档同步 + 六项否决理由），工作流第 ⑦ 步已走完（先 01 → 再 03 → 再代码）
- **遗留**：① **L0 册 A2 本批销账**（移入该册「已闭（备查）」并留一行，见上）· ② **新口径对下游是强制入口**——`T-L1-003`（连线校验）/ `T-L1-004`（Pack 装载）/ `T-ECO-002`（导入校验）一律经 `contracts.schema_check`，不得另造匹配规则；若下游确需更宽方言（`oneOf`/`$ref`/`format`），须**先改 01 §2 方言段再改本件**（第 ⑦ 步），不得在调用侧补规则 · ③ `check_link` 判据含「上游未声明输出面时按下游 `required` 报缺口」——上游 `properties` 缺位不进 `required` 的字段不判，故此判定**不会**为官方 Pack 之间（无 `required`）产生误报 · ④ 旁证：`verify_docs.py` 检查 2 会写盘（派生父状态回写 `tasks/`）——本批因此使 `T-L1-001` 由 `doing` 转 `done`（值正确、父状态禁手写，无害；未在本批处置）· ⑤ 下一就绪仅 `T-L2-001`（P0/M1）· ⑥ 待走 PR 收口（分支 + commit + squash 合入）

## [sc] 2026-09-26T14:44+08:00 — T-L1-001.6 PR 合入收口 · done

- **目标**：把执行输出契约校验批次（方言定案 + 可复用校验件 + runner 接线 + 用例 + 留痕 + 销 A2）走 PR squash 合入 main，完成全链路
- **对齐**：人先选「先等 CI，绿了再合」，随后指示「合入吧」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；分支 `feat/T-L1-001.6-output-contract-check`（分支 type 取主导变更 `feat`，T-ID 绑 `T-L1-001.6`）
- **关键动作**：
  ① **提交**——单笔 `54fc4bf`（13 文件 / +786−19；`pre-commit` 的「文档完整性自检」Passed，未绕过）
  ② **PR 开展**——[PR #18](https://github.com/Dline6/ST-Agent/pull/18) 经 `gh pr create` 创建（标题 = 提交 subject，含全角括号与 `+`）；`ccd_pr.get_status` 报 `bound: true`，app 内 PR 面板通道正常，无需显式 `bind_pr`
  ③ **CI 双跑全绿**——push 与 pull_request 两个 run 均 pass（[36224361199](https://github.com/Dline6/ST-Agent/actions/runs/36224361199) 1m26s / [36224371279](https://github.com/Dline6/ST-Agent/actions/runs/36224371279) 1m25s）；`gh pr checks 18` 两条均 `pass`
  ④ **合入与清理**——`gh pr merge 18 --squash --delete-branch`（显式 `--subject` / `--body-file`，保 `Task: T-L1-001.6` trailer 与署名行）→ squash `31f316e`；**首次调用因 `TLS handshake timeout` 失败且未产生副作用**（`gh pr view` 复核仍 `OPEN`，故直接重试）→ 重试成功；`git fetch --prune` 清远端跟踪
- **变更文件**：项目管理/执行日志.md（+本条）· 项目管理/tasks/T-L1-001.6（verify 补 PR + squash hash）· 项目管理/任务账本.md（render 刷新）
- **验证**：**PR 元数据读回受阻**——`gh pr view` / `gh api` 三次均 `TLS handshake timeout`（GitHub API 此刻不可达），故合入证据改由 git 侧确证：本地 `main` = `origin/main` = `31f316e`（提交 subject 即本批标题）· `git ls-remote --heads origin` 仅 `main`（远端分支已删）· `git branch -a` 仅 `main` + `origin/main`（跟踪已 prune）· 工作区干净 · main 上 pytest **591/591**（2 deselected）· `verify_docs.py --strict` PASS · squash 提交体含 `Task: T-L1-001.6` trailer 与 `Co-Authored-By` 署名行（`git log -1 --pretty=%B` 复核）
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型（方言口径与校验件归属由 [D-009](决策日志.md) 覆盖）
- **遗留**：① **GitHub API 此刻不可达**——PR 页面的 `MERGED` 状态与 `mergedAt` 未取回，如需归档证据可在网络恢复后补一次读回；合入事实不受影响（git 侧已确证）② 本仓库仍禁用 GitHub auto-merge（既有结论未变），合入仍为「等 CI 绿后手动 squash」③ app 内 CI 监视仍为 off（`auto_fix` false）——PR #18 本次已绑定，但 CI 失败不会自动唤醒；如需可开 ④ 上条「下一就绪仅 `T-L2-001`」一说不确——`T-L1-001` 父任务 `done` 后 `T-L1-002`/`003`/`004`/`005`/`006` 与 `T-L4-001` 一并解锁，**就绪集现为 7 条**；按调度规则（P0 → 层号升序 → id 字典序）队首＝`T-L1-002`（MCP Hub，P0/M0/L1），开工走 ④ 对齐（先跑「开工查遗留册」——L0 册 A1 归属即 `T-L1-002`）
