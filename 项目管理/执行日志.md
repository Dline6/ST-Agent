---
title: 执行日志
type: log
mode: append-only
---

# 执行日志（append-only · 每次任务做了什么）

> 规则：任务 `status→done` 前必在此追加一条（宪法第 10 条）。标题格式固定 `## [T-<id>] <ISO时间> — <短标题> · <status>`，便于 `grep "^## \[T-"`。每条必含 **验证** 行与 **假设复核** 行（对齐时任务文件 `## 假设与前提` 的 A<n> 逐条结论：成立 / 已推翻→联动未决或新任务编号）。遗留若产生新任务，同步登记 [任务账本.md](任务账本.md) 并建 `tasks/` 文件。超 500 条按年归档至 `执行日志/`。
>
> 下方为**格式示例（非真实条目，可删）**，展示一条合格记录长什么样：

```markdown
## [T-L2-003.1] 2026-09-24T22:30+08:00 — 契约类型定义 · done

- **目标**：为 Memory Graph 节点写入定义 shared contract 类型
- **对齐**：已与人确认——复用 stock_id、契约置于 shared/（假设 A1–A2 见任务文件 `## 假设与前提`）
- **输入**：story-03 §验收 GWT-1..5；01-平台共享契约 §1 / §4 / §5
- **关键动作**：编码 shared/contracts/memory-node.ts；补测试 memory-node.spec.ts
- **变更文件**：memory-node.ts (+124) · memory-node.spec.ts (+201)
- **验证**：pytest 22/22 · 断链 0 · commit `a3f2b9c`
- **假设复核**：A1 成立（复用 stock_id 无冲突）· A2 成立（shared/ 契约无循环依赖）
- **决策**：契约类型放 shared/ 不放 L2 私有（见 决策日志 D-007）
- **遗留**：GWT-4 边界值需真实 L2 存储接入 → 新增 T-L2-003.2.1
```

---

<!-- 真实条目从此行下方按时间顺序追加 -->

## [sc] 2026-09-24T23:43+08:00 — 仓库初始化与脚手架落地 · done

- **目标**：按 [Git协作规范.md](Git协作规范.md) 建立 Git 仓库——`git init` + `.gitignore` + pre-commit 卡点 + 首次提交，同时补齐规范要求的根 `README.md`
- **对齐**：已与人确认三问——执行全套脚手架（含 README / pre-commit）；规范中的 `AGENTS.md` 统一改为 `CLAUDE.md`（不新建 AGENTS.md，本仓库实际入口即此名）；Git 身份用 `qianq` + 占位邮箱 `qianq@localhost`，正式远端前由人替换
- **输入**：Git协作规范 §1–§2 §5.1 §8；工程宪法第 9 / 10 / 11 条；决策日志 D-002（脚手架留待正式启动时执行 → 本次兑现）
- **关键动作**：`git init -b main` 并配仓库级 config（pull.rebase / autostash / log.date iso8601）；写入 `.gitignore`（数据主权红线）· `.pre-commit-config.yaml`（`verify_docs.py --strict`）· 根 `README.md`（面向人类总览）；把 6 处 `AGENTS.md` 引用改为 `CLAUDE.md`（含 verify_docs 的 `OPS_DOCS` 种子清单）
- **变更文件**：新增 .gitignore / .pre-commit-config.yaml / README.md · 修订 CLAUDE.md · 项目管理/{Git协作规范,README,决策日志,AI-Coding项目管理方案}.md · tools/verify_docs.py
- **验证**：`verify_docs.py --strict` PASS ✅（96 文件 / 803 链接 / **0 断链**，修复了 `项目管理/README.md` 与 `决策日志.md` 两处指向 `../AGENTS.md` 的死链）· 账本 SYNC（活跃 33 / 就绪 1，无任务状态改动故未 render）· `git log --grep "Task: sc"` 命中 `010649c`（106 files / +8101）· 敏感文件扫描 0 命中
- **决策**：入口文件命名统一为 `CLAUDE.md`、脚手架一次性落地（见 决策日志 D-003）
- **遗留**：① 未建远端（`git remote add origin` 待人给仓库地址；§3.3 要求 solo 也建 remote 做备份）；② 未 `pre-commit install`（环境无 pre-commit 包，需 `pip install pre-commit --break-system-packages` 后于本地安装）；③ `user.email` 为占位值，推送前须替换真实邮箱（仓库级配置，勿用 `--amend` 改写历史以外的方式绕过）；④ 下一就绪任务仍为 `T-SC-001`，开工走 ④ 对齐

## [sc] 2026-09-24T23:55+08:00 — Git 身份落定与规范 §8 勘误 · done

- **目标**：把仓库提交身份从占位值换为负责人真实身份；处理上一条遗留 ③ 与 §8 勘误
- **对齐**：人已给出身份 `Dline6 <dline6awsd@163.com>` 并指示删除 §8 中 `commit.cleanup verbose` 一行（本机 Git 2.34 实测非合法值，接受 `strip / whitespace / verbatim / default`）
- **关键动作**：`git config user.name/user.email` 仓库级更新；`--unset commit.cleanup` 撤销先前设的 default；`Git协作规范.md` §8 删除该行
- **变更文件**：项目管理/Git协作规范.md (-1) · 项目管理/执行日志.md (+本条)
- **验证**：`git var GIT_COMMITTER_IDENT` 输出 `Dline6 <dline6awsd@163.com>` · `verify_docs.py --strict` 复检
- **遗留**：既有两条 commit（`010649c` / `5bf9bd7`）作者仍为占位 `qianq <qianq@localhost>`；无远端，如需统一作者历史可 rebase 重写，但会改变 hash、连带更新本日志与 D-003 的引用——默认保留不改

## [T-SC-001] 2026-09-25T11:30+08:00 — 平台共享契约核心类型落地 · done

- **目标**：把 01-平台共享契约的可代码化类型实现为全平台唯一可 import 的契约真相源（`src/st_agent/contracts/`），L0–L6 只 import 不另立
- **对齐**：已与人确认三问——技术栈 Python + pydantic v2（本机已装 2.12.5）；本轮一次完成全部 5 个子任务；pre-commit 顺手装上启用（兑现既有遗留②）；其余 4 个子任务依赖序 .1→.2/.3/.4/.5，各自独立 commit + 日志
- **输入**：任务文件 5 条 GWT + 拆分预判；01-平台共享契约 §1–§11；10-platform-capabilities 契约 1–9；数据库设计 §05（as_of/水位口径）；story-02/04/06（中性词表、参数体系）；09 §5（provenance）
- **关键动作**：
  ③ 拆分——按预判建 5 个叶子任务；与预判差异：§2/§3（SkillDescriptor/LensOpinion）原清单未覆盖并入 .4，§11 事件信封并入 .5；`render_ledger.py` 就绪集修正为只含叶子任务（工作流规定）
  ⑤ 实现——`.1` 12 类 ID frozen 值对象（id_kind 判别防串用；stock_id 走 BaoStock 交易所格式、拒绝本地随机生成）+ ResultEnvelope 六 status 构造期不变量；`.2` Trace append-only（frozen + tuple + append_step 唯一写入口 + 封链语义 + append_degraded 降级留痕 + sha256 摘要）；`.3` NeutralityGuard 三强制执行点（RulePack 可换、API 形状无 bypass 开关；词表 = PRD 明文禁止项 + 人名结构规则）；`.4` SkillDescriptor（imported 强制完整 provenance；name 中性校验回调注入）/ LensOpinion（stance 含 insufficient-data 失败隔离）/ ConfigEntry 七字段 / SemVer 主次兼容判定 / §10 权限语法校验；`.5` DataAnchor（snapshot_id + as_of + 六数据域，时区强制）/ StalenessVerdict / PlatformEvent（10 事件名，构造期强制 trace_id/change_id 关联）
- **变更文件**：src/st_agent/contracts/{__init__,errors,identifiers,result_envelope,trace,neutrality,capability_types,registry_types,time_events}.py（9 文件）· tests/contracts/ 4 个测试文件 · pyproject.toml · tools/render_ledger.py（就绪集叶子过滤）· tasks/T-SC-001{,.1–.5}*.md（6 文件）
- **验证**：pytest **107/107** 通过（.1→37 · .2→46 · .3→68 · .4→95 · .5→107 递增口径）· `verify_docs.py --strict` PASS（101 文件 / 830+ 链接 / 0 断链）· 账本 render 后 SYNC · 就绪集正确推进至 T-L0-001 · 父任务 T-SC-001 由 children 派生 done · pre-commit 钩子实战拦截 2 次账本未同步的提交后放行 · commits：拆分 `d4ba69e` + c8e936f / df70513 / 744467b / e66e767 / 49c3e15
- **决策**：实现选型 pydantic v2 frozen 模型（值对象不可变 + 构造期校验，免手写校验层）；序列化格式定为 JSON 兼容（契约文档不锁定方言，本选型记此不另立 D-NNN）；中性化词表注入采用依赖倒置（契约包不依赖具体词表实现）
- **遗留**：① 三个 API 待 L0/L1 首个消费方回收反馈——`PlatformId.of()` 的前缀正则对 `skill_id` 含版本后缀形态（`sk_xxx_v2`）需在 T-L1-001 注册流程中定义拼接规则；`EvidenceRef.kind` 与 LensOpinion.evidence_refs（裸字符串）双轨，T-L4-002 时统一；`DataAnchor.is_stale` 通用超龄判定与 sync_state 自检 SQL 的整合在 T-L0-005。② pre-commit 已装但 `verify_docs` 每次全量扫描 101 文件约 1–2s，可接受；文件数过 300 时考虑缓存。③ 远端仍未建（遗留①未变），本分支仅本地，PR 流程按规范待远端就绪后补走。

## [sc] 2026-09-25T12:10+08:00 — 远端建立与 T-SC-001 PR 合入收口 · done

- **目标**：兑现上条遗留③——建 GitHub 远端、推送、按规范 §5.2 gate 走 PR squash 合入，完成 T-SC-001 全链路
- **对齐**：人在 `.env`（不入库，.gitignore 红线）配好 `GITHUB_PAT`（scope=repo）与 `GIT_REMOTE_URL` 后指示继续走完整流程
- **关键动作**：API 验证 PAT 与空仓库 `Dline6/ST-Agent` → `git remote add origin` → 推 `main` + `feat/T-SC-001-contract-types` → 建 [PR #1](https://github.com/Dline6/ST-Agent/pull/1) → §5.2 gate（strict PASS / pytest 107 / 就绪集 T-L0-001）→ squash 合入 `acb774f` → 删远端+本地任务分支 → main 与 origin 同步
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：`git ls-remote` 仅剩 `refs/heads/main @ acb774f` · main 上 strict PASS + pytest 107/107 · `git status` 与 origin/main 零偏移 · squash 提交含 `Task: T-SC-001` trailer（`git log --grep` 可回溯）
- **遗留**：① 遗留兑现，`.env` 凭据流转仅经本机 git/curl，未落 `.git/config`（推送用 stdin 临时凭据）；② PAT 建议后续换 fine-grained 最小权限（当前 classic `repo`）；③ 下一就绪任务 `T-L0-001 存储分区落盘加密`，开工走 ④ 对齐

## [T-L0-001] 2026-09-25T14:21+08:00 — 存储分区落盘加密与损坏恢复 · done

- **目标**：实现 02-L0 §2 存储子系统——七分区注册与目录布局、全分区落盘加密、分区级完整性校验与损坏恢复
- **对齐**：已与人确认——④ 对齐结论：PBKDF2（600_000 迭代）+ AES-256-GCM 每分区独立密钥；`secrets` 独立 salt 隔离；每分区加密清单（SHA-256）；每分区一个目录、对写/清空独立；`data_cache` 按 opaque blob 存 SQLite；损坏定位分区级 + 冷启动重建（备份恢复在 T-L0-006）
- **输入**：story-05 验收段 8 条（本任务覆盖分区/加密/损坏 3 组 + 零接触铁律）；02 §2.1–§2.3；01 §5（失败显式化——损坏走 StorageCorruptionError 显式载体）
- **关键动作**：`partition.py`（七分区注册 + 防路径注入）· `crypto.py`（PBKDF2 双 salt + AES-256-GCM + verifier 锚）· `manifest.py`（加密清单 + SHA-256 校验）· `store.py`（Store 门面：create/open/put/get/delete/export/wipe/reset_partition/storage_report）· `tests/l0/test_storage.py`（37 用例对 5 条 GWT）；中途清掉 `verify_backup` 占位与 `keep_files` 死分支（T-L0-006 在分区级校验和口径上定义备份格式）
- **变更文件**：src/st_agent/l0/{__init__,storage/{__init__,crypto,errors,manifest,partition,store}}.py（7 文件）· tests/l0/test_storage.py · pyproject.toml（+cryptography>=42.0）· tasks/T-L0-001（5 条 GWT + doing→done）
- **验证**：pytest **144/144** 通过（contracts 107 + l0 37）· `verify_docs.py --strict` PASS（102 文件 / 831 链接 / 0 断链）· 盘上无明文抽查通过 · SQLite blob 往返通过 · commits：开工 `e4e042a` + 实现 `d01b686`
- **决策**：口令派生取 PBKDF2-HMAC-SHA256 / 600_000 迭代（OWASP 2023，02 §2.2 不锁定算法，安全评审可调）；`secrets` 用独立 salt 重走一遍 PBKDF2 而非 HKDF 子密钥——隔离强度等同主密钥重设；清单文件名固定 `manifest.bin` 且禁用户占用
- **遗留**：① T-L0-002（凭据掩码+使用记录）· T-L0-004（出网审计）· T-L0-005（data_cache 库表）· T-L0-006（备份/恢复/清空）均依赖本任务容器；② KDF 迭代数是性能敏感参数——600k 在本机约 0.3s/次，T-L0-002 登录态缓存会话密钥时复用同一口径

## [sc] 2026-09-25T14:45+08:00 — T-L0-001 PR 合入收口 · done

- **目标**：T-L0-001 走 PR squash 合入 main，完成全链路
- **对齐**：沿用 T-SC-001 既定流程（规范 §5.2 gate + squash 合入即删分支）
- **关键动作**：推 `feat/T-L0-001-storage-encryption` → 建 [PR #2](https://github.com/Dline6/ST-Agent/pull/2)（建 PR 时标题中文经 API 乱码，已 PATCH 修复）→ gate（strict PASS / pytest 144 / 就绪集 T-L0-002·T-L0-004·T-L0-006·T-L2-001）→ squash 合入 `28c61f4` → 删远端+本地任务分支 → main 与 origin 同步
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：`git ls-remote` 仅剩 `refs/heads/main @ 28c61f4` · main 上 strict PASS + pytest 144/144 · `git status` 干净
- **遗留**：下一就绪队首 `T-L0-002 密钥与凭据子系统`，开工走 ④ 对齐

## [T-L0-002] 2026-09-25T15:30+08:00 — 密钥与凭据子系统（掩码显示 + 使用记录） · done

- **目标**：实现 02-L0 §3——敏感凭据加密存 `secrets` 分区、默认掩码可见、取用留痕可查、支持吊销重设、凭据明文零泄漏
- **对齐**：已与人确认（④ 对齐结论：`CredentialVault` 经 `Store` 操作 `secrets` 分区、每凭据一文件；掩码尾部最多 4 位、短凭据全掩码；使用记录存 `execution_log` 分区；失败走 01 §5 显式错误；不改 01 契约）——人回「对」后置 `doing` 开工
- **输入**：story-05 验收段 9 条（本任务覆盖 Key 配置/使用记录 2 组 + 掩码组件 + 零接触铁律）；02 §3 全节；01 §5（失败显式化）/ §8（时间带时区）；T-L0-001 的 `Store` 门面（`secrets` 独立派生密钥、`execution_log` 分区容器）
- **关键动作**：`errors.py`（四错误类型，失败显式化）· `masking.py`（掩码单规则：≤4 全掩、>4 留尾 4）· `models.py`（`CredentialRecord` 内部含明文但 repr/str 强制掩码 + `CredentialView` 对外无 value 字段 + `CredentialUsageRecord` 时刻/发起方/目的/数据量/结果）· `vault.py`（`CredentialVault`：add/rotate/revoke/get_view/list/use/record_usage/query_usage/export_safe，唯一明文出口 `use()` 同步记 `ok` 记录；标识即分区内安全路径段）· `tests/l0/test_secrets.py`（28 用例对 5 条 GWT，含全盘 rglob 无明文扫描）
- **变更文件**：src/st_agent/l0/secrets/{__init__,errors,masking,models,vault}.py（5 文件）· tests/l0/test_secrets.py · tasks/T-L0-002（GWT 补全 + doing→done + frontmatter 补 parent/arch 字段）
- **验证**：pytest **172/172** 通过（contracts 107 + l0 存储 37 + 凭据 28）· `verify_docs.py --strict` 待收工 commit 前复检 · 全盘明文扫描通过 · commits：`7bebee1`（gitignore 收窄）+ `19c582e`（实现）
- **决策**：无新选型（掩码 last4 沿用 Stripe 式可辨识惯例；种类 `kind` 只做形状校验不做白名单，留扩展口；使用记录留存周期沿用 T-L0-006 口径，本任务不定默认值）——均记于对齐结论，不另立 D-NNN
- **遗留**：① 下一就绪 `T-L0-004 出网审计网关`（凭据的网络调用审计点在彼处兑现）· `T-L0-006` 备份时须用 `export_all_safe` 口径（含掩码视图+使用记录，禁明文）；② 本任务未动 01 契约与 02 架构文档，无需 ⑦ 同步

## [T-L0-003] 2026-09-25T16:30+08:00 — LLM 端点抽象（能力协商/流式/多端点/零中转/用量） · done

- **目标**：实现 02-L0 §4——多端点配置并存、调用前能力协商、统一流式协议、失败走 ResultEnvelope、用量本地可见、云端 Key 直连零中转零留存
- **对齐**：已与人确认（④ 对齐结论：EndpointRegistry 经 Store 读写 config 分区；Key 唯一走 CredentialVault.use()；用量记 execution_log 只含计数；用量明细归 execution_log、流式以同步迭代器起步；不改 01 契约）——人回「对」后置 `doing` 开工
- **输入**：story-05 验收段（Key 配置/网络活动/离线标注 3 组 + 零接触铁律）；02 §4 全节 + §6/§7（网关只声明 offline_level，本体在 T-L0-004）；01 §5（失败显式化）/ §8（时间带时区）；T-L0-001 的 Store 门面（config/execution_log 分区容器）+ T-L0-002 的 CredentialVault.use()
- **关键动作**：`errors.py`（配置面三异常；执行面一律走 ResultEnvelope 不抛）· `models.py`（EndpointCapability 供给档位 + CapabilityRequirement 需求档位 + LlmEndpoint 云端必凭据引用/本地禁凭据 + LlmUsageRecord 只记计数禁内容字段 + StreamEvent chunk/done/error 三态互斥 + estimate_tokens 每 4 字符 1 token）· `registry.py`（EndpointRegistry：register/replace/update_priority/remove/get/list/negotiate/pick，配置落 config 分区 llm-endpoint/ 前缀）· `client.py`（LlmClient.invoke：协商 → 取 Key → 流式 → 用量；超上下文/空 prompt/缺归属直接 validation_failed；无传输即 unavailable；取消/超时显式 error；用量成败必记，failed 带 log_ref；Key 内存引用即时释放）· `tests/l0/test_llm.py`（33 用例对 5 条 GWT，含全盘 rglob 无 prompt/response 明文扫描）
- **变更文件**：src/st_agent/l0/llm/{__init__,errors,models,registry,client}.py（5 文件）· tests/l0/test_llm.py · tasks/T-L0-003（GWT 补全 + doing→done + frontmatter 补 parent/arch/verify 字段）
- **验证**：pytest **205/205** 通过（contracts 107 + l0 存储 37 + 凭据 28 + LLM 33）· `verify_docs.py --strict` 待收工 commit 前复检 · 全盘 prompt/response 明文扫描通过 · commits：`07053a2`（实现）
- **决策**：无新选型（token 估算取每 4 字符 1 token 的本地展示口径，提供方账单以其实测为准；传输层以 callable 注入，T-L0-004 网关接入时替换；流式同步迭代器起步，异步形态留 L1 需要时再加）——均记于对齐结论，不另立 D-NNN
- **遗留**：① 实际网络发送待 T-L0-004 出网审计网关统一接入（当前无传输即 unavailable，测试经 transport 注入）；② T-L1-001 Skill 调用 LLM 时复用 LlmClient.invoke + pick 口径；③ 本任务未动 01 契约与 02 架构文档，无需 ⑦ 同步

## [sc] 2026-09-25T17:00+08:00 — T-L0-002 与 T-L0-003 PR 合入收口 · done

- **目标**：网络恢复后补走两个任务的推送 + PR squash 合入全链路，完成收口
- **对齐**：沿用 T-SC-001/T-L0-001 既定流程（规范 §5.2 gate + squash 合入即删分支）；本轮由人指示「尝试推送」后执行
- **关键动作**：推 `feat/T-L0-002-credential-vault` → 建 [PR #3](https://github.com/Dline6/ST-Agent/pull/3) → gate（strict PASS / pytest 205 / SYNC）→ squash 合入 `67941e7` → 删远端+本地分支（`-D`，squash 后未完全合并）→ main 同步；`feat/T-L0-003-llm-endpoint` 变基 `--onto main 46298eb`（剔除已合入的 T-L0-002 部分）→ gate（strict PASS / pytest 205 / SYNC）→ 建 [PR #4](https://github.com/Dline6/ST-Agent/pull/4) → squash 合入 `4a44cba` → 删分支 → main 同步
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：`git ls-remote` 仅剩 `refs/heads/main @ 4a44cba`（+ PR refs 残留）· main 上 `git status` 干净 · 推送凭据经 `.env` GITHUB_PAT 临时注入（env 变量 + credential helper），未落 `.git/config`、未进命令行历史
- **遗留**：① PAT 仍为 classic `repo` scope，建议后续换 fine-grained 最小权限（既有遗留未变）；② 下一就绪队首 `T-L0-004 出网审计网关 + 离线能力分级`，开工走 ④ 对齐

## [T-L0-004] 2026-09-25T15:40+08:00 — 出网审计网关 + 离线能力分级 · done

- **目标**：实现 02-L0 §6+§7——一切出网经统一网关执行并留痕（NetworkEvent），离线三级清单（full/degraded/none），LLM 真实发送经网关适配器接入（兑现 T-L0-003 遗留）
- **对齐**：已与人确认（人回「对」）——④ 对齐结论：审计记 `execution_log/net/` 前缀；补发执行归 L5，本任务仅提供待补发查询；暂不做 socket 级强制拦截，强制性靠唯一出口 + 测试约束；远程 MCP / 官方索引审计点留待 T-L1-002
- **输入**：story-05 验收段（网络活动透明化 / 离线模式指示 2 组）；02 §6–§7 全节；01 §5（失败显式化）/ §11（NetworkRequestLogged 事件）；T-L0-001 的 Store 门面 + T-L0-003 的 LlmClient transport 签名
- **关键动作**：`errors.py`（配置面三异常 + `Egress*` 执行异常体系）· `models.py`（NetworkEvent 六字段 frozen + purpose 内容词黑名单 + CapabilityRecord 复用契约 OfflineLevel + OfflineReport 三类清单）· `gateway.py`（EgressGateway：execute/stream/query/export_report/pending_reconnect/能力登记/llm_transport 适配器）· `tests/l0/test_net.py`（33 用例对 5 条 GWT）；中途修复 `_check_request` 探针缺 `bytes_in`、`detail` 误扫黑名单、适配器缺 `Egress*→Transport*` 翻译三处缺陷
- **变更文件**：src/st_agent/l0/net/{__init__,errors,models,gateway}.py（4 文件）· tests/l0/test_net.py · tasks/T-L0-004（GWT 补全 + doing→done）
- **验证**：pytest **238/238** 通过（既有 205 + 网关 33）· `verify_docs.py --strict` PASS（102 文件 / 834 链接 / 0 断链）· 全盘 prompt/response/key 明文扫描通过 · commits：`45bfd20`（doing+GWT）+ `2d9292c`（实现）
- **决策**：两处选型记此不另立 D-NNN——① `detail` 字段不扫内容词黑名单（发包异常原文可含 value 等子串，零泄漏靠网关签名只收字节数保证）；② `llm_transport` 内把 `Egress*` 译为 `Transport*` 体系（LlmClient 只认后者）
- **遗留**：① 远程 MCP / 官方索引浏览的审计调用点待 T-L1-002 接入；② 补发执行归 L5（本任务仅 `pending_reconnect` 查询口）；③ 下一就绪 `T-L0-006` / `T-L1-001` / `T-L2-001`

## [sc] 2026-09-25T15:55+08:00 — T-L0-004 PR 合入收口 · done

- **目标**：T-L0-004 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）
- **关键动作**：推 `feat/T-L0-004-egress-gateway` → 建 [PR #5](https://github.com/Dline6/ST-Agent/pull/5)（建 PR 时标题中文经 API 乱码，已 PATCH 修复）→ gate（strict PASS / pytest 238 / SYNC）→ squash 合入 `d478789` → 删远端+本地分支 → main 与 origin 同步
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：`git ls-remote` 远端 main @ `d478789` · main 上 strict PASS + pytest 238/238 · `git status` 干净
- **遗留**：下一就绪 `T-L0-006` / `T-L1-001` / `T-L2-001`，开工走 ④ 对齐

## [T-L0-005] 2026-09-25T16:30+08:00 — 数据源缓存 + BaoStock 首个源落地 · done

- **目标**：实现 02-L0 §5——`data_cache` 分区内 SQLite 单库（schema.sql 全量表 + 视图）+ `sync_state`/`data_source` 水位元数据 + 新鲜度自检 + 抓取经网关 `data_fetch`
- **对齐**：已与人确认（人回「对，按此开工」）——④ 对齐结论：`MarketDb` 经 `Store` 读写 `data_cache/market.db` 加密 blob（工作拷贝 + 写回）；fetcher 可注入（`baostock` 包不内置，测试不碰真网）；清洗 / 复权 / 完整性按 DB 文档；快照锚定 `sync_state` 水位组合；分钟线默认禁用；开关状态不落库（调用方传入）
- **输入**：story-05 验收段（首次联网建缓存 / 离线可查 / 数据源开关 3 组）；02 §5 全节 + §6（抓取唯一出口）；01 §5（失败显式化）/ §8（`dataset_snapshot_id`/`as_of` 口径）；数据库设计族 00/01–06 + schema.sql；T-L0-001 的 `Store` 门面 + T-L0-004 的 `EgressGateway`
- **关键动作**：`errors.py`（配置面 `MarketValidationError` + 抓取器 `Fetch*` 异常体系）· `cleaning.py`（空串→NULL、`—` 负号、`或`多值取首值、分钟线时间归一）· `db.py`（`MarketDb`：`init_db`/`transact` 写回/`query` 只读白名单/`freshness`/`as_of`/`snapshot_id`）· `tasks.py`（20 任务注册 + 水位语义 + `window_for` 窗口推导）· `fetch.py`（`Fetcher` 协议 + 06 全量映射：K 线三表/复权因子/分红/`mortgate` 拼写修正/财务六接口合并 `pub_date` 取最大/快报预告/板块快照/宏观五表）· `sync.py`（`BaoStockSync`：`setup` 种子/`run_task` 前置→窗口→网关抓取→入库→水位→校验/`run_all` 初始化顺序/`freshness_verdict`→`StalenessVerdict`）· 网关加按次 `sender` 注入（T-L0-004 GWT-1 预留形态，审计仍唯一）；`schema.sql` 包内副本与文档族同内容；`tests/l0/test_market.py`（39 用例对 5 条 GWT，Fake 注入不碰真网）
- **变更文件**：src/st_agent/l0/market/{__init__,errors,cleaning,db,tasks,fetch,sync}.py + schema.sql（8 文件）· src/st_agent/l0/net/gateway.py（按次 sender 注入）· tests/l0/test_market.py · tasks/T-L0-005（GWT 补全 + doing→done）
- **验证**：pytest **277/277** 通过（既有 238 + 市场 39）· `verify_docs.py --strict` 待收工 commit 前复检 · 盘上库 blob 无建表明文抽查通过 · 审计无行内容（「浦发银行」只在加密 blob）通过 · commits：待收工
- **决策**：两处选型记此不另立 D-NNN——① 网关按次 `sender` 注入而非为同步另立出口（02 §6 唯一出口纪律；构造时 sender 缺省语义不变）；② `MarketDb.transact` 提交后无条件写回（单文件 blob 语义下「提交即落盘」，回滚由下次同步幂等 UPSERT 自愈）
- **遗留**：① T-L0-001 遗留①部分兑现（`data_cache` 库表已落地，`is_stale` 通用判定仍由上层按 `freshness_verdict` 消费）；② T-L1-004 官方 Skill Pack 的行情/财务查询面经 `MarketDb.query` 进入；③ `baostock` 真实抓取器封装待管道联调时注入（协议已定）；④ 下一就绪 `T-L0-006` / `T-L1-001` / `T-L2-001`

## [sc] 2026-09-25T17:40+08:00 — T-L0-005 PR 合入收口 · done

- **目标**：T-L0-005 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；本轮由人指示「推送分支走 PR 合入」后执行；中途 git 协议 443 不通，API 通道走通
- **关键动作**：推 `feat/T-L0-005-baostock-cache` → 建 [PR #6](https://github.com/Dline6/ST-Agent/pull/6)（`gh` 缺失改 API 建 PR；标题中文经 API 乱码，已 PATCH 修复——与 PR #2/#5 同一已知问题）→ gate（strict PASS / pytest 277 / SYNC）→ squash 合入 `3a55f23`（API 确认 merged True）→ 删远端分支（API 422 即已无残留）→ 本地 `git fetch` 不通，改 `merge --ff-only bf0933e` 快进本地 main（内容与 squash 同源 13 文件；`ahead 1` 系 hash 差异，网络恢复后 pull 自动收敛）→ 删本地任务分支 → 登记本条
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：API 查 PR state=closed/merged=True · 远端 main @ `3a55f23` · 本地 main 上 strict PASS + pytest 277/277 + SYNC · `git branch --list` 仅剩 main
- **遗留**：① 本地 main `ahead 1`（`bf0933e` vs 远端 squash `3a55f23` 内容同源），网络恢复后 `git pull --ff-only` 收敛；② PAT 仍为 classic `repo` scope（既有遗留未变）；③ 下一就绪队首 `T-L0-006 备份/恢复/清空`，开工走 ④ 对齐

## [T-L0-005.1] 2026-09-25T18:30+08:00 — BaoStock 真实抓取器接入与首次真实入库 · done

- **目标**：兑现 T-L0-005 遗留③——按 `Fetcher` 协议封装真实 `baostock` 抓取器（`login → 批量 query → logout`），跑通首次真实入库（元数据 + 单证券行情），抓取经网关审计
- **对齐**：任务由人指示「帮我真实入库」后立项（T-L0-005.1，依赖 T-L0-005）；GWT 三条（登录拉取 / 行情样本 / 失败可解释）立任务时已定，人回「请开始」「继续」后开工
- **输入**：`Fetcher` 协议（`fetch.py` 19 方法签名）；BaoStock-API 手册 04/07/11/12（K 线/财务/宏观/板块参数）；T-L0-005 的 `BaoStockSync`/`MarketDb`/`EgressGateway` 按次 sender 注入点
- **关键动作**：`live.py`（`BaoStockFetcher`：上下文管理登录登出 + `_drain` 结果集 + `error_code` 非 0 → `FetchError` + K 线固定 `adjustflag=3` + 准备金 `yearType=0` + 缺包构造即 `FetchUnavailableError`）；`sync.py` 修未知任务裸抛（`get_task` 包进 `validation_failed`，GWT-3 缺口）；`__init__.py` 出口 `BaoStockFetcher`；`pyproject.toml` 加 `baostock>=0.9.4`；`test_market.py` 补 `TestLiveFetcherOffline`（未知任务/缺包/协议对齐 3 用例，不碰真网）；真实入库在 `tmp/live-market`（`.gitignore` 覆盖，不入库）跑通后清理
- **变更文件**：src/st_agent/l0/market/live.py（新增）· src/st_agent/l0/market/{__init__,sync}.py · pyproject.toml · tests/l0/test_market.py（+3）· tasks/T-L0-005.1（新建 + doing→done）
- **验证**：pytest **280/280** 通过 · 真实入库：`security` 8981 行 + `trade_calendar` 8733 交易日 + `sh.600000` 6515 根日线（水位 `2026-09-24`，`as_of kline` 同值）+ 网关 4 条 `data_fetch`/`baostock` 审计 · `verify_docs.py --strict` 待收工 commit 前复检 · commits：待收工
- **决策**：无新选型（`baostock` 标联网可选依赖，缺失走 `unavailable`；真实库只落 `tmp/` 不入库；`pandas` 不引入，逐行 `get_row_data` 取数）——均记于任务备注，不另立 D-NNN
- **遗留**：① `freshness_verdict("kline").stale=True` 预期内（真实库仅 1 只证券，未跑全量，`bs_k_period` 等任务未执行）；② 全量首次同步（全市场 1990 年起）体量大，需单独排期跑 `run_all`；③ 下一就绪 `T-L0-006` / `T-L1-001` / `T-L2-001`

## [sc] 2026-09-25T19:00+08:00 — T-L0-005.1 PR 合入收口 · done

- **目标**：T-L0-005.1 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；本轮由人指示「继续」后执行；git 协议已恢复，全程走通无绕路
- **关键动作**：推 `feat/T-L0-005-1-live-fetcher` → 建 [PR #7](https://github.com/Dline6/ST-Agent/pull/7)（API 建 PR + PATCH 中文标题，同一已知乱码问题）→ gate（strict PASS / pytest 280 / SYNC）→ squash 合入 `03bb048` → `git push --delete` 删远端分支 → `checkout main + pull --ff-only` 干净快进 → `-D` 删本地分支 → 登记本条
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：`git ls-remote` 仅剩 `refs/heads/main @ 03bb048` · main 上 strict PASS + pytest 280/280 + SYNC · `git branch --list` 仅剩 main · 与 origin/main 零偏移
- **遗留**：① 下一就绪队首 `T-L0-006 备份/恢复/清空`，开工走 ④ 对齐；② 全量首次真实同步（`run_all` 全市场）体量大，待排期

## [sc] 2026-09-25T19:30+08:00 — 任务假设记录机制落地 · done

- **目标**：补上"每个任务必有、可校验、可复核"的假设留痕机制（此前假设只散落在执行日志对齐散句里）
- **对齐**：人指示「请开始」（④ 对齐结论：任务文件正文节而非 frontmatter；历史 done 不追补；todo 对齐时填、verify 只卡 doing/blocked/done）——假设见本条 A1–A2
- **关键动作**：工作流 ④/⑥/任务文件字段三处细化（`三块→四块` + 对齐落两处 + 收工含假设复核）；执行日志规则 + 格式示例加 `假设复核` 行；`render_ledger.py` bootstrap 模板加节；`verify_docs.py` 加第 [5] 项假设完整检查（doing/blocked/done 叶子必有实填节，`--strict` 下判失败；机制前 12 个 done 豁免）
- **变更文件**：项目管理/{工作流,执行日志}.md · tools/{render_ledger,verify_docs}.py
- **验证**：`verify_docs.py --strict` PASS ✅（103 文件 / 842 链接 / 0 断链 / 假设 0 缺失）· 合成测试：缺节 doing 任务被 [5] 检出，实填放行 · 账本 SYNC（未改派生逻辑）· 测试残留文件已清理
- **假设复核**：A1 成立（正文节方案：单行 frontmatter 解析器不动，verify 用正文正则即可）· A2 成立（12 个历史 done 列入 `GRANDFATHERED_NO_ASSUME`，不追补）
- **遗留**：下一就绪任务开工即按新机制跑 ④ 对齐（假设编号进任务文件）

## [T-L0-006] 2026-09-25T20:10+08:00 — 备份 / 恢复 / 完全清空 + 数据留存策略 · done

- **目标**：实现 02-L0 §8–§9——一键备份单加密归档、任意设备导入恢复、三次确认完全清空、对话历史与执行日志留存可配；闭环 T-L0-001 的损坏恢复（有备份可恢复）
- **对齐**：人回「对，按此开工」+ 备份范围「仅四分区」+ 脏工作区「先提交再开工」（④ 对齐结论：范围严格按 02 §8.1 四分区 + 可选 data_cache，secrets/execution_log 永不进归档；归档独立 salt 走 PBKDF2；恢复覆盖写；清空三令牌；留存默认 365/90 天；假设 A1–A7 见任务文件）
- **输入**：story-05 验收段（备份/恢复/清空 3 组 + 留存可配 + 损坏联动）；02 §8–§9 全节；01 §7（留存旋钮 ConfigEntry）/ §8（时间带时区）；T-L0-001 的 Store 门面（分区导出/覆盖/清空 + 分区级校验和口径）
- **关键动作**：`errors.py`（五错误类型：密码错/损坏/版本/留存/确认不足，失败显式化）· `models.py`（BackupManifest 归档清单 + RestoreReport/WipeReport + RetentionPolicy + retention_config_entries 01 §7 双通道条目）· `backup.py`（create_backup 四分区导出 + 独立 salt 加密归档 / _open_archive 密码→版本→分区表→逐文件校验 / restore_backup 覆盖写 + 损坏目标内部解锁联动 / wipe_all 三令牌删整目录 / get/set/apply_retention 留存读写清）· `store.py` 加 `sealed_mtime` 公共口径（留存年龄走落盘密文 mtime）· `tests/l0/test_backup.py`（35 用例对 5 条 GWT，含全盘密码明文扫描 + 无网络 import 断言）
- **变更文件**：src/st_agent/l0/backup/{__init__,errors,models,backup}.py（4 文件，684 行）· src/st_agent/l0/storage/store.py（+sealed_mtime）· tests/l0/test_backup.py（35 用例）· tasks/T-L0-006（GWT 补全 + doing→done）
- **验证**：pytest **315/315** 通过（既有 280 + 备份 35）· `verify_docs.py --strict` 待收工 commit 前复检 · 盘上归档无明文抽查通过 · 全盘密码明文扫描通过 · commits：待收工
- **假设复核**：A1 成立（四分区口径：恢复报告分区表断言通过）· A2 成立（独立 salt 归档加密：无明文 + 错密码拒绝通过）· A3 成立（v1 自描述：篡改/版本不兼容拒绝且不写脏通过）· A4 成立（覆盖写 + 损坏目标联动：三类恢复测试通过）· A5 成立（三令牌清空：6 组确认不足拒绝 + 无残留通过）· A6 成立（留存 365/90 默认显式 + 边界当天不删次日删通过）· A7 成立（零接触：无网络 import + 全盘密码扫描通过）
- **决策**：无新选型（归档 KDF 口径沿用 T-L0-001 PBKDF2/600k；iterations 进归档外层元数据供前向兼容；ConfigEntry change_policy 取需确认+记历史+可回滚）——均记于对齐结论，不另立 D-NNN
- **遗留**：① 下一就绪 `T-L1-001` / `T-L2-001`；② 全量首次真实同步（`run_all`）体量大，待排期（既有遗留未变）

## [sc] 2026-09-25T20:30+08:00 — T-L0-006 PR 合入收口 · done

- **目标**：T-L0-006 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；中文标题经 API PATCH 乱码为已知问题（PR #2/#5/#7 同症），本次英文标题保 gate
- **关键动作**：脏工作区先独立提交 `f5e01fb`（机制落地）→ 切 `feat/T-L0-006-backup-retention` → 建 [PR #8](https://github.com/Dline6/ST-Agent/pull/8) → gate（strict PASS / pytest 315 / SYNC）→ squash 合入 `f5c3667` → 删远端+本地分支 → 本地 main 与远端 squash 分叉（`ahead 2` 系 hash 差异），`reset --hard origin/main` 收敛（工作区干净、内容无丢失已验证）→ 登记本条
- **变更文件**：项目管理/执行日志.md（+本条）· tasks/T-L0-006（verify 补 squash hash）
- **验证**：`git ls-remote` 仅剩 `refs/heads/main @ f5c3667` · main 上 pytest 315/315（待复检）· `git branch --list` 仅剩 main · 与 origin/main 零偏移
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① 下一就绪 `T-L1-001` / `T-L2-001`，开工走 ④ 对齐；② 全量首次真实同步（`run_all`）待排期

## [T-L1-001.1] 2026-09-25T21:00+08:00 — Skill 注册发现 + 参数校验 + 版本管理 · done

- **目标**：实现 03-L1 §1.1 + §1.4——SkillDescriptor 注册表经 `Store` 的 `config` 分区持久化，官方 Pack 12 Skill 种子自动可用，参数超范围拦截，版本 `follow-latest/locked` + 主版本待检查；定义 `skill_id` 版本后缀拼接规则（兑现 T-SC-001 遗留①）
- **对齐**：人回「对，按此开工」——④ 对齐结论：拆 4 叶子（.1 注册/.2 流水线/.3 沙箱/.4 复用）；skill_id 取 `sk_<名>_v<主>.<次>`；注册放 config 分区、运行日志放 execution_log；沙箱进程内 enforcement；执行器同步 callable；freshness 复用 L0 口径；空/不可用走 empty/unavailable；Skill 无状态（假设 A1–A7 见子任务文件）
- **输入**：story-02 验收段 9 条（本任务覆盖 GWT-1/5/7 + 注册不变量 GWT-R）；03 §1.1/§1.4；01 §1/§2/§6/§9/§10；T-SC-001 遗留①；T-L0-001 Store 门面 + T-L0-003 EndpointRegistry 布局先例
- **关键动作**：`ids.py`（A1 拼接规则 + parse/base/skill_id_for）· `errors.py`（三异常，失败显式化）· `pack.py`（官方 12 Skill 种子 + 幂等播种）· `registry.py`（register/publish_version/set_parameters/confirm_update/skip_update/get/get_latest/list + validate_call_params，主版本无 changelog 回滚）· `tests/l1/test_registry.py`（28 用例对 4 条 GWT）
- **变更文件**：src/st_agent/l1/{__init__,skills/{__init__,errors,ids,pack,registry}}.py（6 文件）· tests/l1/test_registry.py · tasks/T-L1-001{,.1–.4}*.md（5 文件）
- **验证**：pytest **343/343** 通过（既有 315 + 注册 28）· `verify_docs.py --strict` PASS（107 文件 / 868 链接 / 0 断链 / 假设 0 缺失）· 账本 SYNC（就绪 .2/.3/T-L2-001）· 盘上描述体无明文抽查通过 · commits：待收工
- **假设复核**：A1 成立（拼接规则注册测试通过）· A2 成立（config 前缀落盘断言通过）· A3 成立（中性命名/权限非法拒绝测试通过）
- **决策**：无新选型（KDF/分区口径沿用 L0；命名校验复用 NeutralityGuard 依赖倒置；主版本无日志发布回滚防脏标记）——均记于对齐结论，不另立 D-NNN
- **遗留**：① 下一就绪 `T-L1-001.2`（流水线）/ `T-L1-001.3`（沙箱）/ `T-L2-001`（记忆图谱）；② 全量首次真实同步（`run_all`）待排期（既有遗留未变）

## [sc] 2026-09-25T21:20+08:00 — T-L1-001.1 PR 合入收口 · done

- **目标**：T-L1-001.1 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；中文标题经 API PATCH 乱码为已知问题（PR #2/#5/#7/#8 同症），本次英文标题保 gate（同 PR #8 先例）
- **关键动作**：推 `feat/T-L1-001-1-skill-registry` → 建 [PR #9](https://github.com/Dline6/ST-Agent/pull/9)（API 建 PR + PATCH 英文标题）→ gate（strict PASS / pytest 343 / SYNC）→ squash 合入 `c30a00f` → 删远端分支（API DELETE）→ `checkout main + pull --ff-only` 干净快进 → `-D` 删本地分支 → 补任务 verify squash hash → 登记本条
- **变更文件**：项目管理/执行日志.md（+本条 + 上条 T-L1-001.1）· tasks/T-L1-001.1（verify 补 squash hash）
- **验证**：远端 main @ `c30a00f` · main 上 strict PASS + pytest 343/343 · `git branch --list` 仅剩 main · 与 origin/main 零偏移
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① 下一就绪 `T-L1-001.2`（流水线）/ `T-L1-001.3`（沙箱）/ `T-L2-001`（记忆图谱），开工走 ④ 对齐；② 全量首次真实同步（`run_all`）待排期

## [T-L1-001.2] 2026-09-25T22:10+08:00 — 执行流水线 + 依赖 DAG + 权限 + SkillRun 留痕 · done

- **目标**：实现 03-L1 §1.2 执行流水线——解析 → 参数确认 → 依赖 DAG 解析 → 权限检查 → 执行与留痕（ResultEnvelope + SkillRun 挂 trace_id）
- **对齐**：人回「对，按此开工」——④ 对齐结论：同步 callable 注册（A4，LLM 经 LlmClient.invoke、出网经 EgressGateway）；SkillRun 落 `execution_log/skill-run/*.json` + Trace 追加 skill_run 步（A5）；权限逐项核对、未批准即 validation_failed（A6）
- **输入**：story-02 验收段 9 条（本任务覆盖 GWT-2/3/4/6）；03 §1.2；01 §4（Trace）/ §5（ResultEnvelope）/ §10（权限）；T-L1-001.1 的 SkillRegistry（validate_call_params + 描述体 dependencies）
- **关键动作**：`errors.py`（RunnerValidationError/CycleDetectedError，执行面失败走信封不抛）· `models.py`（SkillRun frozen + RUN_PREFIX）· `runner.py`（SkillRunner：match bigram 匹配/confirm_card 参数确认卡/run 递归执行——循环即拒/缺失即拒/上游非 ok 即 dependency_failed/未批准即拦截/无执行器·裸异常·非法返回即 failed 带 log_ref；每次执行 SkillRun 落盘 + TraceStep 追加）· `tests/l1/test_pipeline.py`（24 用例对 4 条 GWT，不碰真网）
- **变更文件**：src/st_agent/l1/runner/{__init__,errors,models,runner}.py（4 文件）· tests/l1/test_pipeline.py · tasks/T-L1-001.2（doing→done）
- **验证**：pytest **367/367** 通过（既有 343 + 流水线 24）· `verify_docs.py --strict` PASS · 账本 SYNC（就绪 .1.4 待 .2 done 派生/.3/T-L2-001）· commits：待收工
- **假设复核**：A4 成立（同步 callable + 上下文注入 LLM/网关测试通过）· A5 成立（SkillRun 落盘 + Trace 回放断言通过）· A6 成立（未授权拒绝/已批准放行测试通过）
- **决策**：无新选型（成环点内层 validation_failed、入口按纪律透出 dependency_failed；GWT-4 下游措辞含「依赖失败」字样）——均记于此不另立 D-NNN
- **遗留**：① 下一就绪 `T-L1-001.4`（输出复用，依赖本任务 + T-L0-005）/ `T-L1-001.3`（沙箱）/ `T-L2-001`（记忆图谱）；② 全量首次真实同步（`run_all`）待排期（既有遗留未变）

## [sc] 2026-09-25T22:40+08:00 — T-L1-001.2 PR 合入收口 · done

- **目标**：T-L1-001.2 走 PR squash 合入 main，完成全链路
- **对齐**：沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；中文标题经 API PATCH 乱码为已知问题（PR #2/#5/#7/#8 同症），本次英文标题保 gate（同 PR #8 先例）
- **关键动作**：中途出口到 GitHub TCP 443 整体不通（git 协议 + API 双不通，DNS 正常；SSH 无本机私钥亦不可行）→ 退避重试后 HTTPS 恢复 → 推 `feat/T-L1-001.2-pipeline` → 建 [PR #10](https://github.com/Dline6/ST-Agent/pull/10)（API 建 PR + PATCH 英文标题）→ gate（strict PASS / pytest 367 / SYNC）→ squash 合入 `9113029` → 删远端分支（API DELETE，分支 404 确认；`refs/pull/N/head` 为 GitHub 只读保留非残留）→ `checkout main + pull --ff-only` → `fetch --prune` 清过期跟踪 → `-D` 删本地分支 → 登记本条
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：远端 main @ `9113029` · main 上 strict PASS + pytest 367/367 · `git branch -a` 仅剩 main + origin/main · 与 origin/main 零偏移
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① 下一就绪 `T-L1-001.4`（输出复用）/ `T-L1-001.3`（沙箱）/ `T-L2-001`（记忆图谱），开工走 ④ 对齐；② 全量首次真实同步（`run_all`）待排期

## [T-L1-001.3] 2026-09-26T09:55+08:00 — 执行沙箱越界拦截 · done

- **目标**：实现 03 §1.5 执行沙箱——文件访问限声明 `local_read` 范围、网络限 `net_access` 模式、命令执行需 `exec_command` 审批；越界即拦截 + `BehaviorViolation` 事件（01 §11）+ 警示「这个 Skill 行为异常」+ 提供禁用选项
- **对齐**：人回「可以开工了」——④ 对齐结论：进程内声明式 enforcement（A3）；事件载荷最小口径（A7）；禁用取注册表级最小口径（A8）；本任务只交付子系统、不接 runner 注入（A9）；假设编号见任务文件 `## 假设与前提`
- **输入**：story-02 验收段（边缘情况「行为越界拦截」+ 沙箱诉求）；03 §1.5；01 §10（权限声明模型）/ §11（`BehaviorViolation` 事件）；T-L1-001.1 的 SkillRegistry（取 `SkillDescriptor.permissions`）
- **关键动作**：`errors.py`（`SandboxValidationError` + 流式越界异常 `SandboxViolationError`）· `scopes.py`（路径归一化 / 逃逸判定 / `local_read` 前缀与 glob 匹配；`net_access` 主机 glob，含 `*.x` 兼匹配裸域）· `models.py`（`GuardVerdict` 拦截结论 + `ViolationRecord` 留痕 + 前缀常量；A7 最小载荷）· `sandbox.py`（`SkillSandbox` 门面：session / disable / enable / is_disabled / violations；`SandboxSession` 三类出口核对——`check_file`·`read_file`、`check_net`·`guard_gateway`、`check_command`·`run_command`；`SandboxedGateway` 受限出网出口：越界不触碰底层 sender，只暴露 execute/stream/online 无绕过入口）；中途修复 `_scope_of`——`parse_permission` 返回的 scope 含 §10 记法的 `<>` 包裹，剥括号后范围匹配才成立（原实现全判越界）
- **变更文件**：src/st_agent/l1/sandbox/{__init__,errors,scopes,models,sandbox}.py（5 文件）· src/st_agent/l1/__init__.py（docstring 指向 .3）· tests/l1/test_sandbox.py（30 用例）· tasks/T-L1-001.3（doing→done + verify + A8/A9）
- **验证**：pytest **397/397** 通过（既有 367 + 沙箱 30）· `verify_docs.py --strict` PASS · 沙箱 5 模块无 `requests`/`urllib`/`socket`/`subprocess` 等 import（零出网零执行）· 越界留痕落盘抽验不含资源串（A7）· 默认拒绝断言：越界时 reader/runner/sender 均未被调用 · commits：`cdf60dd`（实现 + 收口登记）
- **假设复核**：A3 成立（进程内声明式 enforcement：三类出口统一核对，越界测试通过）· A7 成立（事件载荷仅 `skill_id`+`violation`，磁盘留痕无路径/主机串）· A8 成立（`disable` 后三类出口全拒 + 落 config 可重载 + enable 复原）· A9 成立（未动 runner，runner 既有测试零回归）
- **决策**：D-004（权限作用域匹配口径跨任务统一——剥 `<>` 包裹 + `*.x` 兼匹配裸域 `x`；不改 01 §10）
- **遗留**：① 沙箱**未接 runner 注入**（A9 既定范围）——`SkillRunner` 仍把原始 llm/gateway 交给执行器；接线已立项为 **T-L1-001.5**（禁用拦截 + 受限 ctx 句柄 + LLM 出网路径），待开工；② `llm_transport` 内部经网关自身 `stream` 出网，不接线则该 LLM 路径不受沙箱约束（与①同源，一并由 .5 兑现）；③ 核查 .5 所需 `EndpointRegistry`/`CredentialVault`/`LlmClient` 装配时发现**运行时组合根全账本无人承载**（`src/` 无装配层、docs 零提及），按铁律 10 登记未决 **Q-001** → 决议 **A**（`provider_hosts` 暂归 L1，见 D-005）+ 新立 **T-L1-006** 组合根任务；④ 下一就绪 `T-L1-001.4`（输出复用）/ `T-L1-001.5`（接线）/ `T-L2-001`（记忆图谱）

## [sc] 2026-09-26T10:10+08:00 — T-L1-001.3 PR 合入收口 · done

- **目标**：T-L1-001.3 走 PR squash 合入 main，完成全链路
- **对齐**：人回「现在提交并开 PR」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；中文标题经 API PATCH 乱码为已知问题（PR #2/#5/#7/#8/#9/#10 同症），本次英文标题保 gate
- **关键动作**：本任务 PR 内含三件事——沙箱实现、T-L1-001.3 收口留痕、以及**核查中发现的两个新结构**（未决 Q-001 决议与 T-L1-006 组合根任务）；`pre-commit` 钩子按「账本须与 `tasks/` 同步」拒绝拆分提交（代码提交时账本被 stash 即判 STALE），改为单笔 `cdf60dd` 承载——钩子正常工作，非绕过
- **变更文件**：项目管理/执行日志.md（+本条 + 上条验证行补 hash）· tasks/T-L1-001.3（verify 补 PR + squash hash）
- **验证**：远端 main @ `fa05d8b`（PR #11，API 查 merged=True）· main 上 `verify_docs --strict` PASS + pytest 397/397 · `git branch -a` 仅剩 main + origin/main（`fetch --prune` 清过期跟踪）· 与 origin/main 零偏移 · 远端 PR 无 CI 配置（check-runs total=0），gate 以本地三项为准
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① `ccd_pr.bind_pr` 绑定 PR #11 失败（本机 `gh` 未登录，app 读不到），故 CI 与 PR 状态经 API 直查；如需 app 内 PR 面板/自动修复，需先 `gh auth login`；② 下一就绪 `T-L1-001.4`（输出复用）/ `T-L1-001.5`（沙箱接线）/ `T-L2-001`（记忆图谱），开工走 ④ 对齐；③ 全量首次真实同步（`run_all`）待排期（既有遗留未变）

## [sc] 2026-09-26T11:09+08:00 — 开发流程加固（任务接口面 · 里程碑集成关卡 · CI 机器门禁） · done

- **目标**：兑现流程隐患调查（同会话）的落地批次 ①–③——给任务加输入/输出契约（`## 接口面`）、给每个里程碑加集成关卡任务（`T-INT-*`）、把 PR gate 从人工自觉改为 CI 机器强制；顺带修 render_ledger 回写丢字段与打包配置两处工具缺陷
- **对齐**：人指示「请按照顺序帮我落地」——三批顺序（① 流程文档+工具 → ② 立项 → ③ CI+测试）与四项决策点采纳本会话调查报告的建议（接口面入任务正文而非另立登记表 · INT 用独立层前缀 · T-L1-006 由 M1 挪 M0 · CI 建在 GitHub Actions）；假设 A1–A3 见本条
- **输入**：本会话调查结论（任务无 I/O 契约 · 无集成任务 + 七项其他隐患）；工程宪法第 7/9/10/11 条；[工作流.md](工作流.md) / [tasks/_任务模板.md](tasks/_任务模板.md) / tools/{render_ledger,verify_docs}.py；Git协作规范 §5.2；00-架构总览 §5 端到端数据流；01 §2 `input_schema`/`output_schema`
- **关键动作**：
  ① 流程与工具——任务正文四块→五块（新增 `## 接口面`：输入＝上游任务 id + 具体 API/落盘位置，输出＝本任务交付的公共 API 与落盘位置），工作流 ④/⑤ 步、任务字段说明、任务模板、bootstrap 模板、方案文档、根 `CLAUDE.md` 同步；新增「集成关卡（里程碑收口）」节 + `gate: skip` 例外；`render_ledger.serialize` 改为已知字段定序 + 未知字段原序追加 + 空值键保留（原实现重写任务文件时静默丢字段）；`ID_RE`/`LAYERS` 支持 `INT` 层；`verify_docs` 新增检查 6（接口面实填，豁免 16 个机制前 done 任务）与检查 7（每里程碑必有 `T-INT-*` 且其依赖覆盖本里程碑全部叶子任务，`gate: skip` 除外）
  ② 立项——新增 `T-INT-001..005`（M0–M4 各一，`depends_on` 覆盖本里程碑全部叶子任务并与上一关卡串联；INT-001 已给出 5 条可执行 GWT，锚定 00 §5 端到端数据流）、`T-L1-001.6`（执行输出契约校验——让 01 §2 的 `input_schema`/`output_schema` 从装饰字段变执行期真校验，供 T-L1-003 连线校验复用）、`T-L0-007`（全量首次同步与限流实测，`gate: skip`，兑现 T-L0-005.1 遗留②）；`T-L1-006` 由 M1 挪 M0（组合根是 M0「最小闭环」的组成部分）
  ③ 门禁与测试——`.github/workflows/ci.yml`（push/PR → `verify_docs --strict` + `pytest`，`pip install -e .` 顺带验证打包）；Git协作规范 §5.2 拆为「机器强制 / 人工自查」两部分；`tests/test_layering.py`（铁律 7 层依赖方向机器校验）；`tests/test_tools.py`（render_ledger 回写保真 + INT 层识别 + 接口面/集成关卡检查与 `gate: skip` 例外）；`pyproject.toml` 修正打包配置（src 布局 `where = ["src"]`、`include = ["st_agent*"]`）、baostock 转 `[market]` 可选 extra、注册 `live` 标记并默认排除
- **变更文件**：项目管理/{工作流,README,AI-Coding项目管理方案,Git协作规范,执行日志,决策日志,任务账本}.md · 项目管理/tools/{render_ledger,verify_docs}.py · 项目管理/tasks/{_任务模板,T-INT-001,T-INT-002,T-INT-003,T-INT-004,T-INT-005,T-L0-007,T-L1-001.6,T-L1-006}.md · CLAUDE.md · pyproject.toml · .github/workflows/ci.yml · tests/{test_layering,test_tools}.py
- **验证**：pytest **467/467** 通过（既有 397 + 新增 70）· `verify_docs.py --strict` PASS（116 文件 / 967 链接 / **0 断链** / 检查 5·6·7 全 0）· 账本 SYNC（活跃 52 / 归档 0 / 就绪 5 = T-L1-001.4 / T-L1-001.5 / T-L1-001.6 / T-L2-001 / T-L0-007）· 打包实测：`pip wheel .` 产物含 `st_agent/…`（修前为 `src/st_agent/…`，装后 `import st_agent` 必失败）· render 幂等复跑账本零 diff · 集成关卡依赖覆盖经检查 7 逐里程碑核验（M0 22 / M1 9 / M2 4 / M3 3 / M4 5 个叶子任务）
- **假设复核**：A1 成立（接口面放任务正文：检查 6 落地即 PASS，未引入第二真相源）· A2 成立（INT 独立层前缀：render 正确分组、就绪集排序 INT 天然最后、检查 7 通过）· A3 成立（`gate: skip`：T-L0-007 不计入 T-INT-002 覆盖，其余任务仍受覆盖约束，检查 7 无告警）
- **决策**：D-006（开发流程加固：接口面载体 / 集成关卡形态 / 门禁方式三项选型 + 两项工具修正）
- **遗留**：① 两项新机制**实效待首批任务检验**——T-L1-001.4/.5/.6 与 T-INT-001 是首批按新机制走 ④ 的任务，若字段粒度不当（如「输出」写成了实现细节）再迭代工作流与模板；② 历史 16 个 done 任务未追补接口面（沿用 2026-09-25 假设机制的豁免先例，见 `verify_docs.GRANDFATHERED_NO_IFACE`）；③ CI 需远端仓库启用 Actions 才生效（本机 `gh` 未登录，无法代验），建议同时在分支保护勾选「Require status checks to pass」；④ ruff 已配 `[tool.ruff]` 但从未在任何 gate 执行、存量代码未 lint，未纳入本次 CI（避免首跑即红）；⑤ PAT 仍为 classic `repo` scope（既有遗留未变）；⑥ T-INT-002..005 的 GWT 待各自 ④ 对齐时按 00 §5 对应步骤补全

## [sc] 2026-09-26T11:19+08:00 — 追补历史任务接口面（兑现上条遗留②） · done

- **目标**：为接口面机制落地前已 done 的 15 个叶子任务回填 `## 接口面`，并清空 `verify_docs` 的豁免清单（兑现上一条 `[sc] 2026-09-26T11:09` 的遗留②）
- **对齐**：人指示「帮我补一下已经完成的任务的接口面」——明确覆盖历史 done 任务。与 2026-09-25 假设机制「不追补」的处置**刻意不同**：接口面是跨任务交接点，历史缺失会持续影响下游任务开工，且 15 个任务的文件都还在活跃区（未 archive）
- **输入**：15 个 done 任务文件；已合入代码的公开面（各模块 `__all__` / 门面类方法 / 前缀常量 / 落盘分区）；执行日志各任务的「关键动作」与「变更文件」
- **关键动作**：按任务模板节序把 `## 接口面` 插到 `## 验收标准` 之后（脚本一次性、跑完即删不入库）；内容分「输入（消费的上游任务 id + 具体 API/落盘位置）」与「输出（本任务交付的公共 API / 落盘位置）」，逐条取自代码实测而非复述 GWT——如 `CredentialVault.use` 落 `secrets/cred/<id>.json` + 使用记录 `execution_log/cred-usage/**`、`EgressGateway.llm_transport` 审计落 `execution_log/net/**`、`SkillRunner` 落 `execution_log/skill-run/<skill_run_id>.json`；节首加一行「追补于 2026-09-26（非 ④ 对齐时填写）」标记；`verify_docs.GRANDFATHERED_NO_IFACE` 清空
- **变更文件**：项目管理/tasks/T-SC-001.1 · .2 · .3 · .4 · .5 · T-L0-001 ~ T-L0-006 · T-L0-005.1 · T-L1-001.1 · .2 · .3（15 文件各 +`## 接口面` 节）· 项目管理/tools/verify_docs.py（豁免清单清空）· 项目管理/执行日志.md（+本条）
- **验证**：pytest **467/467** 通过 · `verify_docs.py --strict` PASS（116 文件 / 974 链接 / **0 断链** / **检查 6 在零豁免下 0 缺失**）· 15 个文件的节序与任务模板一致（`## 验收标准` → `## 接口面` → `## 假设与前提`）· 回填内容逐条对照代码公开面（`__all__` / 门面方法名 / 前缀常量 / `store.put` 分区实参）
- **假设复核**：无（纯文档回填，未改代码行为、未改任何接口）
- **决策**：无新选型（回填粒度、节序与「输入 / 输出」两行式沿用 [工作流.md](工作流.md) 与任务模板的既有约定）
- **遗留**：① `## 假设与前提` 节的历史豁免（12 个 done，见 `GRANDFATHERED_NO_ASSUME`）**保持不追补**——本条的「对齐」行已写明与接口面处置不同的理由；② 父任务 `T-SC-001` 未回填（check 按父链跳过，其子任务已各自回填）；③ 下一就绪 `T-L1-001.4` / `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T11:27+08:00 — 行尾归一化 + 工具写文件强制 LF · done

- **目标**：修掉「行尾不一致」这一工具缺陷——上一条接口面回填实际踩到：6 个任务文件被整文件 diff 淹没（真实改动仅 8–9 行，diff 却是 55 增 45 删）
- **对齐**：人指示「归一化行尾并修工具写文件」——分两笔：先修工具（防止再生）、再归一化存量
- **输入**：上条回填的 diff 证据（`git show 03767bd --numstat`）· `git ls-files --eol` 索引态统计 · `render_ledger` / `verify_docs` 的四个写文件调用点
- **关键动作**：① 工具——`render_ledger` 新增唯一写入口 `write_file()`（`newline="\n"` 强制 LF），bootstrap 建任务文件 / 派生父状态回写 / render 写账本三处与 `verify_docs --fix` 一处全部改经它（此前四处皆为文本模式 `open(..., "w")`，在 Windows 上产出 CRLF——33 个 CRLF 任务文件即由此而来）；② 存量——`git add --renormalize .` 只归一化索引，工作区不动（`core.autocrlf=true` 保持检出 CRLF）
- **变更文件**：项目管理/tools/{render_ledger,verify_docs}.py · tests/test_tools.py（+2 用例）· 27 个 tasks/*.md（纯行尾）· 项目管理/执行日志.md（+本条）
- **验证**：索引中存 CRLF 的任务文件数 **33 → 27（上条随提交归一化 6）→ 0**——`git cat-file blob` 逐文件实测 551016f / 03767bd / 6fb6699 / 85464fc 四个提交点 · `git diff --cached --ignore-all-space` 为空（归一化确为纯行尾，内容零差异）· 端到端复验：`render` 后账本 diff 恰为 1 行（时间戳行）· pytest **469/469**（新增 2）· `verify_docs --strict` PASS · 归一化后 `git status` 干净
- **假设复核**：A1 成立（写入口唯一化即可根治——测试 2 例分别覆盖 write_file 与「派生父状态回写」这条历史 CRLF 来源路径）· A2 成立（renormalize 只动索引、不改工作区内容：`--ignore-all-space` 为空且工作区状态干净）
- **决策**：无新选型（LF 定为规范行尾，与仓库其余 198 个 i/lf 文件一致；未引入 `.gitattributes`，见遗留①）
- **遗留**：① **未加 `.gitattributes`**——当前保证依赖各机器的 `core.autocrlf`（本机 true）。若在 `autocrlf=false` 的机器上用会写 CRLF 的编辑器改任务文件，仍可能漂移；彻底解法是 `* text=auto eol=lf` + 一次 renormalize，属独立决定，待定；② 下一就绪 `T-L1-001.4` / `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T11:46+08:00 — PR #12 合入收口（流程加固三批 + 工具修复） · done

- **目标**：把本会话三批流程加固（接口面 / 集成关卡 / CI 门禁）与两处工具修复走 PR squash 合入 main，完成全链路
- **对齐**：人指示「推送分支并开 PR」→「我去加 workflow scope」→「合入 PR #12」——沿用既定流程（§5.2 gate + squash 合入即删分支）
- **关键动作**：
  ① **推送受挫一次**——首次 push 被 GitHub 拒：「refusing to allow a Personal Access Token to create or update workflow `.github/workflows/ci.yml` without `workflow` scope」；只读侦察确认 token 有效（`ghp_` 40 位、classic、scope 仅 `repo`、账号 Dline6）且仓库 admin 权限、Actions 已启用，故确认为纯 scope 缺项 → 人补 `workflow` 后推送成功并保留原 token 值
  ② **PR 开展**——[PR #12](https://github.com/Dline6/ST-Agent/pull/12)（5 提交 / 65 文件 / +2060−1143）；标题与正文经 Python 构造 UTF-8 payload（`ensure_ascii=False` + `charset=utf-8`）提交，**回读中文完好**——推翻此前「API 中文乱码」的判断：那是 Windows 控制台 GBK 拼 curl `-d` 时的客户端编码损坏，不是 API 限制
  ③ **CI 首次生效并双跑全绿**——`push` 与 `pull_request` 两个 run 均 completed/success（ubuntu-latest + Python 3.12）；这是项目**第一次在非 Windows 环境执行** 全量自检与测试，中文文件名路径与 `pythonpath = ["src", "项目管理/tools"]` 配置均无问题
  ④ **合入与清理**——API squash 合入 `3a9687eb`（提交体含 `Task: sc` trailer 与署名行）→ 删远端分支（HTTP 204）→ `fetch --prune` + `checkout main` + `merge --ff-only origin/main` 快进 → `-D` 删本地分支
- **变更文件**：项目管理/执行日志.md（+本条）；本轮无源码改动（内容全部来自本会话前三条 `[sc]` 条目）
- **验证**：PR `mergeable_state: clean` + `check-runs` 2/2 success · 远端 `refs/heads/main @ 3a9687eb` 且 `git branch -a` 仅剩 `main` 与 `origin/main` · main 上 pytest **469/469** · `verify_docs.py --strict` PASS（116 文件 / 975 链接 / **0 断链** / 检查 5·6·7 全 0 / 就绪集 T-L0-007·T-L1-001.4·T-L1-001.5·T-L1-001.6·T-L2-001）· squash 提交体中文字面回读完好
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① **分支保护仍未开**（API 查 `main` protection = 404）——但注意副作用：若勾「Require status checks: CI」且勾了「Include administrators」，本仓库既有的「收口日志直推 main」惯例（`docs(项目管理): 登记 …` 类提交）会被拦，需同时配 bypass 名单或取消 Include administrators；② `ccd_pr.bind_pr` 仍失败（本机无 `gh`、未 `gh auth login`），代价是 app 内 PR 面板与「CI 失败自动唤醒修复」不可用，PR 状态只能经 API 直查；③ PAT 仍为 classic `repo`+`workflow`（既有遗留未变，仍建议换 fine-grained）；④ 下一就绪 `T-L1-001.4` / `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [T-L1-001.4] 2026-09-26T12:01+08:00 — 输出复用 + 新鲜度查询 · done

- **目标**：实现 03 §1.2 步骤 6（输出登记）+ §1.3（输出复用）——`ok`/`empty` 输出登记为可引用结果、后续经 `skill_run_id` 复用；复用时给 freshness 判定（判定只给结论，是否重算由上层自决）；freshness 复用 L0 同步状态口径，不另建第二套
- **对齐**：人回「按实际代码接线，同时更改任务文件」——④ 对齐结论：**按实测代码接线**（`freshness_verdict(domain)` 在 `BaoStockSync` 而非任务文件原写的 `MarketDb`；`MarketDb` 只提供 `as_of`/`snapshot_id`/`freshness` 行），并据此改任务文件「目标 / 涉及契约」两处；登记取索引化（载荷单一来源于 SkillRun，不复制）；`ok`+`empty` 均登记；`domain` 由复用查询方给出；假设 A1–A9 见任务文件 `## 假设与前提`
- **输入**：story-02 验收段（边缘情况「数据源不可用」+ 空状态，2 条 = GWT-8/9）；03 §1.2 步骤 6 / §1.3；01 §5（信封分支）/ §8（`as_of` 与最后更新）/ §11；数据库设计 §05（陈旧判据）；T-L1-001.2 的 `SkillRunner`/`SkillRun`；T-L0-005 的 `BaoStockSync.freshness_verdict`；T-L0-001 的 `Store`
- **关键动作**：新子系统 `l1/reuse/`——`errors.py`（`ReuseError`/`ReuseValidationError`/`OutputNotRegisteredError`）· `models.py`（`OUTPUT_PREFIX` + `REUSABLE_STATUSES=("ok","empty")` + `ReusableOutput` 索引五字段 + `ReuseView`＝索引+原信封+判定）· `freshness.py`（`FreshnessOracle` Protocol；`MarketFreshnessOracle` 对 L0 做**鸭子类型**适配、不合口径对象构造期即拒；`age_based_verdict` 超龄兜底 + `DEFAULT_MAX_AGE=24h`；`unavailable_envelope` 让「最后更新时间」只能取自判定）· `registry.py`（`OutputRegistry`：`register`/`is_registered`/`get`/`load_envelope`/`freshness`/`reuse`；`_verdict_for` 判定口径——有 oracle+domain 时以 L0 判定为基并补判「引用 `as_of` 早于该域当前截止」）；`runner.py` 接线（新增可选参 `outputs`/`freshness`；`SkillContext` 增 `freshness` 句柄；`finish` 内对 `ok`/`empty` 自动登记，登记失败转 `failed` 带 `log_ref`）；`tests/l1/test_reuse.py`（28 用例对 3 条 GWT + A1–A4/A8/A9）
- **变更文件**：src/st_agent/l1/reuse/{__init__,errors,models,freshness,registry}.py（5 新文件）· src/st_agent/l1/runner/runner.py · src/st_agent/l1/__init__.py（docstring 指向 .4）· tests/l1/test_reuse.py · tasks/T-L1-001.4（doing→done）· 项目管理/{任务账本,执行日志,决策日志}.md（父任务 `T-L1-001` 派生状态回到 `todo`——.4 done 后仍有 .5/.6 未开工，与开工时 `doing` 相抵，净零 diff）
- **验证**：pytest **502/502** 通过（既有 474 + 复用 28）· `verify_docs.py --strict` PASS（116 文件 / 979 链接 / 0 断链 / 检查 5·6·7 全 0）· 账本 SYNC · A1 实测：`skill-output/<id>.json` 键集恰为 5 个（无 payload），载荷只在 `skill-run/<id>.json` · A3 实测：`outputs=None` 时 `skill-output/` 为空且 `ctx.freshness is None` · A4 实测：注入必抛 `register` → 信封 `failed` 且 `log_ref` 非空 · commits：`a3dbedf`（实现 + 收口登记）· squash `da2363e`（PR #13）
- **假设复核**：A1 成立（索引五键实测 + 载荷单源）· A2 成立（ok/empty 登记、四类失败分支登记即拒）· A3 成立（None 时零行为变化，回归用例通过）· A4 成立（登记失败显式化，不静默）· A5 成立（适配器委托 L0；无 `freshness_verdict` 的对象构造期拒绝）· A6 成立（empty/unavailable 构造期不变量未被绕过，reason/最后更新时间均齐备）· A7 成立（登记只存定位 + 状态 + 时间锚点，无用户态字段）· A8 成立（`domain` 为查询参：`reuse(domain=…)` 与 `reuse()` 分别走 L0 与超龄两条路径）· A9 成立（L0 为基 + 快照落伍补判；引用落伍时 `stale=True` 且时间戳取自 L0；无 oracle/domain 时 24h 超龄）
- **决策**：D-007（输出复用登记形态与新鲜度判定口径——索引化 / ok+empty / L0 为基 + 快照落伍补判 / 超龄兜底须标明）
- **遗留**：① **`OutputRegistry` 尚无上层装配**——`src/` 无组合根（Q-001 的既有缺口），本任务只交付 L1 子系统 + runner 接线，不建组合根；`outputs`/`freshness` 的端到端注入待 **T-L1-006**（组合根）承担；② **与 T-L1-001.6 的落点顺序**——`.6` 要在 runner 里加 `output_schema` 校验，其要求「不合契约的载荷不得按 `ok` 登记进输出复用」，故 `.6` 必须在校验失败时改走失败信封**再**进 `finish`，不得在 `finish` 之后补拦（当前实现把登记放在 `finish` 内、以最终信封为准，天然满足该序）；③ 下一就绪 `T-L1-001.5`（沙箱接线）/ `T-L1-001.6`（输出契约校验）/ `T-L2-001`（记忆图谱）/ `T-L0-007`（全量同步，P1）

## [sc] 2026-09-26T12:16+08:00 — T-L1-001.4 PR 合入收口 · done

- **目标**：`T-L1-001.4` 走 PR squash 合入 main，完成全链路
- **对齐**：人指示「推送分支并开 PR」→「我安装了github app，请继续尝试」→「我安装了github CLI，请帮我完成后续任务」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）
- **关键动作**：
  ① **app 内 PR 绑定失败并定位根因**——`ccd_pr.bind_pr` 三次与 `<pr-created>` 标记一次均未绑定；排查确认**本机从未安装 GitHub CLI**（`Program Files/GitHub CLI`、`AppData/Local/GitHub CLI`、choco/scoop/WinGet 四处皆无，`gh` 不在 PATH），而 app 读 PR 依赖它。人装 `gh` 2.101.0 后绑定仍失败——因 **app 进程 PATH 是启动时快照**；改用绝对路径 `/c/Program Files/GitHub CLI/gh.exe`、以 git 已存的同一 github.com token 非交互登录（`gh auth login --with-token`，账号 Dline6，scopes 含 `repo`/`workflow`）后，`gh` 本身可用，绑定预计需**重启 app** 才生效（收工时未重启，故本条仍走 `gh`/API 直查口径）
  ② **PR 开展**——[PR #13](https://github.com/Dline6/ST-Agent/pull/13)（1 commit / 12 文件 / +954−14）；中文标题与正文经 Python 构造 UTF-8 payload（`ensure_ascii=False` + `charset=utf-8`）提交，**逐字符回读校验 `title` 一致**（`len=34`）——再次确认「控制台乱码」是 Windows GBK 显示问题而非 API 限制
  ③ **CI 双跑全绿**——push 与 pull_request 两个 run 均 completed/success（ubuntu-latest + Python 3.12）：[36216771308](https://github.com/Dline6/ST-Agent/actions/runs/36216771308) / [36216786116](https://github.com/Dline6/ST-Agent/actions/runs/36216786116)；`gh pr checks 13` 两条 `pass`，`mergeStateStatus: CLEAN`
  ④ **合入与清理**——`gh pr merge 13 --squash --delete-branch`（显式 `--subject` / `--body-file`，保 `Task:` trailer 与署名行）→ squash `da2363e` → `git fetch --prune`（远端任务分支已由 gh 删除）→ 本地 `main` 已快进至 `da2363e`、与 `origin/main` 零偏移
- **变更文件**：项目管理/执行日志.md（+本条 + 上条验证行补 hash）· tasks/T-L1-001.4（verify 补 PR + squash hash）
- **验证**：PR `state: MERGED`（`mergedAt 2026-09-26T04:14:35Z`）· main 上 pytest **502/502** · `verify_docs.py --strict` PASS · `git ls-remote --heads origin` 仅 `main @ da2363e` · 本地 `git branch -a` 无任务分支 · squash 提交体含 `Task: T-L1-001.4` trailer 与署名行
- **假设复核**：无（纯流程收口，无新增假设）
- **遗留**：① **app 内 PR 面板与「CI 失败自动唤醒修复」仍不可用**——根因已定位：本机缺 `gh`（已补齐并登录）**且 app 进程 PATH 未刷新**；重启 Claude 桌面版后 `bind_pr` 预计可成，未重启前 PR 状态只能经 `gh`/API 直查；② **分支保护仍未开**（`main` protection 404，既有遗留未变）——CI 属建议性不拦合入，本仓库「收口日志直推 main」惯例亦依赖这一点；③ 下一就绪 `T-L1-001.5`（沙箱接线）/ `T-L1-001.6`（输出契约校验）/ `T-L2-001`（记忆图谱）均 P0，`T-L0-007`（全量同步）P1，开工走 ④ 对齐

## [T-L0-007.1] 2026-09-26T12:34+08:00 — 脚本化全量同步与续跑语义 · done

- **目标**：兑现 T-L0-007 拆分后可离线验证的部分——把全市场首次全量同步从一次性手工操作变为可重复流程（长跑脚本 + JSON 摘要），并以 Fake 注入兑现续跑语义（中断后续跑不重复写、水位单调不回退、失败不静默）
- **对齐**：人回「对」——④ 对齐结论：`T-L0-007` 拆为 `.1`（脚本化 + 续跑语义，本批交付）/ `.2`（真实全市场长跑，人择时）；两子均 `gate: skip` 沿用父的运营类定性，避免 T-INT-002 覆盖涟漪；脚本落 `项目管理/tools/`（不引入包内 CLI）；为 `run_all` 加公开 `RUN_ORDER` 别名供脚本逐任务计时；假设 A1–A5 见任务文件 `## 假设与前提`
- **输入**：T-L0-005 `BaoStockSync.setup/run_task/run_all/freshness_verdict` + `MarketDb` + `SYNC_TASKS`/`window_for`；T-L0-005.1 `BaoStockFetcher`；T-L0-004 `EgressGateway.execute(kind="data_fetch", …)`；T-L0-001 `Store`；02 §5/§6 + 数据库设计族 05
- **关键动作**：`sync.py` 把 `_RUN_ORDER` 提为公开 `RUN_ORDER`（含 `__all__` 与 `market/__init__` 出口），`run_all` 两处引用同步；新增 `项目管理/tools/run_full_sync.py`（`summarize_run` 逐任务计时 + 参数块；CLI `--root/--out/--init/--watchlist/--enable-minute/--timeout-ms`，主口令只经 `ST_AGENT_PASSPHRASE`，不进 argv 与摘要）；新增 `tests/l0/test_full_sync.py`（13 用例：FlakyFetcher 中断→续跑不重复写、RecordingFetcher 验窗口起点随水位前进、失败记 `last_error` 且 `freshness_verdict` 点名、审计条数对位、摘要形状与口令零泄漏、脚本序 == `run_all` 序）；新增 `tests/live/test_full_sync_live.py`（`@pytest.mark.live` + `ST_AGENT_LIVE_FULL=1` 双重开关，默认排除）
- **变更文件**：src/st_agent/l0/market/{sync,__init__}.py · 项目管理/tools/run_full_sync.py（新）· tests/l0/test_full_sync.py（新）· tests/live/test_full_sync_live.py（新）· 项目管理/tasks/{T-L0-007（转父）,T-L0-007.1（新）,T-L0-007.2（新）,T-L0-005（接口面 +`RUN_ORDER` 行）}.md · 项目管理/{任务账本,执行日志}.md
- **验证**：pytest **515/515** 通过（既有 502 + 续跑语义 13；live **1 deselected**）· `verify_docs.py --strict` PASS（118 文件 / 1018 链接 / **0 断链** / 检查 5·6·7 全 0）· 账本 SYNC（活跃 54 / 就绪集 T-L1-001.5·T-L1-001.6·T-L2-001）· 实测并修正一处命名冲突：`tests/l0/test_full_sync.py` 与 `tests/live/test_full_sync.py` 在无 `__init__.py` 下触发 pytest import mismatch，live 侧改名 `test_full_sync_live.py` 后全绿 · commits：`0d59fa9`（分支实现）· PR #14 · squash `1b53f7a`
- **假设复核**：A1 成立（Fake 中断→续跑用例：已入库行不重复、缺失行补齐、失败不推进水位、窗口起点 = 水位次日，无需新增退避逻辑）· A2 成立（`RUN_ORDER` 公开别名落地，`run_all` 引用同源，`T-L0-005` 接口面已同步该行）· A3 成立（脚本落 `项目管理/tools/`，`pyproject` 的 `pythonpath` 已含该目录，用例可 `import run_full_sync`）· A4 成立（脚本默认 `--root` 指向 `tmp/live-market`；本批未跑真实同步，无真实库产生）· A5 成立（live 用例默认 `1 deselected`，不进 CI；另加元用例断言 `tests/live/` 下的用例必带 `live` 标记）
- **决策**：无新选型（拆分沿用 T-L0-005 → 005.1 先例；`RUN_ORDER` 公开化属接口面补充非选型；脚本落点与摘要形状按对齐结论落地）——记此不另立 D-NNN
- **遗留**：① **真实全市场长跑未执行**——归 [T-L0-007.2](tasks/T-L0-007.2-真实全量长跑与限流续跑实测.md)（`gate: skip`，人择时；本批只交付脚本与续跑语义）· ② 下一就绪 `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T12:44+08:00 — L0 遗留处置（两处小尾巴定案）+ 遗留登记册落地 · done

- **目标**：处置 L0 遗留清单中「L0 自身能闭」的项，并把**未解决**的遗留单独立册（`项目管理/遗留问题/`），与 [阻塞与未决.md](阻塞与未决.md)（Q-NNN 规格空白台账）分工
- **对齐**：人指示「先解决能解决的遗留问题，再新建一个文件夹把还没有解决的遗留问题单独形成文件单独存放进去」；经提问定范围「闭小尾巴 + 开工 T-L0-007」与形态「`项目管理/遗留问题/` · 每层一文件」
- **输入**：L0 全量遗留盘点（各任务 done 时的执行日志「遗留」行 + 未决 Q-001 / 决策 D-005 + T-L0-001 悬空假设 A6）；`export_all_safe` 与 `is_stale` 的源码 docstring
- **关键动作**：
  ① **修正盘点初稿的判断**——初稿把 `export_all_safe`（`src/st_agent/l0/secrets/vault.py:217`）与 `is_stale`（`src/st_agent/contracts/time_events.py:66`）记为「死代码小尾巴」，读码后推翻：前者 docstring 已写明消费方是**分享物导出**（备份路径改由「secrets 永不进归档」满足禁明文），后者 docstring 写明「L0 应以 `sync_state` 自检 SQL 为准，本方法仅用于无 `sync_state` 可查的内存路径」且 L0 确已如此落地——两者都不是欠账，而是**归属已定、归属方尚未开工**（与「远程 MCP 审计点」「Skill 复用 LLM 口径」同类）
  ② **处置定案**：`export_all_safe` 归属 **T-ECO-001**（分享物类型/格式 + 导出流程），保留不删；`is_stale` 记为**已由 T-L0-005 的设计关闭**（通用超龄判定保留给内存路径；L1 复用层的 `age_based_verdict` 为其兜底对位实现）
  ③ **立册**：新建 `项目管理/遗留问题/`——`README.md`（登记规则 + 与 [阻塞与未决.md](阻塞与未决.md) 的分工：那册收 Q-NNN 规格空白，本册收任务收工时的转移/待办）+ [`L0-遗留问题.md`](遗留问题/L0-遗留问题.md)（未解决 9 项，逐条列归属任务 / 阻塞根因 / 解封条件）
- **变更文件**：项目管理/遗留问题/README.md（新）· 项目管理/遗留问题/L0-遗留问题.md（新）· 项目管理/执行日志.md（+本条）
- **验证**：`verify_docs.py --strict` PASS（新目录 2 文件纳入断链校验，仍 **0 断链**）· 登记册 9 项与执行日志原始「遗留」行逐条可回溯（册内附来源任务与行锚点）· L0 源码零改动（本批只动文档）
- **假设复核**：无（纯文档处置，未改代码行为、未改任何接口）
- **决策**：无新选型（「保留既有公共 API 而非删除」属保守处置非选型；登记册形态按人指定落地）——记此不另立 D-NNN
- **遗留**：① T-ECO-001 若最终不走「分享物携带凭据安全视图」，`export_all_safe` 需再评估存废（已记入 [`L0-遗留问题.md`](遗留问题/L0-遗留问题.md) 的观察项）· ② 遗留登记册机制**实效待后续层检验**（当前仅 L0 一册，L1/L2… 收工遇转移项时同构加文件）· ③ 下一就绪 `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T12:57+08:00 — T-L0-007.1 PR 合入收口 · done

- **目标**：把本会话的 L0 批次（脚本化全量同步 + 遗留处置 + 遗留登记册）走 PR squash 合入 main，完成全链路
- **对齐**：人指示「按规范走分支 + PR 合入」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；分支 `feat/T-L0-007-full-sync`（分支 type 取主导变更 `feat`，T-ID 绑 `T-L0-007`）
- **关键动作**：
  ① **提交**——单笔 `0d59fa9` 承载本批三件事（T-L0-007.1 实现 + 两处遗留处置 + 遗留登记册）；`pre-commit` 钩子（`verify_docs.py --strict`）在 commit 前自动跑并 Passed，未绕过
  ② **PR 开展**——[PR #14](https://github.com/Dline6/ST-Agent/pull/14)（1 commit / 13 文件 / +770−15）；经 `gh api --input`（Python 构造 UTF-8 JSON 载荷）建 PR，**逐字符回读 `title` 一致**（`len=36`）——再次确认中文走 JSON 载荷无编码问题
  ③ **CI 双跑全绿**——push 与 pull_request 两个 run 均 pass（[36218453320](https://github.com/Dline6/ST-Agent/actions/runs/36218453320) 1m18s / [36218464804](https://github.com/Dline6/ST-Agent/actions/runs/36218464804) 1m4s）；`mergeable: MERGEABLE`
  ④ **合入与清理**——`gh pr merge 14 --squash --delete-branch`（显式 `--subject` / `--body-file`，保 `Task: T-L0-007.1` trailer 与署名行）→ squash `1b53f7a` → gh 顺带切回 main 并删本地分支 → `fetch --prune` 清远端跟踪 → main 与 origin/main 零偏移
- **变更文件**：项目管理/执行日志.md（+本条 + 上条验证行补 hash）· 项目管理/tasks/T-L0-007.1（verify 补 PR + squash hash）
- **验证**：PR `state: MERGED`（`mergedAt 2026-09-26T04:42:15Z`）· 本地 `main` = `origin/main` = `1b53f7a` · `git ls-remote --heads origin` 仅 `main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · pre-commit 自检 Passed
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型
- **遗留**：① **本仓库禁用 GitHub auto-merge**（`set_auto_merge` 报 "Auto merge is not allowed for this repository"）——故合入只能「等 CI 绿后手动 squash」；若想让 CI 绿即自动落地，需在仓库设置开启 Allow auto-merge · ② **app 内 CI 监视未开**（`auto_fix` off）——CI 失败不会自动唤醒；如需要可开 · ③ 下一就绪 `T-L1-001.5`（沙箱接线）/ `T-L1-001.6`（输出契约校验）/ `T-L2-001`（记忆图谱）均 P0，`T-L0-007.2`（真实全量长跑）P1 待人择时，开工走 ④ 对齐

## [T-L0-007.2] 2026-09-26T13:05+08:00 — 真实链路冒烟（小样本） · doing（冒烟通过，全量待择时）

- **目标**：先用小样本把「Store → MarketDb → EgressGateway → BaoStockFetcher → BaoStockSync → 落盘/审计/新鲜度」整条真实链路走通一次，证明管道可用（全市场全量长跑仍待择时）
- **对齐**：人指示「随便生成一个主口令，然后再做一个简单的小样本真实冒烟测试，能证明链路能跑通就行」——范围＝小样本冒烟（非全量）；交付＝可复跑的 live 冒烟用例 + 真实基线证据；假设 A1–A4 见任务文件 `## 假设与前提`
- **输入**：T-L0-005 `BaoStockSync.setup/run_task/freshness_verdict/dataset_snapshot` + `MarketDb`；T-L0-005.1 `BaoStockFetcher`；T-L0-004 `EgressGateway`；T-L0-001 `Store`；02 §5/§6 + 数据库设计族 05
- **关键动作**：
  ① **口令**——生成 32 位随机口令写入 `.env` 的 `ST_AGENT_PASSPHRASE`（该文件已 gitignore；口令值不进会话、不进 argv）——仅服务开发/冒烟存储，产品路径仍由用户登录时提供
  ② **新增 live 冒烟用例** `tests/live/test_smoke_sync.py`——真实 `Store.create`（PBKDF2 600k + 七分区）→ 真实 `login` → `setup` → 日历 → 证券主档 → 全量快照（取日历内最近交易日）→ 3 只真实 A 股（浦发/平安/工商）日线全历史；断言数据面非空、抓取全经网关审计、`freshness_verdict` 口径可查、**全盘明文 0 命中**
  ③ **首跑失败并据实修正**——`run_task` 直调未先 `setup()` → 显式 `unavailable`（「本地市场数据库尚未建立」），失败显式化如期工作；补 `setup()` 后第二跑通过——该点记入任务假设 A4
- **变更文件**：tests/live/test_smoke_sync.py（新）· 项目管理/tasks/T-L0-007.2（doing + GWT-0 + 接口面 + 假设）· 项目管理/遗留问题/L0-遗留问题.md（C1 更新）· .env（+`ST_AGENT_PASSPHRASE`，未入库）
- **验证**：`python -m pytest -m live -s tests/live/test_smoke_sync.py` → **1 passed in 185.52s**；实测＝密钥派生+建库 0.21s · setup 0.18s · 日历 34.5s/13066 行 · 主档 35.3s/8981 行 · 全量快照 20.4s（交易日 `2026-09-24`）· 日线 90.4s/**3 只 / 20018 行**（`1990-12-19` 起全历史）· 网关 `data_fetch` 审计 **6 条** · 快照串 `bs_all_stock:2026-09-24@…|bs_calendar:-@…|bs_k_daily:2026-09-24@…|bs_security_basic:-@…` · **全盘明文扫描 0 命中**（24 条目）
- **假设复核**：A1 成立（3 码 × 全历史未见限流；全量风险见遗留①）· A2 成立（数据只落 pytest 临时目录加密分区，明文 0 命中、`git status` 无真实库）· A3 成立（冒烟覆盖 setup / 日历 / 主档 / 全量快照 / 日线五类调用 + 网关审计）· A4 成立（首跑踩到「直调须先 setup」并据报错修正，第二跑通过）
- **决策**：无新选型（口令生成与存放位置、冒烟范围均按人指示）——记此不另立 D-NNN
- **遗留**：① **全市场全量长跑仍未执行**，且侦察实测其成本 ≈ **85 小时**（`k_daily` 50h + `k_period` 23h + 其余 12h；8981 码 × 全历史），远超本任务备注原写的「数小时」；同批侦察另发现三处结构性障碍——**码分片会被全局水位跳历史**（分片 2 的窗口起点 = 分片 1 的水位次日）、**任务内中断水位不推进**（重跑全码重拉，40h 进度即白跑）、`live.py` 抛错时**丢失 `error_code`** 致限流无法识别 → 全量长跑须先补「分批 + 退避」能力（建议新立 `T-L0-008`，待定）· ② **冒烟基线**：单码全历史日线 ≈ 18–30s、主档 35s、日历 34s，可作排期估算锚点 · ③ 下一就绪 `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T13:14+08:00 — T-L0-007.2 冒烟 PR 合入收口 · done

- **目标**：把小样本真实链路冒烟批次（冒烟用例 + 任务/登记册留痕）走 PR squash 合入 main，完成全链路
- **对齐**：人指示「走分支 + PR 合入」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；分支 `test/T-L0-007.2-real-smoke`（分支 type 取主导变更 `test`）
- **关键动作**：单笔 `4bc14a5`（`pre-commit` 自检 Passed，未绕过）→ [PR #15](https://github.com/Dline6/ST-Agent/pull/15)（1 commit / 6 文件 / +158−9）经 `gh api --input` 的 UTF-8 JSON 载荷创建、**标题逐字符回读一致**（`len=25`）→ CI 双跑全绿（[36219908913](https://github.com/Dline6/ST-Agent/actions/runs/36219908913) 1m20s / [36219918559](https://github.com/Dline6/ST-Agent/actions/runs/36219918559) 1m18s）→ `gh pr merge 15 --squash --delete-branch`（显式 `--subject` / `--body-file`，保 `Task: T-L0-007.2` trailer 与署名行）→ squash `a80a252` → gh 顺带切回 main 并删本地分支 → `fetch --prune` 清远端跟踪
- **变更文件**：项目管理/执行日志.md（+本条）
- **验证**：PR `state: MERGED`（`mergedAt 2026-09-26T05:11:10Z`）· 本地 `main` = `origin/main` = `a80a252` · `git ls-remote --heads origin` 仅 `main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · `pre-commit` 自检 Passed
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型
- **遗留**：① **`T-L0-007.2` 仍为 `doing`**——冒烟（GWT-0）已通过，GWT-1/2/3 的全市场长跑未跑（≈85h），故不置 `done`；收口不写任务 `verify`（该字段 `done` 时填）· ② **`T-L0-008`（分批 + 退避）待立项**（登记册 C2；待人定）· ③ 下一就绪 `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐

## [sc] 2026-09-26T13:36+08:00 — 遗留册接入工作流（开工查 · 收工销）+ 检查 8 机器兜底 · done

- **目标**：把「开工前先看当前任务能解决哪些遗留、能解决就解决并销账」从册规里的一句口头约定，变成 [工作流.md](工作流.md) 的显式步骤 + 任务文件的一个节 + `verify_docs.py` 的一道门禁——让遗留条目**必然被翻开**，而不是靠自觉
- **对齐**：人指示「帮我在工作流中加一条规则，看看开始任务前先查看当前任务能解决哪些遗留问题，如果能解决，解决后更新遗留问题」——三问定案：强度＝**工作流 + 模板节 + 机器门禁**· 遗留关闭**计入本任务验收** · 兜底＝**机器检查 8**（非冷启动人工盘点）
- **输入**：工作流七步现状（全篇无「遗留」字样）· [遗留问题/README.md](遗留问题/README.md) 分工表与规则 1–5 · [遗留问题/L0-遗留问题.md](遗留问题/L0-遗留问题.md) 未闭 9 项 · `verify_docs.py` 检查 5/6/7 的既有形态（`_check_task_section` 复用点）· `tests/test_tools.py` 的 `fake_tasks` 夹具
- **关键动作**：
  ① **工作流.md**：新增「遗留消费（开工查 · 收工销）」节——四步＝**开工查**（筛「归属＝本任务」或「解封条件＝本任务 `done`」的条目）/ **能闭则闭**（闭不了的**就地修正册内归属或解封条件并写明理由**，判据仍为「谁来写这段代码不清楚 → 进未决；清楚但没轮到 → 留本册」）/ **收工销账**（移出未闭区 → 册尾「已闭（备查）」留一行 + 执行日志写关闭留痕）/ **机器兜底**（检查 8）
  ② **四处联动改**：④ 复述清单加「**遗留面**」一条；对齐结论「任务文件两节」→「**三节**」（加 `## 可关闭的遗留`）；调度规则第 3 步收工顺序插入「开工查遗留册」与「销账」；正文「五块」→「**六块**」并新增该节格式段（含「该节本身不加门禁」的理由）
  ③ **`tasks/_任务模板.md`**：加 `## 可关闭的遗留` 节（含 `无` 的写法与册链接）
  ④ **[遗留问题/README.md](遗留问题/README.md)**：加规则 6「开工消费 · 收工销账」，并把分工表「处置」列补成 `不改任务状态；归属任务开工时消费 → 收工时销账（规则 6）`
  ⑤ **`verify_docs.py`**：新增检查 8 `check_legacy_settlement()`——扫 `遗留问题/*.md` 的**未闭区**（`## 已闭` 之前）表格行，取「归属」「解封条件」两列点名的任务 id，**若已全部 `done` → 命中**（该销的账没销）。三条不误报约定：只认这两列（「来源」列的已 `done` 任务不算，那是留痕不是归属）· 册内点到尚不存在的 id（如「待立项」`T-L0-008`）不判 · 「已闭（备查）」区不判。警告级，`--strict` 判失败（CI 与 pre-commit 都跑 `--strict`，故实为门禁）
  ⑥ **`tests/test_tools.py`**：+3 用例（归属已 `done` → 命中 / 归属未轮到 → 不判 / 已闭区与不存在 id → 不判），沿用既有 `fake_tasks` 夹具 + `monkeypatch` 换 `LEGACY_DIR`
- **变更文件**：项目管理/工作流.md · 项目管理/tasks/_任务模板.md · 项目管理/遗留问题/README.md · 项目管理/tools/verify_docs.py · tests/test_tools.py · 项目管理/{执行日志,决策日志}.md · （`tasks/*.md` frontmatter 零改动——本次不改任务状态，账本仍 SYNC）
- **验证**：pytest **518/518** 通过（既有 515 + 检查 8 的 3；live 2 deselected）· `verify_docs.py --strict` PASS（120 文件 / 1057 链接 / **0 断链** / 检查 4·5·6·7·8 全 0 / 就绪集不变）· 检查 8 **反向探针**（独立脚本注入合成册，跑完即删）：归属＝已 `done` 任务 → 命中 1 条；归属＝`todo` → 不判；id 不存在（待立项）→ 不判；落在「已闭（备查）」区 → 不判——**证明门禁会开火且只对该开火的开火**。探针曾抓到一处真 bug 并已修：id 正则原写成 `T-[A-Za-z]+-\d{3}`，**层号里的数字（`L0`/`L1`）匹配不上**，致检查恒为 0（假绿）；改为复用 `rl.ID_RE` 后命中 · commits：`13c4ab0`（分支实现）· PR [#16](https://github.com/Dline6/ST-Agent/pull/16) · squash `424592d`
- **假设复核**：无（纯流程 + 工具改动：未改 `docs/` 架构与 01 契约、未改 L0/L1 代码行为——不触发工作流第 ⑦ 步）
- **决策**：D-008（遗留册接入工作流：开工查 · 收工销 · 检查 8 机器兜底）
- **遗留**：① **任务侧 `## 可关闭的遗留` 节未加机器检查**（只做册侧检查 8）——机制落地前已有 20+ 个 `done`/`doing` 任务未带该节，加检查即需历史豁免清单；若后续要加，先出豁免清单再落 ② **新规则即刻生效于下一就绪任务**：`T-L1-001.5` 的遗留面命中册内 **A2**（Skill 调 LLM 复用 `LlmClient.invoke` + `pick` 口径；归属 `T-L1-001`，解封条件含 `.5` / `.6`）——该任务 ④ 对齐时须点名，`.5` / `.6` 全 `done` 后方可销账 ③ 下一就绪 `T-L1-001.5` / `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐（先跑「开工查遗留册」）

## [sc] 2026-09-26T13:56+08:00 — 遗留册接入工作流 PR 合入收口 · done

- **目标**：把遗留册接入工作流这批（工作流规则 + 模板节 + 检查 8 + 用例 + 留痕）走 PR squash 合入 main，完成全链路
- **对齐**：人指示「走分支 + PR 合入」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；分支 `docs/sc-legacy-intake`（本批无任务号，按规范 §3.2 的 T-ID 段用 `sc` 占位；type 取主导变更 `docs`）
- **关键动作**：单笔 `13c4ab0`（`pre-commit` 自检 Passed，未绕过）→ [PR #16](https://github.com/Dline6/ST-Agent/pull/16)（1 commit / 7 文件 / +135−5）经 `gh api --input` 的 UTF-8 JSON 载荷创建、**标题逐字符回读一致**（`len=49`，含 `（开工查 · 收工销）` 全角括号与间隔号）→ CI 双跑全绿（[36222022822](https://github.com/Dline6/ST-Agent/actions/runs/36222022822) 1m18s / [36222082350](https://github.com/Dline6/ST-Agent/actions/runs/36222082350) 1m20s；`mergeable: MERGEABLE`）→ 经 `gh api -X PUT …/pulls/16/merge`（`merge_method: squash` + 显式 `commit_title` / `commit_message`，保 `Task: sc` trailer 与署名行）→ squash `424592d` → 经 API 删远端分支 → 本地 `checkout main` + `fetch --prune` + `merge --ff-only` 快进至 `424592d`
- **分支清理的额外一步**：本次 PR 经 API 合入（非 `gh pr merge`），**本地分支不会被自动删除**；`git branch -d` 因 squash 与分支无祖先关系而拒绝删除，故先以 `git diff --quiet main docs/sc-legacy-intake` **证明二者树完全一致**（分支无独有提交）再 `-D`
- **变更文件**：项目管理/执行日志.md（+本条 + 上条验证行补 hash）
- **验证**：PR `state: MERGED`（合入 API 返回 `merged: true`）· 本地 `main` = `origin/main` = `424592d` · `git ls-remote --heads origin` 仅 `main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · main 上 pytest **518/518** · `verify_docs.py --strict` PASS（检查 8 = 0）· squash 提交体含 `Task: sc` trailer 与署名行（标题 49 字符以 UTF-8 解码核对一致）
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型
- **遗留**：① **`ccd_pr.bind_pr` 本次成功**——本机 `gh` 已装且 app 进程 PATH 已刷新（此前执行日志记为「待重启复验」），PR #16 已绑定本会话，app 内 PR 面板与「CI 失败自动唤醒」通道可用（`auto_fix` 仍 off，未改）· ② 本仓库仍禁用 GitHub auto-merge（既有结论未变，故合入仍为「等 CI 绿后手动 squash」）· ③ 下一就绪 `T-L1-001.5`（其遗留面命中册内 A2）/ `T-L1-001.6` / `T-L2-001`（均 P0），开工走 ④ 对齐（先跑「开工查遗留册」）

## [T-L1-001.5] 2026-09-26T14:11+08:00 — 沙箱接线 SkillRunner 注入 · done

- **目标**：兑现 T-L1-001.3 遗留①②——把执行沙箱真正接进 `SkillRunner` 的一次执行（禁用 Skill 直接拦截、执行器拿到的 `ctx.gateway` / `ctx.llm` 为受限出口、经 `ctx.sandbox` 暴露受限会话供文件/命令出口核对），并补齐 LLM 出网路径（`SandboxedGateway.llm_transport` 结构性 API + `provider → host` 映射按 01 §7 条目形态落配置）
- **对齐**：人回「对」——④ 对齐结论：交付面 = GWT-W1–W4 + 回归；沙箱经 `SkillRunner(sandbox=…)` 可选参注入（`None` 时逐字节回归）；LLM 侧取行为性代理 `session.guard_llm(client)`（A3，因 .2 注入的 `LlmClient` 其 transport 构造期已绑真网关、runner 无法重建）；`provider → host` 映射按 [D-005](决策日志.md) 暂归 L1 沙箱；假设 A1–A6 见任务文件 `## 假设与前提`；遗留面命中 L0 册 A2（本批**不销账**）
- **输入**：T-L1-001.2 `SkillRunner.__init__` / `_execute` / `SkillContext` + `RUN_PREFIX`；T-L1-001.3 `SkillSandbox.session` / `SandboxSession` 三类出口 / `SandboxedGateway` / `GuardVerdict`；T-L0-004 `EgressGateway.execute`/`stream`/`online` + `llm_transport`；T-L0-003 `LlmClient.invoke` / `LlmEndpoint.provider` / `EndpointRegistry.get`；T-L0-001 `Store`；01 §5/§7/§10/§11 + 03 §1.2/§1.5
- **关键动作**：
  ① **映射子系统**：新增 `st_agent.l1.sandbox.provider_hosts.ProviderHostRegistry`（`register` / `update` / `resolve` / `get` / `list`），按 01 §7 `ConfigEntry` 七字段形态落 `config` 分区 `llm-provider-host/<provider>.json`（A6 / D-005）；`SkillSandbox` 增可选注入 `endpoints` / `provider_hosts`；提供方标识复刻端点标识「安全路径段」口径（防目录穿越），主机拒空白与 `/`
  ② **三类出口接线**：`_execute` 在描述体取到后即时查 `is_disabled` → 直接 `validation_failed`（**不解析依赖、不调执行器**）；执行前开 `sandbox.session(descriptor, approved, trace_id=…)`，`ctx.gateway` → `session.guard_gateway(...)`、`ctx.sandbox` → 会话本体
  ③ **LLM 出口（A3 行为性代理）**：`SandboxSession.guard_llm(client)` → `GuardedLlmClient`，在 `invoke` 边界按「端点 provider 的目标主机」核对 `net_access`，越界产单条 `error` 事件且**不调用底层 client**；主机无法解析（未配端点/提供方映射，或该提供方未登记）**fail-closed** 拒绝（沙箱是 09 导入第三方 Skill 的拦截基础，取保守侧）
  ④ **A4 结构性 API**：`SandboxedGateway.llm_transport(provider_hosts)`——签名与 L0 同款，核对后经受限 `stream` 发出；越界 / 未登记主机一律抛 `SandboxViolationError` 且不触碰底层 sender；runner 路径**不依赖**它
  ⑤ **配套**：`errors.py` 增 `ProviderHostError` / `ProviderHostExistsError` / `ProviderHostNotFoundError`；`sandbox/__init__.py` 出口同步
- **变更文件**：src/st_agent/l1/sandbox/provider_hosts.py（新）· src/st_agent/l1/sandbox/{sandbox,errors,__init__}.py · src/st_agent/l1/runner/runner.py · tests/l1/test_runner_sandbox.py（新，26 用例）· 项目管理/tasks/T-L1-001.5（doing→done，补 `## 接口面` / `## 可关闭的遗留` / `verify`）· 项目管理/{任务账本,执行日志}.md
- **验证**：pytest **545/545** 通过（既有 518 + 新 26 + `test_layering` 因新增 `provider_hosts.py` 多出 1 条参数化；live 2 deselected）· `verify_docs.py --strict` PASS（120 文件 / 1058 链接 / 0 断链 / 检查 5·6·7·8 全 0）· 账本 SYNC（就绪集 `T-L1-001.6` / `T-L2-001`）· 实测：禁用后执行器**零调用**且信封 `validation_failed`；`ctx.gateway` 出网 `evil.example.com` 被拦、spy sender 零调用、落盘留痕内**不含 `evil`**（A7）；`ctx.sandbox.read_file` 越界时 reader 零调用；provider 主机越界时 `RecordingLlm.calls == []`（不触碰底层）且 `invoke` 只产 `error` 事件；映射经 fresh `ProviderHostRegistry` 读回为七字段 `ConfigEntry`
- **假设复核**：A1 成立（`sandbox=None` 时 `ctx.sandbox is None` 且 `ctx.gateway` / `ctx.llm` 与传入对象同一）· A2 成立（禁用拦截在描述体取到后、执行器调用前；依赖被禁用时下游显式见 `dependency_failed`）· A3 成立（行为性代理落地，越界底零调用）· A4 成立（结构性 API 交付且 runner 不依赖）· A5 成立（`SkillContext` 增 `sandbox` 字段，01 契约未动）· A6 成立（映射暂归 L1 沙箱并按 01 §7 形态落盘；**未迁移**，留 `T-L1-006` 复核）
- **决策**：无新选型——映射归属与 LLM 出口形态由 [D-005](决策日志.md) 与假设 A3/A6 覆盖，沙箱注入沿用 `.4` 的「可选参 + None 即回归」先例。记此不另立 D-NNN
- **遗留**：① **`provider → host` 最终归属待复核**——本批按 D-005 A 案暂归 L1 沙箱，复核与可能的迁移归 `T-L1-006`；该转移项已由 [L0-遗留问题.md](遗留问题/L0-遗留问题.md) **B1**（解封条件含「复核 `provider_hosts` 是否迁回 L0」）与 D-005 承载，故**不新建条目、册内归属与解封条件不变** · ② **L0 册 A2 未销账**（解封条件＝父 `T-L1-001` 含 `.6` 全 `done`；本批已兑现其在 `.5` 侧的实现面）——待 `.6` 收工后销账 · ③ **旁证：`verify_docs.py` 检查 2 会写盘**——其 `rl.compute_status(tasks)` 对真实 `tasks/` 派生父状态并回写（本批即因此使 `T-L1-001` 被改写为 `doing`；值正确且文档明令父状态禁手写，故无害，但「跑自检会改真相源」值得知悉，未在本批处置）· ④ 下一就绪 `T-L1-001.6`（执行输出契约校验）/ `T-L2-001`（记忆图谱）均 P0，开工走 ④ 对齐（先跑「开工查遗留册」）

## [sc] 2026-09-26T14:19+08:00 — T-L1-001.5 PR 合入收口 · done

- **目标**：把沙箱接线批次（沙箱↔runner 接线 + `provider → host` 映射 + 用例 + 留痕）走 PR squash 合入 main，完成全链路
- **对齐**：人指示「走分支 + commit + PR 合入」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；分支 `feat/T-L1-001.5-sandbox-runner-wiring`（分支 type 取主导变更 `feat`，T-ID 绑 `T-L1-001.5`）
- **关键动作**：
  ① **提交**——单笔 `203987a`（9 文件 / +921−15；`pre-commit` 的「文档完整性自检」Passed，未绕过）
  ② **PR 开展**——[PR #17](https://github.com/Dline6/ST-Agent/pull/17) 经 `gh pr create` 创建（**标题逐字符回读一致**：`len=54`，含全角括号与 `→`），故无需再显式 `bind_pr`——`ccd_pr.get_status` 即报 `bound: true`，app 内 PR 面板通道正常
  ③ **CI 双跑全绿**——push 与 pull_request 两个 run 均 pass（[36223221936](https://github.com/Dline6/ST-Agent/actions/runs/36223221936) 1m22s / [36223232997](https://github.com/Dline6/ST-Agent/actions/runs/36223232997) 1m30s）
  ④ **合入与清理**——`gh pr merge 17 --squash --delete-branch`（显式 `--subject` / `--body-file`，保 `Task: T-L1-001.5` trailer 与署名行）→ squash `83ee6da` → gh 顺带切回 main 并删本地分支 → `fetch --prune` 清远端跟踪
- **变更文件**：项目管理/执行日志.md（+本条 + 上条 verify 补 hash）· 项目管理/tasks/T-L1-001.5（verify 补 PR + squash hash）
- **验证**：PR `state: MERGED`（`mergedAt 2026-09-26T06:18:09Z`）· 本地 `main` = `origin/main` = `83ee6da` · `git ls-remote --heads origin` 仅 `main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · main 上 pytest **545/545**（2 deselected）· `verify_docs.py --strict` PASS（120 文件 / 1060 链接 / 0 断链 / 检查 5–8 全 0 / 账本 SYNC）· squash 提交体含 `Task: T-L1-001.5` trailer 与 `Co-Authored-By` 署名行
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型
- **遗留**：① 本仓库仍禁用 GitHub auto-merge（既有结论未变），故合入仍为「等 CI 绿后手动 squash」· ② app 内 CI 监视仍为 off（`auto_fix` false）——PR #17 本次已绑定，但 CI 失败不会自动唤醒；如需可开 · ③ 下一就绪 `T-L1-001.6`（执行输出契约校验）/ `T-L2-001`（记忆图谱）均 P0，开工走 ④ 对齐（先跑「开工查遗留册」；`.6` 收工时须一并销 L0 册 A2）

## [T-L1-001.6] 2026-09-26T14:35+08:00 — 执行输出契约校验 · done

- **目标**：兑现 01 §2 的 `input_schema` / `output_schema` 执行期真校验——`SkillRunner` 在「执行成功 → 输出登记」之间按描述体 `output_schema` 核验 `ok` 载荷，不合契约即显式失败且**不登记**为可复用结果；同一校验件交付为可复用件，供 `T-L1-003` / `T-L1-004` / `T-ECO-002` 判「上游输出 → 下游输入」匹配，避免各层各造一套口径
- **对齐**：人回「对」，三处岔口均取推荐——**方言＝最小 JSON Schema 子集**（`type`/`properties`/`required`/`items`）· **01 §2 加方言澄清段**（触发工作流第 ⑦ 步）· **违规分支取 `failed` + `log_ref`**；假设 A1–A5 见任务文件 `## 假设与前提`；遗留面命中 L0 册 A2（解封条件＝`T-L1-001` 含 `.5`/`.6` 全 `done`，`.6` 为最后一个叶子，本批**关闭并销账**）
- **输入**：T-L1-001.1 `SkillRegistry.get` / `SkillDescriptor.input_schema`·`output_schema`（`capability_types.py:108`）+ 官方 Pack 12 个描述体形态（`pack.py`，均 `{"type": "object", "properties": {…}}` 无 `required`）；T-L1-001.2 `SkillRunner._execute` 执行器返回点与 `finish()` 登记点 + `REUSABLE_STATUSES`（`reuse/models.py`）；01 §2/§5；03 §1.2 步骤 5/6 + §3.1 连线校验
- **关键动作**：
  ① **第 ⑦ 步顺序**：先改 [01 §2](../docs/技术架构-v2/01-平台共享契约.md)（新增「方言澄清（2026-09-26 定案）」段 + `input_schema` 行说明改写）→ 同步 [03 §1.2](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 步骤 5/6（核验点 + 「未过核验不登记」）→ 最后改代码
  ② **可复用校验件**：新增 `src/st_agent/contracts/schema_check.py`——`check_payload`（递归下钻，`$.a[0].b` 定位 + 人可读说明）、`check_link`（下游 `required` 每项须由上游 `properties` 声明且类型兼容 + 两侧同名属性类型兼容；`integer → number` 放行）、`PayloadCheck` / `LinkCheck` / `SchemaViolation`；宽松面＝空 schema、未进 `required` 的可选属性缺省、子集外关键字（`oneOf`/`$ref`/`format`）、未知 `type` 取值——均不解析不报错
  ③ **runner 接线**：`_execute` 在执行器返回且类型合法后、`finish`（登记）**之前**插入核验——`status == "ok"` 时 `check_payload(descriptor.output_schema, envelope.data)`，不合契约 → `ResultEnvelope.failed(reason=违规点, log_ref=本次 skill-run 路径)`；该分支不在 `REUSABLE_STATUSES` 内，**天然不登记**（GWT-C1 下半，无需额外判断）
  ④ **导出**：`contracts/__init__.py` 增 7 个出口名（`check_payload` / `check_link` / `PayloadCheck` / `LinkCheck` / `SchemaViolation` / `SCHEMA_TYPES` / `DIALECT_KEYWORDS`），消费方只经 `st_agent.contracts`
- **变更文件**：docs/技术架构-v2/{01-平台共享契约,03-L1-能力底座-Skills与MCP}.md · src/st_agent/contracts/{schema_check,__init__}.py · src/st_agent/l1/runner/runner.py · tests/contracts/test_schema_check.py（新，33 用例）· tests/l1/test_output_contract.py（新，12 用例）· 项目管理/tasks/T-L1-001.6（doing→done，补 `## 接口面` / `## 可关闭的遗留` / `## 假设与前提` / `verify`）· 项目管理/决策日志.md（D-009）· 项目管理/遗留问题/L0-遗留问题.md（销 A2）· 项目管理/{任务账本,执行日志}.md
- **验证**：pytest **591/591** 通过（既有 545 + 新 45 + `test_layering` 因新增 `schema_check.py` 多出 1 条参数化；live 2 deselected）· `verify_docs.py --strict` PASS（120 文件 / 0 断链 / 检查 2 就绪集 `T-L2-001` · 检查 5·6·7·8 全 0）· 账本 SYNC · **实测**：缺必填（`{"other": 1}` 对 `required: [risk_level]`）→ 信封 `failed`、`reason` 含 `risk_level`、`log_ref` == `skill-run/<run_id>.json`，且 `OutputRegistry.get(run_id)` 抛 `OutputNotRegisteredError`（不登记）；类型不符（`{"risk_level": 3}`）→ `reason` 含 `string`；合规载荷 → `ok` 且登记可复用；`status=empty` 且 schema 声明 `required` → 仍 `empty`（不误伤）；`output_schema` 为空 → 任意载荷（含 `[1, {"a": 2}]`）照常 `ok` + 登记；违规上游 → 下游 `dependency_failed`；官方 Pack 描述体 + 既有执行器输出全绿（零迁移）
- **假设复核**：**A1 成立**（最小子集覆盖 Pack 全部 12 描述体，回归 591 全绿，零迁移）· **A2 成立**（`empty` 与空 schema 两路均不校验，实测见上）· **A3 成立**（`failed` + `log_ref` 为构造期不变量，实测 `log_ref` 指向本次 `skill-run` 记录）· **A4 成立**（消费方只经 `st_agent.contracts`，`test_layering` 通过）· **A5 成立**（`bool` 对 `number`/`integer` 反例用例通过）
- **决策**：[D-009](决策日志.md)（方言＝最小 JSON Schema 子集 + 校验件单一口径 + 01 §2 文档同步 + 六项否决理由），工作流第 ⑦ 步已走完（先 01 → 再 03 → 再代码）
- **遗留**：① **L0 册 A2 本批销账**（移入该册「已闭（备查）」并留一行，见上）· ② **新口径对下游是强制入口**——`T-L1-003`（连线校验）/ `T-L1-004`（Pack 装载）/ `T-ECO-002`（导入校验）一律经 `contracts.schema_check`，不得另造匹配规则；若下游确需更宽方言（`oneOf`/`$ref`/`format`），须**先改 01 §2 方言段再改本件**（第 ⑦ 步），不得在调用侧补规则 · ③ `check_link` 判据含「上游未声明输出面时按下游 `required` 报缺口」——上游 `properties` 缺位不进 `required` 的字段不判，故此判定**不会**为官方 Pack 之间（无 `required`）产生误报 · ④ 旁证：`verify_docs.py` 检查 2 会写盘（派生父状态回写 `tasks/`）——本批因此使 `T-L1-001` 由 `doing` 转 `done`（值正确、父状态禁手写，无害；未在本批处置）· ⑤ 下一就绪仅 `T-L2-001`（P0/M1）· ⑥ 待走 PR 收口（分支 + commit + squash 合入）

## [sc] 2026-09-26T14:44+08:00 — T-L1-001.6 PR 合入收口 · done

- **目标**：把执行输出契约校验批次（方言定案 + 可复用校验件 + runner 接线 + 用例 + 留痕 + 销 A2）走 PR squash 合入 main，完成全链路
- **对齐**：人先选「先等 CI，绿了再合」，随后指示「合入吧」——沿用既定流程（规范 §5.2 gate + squash 合入即删分支）；分支 `feat/T-L1-001.6-output-contract-check`（分支 type 取主导变更 `feat`，T-ID 绑 `T-L1-001.6`）
- **关键动作**：
  ① **提交**——单笔 `54fc4bf`（13 文件 / +786−19；`pre-commit` 的「文档完整性自检」Passed，未绕过）
  ② **PR 开展**——[PR #18](https://github.com/Dline6/ST-Agent/pull/18) 经 `gh pr create` 创建（标题 = 提交 subject，含全角括号与 `+`）；`ccd_pr.get_status` 报 `bound: true`，app 内 PR 面板通道正常，无需显式 `bind_pr`
  ③ **CI 双跑全绿**——push 与 pull_request 两个 run 均 pass（[36224361199](https://github.com/Dline6/ST-Agent/actions/runs/36224361199) 1m26s / [36224371279](https://github.com/Dline6/ST-Agent/actions/runs/36224371279) 1m25s）；`gh pr checks 18` 两条均 `pass`
  ④ **合入与清理**——`gh pr merge 18 --squash --delete-branch`（显式 `--subject` / `--body-file`，保 `Task: T-L1-001.6` trailer 与署名行）→ squash `31f316e`；**首次调用因 `TLS handshake timeout` 失败且未产生副作用**（`gh pr view` 复核仍 `OPEN`，故直接重试）→ 重试成功；`git fetch --prune` 清远端跟踪
- **变更文件**：项目管理/执行日志.md（+本条）· 项目管理/tasks/T-L1-001.6（verify 补 PR + squash hash）· 项目管理/任务账本.md（render 刷新）
- **验证**：**PR 元数据读回受阻**——`gh pr view` / `gh api` 三次均 `TLS handshake timeout`（GitHub API 此刻不可达），故合入证据改由 git 侧确证：本地 `main` = `origin/main` = `31f316e`（提交 subject 即本批标题）· `git ls-remote --heads origin` 仅 `main`（远端分支已删）· `git branch -a` 仅 `main` + `origin/main`（跟踪已 prune）· 工作区干净 · main 上 pytest **591/591**（2 deselected）· `verify_docs.py --strict` PASS · squash 提交体含 `Task: T-L1-001.6` trailer 与 `Co-Authored-By` 署名行（`git log -1 --pretty=%B` 复核）
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型（方言口径与校验件归属由 [D-009](决策日志.md) 覆盖）
- **遗留**：① **GitHub API 此刻不可达**——PR 页面的 `MERGED` 状态与 `mergedAt` 未取回，如需归档证据可在网络恢复后补一次读回；合入事实不受影响（git 侧已确证）② 本仓库仍禁用 GitHub auto-merge（既有结论未变），合入仍为「等 CI 绿后手动 squash」③ app 内 CI 监视仍为 off（`auto_fix` false）——PR #18 本次已绑定，但 CI 失败不会自动唤醒；如需可开 ④ 上条「下一就绪仅 `T-L2-001`」一说不确——`T-L1-001` 父任务 `done` 后 `T-L1-002`/`003`/`004`/`005`/`006` 与 `T-L4-001` 一并解锁，**就绪集现为 7 条**；按调度规则（P0 → 层号升序 → id 字典序）队首＝`T-L1-002`（MCP Hub，P0/M0/L1），开工走 ④ 对齐（先跑「开工查遗留册」——L0 册 A1 归属即 `T-L1-002`）

## [T-L1-002.1] 2026-09-26T15:15+08:00 — MCP 传输与 Server 注册表 + 权限批准 · done

- **目标**：交付 MCP Hub 的接入面——两种传输（本地 stdio / 远程 HTTP-SSE）、Server 注册表增删启停与「添加即连接测试」、01 §10 权限的「声明 → 逐项批准」闸门；默认只允许本地 stdio，远程须显式开启并确认「数据会离开本机」，其出网一律经 L0 网关登记
- **对齐**：三处岔口经两轮 ④ 定案——① 拆分＝3 个叶子（`.1` 传输+注册+权限 / `.2` tool→Skill 映射 / `.3` 状态机+降级+网关登记）；② **客户端路线先选官方 `mcp` SDK，实测 wheel 后推翻、改自研最小客户端**（见「关键动作①」）；③ stdio 子进程自身出网记为已知边界。假设 A1–A5 见任务文件 `## 假设与前提`
- **输入**：T-L0-001 `Store`；T-L0-004 `EgressGateway.execute/query` + `Sender` 协议 + `NetworkRequestKind`（已含 `remote_mcp`）+ `initiator` 允许 `mcp_server_id`（`net/models.py:89`）；T-SC-001 `validate_permissions` / `parse_permission`（`contracts/registry_types.py:170`，01 §10）；01 §7·§10 + 02 §6·§7 + 03 §5.1·§5.4
- **关键动作**：
  ① **路线先证伪再写码**：解包 `mcp-2.2.0` wheel 查得三笔硬代价——12 项传递依赖（含 `starlette` / `uvicorn` / `sse-starlette` 整套服务端栈）、`ClientSession` 全 `async def` 且无 sync 桥（会话须跨三次调用存活，等于常驻事件循环线程）、**网关载不动响应**（`Sender` 签名不含请求体；`execute()` 只回 `{"bytes_in": n}` 丢响应体；`stream()` 能回文本块却无按次 `sender` 形参——要落地须改已 `done` 的 `T-L0-004` 公共 API）。据此回禀并重定路线，见 [D-010](决策日志.md)
  ② **拆分与账本联动**：`T-L1-002` 转父任务（3 个叶子）；`T-INT-001` 的 `depends_on` 由 `T-L1-002` 换为其三子（否则检查 7「关卡依赖未覆盖本里程碑叶子」判失败）；L0 册 A1 按册规 2 就地拆为 `A1`（远程 MCP → `T-L1-002`）与 `A1b`（官方索引浏览 → `T-ECO-002`，其载体是 09 §4）
  ③ **传输骨架**：`McpTransport`（ABC）统一编号 / 收发 / 握手 / 错误解包，子类只实现 `_write` / `_read`；`StdioTransport` 走子进程 + 行分隔 JSON，stdout 由后台线程泵入队列（Windows 管道上不能 `select`，故用队列超时实现可中断读取）
  ④ **远程传输经网关（不动 L0）**：`HttpSseTransport` 在**按次 `sender` 闭包内**完成真实 POST，响应体经闭包带回；网关仍独占在线判定 / 审计落盘 / 超时与取消。沿用 T-L0-005「注入按操作抓取闭包」先例（A2）。发包失败按**网关自己的错误词汇**（`EgressError` / `EgressUnavailableError`）上报，使失败也照常落审计
  ⑤ **注册表与权限**：`McpServerRegistry`（增删启停 + `test_connection`；可注入 `transport_factory` 供离线验证、`env_resolver` 供凭据取值）+ `McpPermissionBook`（逐项批准；`declare` 保留仍在清单内的既有决定，避免更新记录把已批准权限打回待批）
  ⑥ **离线对端**：`tests/l1/_mcp_stub_server.py`（最小 MCP Server，含 `--crash` / `--silent` 两个故障开关）
- **变更文件**：src/st_agent/l1/mcp/{__init__,errors,ids,models,transports,client,permissions,registry}.py（新，8 文件）· tests/l1/_mcp_stub_server.py（新）· tests/l1/test_mcp_transport.py（新，14 用例）· tests/l1/test_mcp_registry.py（新，26 用例）· 项目管理/tasks/{T-L1-002（转父 + 拆分记录）, T-L1-002.1（新）, T-L1-002.2（新）, T-L1-002.3（新）, T-INT-001（依赖改写）} · 项目管理/遗留问题/{L0-遗留问题.md（拆 A1/A1b）, L1-遗留问题.md（新）, README.md（索引）} · 项目管理/决策日志.md（D-010）· 项目管理/{任务账本,执行日志}.md
- **验证**：pytest **639/639** 通过（既有 591 + 本批 40 用例 + `test_layering` 因新增 8 个模块文件多出 8 条参数化；live 2 deselected）· `verify_docs.py --strict` PASS（123 文件 / 1133 链接 / 0 断链 / 检查 4–8 全 0 / 账本 SYNC）· **一次性实测探针**（独立脚本，跑完即删）六项：① stdio 真实子进程 → `ok=True`、`server_name='stub-mcp-server'`、`protocol='2025-06-18'`、tools=`['echo_symbol','list_watch_groups']`、落盘 `mcp-server/stub_local.json`；② `--crash` → `ok=False`、原因「MCP Server 进程已退出（stdout 关闭）」；③ 远程未确认 → `McpValidationError`「这是远程 Server，数据会离开本机」；④ 远程确认后 → `ok=True`、风险标注非空、**审计 3 条全 `ok`、主机 `mcp.example.com`、载荷含明文 0 条**；⑤ 权限：初始 pending 两条 → 批准 `exec_command` 后 `approved=('exec_command',)` / `pending=('net_access:<*.baostock.com>',)`，展示逐条含「这个 Server 想做什么」与批准状态；⑥ 禁用落盘可复读、移除后读取 `McpServerNotFoundError`、**移除后审计仍可查 3 条**
- **假设复核**：**A1 成立**（最小协议面覆盖 03 §5.1 所需，stub 三方法走通）· **A2 成立且已实证**（`git status -- src/st_agent/l0 docs/` 为空——L0 与 `docs/` 零改动，故不触发工作流第 ⑦ 步；远程响应经 sender 闭包带回，审计与响应体同时可得）· **A3 成立**（失败不落盘：单测在干净 store 上断言 `_persisted(store) == []`，探针步骤③同证）· **A4 成立**（本批只承诺经网关的远程登记，stdio 边界按已知边界登记遗留）· **A5 成立**（`McpServerRecord` 为 L1 内部类型；`test_layering` 通过，无跨层 import，01 契约未动）
- **决策**：[D-010](决策日志.md)（自研最小客户端 + 远程响应经 sender 闭包带回 + stdio 审计记为已知边界；含 SDK 路线被实测推翻的三条依据）
- **遗留**：① **stdio 子进程自身出网不在审计范围**——已登记新建的 [L1 册 A1](遗留问题/L1-遗留问题.md)，归属「人决」（是否立项做进程级网络隔离）② **L0 册 A1 未销账**——本批只建好远程出网的登记调用点，闭环由 `.3` 接成后统一销账（`A1b` 已改归 `T-ECO-002`，本批不碰）③ 旁证：`render_ledger.compute_status` 会写盘派生父状态，本批因此使 `T-L1-002` 由 `todo` 转 `doing`（值正确、父状态禁手写，无害；未在本批处置）④ 下一就绪按调度规则为 `T-L1-002.2`（依赖 `.1` 与 `T-L1-001.1`；`.3` 其时尚不就绪），届时走 ④ 对齐（先跑「开工查遗留册」——L1 册 A1 归属「人」，不在本任务范围）⑤ 待走 PR 收口（分支 + commit + squash 合入）

## [sc] 2026-09-26T15:30+08:00 — T-L1-002.1 PR 合入收口 + PR 惯例改启用 auto-merge · done

- **目标**：① 把 MCP 传输批次走 PR squash 合入 main；② 按负责人指示把 PR 合入惯例从「等 CI 绿后手动 squash」改为**启用 auto-merge、CI 通过后自动合入**
- **对齐**：负责人指示「帮我把惯例改成启用auto-merge吧，CI通过后自动合入」——取代此前历次收口日志所记的「本仓库仍禁用 GitHub auto-merge」结论。该禁用源于**平台限制**（私有仓库 + free 计划不支持 auto-merge），故负责人同批**把仓库可见性由私有改为公开并手动开启 auto-merge**；本批据此落地惯例
- **关键动作**：
  ① **平台限制与前置动作（本条初稿曾写错因果，已按负责人指正更正）**：GitHub 的 auto-merge **不支持私有仓库 + free 计划**——这正是历次收口日志所记「本仓库仍禁用 auto-merge」的真正原因，非配置疏漏。负责人**将仓库可见性由私有改为公开并手动开启 auto-merge**后，冷读 REST `allow_auto_merge` 与 GraphQL `repository.autoMergeAllowed` **双双为 `true`**。我一度把「值变 true」归功于自己的 `-f` PATCH，属归因错误（两次读取之间发生了外部人工变更）——教训记入 [D-011](决策日志.md)
  ② **PR #19 合入**：`gh pr merge 19 --auto --squash --delete-branch`——当时仓库开关**尚未**生效而检查已全绿，故走的是 gh 回退路径直接合入（`autoMergeRequest: null` 即此证）→ squash `b1bdb31`；gh 顺带切回 main 并删本地分支，`git fetch --prune` 清远端跟踪
  ③ **惯例落地**：[Git协作规范.md](Git协作规范.md) 四处同步——§3.3「合入 / 删除」两条、§3.4 映射表「合入」行、§5.2 新增「合入方式」段、§9 速查卡片；[D-011](决策日志.md) 记选型与代价（CI 绿即落地 → §5.2 人工自查须前置到**建 PR 之前**；squash 体取自**分支单条 commit** → 「一任务一条干净 commit」更要紧）
  ④ **本批自身即首个验证样本**：本收口批改走 PR + auto-merge 合入（此前「收口日志直推 main」的做法本次让位给 PR，以取得**真正带 pending 检查**的 auto-merge 证据）
- **变更文件**：项目管理/Git协作规范.md · 项目管理/决策日志.md（D-011）· 项目管理/执行日志.md · 项目管理/tasks/T-L1-002.1-传输与Server注册表权限批准.md（verify 补 PR + squash hash）· 项目管理/任务账本.md
- **验证**：仓库侧**冷读双路**——REST `allow_auto_merge: true` · GraphQL `repository.autoMergeAllowed: true`（两者均由负责人**改仓库为公开 + 手动开启**后才成立，非 Agent 侧操作结果）· 仓库可见性 `private → public` 的变更已记入 [D-011](决策日志.md) 备查 · PR #19 `state: MERGED`（`mergedAt 2026-09-26T07:23:06Z`，`autoMergeRequest: null` —— 属启用前的回退合入，非 auto-merge 路径）· squash `b1bdb31` 提交体含 `Task: T-L1-002.1` trailer 与 `Co-authored-by: Claude Haiku 4.5` 署名行（`git log -1 --pretty=%B` 复核）· 本地 `main` = `origin/main` = `b1bdb31` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · main 上 pytest **639/639**（2 deselected）· `verify_docs.py --strict` PASS
- **假设复核**：无（流程收口，无新增假设）
- **决策**：[D-011](决策日志.md)（启用 auto-merge；含操作教训与代价）
- **遗留**：① **本批 PR 的合入路径即惯例生效证据**——若 CI 未绿时 `--auto` 未被挂起而是报错，说明 gh / 仓库侧仍有落差，须在下次收口复核并回填 ② L0 册 D1（分支保护未开）取舍未变；但 auto-merge 落地后「等 CI 绿手动合入」这笔人工成本已消除 ③ L0 册 D2（PAT 仍为 classic `repo` + `workflow`）未变 ④ 下一就绪 `T-L1-002.2`（tool → Skill 自动映射与版本化，P0/M0/L1），开工走 ④ 对齐（先跑「开工查遗留册」——L1 册 A1 归属「人」，不在该任务范围）

## [sc] 2026-09-26T15:45+08:00 — auto-merge 生效条件补正批次 PR 合入收口 · done

- **目标**：把「补齐 auto-merge 生效条件」（ruleset 必检 + 管理员 bypass + 文档更正 + [D-012](决策日志.md)）走 PR 合入 main，并**实测**惯例是否真的成立
- **对齐**：人选定「加 ruleset 要求 CI 检查」+「配 bypass、保留收口日志直推 main 惯例」；随后指示「补收口日志」
- **关键动作**：
  ① **PR #21 走真 auto-merge**：CI 仍在跑时挂 `--auto`（`enabledAt 07:41:02Z`）→ 等待 → CI 于 `07:42:22Z` 转 success → PR 于 `07:42:33Z` **自动合入**（绿后 **11 秒**），squash `c1ff998`
  ② **本批即 [D-012](决策日志.md) 的验证样本**：挂上后读 `autoMergeRequest` **非空**、`mergeStateStatus: BLOCKED`、`state: OPEN`（真在等）——与 PR #20 的「立即合入」形成对照，证明**必检规则是把 auto-merge 从「不等 CI」掰成「等 CI」的唯一开关**
  ③ **收口日志直推 main**：本条目以直推方式落到 main，同时**实测 ruleset 的管理员 bypass 是否放行**
- **变更文件**：项目管理/执行日志.md（本条）
- **验证**：PR #21 `state: MERGED`（`mergedAt 2026-09-26T07:42:33Z`）· **时序链完整**：`da03f15` 的两个 CI run `07:40:47Z` / `07:40:51Z` 启动、`07:42:22Z` 双双 `success`；auto-merge `07:41:02Z` 挂上（此时二者仍 `in_progress`）；合入 `07:42:33Z`（绿后 11 秒）· squash `c1ff998` 提交体含 `Task: sc` trailer 与 `Co-authored-by: Claude Haiku 4.5` 署名行 · 本地 `main` = `origin/main` = `c1ff998` · 工作区干净 · **直推 main 未被 ruleset 拦下**（bypass 生效——若被拒，本条 commit 的 push 会失败）
- **假设复核**：无新增假设（上条已复核项不变）
- **决策**：无新选型（[D-011](决策日志.md) / [D-012](决策日志.md) 覆盖）
- **遗留**：① **auto-merge 机制已实测成立**——D-011 遗留①「本批 PR 的合入路径即惯例生效证据」与 [D-012](决策日志.md) 的验证项**均已回填兑现** ② **[D-012](决策日志.md) 的 bypass 验证亦兑现**（本次直推成功）③ `ci.yml` 的 `on: push` 无分支过滤，故同一 SHA 会出现两个同名 `check` run（PR #21 上即两条，`BLOCKED` 判定正常）——若将来出现「Expected — Waiting for status to be reported」卡住，把 `push` 收窄为 `branches: [main]` 即可；已记 D-012 遗留，本批不动 ④ 下一就绪 `T-L1-002.2`（tool → Skill 自动映射与版本化，P0/M0/L1），开工走 ④ 对齐（先跑「开工查遗留册」——L1 册 A1 归属「人」，不在该任务范围）

## [sc] 2026-09-26T15:50+08:00 — 补开 `delete_branch_on_merge`（真 auto-merge 下的分支清理缺口）· done

- **目标**：收口时实测发现「真 auto-merge 下 `--delete-branch` 对**远端**分支无效」，补上分支清理这一步，使规范里「合入即删分支」真正自动
- **对齐**：负责人选定「开启 `delete_branch_on_merge`」
- **关键动作**：
  ① **实测发现**：PR #21 以 `--auto --delete-branch` 合入后，远端分支 `docs/sc-automerge-required-check` **仍在**（`git ls-remote --heads` 可见）；对照 PR #19 / #20 的远端分支已被删。差别在**合入主体**：那两次是 gh **直接合入**（gh 自行删分支），而真 auto-merge 是**服务端异步合入**，gh 的删除动作不参与
  ② **即时清理**：`git push origin --delete` + `git branch -D` + `fetch --prune`，本地与远端均只剩 `main`
  ③ **开仓库开关**：`delete_branch_on_merge=true`（冷读 REST 与 GraphQL **双路确认**——吸取本批前段的教训，不看 PATCH 回显）。此后任何 PR 合入后 GitHub 自动删 head 分支
  ④ **规范同步**：[Git协作规范.md](Git协作规范.md) §3.3「删除」条改写，写明「删远端分支靠该开关，不能指望 `--delete-branch`」及其原因
- **变更文件**：项目管理/Git协作规范.md · 项目管理/执行日志.md · 项目管理/任务账本.md
- **验证**：冷读双路 —— REST `delete_branch_on_merge: true` · GraphQL `repository.deleteBranchOnMerge: true`；同批读到 `allow_auto_merge: true`、`visibility: public` · 清理后 `git branch -a` 仅 `main` + `origin/main`、`git ls-remote --heads origin` 仅 `refs/heads/main` · `verify_docs.py --strict` PASS · 本批以**直推 main** 落库（bypass 再次放行）
- **假设复核**：无新增假设（上条已复核项不变）
- **决策**：**未新立 `D-NNN`**——本项属仓库设置的操作性补齐（非契约 / 架构 / 跨任务复用规则选型），故只落规范与日志；auto-merge 的**生效条件**仍由 [D-011](决策日志.md) / [D-012](决策日志.md) 承载，本条是 D-012 影响面的延伸
- **遗留**：① 顺带记下仓库的 squash 默认——`squash_merge_commit_title: COMMIT_OR_PR_TITLE`、`squash_merge_commit_message: COMMIT_MESSAGES`：**单 commit 分支**时提交体即该 commit 的消息（`Task:` trailer 与署名行得以保留），**多 commit 分支**则会拼成多段，故「一任务一条干净 commit」这条要求属实 ② 下一就绪 `T-L1-002.2`（tool → Skill 自动映射与版本化，P0/M0/L1），开工走 ④ 对齐（先跑「开工查遗留册」——L1 册 A1 归属「人」，不在该任务范围）

## [T-L1-002.2] 2026-09-26T16:09+08:00 — MCP tool → Skill 自动映射与版本化 · done

- **目标**：兑现 03 §5.2——一台已批准权限的 MCP Server 的每个 tool 自动注册为平台 Skill（`source: mcp-mapped`，元数据从 MCP schema 派生），映射关系落盘版本化，并按 tool 契约**指纹**判断「是否需要重新映射」
- **对齐**：人回「对，按 A 方案做」——④ 对齐结论：交付面＝GWT-1–5 + 回归；三处补充获准——① `tool` 名兼作落盘路径段，故新增安全段校验（显式拒非法 / 超长）；② GWT-5 的「可用 Skill 列表」入口由本任务新增 `available_skills()`（任务原声明的三方法里没有）；③ **A 方案**＝本任务加 `apply_remap`，否则 `pending-remap` 在本层无出口（原 GWT-3 只要求「标记 + 提示」）。假设 A1–A8 见任务文件 `## 假设与前提`；遗留面命中 0（L0 册 A1 归属 `T-L1-002` 但销账归 `.3`；L1 册 A1 归属「人」），本批不销账
- **输入**：`T-L1-002.1`（已 done）`McpServerRegistry.get_server`/`list_servers`/`test_connection`（**取 tool 列表复用连接测试路径**——`.1` 未交付任务原接口面所写的 `McpClient.list_tools(server_id)` 形态，故按实际面消费，`docs/` 与 `.1` 零改动）、`McpPermissionBook.approvals/approve/reject`；`T-L1-001.1` `SkillRegistry.register`/`publish_version`/`get`/`list_all`/`list_versions`、`skill_id_for`/`parse_skill_id`；`T-SC-001` `SkillDescriptor`/`NeutralityGuard`；`T-L1-001.6` `contracts.schema_check.check_link`；01 §2·§6·§9 + 03 §5.2
- **关键动作**：
  ① **指纹口径（A6）**：`tool_fingerprints` 对 `input_schema` / `output_schema` 各自做**规范化 JSON**（`sort_keys` 稳定键序）的 sha256，再合成总指纹；分侧指纹随记录落盘，故变化说明能点名是输入面还是输出面变了。**只含 schema、不含 `description`**（GWT-3 原话「MCP schema 指纹」）——纯文案改动不触发重映射，代价记 A6
  ② **自动注册（GWT-1）**：`sync_tools` 逐 tool `SkillRegistry.register(base, source="mcp-mapped", parameters=(), offline_level="none", permissions=<Server 声明>)`；`base` 按 A1 拼 `sk_mcp_<server>_<tool>`（小写安全段、两段归一、超长显式拒）；`name` 走既有 §6 中性化校验（`SkillValidationError` 包为 `McpValidationError` 并点出 tool 名）
  ③ **幂等与增量（GWT-2）**：装载前先读映射记录——指纹一致即**原样返回**（不重复注册、不报错）；skill_id 已在册但记录缺失时「认领」而不重复注册；新增 tool 只注册新增者。**净效果**：同一 Server 重复装载零写盘
  ④ **契约变化（GWT-3）**：指纹不一致 → 只把记录置 `pending-remap` 并写变化说明，**基线指纹不动、已注册描述体一字不改**（「不静默覆盖」因此可复查）；契约回到基线 → 自愈为 `active`。检测阶段**不**写 `UpdateInfo`（A2，单测断言无 `skill-update/` 落盘）
  ⑤ **确认重新映射（A 方案，GWT-3 下半）**：`apply_remap` 取 `SkillRegistry.get` 的**旧 input_schema** 与实时新 schema，**双向** `check_link` 任一不兼容（下游 `required` 未被上游 `properties` 声明）→ 主版本（`publish_version` 自然产生 `skill-update/` 待检查标记，引用方须人工确认），皆兼容 → 次版本；判定**只经** `check_link`，不另造匹配规则（D-009）。老版本文件保留（§9）
  ⑥ **禁用联动（GWT-5）**：`available_skills()` = `list_all()` 减去**不可用 Server** 派生者——「不可用」＝ Server 已禁用**或记录已被移除**（`remove_server` 不清映射，故只能在这一层判定；后者是 `.1` 交付后才显形的缺口，本批用同一规则一并覆盖）；映射记录保留，重新启用即恢复
  ⑦ **落盘路径安全**：`check_tool_name` 拒空白 / 路径分隔符 / 首字符非字母数字（故 `..`、`_private`、`a/b`、`a\b` 皆拒）；另设跨 Server 归一撞车检测（仅大小写不同会撞同一 skill_id）→ 显式拒，不静默别名
- **变更文件**：src/st_agent/l1/mcp/mapping.py（新）· src/st_agent/l1/mcp/{errors,__init__}.py · tests/l1/test_mcp_mapping.py（新，31 用例）· 项目管理/tasks/T-L1-002.2-tool映射Skill与版本化.md（todo→doing→done，补 `## 接口面`（含上游指针改正）/ `## 可关闭的遗留` / A5–A8 / verify）· 项目管理/遗留问题/L1-遗留问题.md（新增 `A2`）· 项目管理/决策日志.md（[D-013](决策日志.md)）· 项目管理/{任务账本,执行日志}.md
- **验证**：pytest **671/671** 通过（既有 639 + 本批 31 + `test_layering` 因新增 `mapping.py` 多 1 条参数化；live 2 deselected）· `verify_docs.py --strict` PASS · **真实子进程路径**由 `test_real_stdio_stub_server_end_to_end` 覆盖（真实 stdio stub server → 2 个 tool 全落映射、`available_skills()` 可查、无 output schema 者走宽松态）· 离线段逐条覆盖：GWT-1 注册与元数据（`source`/`input_schema`/`name`/`parameters==()`/`offline_level=="none"`）、GWT-2 幂等与增量（重复装载记录逐字段相等、只注册新增者）、GWT-3 变化检测（分侧变化说明、描述体未被覆盖、无 `skill-update/`）+ 自愈、A 方案主/次版本两条路径（1.1 无标记 / 2.0 有标记 + 老版本保留）、GWT-4 记录字段与落盘位置、GWT-5 禁用/移除/恢复与非 MCP Skill 不受影响、边界（A1 超长、A4 待批与已拒、A7 查询不重探、落盘路径安全、归一撞车、指纹键序无关且忽略 description）
- **假设复核**：**A1 成立**（超长派生名显式拒，未静默截断）· **A2 成立**（`test_detection_writes_no_update_marker` 断言检测后无 `skill-update/`）· **A3 成立**（MCP 未给 output schema → 描述体空 schema → `check_payload` 宽松放行，不误伤执行）· **A4 成立**（待批 / 已拒两种未批准态均拒绝装载；批准后放行）· **A5 成立**（`parameters == ()`、`offline_level == "none"`、权限随 Server 声明）· **A6 成立**（双向 `check_link` 判主/次——兼容改动走 1.1 且无待检查标记，不兼容走 2.0 且产生标记；指纹忽略 description）· **A7 成立**（`pending_remap`/`mappings`/`available_skills` 调用前后探针计数不变）· **A8 成立**（tool 消失与 Server 移除均不回收记录，`available_skills()` 滤掉；对已消失 tool 的 `apply_remap` 显式拒）——A8 已登记 [L1 册 A2](遗留问题/L1-遗留问题.md)
- **决策**：[D-013](决策日志.md)（映射记录形态 + 指纹口径 + 重新映射版本判定 + 回收边界；含「上游接口面预测与实际交付不符」的处置留痕）
- **遗留**：① **映射记录与派生 Skill 不回收**（tool 消失 / Server 移除）——已登记 [L1 册 A2](遗留问题/L1-遗留问题.md)，归属「人」（是否立项回收）② **L0 册 A1 未销账**——本批只做映射层，远程 MCP 的网关闭环归 `.3` ③ **任务 `## 接口面` 的上游指针曾预测偏差**——原写「待 `.1` 交付 `McpClient.list_tools(server_id)`」，实际 `.1` 交付的是 `McpServerRegistry.test_connection`；已在开工时按实际面改正并记入 D-013，后续任务写接口面时宜以**已交付代码**为准而非推测 API ④ 下一就绪按调度规则为 `T-L1-002.3`（状态机 + 崩溃重连 + 降级 + 网关登记，P0/M0/L1；依赖 `.1`+`.2` 均已 done），届时走 ④ 对齐（先跑「开工查遗留册」——L1 册 A2 归属「人」，不在该任务范围）⑤ 待走 PR 收口（分支 + commit + auto-merge 合入）

## [sc] 2026-09-26T16:19+08:00 — T-L1-002.2 PR 合入收口 · done

- **目标**：把 MCP tool → Skill 映射批次（映射模块 + 31 用例 + [D-013](决策日志.md) + [L1 册 A2](遗留问题/L1-遗留问题.md)）走 PR squash 合入 main，并顺带**实测** auto-merge 在必检规则下仍按预期工作
- **对齐**：沿用既定流程（[Git协作规范](Git协作规范.md) §5.2 gate + auto-merge 合入即删分支；分支 `feat/T-L1-002.2-mcp-skill-mapping`，基于 main HEAD `80cc346`）
- **关键动作**：
  ① **提交**——单笔 `783385b`（9 文件 / +1080−18；`pre-commit` 的「文档完整性自检」Passed，未绕过）
  ② **PR 开展**——[PR #22](https://github.com/Dline6/ST-Agent/pull/22) 经 `gh pr create` 创建（标题 = 提交 subject）；`ccd_pr.get_status` 报 `bound: true`，app 内 PR 面板通道正常，无需显式 `bind_pr`
  ③ **挂 auto-merge 即等**——`gh pr merge 22 --auto --squash --delete-branch` 于 `08:13:04Z` 挂上；读回 `autoMergeRequest` **非空**、`state: OPEN`、`mergeStateStatus: BLOCKED`、CI 1 条 `pending`——与 [D-012](决策日志.md) 的验证特征一致（真在等必检，非「立即合入」）
  ④ **自动合入**——CI 绿后 PR 于 `08:14:23Z` **自动合入**（挂上后约 79 秒），squash `6302214`
  ⑤ **分支清理走仓库开关**——`delete_branch_on_merge` 生效：`git fetch --prune` 报 `[deleted] origin/feat/T-L1-002.2-mcp-skill-mapping`，`git ls-remote --heads origin` 仅 `refs/heads/main`（真 auto-merge 是服务端异步合入，gh 的 `--delete-branch` 不参与远端删除——上条收口日志已记此因）
- **变更文件**：项目管理/执行日志.md（+本条）· 项目管理/tasks/T-L1-002.2-tool映射Skill与版本化.md（verify 补 PR + squash hash + D-013 / 册 A2 引用）· 项目管理/任务账本.md（render 刷新）
- **验证**：PR #22 `state: MERGED`（`mergedAt 2026-09-26T08:14:23Z`）· squash `6302214` 提交体含 `Task: T-L1-002.2` trailer 与 `Co-authored-by: Claude Haiku 4.5` 署名行（`git log -1 --pretty=%B` 复核）· 本地 `main` = `origin/main` = `6302214` · `git branch -a` 仅 `main` + `origin/main`（本地分支先 `git diff --quiet main <branch>` 证明树一致再 `-D` 删除）· 工作区干净 · **main 上 pytest 671/671**（2 deselected，1:45）· `verify_docs.py --strict` PASS
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型（映射口径由 [D-013](决策日志.md) 覆盖）
- **遗留**：① **下一就绪按调度规则为 `T-L1-002.3`**（MCP Server 状态机 + 崩溃重连 + 降级 + 网关登记，P0/M0/L1；依赖 `.1`+`.2` 均已 done），届时走 ④ 对齐（先跑「开工查遗留册」——L1 册 A1 / A2 归属均「人」，不在该任务范围）② M0 收口尚需 `T-L1-003`–`006` 与 `T-INT-001`；归档前注意关卡依赖覆盖 ③ app 内 CI 监视仍为 off（`auto_fix` false）——本批 PR 已绑定，但 CI 失败不会自动唤醒；如需可开 ④ 上批遗留「`ci.yml` 的 `on: push` 无分支过滤，同一 SHA 会出现两条同名 `check`」未变，本批 `BLOCKED` 判定正常，暂无动作

## [T-L1-002.3] 2026-09-26T16:45+08:00 — MCP Server 状态机 + 崩溃重连 + 降级 + 网关登记 · done

- **目标**：兑现 03 §5.3——五态状态机 + 转移留痕、崩溃显式提示与可配次数的自动重连、重连耗尽后的降级候选**并列陈列**、禁用中止进行中调用，并把远程 MCP 的出网接成完整闭环（兑现 [L0 册 A1](遗留问题/L0-遗留问题.md) 的「远程 MCP」半）
- **对齐**：人回「按推荐口径」——④ 对齐三处岔口均取推荐：**提示取 L1 本地结构化提示**（不扩 01 §11，故不触发第 ⑦ 步）· **降级候选 = 平台内置 Skill 全量并列**（`source != "mcp-mapped"`，不排序不建议）· **不拆子任务**（一次交付）；假设 A1–A7 见任务文件 `## 假设与前提`；遗留面命中 L0 册 A1（本批部分关闭 + 修正归属）
- **输入**：story-08 验收段「边缘情况：MCP Server 崩溃 → 明确提示 + 自动重连（可配次数）+ 降级到官方数据源」+「禁用 → 进行中调用中止」+「所有请求都在网络活动面板」；03 §5.2/§5.3/§1.5；01 §7（配置元模型）/ §10 / §11；02 §6；`T-L1-002.1` 的 `McpServerRegistry`/`McpPermissionBook`/`HttpSseTransport`；`T-L1-002.2` 的 `McpSkillMapper`；`T-L0-004` 的 `EgressGateway.execute(cancel=…)`/`query`；`T-L1-001.1` 的 `SkillRegistry.list_all()`
- **关键动作**：
  ① **状态机**：`lifecycle.py` 的 `McpHubStateMachine.transition` 按 `ALLOWED_TRANSITIONS` 判合法——同态为**幂等写**（刷新当前态、不记痕），非法抛 `McpStateTransitionError` 且不改态。**唯一的来源约束在 `disabled` 上**（`disabled → connected`/`reconnecting` 一律拒），其余态之间放开，因断连事件可能在任何非禁用态被观测到
  ② **状态推导与留痕分置**（A5）：当前态落 `config/mcp-lifecycle/<sid>.json`；转移 `execution_log/mcp-lifecycle/transition/**`、提示 `.../notice/**`（append-only）。`state_of` 是**纯读**且注册表事实优先——`disabled` 由启停开关推导、`permission_pending` 由 `.1` 权限账本推导，且两者**都不粘住**（权限批准/重新启用即离开）；`sync_from_registry` 是唯一的「核对并落定」显式动作
  ③ **重连**（GWT-2）：`on_disconnect` → 提示 `server-disconnected` + 转 `reconnecting` → 按配置次数重试（**首次立即、间隔只在两次尝试之间**）→ 成功转 `connected` + `reconnect-recovered`，耗尽转 `disconnected` + `reconnect-exhausted`。`reconnecting` 态下另有单次 `reconnect` 原语
  ④ **策略注册**（A6）：`McpHubConfig` 把「次数」「间隔」拆为**两条**标量 01 §7 条目（`mcp-hub/reconnect-max-retries` / `-interval-ms`）——一条目只有一个单字段 `panel_form_spec`；`set()` 每次真实改动产生 `ChangeRecord`（`chg_<20hex>`）落 `execution_log/mcp-hub-change/<change_id>.json`
  ⑤ **降级候选**（GWT-3，A2）：`degrade_candidates` 返回平台内置 Skill 并列列表（每条带 `source` / `offline_level` / 可用性说明），排除 `mcp-mapped`（失效 Server 自己派生的正是被遮蔽者）；可选叠加 `SkillSandbox.is_disabled` 判「已被禁用」。**纯读**：不排序权重、不合并、不给建议、不自动切换（铁律 4）
  ⑥ **禁用中止**（GWT-4，A7）：`begin_call` 返回 `ActiveCall` 句柄（`__enter__` 登记 / `__exit__` 注销），`cancel_inflight` 只置信号不写盘，`disable` = 关开关 → 置信号 → 转 `disabled`（顺序有意让信号先于状态生效，杜绝「已禁用却算成功」的窗口）。`HttpSseTransport` 增可选 `cancel`，在按次 `sender` 闭包内**发包前后各查一次**——已置即按 `EgressCancelledError` 上报，网关落 `cancelled` 审计、信封走失败分支
  ⑦ **离线替身**：`FakeTransport`（连接测试）· `Probe`（重连次数可控）· `FakeSandbox` · `Clock`（严格递增时钟，保证留痕排序确定）· `blocking_post`（在途调用）
- **变更文件**：src/st_agent/l1/mcp/lifecycle.py（新，约 700 行）· src/st_agent/l1/mcp/{errors,transports,__init__}.py · tests/l1/test_mcp_lifecycle.py（新，26 用例）· 项目管理/tasks/T-L1-002.3（doing→done + 接口面按实测校正 + A1–A7 + verify）· 项目管理/遗留问题/{L0-遗留问题（销 A1 + 新增 E1）, L1-遗留问题（快照）}.md · 项目管理/决策日志.md（[D-014](决策日志.md)）· 项目管理/{任务账本,执行日志}.md
- **验证**：pytest **698/698** 通过（既有 671 + 本批 26 + `test_layering` 因新增 `lifecycle.py` 多 1 条参数化；live 2 deselected）· `verify_docs.py --strict` PASS（124 文件 / 1207 链接 / **0 断链** / 检查 4–8 全 0）· 账本 SYNC（就绪集 `T-L1-003`/`T-L1-004`/`T-L1-005`/`T-L1-006`/`T-L2-001`/`T-L4-001`，父任务 `T-L1-002` 由 children 派生 `done`）· **实测**：五态合法链逐条留痕（含 `trace_id`）· `disabled → connected` / `disabled → reconnecting` 被拒且不记痕 · 同态幂等零留痕 · 3 次重连探测试例实得 `probe.calls == 3` 且 `sleep` 只在尝试之间（`[0.25, 0.25]`）· `max_retries=0` 时零探测直落 `disconnected` · 两条策略条目读回八字段 `ConfigEntry`、改值产生 `change_id` 且同值再设零留痕、负值显式拒 · 降级候选：官方 Pack 12 项全列（`source` 全 `official`、按 `skill_id` 升序）、自建/`mcp-mapped` 的取舍符合 A2、调用前后 `config` 分区零变化 · 禁用后 `state_of == "disabled"`、在途句柄 `cancelled`、提示 `call-cancelled` · **中止全链路**（真实 `HttpSseTransport` → 网关）落 1 条 `status="cancelled"` 审计 + 信封 `failed`、审计不含请求体明文 · 远程 2 次 `tools/list` 落 2 条 `ok` 审计（主机 `mcp.example.com`、字节数非零、目的非空），`remove_server` 后仍可查 · commits：待收工
- **假设复核**：**A1 成立**（提示取本层 `McpHubNotice`：`git status -- docs/ src/st_agent/contracts/` 为空，01 §11 与 `PlatformEvent` 零改动，未触发第 ⑦ 步）· **A2 成立**（候选 = `source != "mcp-mapped"` 全量并列；三个已 seed 的 Skill 取舍与可用性标志符合预期；纯读断言通过——不写盘、不留痕、不改状态）· **A3 成立**（禁用即中止：在途句柄被信号化且结果走失败分支，无「成功」假象）· **A4 成立**（`remove_server` 后 `gateway.query` 仍得 2 条历史审计）· **A5 成立**（状态落 `config`、留痕落 `execution_log`，两分区各见对应文件）· **A6 成立**（两条独立标量条目，各带自己的 `panel_form_spec` 与 `change_id`）· **A7 成立**（中止为**检查点**语义：传输层发包前后各查一次信号；网关照 `cancelled` 落审计。已实测，非纸面）——A7 的边界（会话中间无法抢占）属 02 §6 既有口径，不在本任务返工面
- **决策**：[D-014](决策日志.md)（提示载体 / 降级候选口径 / 策略拆条目 / 中止粒度四项选型 + 理由与代价）
- **遗留**：① **实现期新发现 `Store` 写并发不安全**——分区清单内存缓存 + 「读缓存 → 重建 → 写回」无锁，两个线程写**同一分区**会丢条目；本任务 GWT-4 首版用例（在途调用线程与禁用线程同写 `execution_log`）即实证审计记录被覆盖而查不到。已按册规 1 登记 [L0 册 E1](遗留问题/L0-遗留问题.md)（归属「人」定策略：加锁 / 写前重读 / 单写者队列），定案前 L1 侧以「并发窗口内单一写者」为约定（已写入用例注释），**本批不修 L0** ② **L0 册 A1 本批销账**（「远程 MCP」半已闭环；「官方索引浏览」半早前已改归 `T-ECO-002`，见册内拆分留痕）· **L1 册 A2 就地修正解封条件**（其原解封条件把 `T-L1-002.3` 列为回收工作的候选归属，但回收属产品决策、不在本任务的状态机 / 重连 / 降级面内，故本任务未承接；`.3` 已 `done` 使检查 8 判「该销的账没销」，据册规 2 去掉该指向并写明理由，归属仍为「人」）③ `provider → host` 映射的最终归属仍待 `T-L1-006` 复核（既有项，由 L0 册 B1 与 [D-005](决策日志.md) 承载，本批不动）④ **`T-L1-002` 全叶子已 `done`**，父任务派生 `done`；M0 集成关卡 [T-INT-001](tasks/T-INT-001-M0集成关卡骨架打通冒烟.md) 的 MCP 环节随之解封 ⑤ 待走 PR 收口（分支 + commit + squash 合入）

## [sc] 2026-09-26T16:59+08:00 — T-L1-002.3 PR 合入收口 · done

- **目标**：把 MCP 生命周期批次（五态状态机 + 崩溃重连 + 降级并列 + 禁用中止 + 26 用例 + 留痕 + 销 L0 册 A1 / 登记 E1）走 PR 合入 main，完成全链路
- **对齐**：人指示「走分支 + PR + auto-merge」→ 合入后「补收口日志」——沿用既定流程（[Git协作规范](Git协作规范.md) §5.2 gate + auto-merge 合入即删分支；分支 `feat/T-L1-002.3-mcp-lifecycle`，基于 main HEAD `8537a01`）
- **关键动作**：
  ① **提交**——单笔 `035716e`（12 文件 / +1671−39；`pre-commit` 的「文档完整性自检」Passed，未绕过）
  ② **PR 开展**——[PR #23](https://github.com/Dline6/ST-Agent/pull/23) 经 `gh pr create` 创建（**标题逐字符回读一致**：`len=34`，含全角括号）；app 侧 `ccd_pr` 自动绑定，无需显式 `bind_pr`。中途一次 GitHub API 不可达（`dial tcp 20.205.243.168:443` 连接超时），**未产生副作用**，重试即通
  ③ **挂 auto-merge 即等**——`gh pr merge 23 --auto --squash --delete-branch` 于 `08:54:42Z` 挂上；回读 `autoMergeRequest` **非空**、`state: OPEN`、`mergeStateStatus: BLOCKED`、一条检查 `IN_PROGRESS`（另一条已 `SUCCESS`）——与 [D-012](决策日志.md) 的验证特征一致，即**真在等必检**而非「立即合入」
  ④ **自动合入**——CI 绿后 PR 于 `08:55:55Z` **自动合入**（挂上后约 73 秒），squash `6861db8`
  ⑤ **分支清理走仓库开关**——`delete_branch_on_merge` 生效：`git fetch --prune` 报 `[deleted] origin/feat/T-L1-002.3-mcp-lifecycle`；本地侧先以 `git diff --quiet main feat/T-L1-002.3-mcp-lifecycle` **证明树完全一致**再 `-D`
  ⑥ **CI 监视开启**——`auto_fix` + `address_comments` on（CI 失败 / 合并冲突 / 评审评论会唤醒本会话，**不轮询**）
- **变更文件**：项目管理/执行日志.md（+本条）· 项目管理/tasks/T-L1-002.3-状态机重连降级网关登记.md（verify 补 PR + squash hash）· 项目管理/任务账本.md（render 刷新）
- **验证**：PR #23 `state: MERGED`（`mergedAt 2026-09-26T08:55:55Z`）· squash `6861db8` 提交体含 `Task: T-L1-002.3` trailer 与 `Co-authored-by: Claude Haiku 4.5` 署名行（`git log -1 --pretty=%B` 复核）· 本地 `main` = `origin/main` = `6861db8` · `git ls-remote --heads origin` 仅 `refs/heads/main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · **main 上 pytest 698/698**（2 deselected，1:55）· `verify_docs.py --strict` PASS
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型（四项选型由 [D-014](决策日志.md) 覆盖）
- **遗留**：① **下一就绪集 6 条**：`T-L1-003`（工作流模型 WorkflowDAG + 复合 Skill + Skill Studio，P0/M0/L1）· `T-L1-004`（官方 Skill Pack，P0/M0/L1）· `T-L1-005`（定时调度，P0/M0/L1）· `T-L1-006`（L1 运行时装配组合根，P0/M0/L1）· `T-L2-001`（节点/边模型 + 读写接口，P0/M1/L2）· `T-L4-001`（视角模型 Lens + 常设阵容，P0/M2/L4）；按调度规则（P0 → 层号升序 → id 字典序）**队首＝`T-L1-003`**，开工走 ④ 对齐（先跑「开工查遗留册」——`T-L1-003` 涉及官方 Pack 装载与连线校验，须先看 L0 册 A3 与本册相关项）② **M0 收口尚需 `T-L1-003`–`006` 与 `T-INT-001`**；`T-L1-002` 已全叶子 `done` ③ L0 册 E1（`Store` 写并发不安全）待人定策略 ④ app 内 PR 面板与 CI 监视通道本批正常（PR #23 已在 app 侧绑定；合入后绑定随之释放）

## [T-L0-008] 2026-09-26T17:17+08:00 — 存储写并发串行化与原子落盘 · done

- **目标**：关闭 L0 遗留册 E1——给 `Store` 的分区读写路径加**分区级互斥**、把清单与文件落盘改为「同目录临时文件 + `os.replace`」原子替换，公共 API 签名零变更
- **对齐**：人指示「解决 L0 遗留问题 E1」→ 三问选定 **进程内串行化 / 一并做原子替换 / 新立 `T-L0-008`**；④ 复述时**更正首轮「读路径无需加锁」的判断**（`get` 依赖「清单 + 文件」两份共享状态，写者更新两者之间的窗口会让读方误报损坏）并获「对，按这个方案做」（假设 A1–A5 见任务文件）
- **输入**：L0 册 E1（实证来源 `T-L1-002.3` GWT-4）；[02 §2](../docs/技术架构-v2/02-L0-本地优先基座.md) 存储子系统（未定并发口径）；T-L0-001 `store.py` / `manifest.py` / `crypto.py`
- **关键动作**：
  ② **文档先改**（宪法 8：契约/架构优先于代码）：02 §2 新增 **§2.4 并发访问口径**——同进程同分区读写须安全、分区间互不阻塞、**显式排除**「多进程同开同一 root」、落盘须原子替换
  ③ **`store.py` 改造**：新增 `_part_locks`（分区级 `RLock`，延迟创建 + 双检，`_part_locks_guard` 保护字典自身）与 `_atomic_write`（同目录 `.<名>.tmp-<hex>` + `os.replace`，失败清理残留并原样抛）；`put` / `get` / `delete` / `list_files` / `sealed_mtime` / `export_partition` / `wipe_partition` / `reset_partition` / `partition_state` 全部纳入分区锁（**读也进锁**——消除「清单登记与文件内容配对不一致」的假损坏）；`_write_manifest` 走原子替换
  ④ **删除反序**：`delete` 由「先删文件、后写清单」改为「**先写清单、后删文件**」——反序下删除中途崩溃会让清单指向已删文件、重开被判分区损坏；反序后只留不在清单口径内的孤儿文件
  ⑤ **回归用例**：新增 `tests/l0/test_store_concurrency.py`（6 用例 = GWT-1 + GWT-2 + GWT-3 + GWT-4 两例 + GWT-5），并发量用 `threading.Barrier` 放大竞争窗口，中断注入用 monkeypatch（`os.replace` / `Path.unlink`）
  ⑥ **撤退让**：`tests/l1/test_mcp_lifecycle.py` 的「并发窗口内单一写者」docstring 更新为「该场景的天然形态，不再是规避手段」
  ⑦ **验证期加固**（A6，经人确认后本批内做）：全量跑出一次偶发 `PermissionError: [WinError 5]`——`Store.create` 写清单时 `os.replace` 撞上外部句柄短暂持有（Defender / 索引器）。处置为 `_atomic_write` 内对 `PermissionError` **有界重试**（`_REPLACE_ATTEMPTS = 3` / `_REPLACE_BACKOFF_S = 0.02`），其余 `OSError` 不重试、持续冲突照样显式失败
- **变更文件**：src/st_agent/l0/storage/store.py（+120/−62）· docs/技术架构-v2/02-L0-本地优先基座.md（§2.4 新增）· tests/l0/test_store_concurrency.py（新，8 用例）· tests/l1/test_mcp_lifecycle.py（注释更新）· 项目管理/tasks/{T-L0-008（新）, T-INT-001（依赖补 `T-L0-008`，否则检查 7 失败）}.md · 项目管理/遗留问题/L0-遗留问题.md（销 E1 + 登记 E2 + C2 编号顺延）· 项目管理/{决策日志（[D-015](决策日志.md)）, 执行日志, 任务账本}.md
- **验证**：pytest **706/706** 通过（既有 698 + 本批 8；2 deselected）· **证伪对照**：`git stash push -- src/st_agent/l0/storage/store.py` 还原到修复前实现后，先期 6 用例 **6/6 全 FAIL**，再 `git stash pop` 还原——确认用例真钉住该缺陷而非空转 · **偶发故障处置留痕**：加固前全量 7 轮中出现 1 次 `PermissionError [WinError 5]`（`store.py` 的 `os.replace`，约 2000+ 次建库 / 写盘），单跑 3 轮、5 轮压测（345 用例）与加固后 2 轮全量均不复现；**重试用例**钉住「抖动被吸收」与「次数有界 3 + 残留 tmp 已清 + 未登记」，且 `always_denied` 注入证明持续冲突仍按 `PermissionError` 原样冒出不静默 · `verify_docs.py --strict` **PASS**（125 文件 / 1243 链接 / 0 断链；检查 4–8 全 0，其中检查 7 依赖覆盖与检查 8 遗留销账均过）· 账本 SYNC（活跃 58、就绪集不变）· **静态核对**：`_part_lock` 只在单一分区内取锁、任何路径都不同时持有两个分区的锁 → 无死锁序；`git diff --name-only` 确认 `src/st_agent/l1/` 仅测试注释一处、其余层零改动；重试退避最长 60ms 且只在错误路径上
- **假设复核**：**A1 成立**（GWT-1 在修复前实现上确实丢条目，非纸面）· **A2 成立**（分区级锁下既有 698 用例全绿，无调用方依赖跨分区原子性）· **A3 成立**（GWT-3 同键并发读写 400 轮零 `StorageCorruptionError`、零非完整版本）· **A4 成立**（GWT-4 两例：全量替换失败保完整旧值且分区 `ok`；仅清单替换失败留孤儿且分区 `ok`）· **A5 成立**（02 属 L0 架构面、01 契约不定义 `Store`，故未触发第 ⑦ 步跨层链）· **A6 成立**（验证期实测的 Windows 瞬时句柄冲突经有界重试吸收；持续冲突仍显式失败、次数有界、残留已清——两条新用例钉住）
- **决策**：[D-015](决策日志.md)（并发覆盖域 / 锁粒度与读路径 / 落盘原子性 / 删除顺序四项选型 + 理由与代价 + 一条判断更正的教训）· [D-016](决策日志.md)（原子替换的 Windows 瞬时句柄冲突：有界重试的选型与代价）
- **遗留**：① **本批关闭 L0 册 E1**（册内移入「已闭（备查）」+ 执行日志本条留痕；开工时按册规 2 已把其「归属」由「人」改为 `T-L0-008`）② **新登记 L0 册 E2**：原子写在进程被强杀时会留下分区内残留 tmp（不被清单驱动路径读取，故不产生损坏报告；大小可达待写文件本身，`data_cache` 的 `market.db` blob 可至数十 MB），归属「人」待人定是否立项清理 ③ **C2 编号顺延**：C2 行内「建议新立 `T-L0-008`」改为 `T-L0-009`（本任务占号），属册规 2 就地修正 ④ **`T-INT-001` 依赖已补 `T-L0-008`**（M0 关卡覆盖度）⑤ 待走 PR 收口（分支 + commit + squash 合入）

## [T-L0-009] 2026-09-26T17:44+08:00 — 原子写残留目录与打开时回收 · done

- **目标**：关闭 L0 遗留册 E2——把原子写的中间产物从**分区目录内**移到存储根下的**专属临时目录** `root/.st-agent-tmp/`，并在 `Store.open` 时整目录回收
- **对齐**：人指示「把 E2 也一并解决」→ 两问选定 **独立 tmp 目录 / 新立 `T-L0-009` + C2 改为不写死号**；因 PR #24 已由 auto-merge 落 main，本批**另起分支与 PR**（假设 A1–A6 见任务文件）
- **输入**：L0 册 E2（来源 `T-L0-008` 实现期发现）；`T-L0-008` 的 `store.py`（中间产物落分区目录）与 [D-015](决策日志.md) / [D-016](决策日志.md)；`T-L0-006` 的 `backup.py::wipe_all`；02 §2.4
- **关键动作**：
  ① **风险前置核查**：全仓搜 `iterdir|os.walk|listdir|rglob` **0 命中**（无任何按目录遍历 root 的代码）；`wipe_all` 走 `shutil.rmtree(root)` 故自动覆盖新目录；备份按分区名经 `Store` 取数、不受影响——先确认「存储根加一个目录」不破坏既有面，再动代码
  ② **文档先改**（宪法 8）：02 §2.4「落盘原子性」补「崩溃残留须在**下次打开存储时回收**（不得无限累积），且中间产物不得落在分区目录内」
  ③ **`store.py` 改造**：新增模块级 `TMP_DIR_NAME = ".st-agent-tmp"` 与 `_reset_tmp_dir()`（`rmtree` + `mkdir`）；`_atomic_write` 由 `@staticmethod` 改为**实例方法**并寻址 `self._tmp_dir`；`create` / `open` 均在 `_unlock` 后先调 `_reset_tmp_dir()`（前者就位、后者回收 + 为早于该机制的既有存储**补建**）
  ④ **回收语义**：回收对象是**专属目录**而非按文件名模式匹配 → 不可能误删用户数据，也不必收紧 `_check_rel_name` 的公共校验语义；分区目录随之保持「只有清单 + 数据文件」
  ⑤ **用例**：追加 4 例（GWT-1/3 残留回收 + 数据面无损、GWT-2 中间产物不落分区目录、GWT-4 缺目录自动补建、GWT-5 完全清空覆盖临时目录），并把既有两处「分区目录无 dot 残留」断言改指临时目录
- **变更文件**：src/st_agent/l0/storage/store.py · docs/技术架构-v2/02-L0-本地优先基座.md（§2.4 补句）· tests/l0/test_store_concurrency.py（+4 用例；docstring 范围扩为「并发 + 原子性 + 残留回收」）· 项目管理/tasks/{T-L0-009（新）, T-INT-001（依赖补 `T-L0-009`）}.md · 项目管理/遗留问题/L0-遗留问题.md（销 E2 + C2 改措辞）· 项目管理/{决策日志（[D-017](决策日志.md)）, 执行日志, 任务账本}.md
- **验证**：pytest **710/710** 通过（既有 706 + 本批 4；2 deselected）· **行为级证伪对照**（临时脚本 `tmp/check_e2.py`：抛非 `OSError` 的 `KeyboardInterrupt` 绕过清理分支，逼出真实崩溃残留）——**修复前**（`git stash` 还原 `store.py`）中间产物落在 `execution_log/net/.b.json.tmp-…` 且 `open` 后**仍在**；**修复后**落在 `.st-agent-tmp/b.json.tmp-…`、`open` 后**已回收**；两种实现下分区均 `ok`、数据均逐字节可读。脚本跑完即删（`tmp/` 本就在 `.gitignore`）· `verify_docs.py --strict` **PASS**（126 文件 / 1295 链接 / 0 断链；检查 4–8 全 0）· 账本 SYNC（活跃 59）· **静态核对**：`TMP_DIR_NAME` 引用仅 `_atomic_write` / `create` / `open` 三处（A1）；中间产物为 `seal_bytes` 输出、只含密文（A2）；`git diff --name-only` 无其他层代码
- **假设复核**：**A1 成立**（`TMP_DIR_NAME` 仅三处引用，整目录回收不触数据面）· **A2 成立**（中间产物只含密文，回收不涉明文数据主权）· **A3 成立**（回收时机取 `open`，与「跨进程重启才产生残留」对位）· **A4 成立**（临时目录在存储根下、与目标同卷，`os.replace` 原子性成立；12 条 store 用例全绿）· **A5 成立**（全仓无按目录遍历 root 的代码；`wipe_all` 一并覆盖；全量 710 含 `test_backup.py` 全绿）· **A6 成立**（公共 API 签名零变更、02 仅补一句，未触发第 ⑦ 步）
- **决策**：[D-017](决策日志.md)（中间产物承载位置：专属临时目录 + `open` 回收，含 A/B/C 取舍与代价）
- **遗留**：① **本批关闭 L0 册 E2**（册内移入「已闭（备查）」+ 本条留痕；开工时按册规 2 已把其「归属」由「人」改为 `T-L0-009`）→ **E 段当前 0 条未闭** ② **C2 归属改措辞**：C2 行内「建议新立 `T-L0-00N`」改为「建议新立 **L0 序列下一可用号**」——C2 至今未立项，写死号会被每个新建的 L0 任务反复撞上（同日已撞 `008` / `009` 两次），属册规 2 就地修正 ③ **`T-INT-001` 依赖已补 `T-L0-009`**（M0 关卡覆盖度）④ 待走 PR 收口（分支 + commit + squash 合入）

## [sc] 2026-09-26T17:52+08:00 — T-L0-008 / T-L0-009 两批 PR 合入收口 · done

- **目标**：把「`Store` 写并发串行化与原子落盘」（`T-L0-008`，销 L0 册 E1）与「原子写残留目录与打开时回收」（`T-L0-009`，销 L0 册 E2）两批走 PR 合入 main，完成全链路
- **对齐**：人先指示「解决 L0 遗留问题 E1」，周期内追加「把 E2 也一并解决」；两批均沿用既定流程（[Git协作规范](Git协作规范.md) §5.2 gate + auto-merge 合入即删分支），分支 `fix/T-L0-008-store-concurrency` / `fix/T-L0-009-store-tmp-residue`
- **关键动作**：
  ① **`T-L0-008` 批次**——分支两条 commit（主体 `5c13903` + 验证期重试加固 `53d622e`），建 [PR #24](https://github.com/Dline6/ST-Agent/pull/24)（标题逐字符回读一致）；`gh pr merge 24 --auto --squash --delete-branch --subject/--body` 挂上后回读 `autoMergeRequest.commitHeadline` / `commitBody` **逐字符落位**、`mergeStateStatus: BLOCKED`——与 [D-012](决策日志.md) 的验证特征一致，即**真在等必检**而非「立即合入」
  ② **多 commit 分支的 squash 提交信息**（本批新踩到的点）——分支有 2 条 commit 时 GitHub 默认用「PR 标题 + 各 commit 主题清单」，那会**丢掉** `Task:` trailer 与署名行；故显式传 `--subject` / `--body`（落到 GraphQL `enablePullRequestAutoMerge` 的 `commitHeadline` / `commitBody`）。合入后 `git log -1 --pretty=%B` 复核：squash `d19b9b3` 提交体含双 trailer ✅
  ③ **`T-L0-009` 批次**——PR #24 合入后另起分支与 [PR #25](https://github.com/Dline6/ST-Agent/pull/25)（单条 commit `49dba46`，仍显式传 subject/body 以求确定），挂 auto-merge 后自动合入
  ④ **分支清理**——两批均由 `delete_branch_on_merge` 删除远端分支；本地侧先 `git diff --quiet origin/main <branch>` **证明树完全一致**再 `-D`（`T-L0-008` 分支）；`T-L0-009` 分支由 gh `--delete-branch` 连同本地副本一并删除，事后 `git fetch --prune` 复核远端引用已清
  ⑤ **中途故障留痕**——`T-L0-009` 挂 auto-merge 期间 GitHub API 连续不可达（`unexpected EOF` / `TLS handshake timeout` / `dial tcp …:443`）；有界重试后恢复，且**首次 `unexpected EOF` 未落下任何副作用**（恢复后回读 `autoMergeRequest` 为空，重新执行即成功）——印证「写类 API 失败后须回读复核、不可假定已生效」
- **变更文件**：项目管理/执行日志.md（+本条）· 项目管理/tasks/{T-L0-008, T-L0-009}（verify 补 PR + squash hash）· 项目管理/任务账本.md（render 刷新）
- **验证**：PR #24 `state: MERGED` · squash `d19b9b3`；PR #25 `state: MERGED` · squash `8b2fb3a` · 两条 squash 提交体均含对应 `Task: T-L0-00N` trailer 与 `Co-authored-by` 署名行（`git log -1 --pretty=%B` 复核）· `git log --oneline --grep "Task: T-L0-008"` / `"Task: T-L0-009"` 两度命中 · 本地 `main` = `origin/main` = `8b2fb3a` · `git ls-remote --heads origin` 仅 `refs/heads/main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · **main 上 pytest 710/710**（2 deselected）· `verify_docs.py --strict` PASS（126 文件 / 0 断链；检查 4–8 全 0）
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型（[D-015](决策日志.md) / [D-016](决策日志.md) / [D-017](决策日志.md) 已覆盖本批全部选型）
- **遗留**：① **下一就绪集 6 条不变**：`T-L1-003`（队首）· `T-L1-004` · `T-L1-005` · `T-L1-006` · `T-L2-001` · `T-L4-001` ② **M0 收口尚需 `T-L1-003`–`006` 与 `T-INT-001`**；L0 侧 **E 段已归零**（E1 / E2 双双关闭），A / B / C / D 段条目的归属与解封条件均未被本批触及 ③ **L0 册 C2**（分批 + 退避缺失，实测全市场全量 ≈ 85 小时）仍**待人定是否立项**，其行内建议已改为「L0 序列下一可用号」以免撞号 ④ **原子写的统一口径已建立**（[D-016](决策日志.md) 瞬时冲突重试 + [D-017](决策日志.md) 承载位置与回收）：后续新增 tmp+replace 落盘须复用 `TMP_DIR_NAME` / `_reset_tmp_dir` 与同一重试常量 ⑤ **CI 监视**：PR #24 建后已 `set_monitor(auto_fix + address_comments)`，其合入后绑定随即释放；PR #25 因 CI 当时已绿、auto-merge 秒级落地，未及开启即无监视对象——两 PR 均已合入，**不构成遗留**

## [T-L1-003.1] 2026-09-26T18:22+08:00 — WorkflowDAG 模型 + 校验 + 持久化与版本 · done

- **目标**：落地 03 §3.1 的 WorkflowDAG（节点/边/分组/`schedule`/`version`）与其**静态校验**（成环 / 连线契约匹配 / 中性化命名 / 参数绑定）、**持久化**（多版本共存 + 激活指针）与**版本操作**（diff / 回滚 / 引用方「待检查」）
- **对齐**：本会话先做 ③ 拆分——`T-L1-003` 命中拆分触发（Story-02 + Story-06 合计 18 条 GWT ≫ 5、跨 §3/§4 两个契约小节、预计会话轮次 > 20），按 §3/§4 小节边界拆为 `.1`–`.5`（父文件「拆分记录」节即留痕；`T-INT-001` 依赖同步改指 5 个叶子）。三问经人选定：**拆分粒度 5 个叶子** / 复合 Skill 契约**默认由未连线端口推导 + 可显式覆盖** / 官方模板**机制与内容全归 `T-L1-003`**。落盘中另发现契约边界问题并单列一问：`flow_id` **不在** 01 §1 的 12 类 ID 表内——人选定**升格为契约 ID**（见下 ①）。假设 A1–A7 见任务文件
- **输入**：[03 §3.1](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) WorkflowDAG schema；[01 §2 方言澄清](../docs/技术架构-v2/01-平台共享契约.md)（判定件注释已点名本任务复用 `check_link`）；[01 §6](../docs/技术架构-v2/01-平台共享契约.md) / [§9](../docs/技术架构-v2/01-平台共享契约.md)；`T-L1-001.1` 的 `SkillRegistry`（`pending_updates` / `get_latest` / `base_of`）、`T-SC-001.4` 的 `SemVer`、`T-L0-001` 的 `Store`（`config` 分区）
- **关键动作**：
  ① **⑦ 链（契约先行，宪法 8）**：`flow_id` 升格为 01 §1 第 13 类 ID——**先**改 [01 §1](../docs/技术架构-v2/01-平台共享契约.md) 加行（形态 `wf_<注册名>_v<主>.<次>`）、**再**同步 [03 §3.1](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 的 `flow_id` 行、**最后**改代码。选型 [D-018](决策日志.md) 采纳「显式 pattern 对齐真实格式」而非 `SkillId` 式占位：`contracts/identifiers.py` 新增 `FLOW_ID_PATTERN`（带捕获组）与 `FlowId`，`_make_id_type` 加可选 `pattern` 参数，`PlatformId.generate()` 由 `if cls is StockId` 硬编码改为类属性 `_generated_by`（`StockId` 原文案逐字保留，含测试匹配的 `security`），`FlowId.generate()` 抛 `ContractViolation` 指向 `flow_id_for`
  ② **零 DB 影响的核查**（人问「升格是否影响数据库设计」）：`docs/数据库设计-BaoStock数据层/` 的「共享契约概念 → 本库落地」表只映射 `stock_id` / `dataset_snapshot_id` / `as_of`；逐类核 12 个 §1 ID 在该目录的命中，只有 `stock_id` 与 `dataset_snapshot_id` 出现、其余 10 类 0 命中，`schema.sql` 无 §1 ID 列；工作流落 `config` 分区加密 JSON、不入 SQL
  ③ **模型**（`workflow/models.py`）：`WorkflowDAG` / `WorkflowNode` / `WorkflowEdge` / `WorkflowGroup` / `WorkflowSchedule` / `ParamBinding`（literal 与 ref 两态，用 `model_fields_set` 区分「显式给出 None」与「未给出」）；`flow_id` 版本后缀与 `version` 字段**构造期互校**（不一致即拒）
  ④ **校验**（`workflow/validate.py`）：判定件**零新造**——连线一律走 `contracts.schema_check.check_link`、命名走 `NeutralityGuard`、skill 形态走 `check_skill_id`。**依赖图取「显式连线 ∪ `kind=ref` 绑定」的并图**，故「A 引用 B 的输出 + B → A 的连线」这类绕开显式连线的环同样被拦；校验**返回发现**（`WorkflowValidation`）而非抛异常，供 Studio 回显
  ⑤ **持久化与版本**（`workflow/store.py`）：`config` 分区 `workflow/<flow_id>.json`（定义）+ `workflow-active/<base>.json`（激活指针，两前缀互不串扫）；**回滚 = 换指针**而非改内容（版本文件不可变，可再换回去）；`diff` 出结构化差异；「待检查」按**各 base 的生效版本**派生（历史版本不误报，显式回滚后旧版本才被标），`follow_latest` 跟到各 base 最新版本并落新版本（原版本保留可回滚）
  ⑥ **验证期修正**：首版 `pending_checks` 扫**全部版本文件**，导致 `follow_latest` 后旧版本仍被标——改为按 `active(base)` 派生，并补两例钉住（历史版本不误报 / 显式回滚后旧版本被标）
- **变更文件**：docs/技术架构-v2/01-平台共享契约.md（§1 +1 行）· docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md（§3.1 行）· src/st_agent/contracts/identifiers.py（+65/−20）· src/st_agent/contracts/__init__.py（+4）· src/st_agent/l1/workflow/{__init__,errors,ids,models,store,validate}.py（新，6 文件）· tests/contracts/test_identifiers_result_envelope.py（+18）· tests/l1/test_workflow.py（新，33 用例）· 项目管理/tasks/{T-L1-003（转父 + 拆分记录）, T-L1-003.1–.5（新）, T-INT-001（依赖改指叶子）}.md · 项目管理/{决策日志（[D-018](决策日志.md)）, 执行日志, 任务账本}.md
- **验证**：pytest **751/751** 通过（既有 710 + 本批 41 = 契约 2 + `test_workflow.py` 33 + 分层校验 6 个新源文件各 1；2 deselected）· `verify_docs.py --strict` **PASS**（131 文件 / 1402 链接 / 0 断链；检查 4–8 全 0，其中检查 7 依赖覆盖与检查 8 遗留销账均过）· 账本 SYNC（活跃 64、就绪 5）· **grep 证伪**：`FLOW_ID_PATTERN` 全仓仅 `contracts/identifiers.py` 定义、`l1/workflow/ids.py` 引用（无第二份副本，A1 的「不漂移」成立）· **落盘面核对**：`workflow/` 与 `workflow-active/` 前缀互不串扫，已用例钉住
- **假设复核**：**A1 成立**（升格落 01 §1 + `FlowId` 显式 pattern，零 DB 影响已逐目录核实；⑦ 链 01 → 03 → 代码按序执行）· **A2 成立**（连线判定零新造，`check_link` 双向用例过：匹配放行 / 不匹配报「这里需要一个 string 类型的输入」）· **A3 成立**（工作流落 `config` 分区，与 `skill-registry/` 同分区同构；多版本共存 + 回滚用例过）· **A4 成立**（「待检查」为派生结论、不另存标记；验证期按生效版本修正后两种情形均有用例）· **A5 成立**（`schedule` 只定义形态，触发实现留给 `T-L1-005`；本任务未引入任何调度逻辑）· **A6 成立**（`git diff --name-only` 仅 01/03 两文档 + `contracts/{identifiers,__init__}.py` + `l1/workflow/**` + `tests/{contracts,l1}/**`）· **A7 成立**（`follow_latest` 取次版本递增；`T-L1-003.2` 落地 IO 契约派生时按假设复核一次）
- **决策**：[D-018](决策日志.md)（`flow_id` 升格为 §1 契约 ID + 登记形态直接对齐真实格式；含 A/B/C 取舍、零 DB 影响的核查证据、`SkillId` 双轨债的教训）
- **遗留**：① **`T-L1-003` 拆分为 5 个叶子**，本批完成 `.1`；`.2`–`.5` 均为 `todo`（依赖 `.1`，现已解锁）② **两册遗留均无命中**（已核 [L0 册](遗留问题/L0-遗留问题.md) / [L1 册](遗留问题/L1-遗留问题.md) 未闭区，无一条的归属或解封条件指向本任务）→ 本批**不销账** ③ **后续新增实体类 ID 一律照 [D-018](决策日志.md) 口径**（升格进 §1 时用显式 pattern 对齐真实格式，不再造占位类型）④ **`flow_id` 与 `skill_id` 的形态差**已固化：`flow_id` 的 §1 登记即真实格式，`skill_id` 仍是 `SkillId`（`^sk_[0-9a-f]{20}$`）与真实 `sk_<名>_v<主>.<次>` 的双轨——**本次未动 `SkillId`**（属 `T-SC-001.1` 已完成面，改动会波及既有断言），如需统一另立任务 ⑤ 待走 PR 收口（分支 + commit + squash 合入）

## [sc] 2026-09-26T18:29+08:00 — T-L1-003.1 PR 合入收口 · done

- **目标**：把「工作流模型 WorkflowDAG 与静态校验」（`T-L1-003.1`，含 `flow_id` 升格 01 §1 的 ⑦ 链）走 PR 合入 main，完成全链路
- **对齐**：沿用既定流程（[Git协作规范](Git协作规范.md) §5.2 gate + auto-merge 合入即删分支），分支 `feat/T-L1-003.1-workflow-dag`
- **关键动作**：
  ① **建 PR**——单条 commit `d262add`，建 [PR #26](https://github.com/Dline6/ST-Agent/pull/26)（标题逐字符回读一致）；`gh pr merge 26 --auto --squash --delete-branch` 并显式传 `--subject` / `--body`
  ② **机制更正（本批新踩到，值得复用）**——挂 auto-merge 后 GraphQL 回读 `commitHeadline` / `commitBody` **逐字符落位**（与我传入的传参一致），但合入后 `git log -1 --pretty=%B` 显示 main 上的 squash 提交体取的是**分支 commit 自身的信息**、并非传入的 `commitBody`（两者文案不同、可区分）。即：**单 commit 分支挂 auto-merge 时，`commitHeadline` / `commitBody` 被 API 接受但不被采用**。本批因分支 commit 信息本身完整（含 `Task:` trailer 与署名行）故结果正确——**教训**：不可靠 `--subject` / `--body` 兜底，**分支的 commit 信息本身必须完整**。此结论更正 `[sc] 2026-09-26T17:52` 条对该机制的叙述（「显式传 subject/body 落到 commitHeadline/commitBody」）
  ③ **分支清理**——远端由 `delete_branch_on_merge` 删除（`git ls-remote --heads origin` 仅剩 `refs/heads/main`）；本地先 `git diff --quiet main <branch>` **证明树完全一致**再 `-D`；事后 `git branch -a` 仅 `main` + `origin/main`
  ④ **状态核对**——挂 auto-merge 后 `mergeStateStatus: BLOCKED`，与 [D-012](决策日志.md) 的验证特征一致（真在等必检而非「立即合入」）；合入后 `git merge --ff-only origin/main` 使本地与远端同步至 `657589d`
- **变更文件**：项目管理/执行日志.md（+本条）· 项目管理/tasks/T-L1-003.1（verify 补 PR + squash hash）· 项目管理/任务账本.md（render 刷新）
- **验证**：PR #26 `state: MERGED` · squash `657589d` · 提交体含 `Task: T-L1-003.1` trailer 与 `Co-Authored-By` 署名行（`git log -1 --pretty=%B` 复核）· 本地 `main` = `origin/main` = `657589d` · `git ls-remote --heads origin` 仅 `refs/heads/main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · **main 上 pytest 751/751**（2 deselected）· `verify_docs.py --strict` PASS（131 文件 / 0 断链；检查 4–8 全 0）
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型（[D-018](决策日志.md) 已覆盖本批全部选型）；本条的 auto-merge 机制更正属**叙述修订**，已在关键动作 ② 留痕并指回 `[sc] 2026-09-26T17:52`
- **遗留**：① **就绪集 9 条**：`T-L1-003.2`（队首）· `.3` · `.4` · `.5` · `T-L1-004` · `T-L1-005` · `T-L1-006` · `T-L2-001` · `T-L4-001` ② **M0 收口尚需 `T-L1-003.2`–`.5`、`T-L1-004`–`006` 与 `T-INT-001`** ③ **CI 监视**：PR #26 建后已 `set_monitor(auto_fix + address_comments)`，其合入后绑定随之释放——**不构成遗留** ④ **`SkillId` 双轨债**（见 `[T-L1-003.1]` 遗留④）仍挂着：本次只让 `flow_id` 不再重蹈，未回收 `skill_id` 的既有偏差，如需统一应另立任务

## [T-L1-003.2] 2026-09-26T18:55+08:00 — 复合 Skill（工作流 → 命名 Skill + 依赖提示） · done

- **目标**：落地 03 §3.2——把一条**校验通过**的工作流保存为复合 Skill（「命名的工作流」），对外暴露统一输入输出契约与参数，以 `source=user-built` 进 `SkillRegistry`（复用 `config` 分区 `skill-registry/<skill_id>.json`），依赖的原 Skill 缺失时走依赖提示流程
- **对齐**：读 Spec（[story-06](../docs/PRD-v2-Agent/story-06-skill-studio.md)）/ Plan（[03 §3.2](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) + [01 §2](../docs/技术架构-v2/01-平台共享契约.md)）/ 前置实现（`l1/workflow` + `l1/skills`）后做 **④ 对齐**——向人复述「交付什么 / 验收怎么算过 / 接口面 / 遗留面 / 技术路径」并获确认；03 §3.2 与 01 §2 未定的六处口径经**两轮六问**由人逐项选定，全部采纳推荐项（见下 ① 与本条「决策」）。假设 A1–A11 见任务文件
- **输入**：[03 §3.2](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 复合 Skill + §3.1 的 `WorkflowEdge` 粗粒度边模型；[01 §2 方言澄清](../docs/技术架构-v2/01-平台共享契约.md)（`input_schema` / `output_schema` 唯一判定口径 `contracts.schema_check`）· [01 §6](../docs/技术架构-v2/01-平台共享契约.md) 中性化 · [01 §9](../docs/技术架构-v2/01-平台共享契约.md) 版本语义；`T-L1-003.1` 的 `WorkflowDAG` / `validate_dag` / `WorkflowStore` / `flow_id` 拼接；`T-L1-001.1` 的 `SkillRegistry`（`register` / `get` / `get_latest` / `list_all` / `set_parameters`）；`T-SC-001.4` 的 `SkillDescriptor` / `ParameterSpec` / `Provenance`
- **关键动作**：
  ① **⑦ 链（宪法 8：文档先改）**——[03 §3.2](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 新增「落地口径」表（标识派生 / 输入输出契约 / 已连线判据 / 显式覆盖 / 参数暴露 / 能力字段）+「依赖缺失不落半成品」段，**先于代码**落地；**01 平台契约未动**——复合 Skill 在契约上就是普通 `SkillDescriptor`（11 组字段零新增），回指靠标识派生而非新义字段，故未触发 01 侧变更
  ② **新增 `workflow/composite.py`**（323 行）——`composite_skill_id` / `source_flow_id`（`wf_x_v<主>.<次>` ↔ `sk_x_v<主>.<次>`，互为逆，即 A1「不加字段回指工作流」的落点）、`derive_io_contract`（未连线端口 → 节点限定点号键）、`missing_dependencies`（纯查询，区分「base 从未注册」与「该版本未注册」）、`save_as_composite_skill`（组装并注册）、`DependencyGap` / `CompositeDependencyError`
  ③ **判定件零新造**——命名走 `NeutralityGuard`（经 `registry.register`）、`skill_id` 形态走 `check_skill_id`、参数默认值走 `validate_param_values`（超范围即拒）、版本走 `SemVer` + `flow_id_for`；本模块只做「复合」这一层编排
  ④ **依赖门前置于校验之前**（实现期发现并修正）——首版按「先 `validate_dag` 再查依赖」写，但 `validate_dag` 只会把未注册 Skill 报成一条 `unknown_skill` 发现，GWT-4 要的是**逐条结构化提示**（`node_id` + `skill_id` + 原因），故把 `missing_dependencies` 提到 `validate_dag` 之前——依赖齐不齐本就是「能否物化」的先决条件
  ⑤ **能力字段派生**——`offline_level` 取所引用 Skill 最差（`full` > `degraded` > `none`）、`permissions` 取并集：复合体只声明其部件能支持的面（固定 `full` + 空权限会谎报，与 09 §4 导入侧权限申请展示冲突）
  ⑥ **用例**（`tests/l1/test_workflow_composite.py`，351 行 / 30 用例）——GWT-1 六例（落库 + 能力字段派生 + 任一 dep `none` → `none` + 重存拒 + base 撞所引用 Skill + 校验未过不落盘）、GWT-2 九例（推导三例 + ref/literal 两种绑定判据 + 覆盖放行 + 多键/少键/type 不符三类被拒）、GWT-3 六例（字面量进参数并可经 `set_parameters` 改 + 未声明不上浮 + 未绑定取 Skill 默认 + 超范围拒 + ref 参数不可暴露 + 未知参数/节点拒）、GWT-4 三例、GWT-5 四例；**每一条拒绝路径都断言 `store.list_files("config")` 逐项不变**（不落半成品是可验证的）
- **变更文件**：docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md（§3.2 +13 行）· src/st_agent/l1/workflow/composite.py（新，323 行）· src/st_agent/l1/workflow/__init__.py（+27/−5，导出 7 名）· tests/l1/test_workflow_composite.py（新，351 行 / 30 用例）· 项目管理/tasks/T-L1-003.2（④ 三节实填 + `verify`）· 项目管理/{决策日志（[D-019](决策日志.md)）, 执行日志, 任务账本}.md
- **验证**：pytest **782 passed / 2 deselected**（既有 751 + 本批 31 = 30 用例 + 分层校验新增源文件 1 案）· **证伪对照**：把 `derive_io_contract` 的边覆盖判据临时改成「只认 `kind=ref` 绑定」（即备选 E 口径），`test_workflow_composite.py` **5 例精准失败**（推导默认、入边覆盖、出边覆盖、字面量填端口、显式覆盖一致放行）——确认 A7 口径被用例真钉住而非空转；还原后 30/30 绿 · `verify_docs.py --strict`（任务转 `done` 后重跑，见收口条）· **静态核对**：`git diff --name-only` 无其他层代码（仅 `l1/workflow/{composite,__init__}.py` + 03 文档）；`python -m pytest tests/test_layering.py` 覆盖新源文件、无向上依赖
- **假设复核**：**A1 成立**（复合 Skill 是普通 `SkillDescriptor`，`registry.get(list_all())` 往返自洽，既有 Skill 用例全绿）· **A2 成立**（默认推导 + 显式覆盖两路均有用例；覆盖不一致的三类各有用例）· **A3 成立**（未暴露的节点参数不上浮，`test_unexposed_params_stay_internal` 钉住）· **A4 成立**（依赖缺失返回逐条 `DependencyGap`，且**不落盘**——`store.list_files("config")` 前后逐项相等）· **A5 成立**（`provenance == Provenance()`、`source == "user-built"` 均有断言）· **A6 成立**（点号键 `<node_id>.<prop>` 与回指已在 03 §3.2 登记，供 `T-ECO-001` / L3 遵循；若下游要裸名须另立变更）· **A7 成立**（边与绑定对称判据；证伪对照证明用例真钉住）· **A8 成立**（`wf_demo_v1.0` → `sk_demo_v1.0`，`source_flow_id` 往返可逆；工作流 base 与所引用 Skill 同名时显式拒绝）· **A9 成立**（键集相等 + 同名键 `type` 相等，其余关键字随显式——`required` 与描述随显式的用例通过）· **A10 成立**（抛 `CompositeDependencyError` 携 `gaps`；草稿留存由 `WorkflowStore` 承担，故 A4 与 GWT-4「不落半成品」得以调和而非取舍）· **A11 成立**（`offline_level` 取最差 + `permissions` 取并集，两例断言）
- **决策**：[D-019](决策日志.md)（六项落地口径：端口键 / 已连线判据 / 标识派生 / 覆盖校验 / 依赖缺失 / 能力字段，含 A–R 各备选的取舍与代价）
- **遗留**：① **L0 / L1 两册均无命中**（已核未闭区：L0 册 E 段归零、L1 册 A1 / A2 归属均为「人」且指向 MCP 面）→ 本批**不销账** ② **复合 Skill 的删除联动未建**（删 Skill 是否连带删工作流 / 复合物化后工作流改版如何跟进）——本任务只建注册与提示路径，无删除路径；如需要应另立任务（不属 L1 册 A2 的 MCP 映射面）③ **就绪集 8 条**：`T-L1-003.3`（队首）· `.4` · `.5` · `T-L1-004` · `T-L1-005` · `T-L1-006` · `T-L2-001` · `T-L4-001` ④ **M0 收口尚需 `T-L1-003.3`–`.5`、`T-L1-004`–`006` 与 `T-INT-001`** ⑤ 待走 PR 收口（分支 + commit + squash 合入）

## [sc] 2026-09-26T19:03+08:00 — T-L1-003.2 PR 合入收口 · done

- **目标**：把「复合 Skill（工作流 → 命名 Skill + 依赖提示）」（`T-L1-003.2`，含 03 §3.2 六项落地口径的 ⑦ 链）走 PR 合入 main，完成全链路
- **对齐**：沿用既定流程（[Git协作规范](Git协作规范.md) §5.2 gate + auto-merge 合入即删分支），分支 `feat/T-L1-003.2-composite-skill`
- **关键动作**：
  ① **建 PR**——单条 commit `71c252a`（提交体含 `Task:` trailer 与署名行，逐项复核过），建 [PR #27](https://github.com/Dline6/ST-Agent/pull/27)（标题逐字符回读一致）；`gh pr merge 27 --auto --squash --delete-branch` **并显式传了 `--subject` / `--body`**——**本批的失误就在这个「显式传」上**（见 ②）
  ② **机制更正（本批实测推翻 `[sc] 2026-09-26T18:29` 的叙述）**——该条称「单 commit 分支挂 auto-merge 时 `commitHeadline` / `commitBody` 被 API 接受但**不被采用**，squash 取分支 commit 自身信息，故分支 commit 信息必须完整」。本批实测**相反**：合入后 `git log -1 --pretty=%B` 显示 squash 提交体 = 我传入的 `--subject` / `--body`（body 正是占位文案「见分支 commit 与 PR 描述」），**分支 commit 自身完整的 `Task:` trailer 与 `Co-Authored-By` 署名行被覆盖丢弃**。两种情形各自都能解释自己的观察（#24/#25/#26 未传或传入得与分支信息一致），故可操作结论只认一条：**不传** `--subject` / `--body`（让 GitHub 用分支 commit 信息），或传入**完整**（含 trailer 与署名行）。**教训**：不确知某参数是否被采用时，**不得用占位值试探**——占位值一旦被采用就是不可逆的历史污染；本次正是因为「按 18:29 的结论认定 body 不会被采用」而随手写了占位文案
  ③ **补救与留痕（不重写 main 历史）**——`1ed4450` 已在 `origin/main`，强制改写属破坏性操作、需人明示故**本批不做**；改以本条收口 commit 携带 `Task: T-L1-003.2` trailer 恢复 `git log --grep` 可检索性，并在任务 `verify` 字段显式标注「squash 提交体缺 trailer」的事实，保持「任务文件 → hash」方向的双向追溯不断
  ④ **分支清理**——远端由 `delete_branch_on_merge` 删除（`git ls-remote --heads origin` 仅 `refs/heads/main`）；本地先 `git diff --quiet main <branch>` **证明树完全一致**再 `-D`；事后 `git branch -a` 仅 `main` + `origin/main`
  ⑤ **CI 监视**——PR #27 建后即 `set_monitor(auto_fix + address_comments)`，其合入后绑定随之释放；挂 auto-merge 时 `mergeStateStatus: BLOCKED`（与 [D-012](决策日志.md) 的验证特征一致，真在等必检），约 2 分钟后自动合入
- **变更文件**：项目管理/执行日志.md（+本条）· 项目管理/tasks/T-L1-003.2（verify 补 PR + squash hash + trailer 缺失标注）· 项目管理/任务账本.md（render 刷新）
- **验证**：PR #27 `state: MERGED`（`mergedAt 2026-09-26T11:00:27Z`）· squash `1ed4450` · 本地 `main` = `origin/main` = `1ed4450` · `git ls-remote --heads origin` 仅 `refs/heads/main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · **main 上 pytest 782 passed / 2 deselected**（135s）· `tests/test_layering.py` 单跑 **82 passed**（含本批新增源文件 1 案）· `verify_docs.py --strict` PASS（131 文件 / 1443 链接 / 0 断链；检查 4–8 全 0）· **提交体核对**：`git log -1 --pretty=%B` 确认 squash 体**不含** `Task:` trailer 与署名行——即本条 ② 的实测证据（非推断）
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型（[D-019](决策日志.md) 已覆盖本批全部选型）；② 的机制更正属**叙述修订**（推翻 `[sc] 2026-09-26T18:29` 对该机制的叙述），已在本条留痕；**是否重写 main 历史（force-push 修正 `1ed4450` 的提交体）留待人定**，本批取非破坏性路径
- **遗留**：① **就绪集 8 条**：`T-L1-003.3`（队首）· `.4` · `.5` · `T-L1-004` · `T-L1-005` · `T-L1-006` · `T-L2-001` · `T-L4-001` ② **M0 收口尚需 `T-L1-003.3`–`.5`、`T-L1-004`–`006` 与 `T-INT-001`** ③ **squash `1ed4450` 缺 `Task:` trailer 与署名行**（见 ②③）——若要求历史本身干净，须 force-push 重写 main 上这一条，**需人明示**；本批以「收口 commit 补 trailer + verify 标注」兜住可检索性与留痕 ④ **后续挂 auto-merge 一律不传 `--subject` / `--body`**（或传完整信息）——不传时「分支 commit 信息必须完整」仍是前提

## [T-L1-003.3] 2026-09-26T19:38+08:00 — 工作流试跑（调试协议） · done

- **目标**：落地 [03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 调试协议——任何工作流可试跑：按拓扑序**逐节点**执行并留快照，支持**单步**、**输入注入**、**暂停 / 继续 / 中止**；每次试跑留存历史可对比（story-06「调试模式」「试跑历史」）
- **对齐**：读 Spec（[story-06](../docs/PRD-v2-Agent/story-06-skill-studio.md)「单步执行 + 输入注入 + 输出快照」「试跑历史：每次试跑留存记录，可对比」）/ Plan（[03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) + §1.2 + §1.3、[01 §1/§4/§5](../docs/技术架构-v2/01-平台共享契约.md)）/ 前置实现（`l1/workflow` + `l1/runner` + `l1/reuse`）后做 **④ 对齐**——向人复述「交付什么 / 验收怎么算过（GWT-1–5）/ 接口面 / 遗留面 / 技术路径」，并就 03 §4 与 01 §1 未定的**三处口径**三问，人**逐项采纳推荐项**：① **试跑记录新立契约 ID `trial_id`**（走 ⑦ 链）② **试跑门禁只拦「不可执行」类结构问题**（成环 / 自环 / 重复节点 / 悬空连线），其余发现落节点级以失败信封装载 ③ **试跑不登记输出复用**（彩排产物不进生产复用面）。口径全部落 [D-020](决策日志.md)；假设 A1–A5 见任务文件
- **输入**：[03 §4 调试协议](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) · [03 §1.2 / §1.3](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) · [01 §1 标识 / §4 Trace / §5 ResultEnvelope](../docs/技术架构-v2/01-平台共享契约.md)；`T-L1-003.1` 的 `WorkflowDAG` / `validate_dag` / `topological_order` / `dependency_graph`；`T-L1-001.2` 的 `SkillRunner.run`；`T-L1-001.4` 的 `OutputRegistry`；`T-L0-001` 的 `Store`
- **关键动作**：
  ① **⑦ 链（宪法 8：文档先改）**——`trial_id` 升格 01 §1 第 14 类（**先**改 [01 §1](../docs/技术架构-v2/01-平台共享契约.md) 加行、**再**同步 [03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md)、**最后**改代码）：`contracts/identifiers.py` 新增 `TrialId` 并登记进 `ID_REGISTRY` / `ID_ALIASES`（`ID_KINDS` 13→14，既有断言同步；形态沿用本机生成类同构的 `trial_<uuid4 前 20 位>`）
  ② **03 §4 新增「调试协议落地口径」表**（9 行：记录标识 / 门禁 / 会话驱动 / 逐节点执行 / 输入注入 / 值来源标注 / 失败短路 / 复用面 / 历史对比）+ 收束句「试跑与正式执行的差别**仅四点**，权限口径照旧」
  ③ **`trial.py`（新，520 行）**——`WorkflowTrial` 步进式状态机（`running｜paused → completed｜failed｜aborted`；`pause`/`resume`/`abort` 同态幂等、跨终态显式拒；暂停态仍可单步）；`TrialRunner.start` 先跑门禁（只拦 `FATAL_ISSUE_CODES = {cycle, self_loop, duplicate_node, unknown_node}`）再开会话；`_resolve` 解析输入（`literal` 取字面值 / `ref` 自上游信封 `data` 按 path 取值、路径缺失即该节点 `validation_failed` / 注入覆盖 `literal` 并标注来源 / 已声明未绑定的参数同样可注入 / `ref` 绑定与未声明参数名显式拒绝）；`_advance` 逐节点经 `SkillRunner.run`（同一链、同已批准权限）→ 失败即 `_block_descendants` 把依赖图上的**后代**标短路（旁支照常执行）
  ④ **构造期口径断言**——`TrialRunner` 拒收带 `OutputRegistry` 的 `SkillRunner`，即「试跑不登记输出复用」的落点
  ⑤ **`trial_history.py`（新）**——`TrialHistory`（`save` / `get` / `list_all` / `list_for_flow` / `compare`）；落 `execution_log` 分区 `workflow-trial/<trial_id>.json`（与 `skill-run/` 同分区**不同前缀**、互不串扫）；对比判据取**状态码 + 载荷摘要**（时间等运行元信息不参与，见 [D-020](决策日志.md) 的 K）
  ⑥ **验证期修正**——零节点工作流原本 `run_to_end` 后仍停在 `running`（循环条件一次都不成立 → 永不收尾），补「无步可推即当场完成」分支并加用例钉住
- **变更文件**：docs/技术架构-v2/01-平台共享契约.md（§1 +1 行）· docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md（§4 +18 行）· src/st_agent/contracts/identifiers.py · src/st_agent/contracts/__init__.py · src/st_agent/l1/workflow/trial.py（新）· src/st_agent/l1/workflow/trial_history.py（新）· src/st_agent/l1/workflow/{__init__,errors}.py · tests/l1/test_workflow_trial.py（新，35 用例）· tests/contracts/test_identifiers_result_envelope.py · 项目管理/tasks/{T-L1-003.3, T-L1-003（父状态派生）}.md · 项目管理/{决策日志（[D-020](决策日志.md)）, 执行日志, 任务账本}.md
- **验证**：pytest **819/819** 通过（既有 782 + 本批 35 用例 + 2 分层校验新增源文件；2 deselected）· `verify_docs.py --strict` **PASS**（131 文件 / 1455 链接 / 0 断链；检查 4–8 全 0）· 账本 SYNC（活跃 64、就绪 7）· **证伪对照（口径非纸面）**：① **短路口径**——临时去掉 `_block_descendants` 调用后 `test_workflow_trial.py` **2 用例精准失败**（下游未标 `dependency_failed`、未注册 Skill 未短路），还原后 35/35 绿 ② **复用面口径**——绕过门面直连带 `OutputRegistry` 的 runner，实测同一 store 上**确实**落 `skill-output/run_….json` 且 `is_registered == True`，故构造期断言是**承重**而非装饰 · **静态核对**：`git status --short` 与 `git diff --stat` 显示的改动仅 `docs/`（01 / 03 两份）+ `src/st_agent/contracts/`（⑦ 链要求）+ `src/st_agent/l1/workflow/` + `tests/` + `项目管理/`——**不含** `src/st_agent/l1/runner/`（A1）及其他层 · `TrialRecord` 经 `Store` 往返可读（用例内的 `history.get` 即验证）
- **假设复核**：**A1 成立**（不改 `SkillRunner`；逐节点走其完整流水线，既有流水线用例全绿）· **A2 成立**（`ref` 自上游 `data` 按路径取；路径缺失 → 该节点 `validation_failed` 且执行器未被调用，有用例）· **A3 成立**（状态机为会话对象自有状态、单进程确定性；执行期置暂停 / 中止的用例证明停在**节点边界**；未引入线程）· **A4 成立**（记录落 `execution_log` 分区 `workflow-trial/`，与 `skill-run/` 前缀可分辨且互不串扫，有用例）· **A5 成立**（`approved_permissions` 照传并计入记录，权限口径未放宽；试跑与正式执行的差别仅四点）
- **决策**：[D-020](决策日志.md)（五项落地口径：`trial_id` 升格契约 ID / 门禁只拦不可执行类 / 不登记输出复用 / 暂停可单步 / 对比只认状态与载荷；含 A–L 备选取舍与代价）
- **遗留**：① **L0 / L1 两册均无命中**（已核未闭区：L0 册 E 段归零、A1b→`T-ECO-002`、A3→`T-L1-004`、A4→`T-ECO-001`、A5→L5、B1/B2→`T-L1-006`、C1/C2/D1–D3→人；L1 册 A1/A2 归属「人」）→ 本批**不销账** ② **`T-L1-003` 父任务仍余 `.4` / `.5` 两个叶子**（本批完成 `.3`）③ **就绪集 7 条**：`T-L1-003.4`（队首）· `.5` · `T-L1-004` · `T-L1-005` · `T-L1-006` · `T-L2-001` · `T-L4-001` ④ **M0 收口尚需 `T-L1-003.4`–`.5`、`T-L1-004`–`006` 与 `T-INT-001`** ⑤ 待走 PR 收口（分支 + commit + squash 合入）

## [sc] 2026-09-26T19:46+08:00 — T-L1-003.3 PR 合入收口 · done

- **目标**：把「工作流试跑（调试协议）」（`T-L1-003.3`，含 `trial_id` 升格 [01 §1](../docs/技术架构-v2/01-平台共享契约.md) 的 ⑦ 链与 [03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 落地口径表）走 PR 合入 main，完成全链路
- **对齐**：沿用既定流程（[Git协作规范](Git协作规范.md) §5.2 gate + auto-merge 合入即删分支），分支 `feat/T-L1-003.3-workflow-trial`（基于 main HEAD `33de854`）
- **关键动作**：
  ① **建 PR** —— 单条 commit `5924fab`（提交体含 `Task: T-L1-003.3` trailer 与署名行，逐项复核过；`pre-commit` 的「文档完整性自检」Passed，未绕过），建 [PR #28](https://github.com/Dline6/ST-Agent/pull/28)（**标题逐字符回读一致**）；`gh pr merge 28 --auto --squash --delete-branch` **未传** `--subject` / `--body`——按 `[sc] 2026-09-26T19:03` 的教训（占位值一旦被采用即不可逆污染），让 GitHub 取分支 commit 信息
  ② **真在等必检** —— 挂上后回读 `autoMergeRequest.mergeMethod = SQUASH`、`mergeStateStatus: BLOCKED`、`commitHeadline: null`（未传即不落），与 [D-012](决策日志.md) 的验证特征一致，即**真在等必检**而非「立即合入」
  ③ **自动合入** —— CI 绿后 PR 于 `11:38:11Z` **自动合入**，squash `2e7bd7f`
  ④ **分支清理** —— 远端由 `delete_branch_on_merge` 删除（`git fetch --prune` 报 `[deleted] origin/feat/T-L1-003.3-workflow-trial`）；本地侧先 `git diff --quiet main feat/T-L1-003.3-workflow-trial` **证明树完全一致**再 `-D`
  ⑤ **CI 监视开启** —— `set_monitor(auto_fix + address_comments)`（app 侧建 PR 时已自动绑定，无需显式 `bind_pr`）；监视期间未收到任何事件，**不轮询**
- **变更文件**：项目管理/执行日志.md（+本条）· 项目管理/tasks/T-L1-003.3（verify 补 PR + squash hash）· 项目管理/任务账本.md（render 刷新）
- **验证**：PR #28 `state: MERGED`（`mergedAt 2026-09-26T11:38:11Z`）· squash `2e7bd7f` 提交体含 `Task: T-L1-003.3` trailer 与 `Co-authored-by: Claude Haiku 4.5` 署名行（`git log -1 --pretty=%B` 复核）· 本地 `main` = `origin/main` = `2e7bd7f` · `git ls-remote --heads origin` 仅 `refs/heads/main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · **main 上 pytest 819/819**（2 deselected）· `verify_docs.py --strict` PASS
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型（五项落地口径由 [D-020](决策日志.md) 覆盖）
- **遗留**：① **就绪集 7 条**：`T-L1-003.4`（队首）· `.5` · `T-L1-004` · `T-L1-005` · `T-L1-006` · `T-L2-001` · `T-L4-001` ② **`T-L1-003` 余 `.4` / `.5` 两个叶子**，M0 收口尚需 `.4`–`.5`、`T-L1-004`–`006` 与 `T-INT-001` ③ **CI 监视**：PR #28 建后已开 `auto_fix + address_comments`，其合入后绑定随之释放——**不构成遗留**

## [T-L1-003.4] 2026-09-26T20:05+08:00 — 编辑面（画布 mutation + 草稿落画布） · done

- **目标**：落地 [03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 的双通道——① **可视化画布编辑**的增量操作 API（增删节点、连线断开、分组解组）与**连线即时契约 / 成环提示**；② **对话式生成**的接收面——把 L3 的工作流草稿对象落到画布，供用户**接受 / 微调 / 拒绝**（接受即落盘 v1.0 并产生 `change_id`）。仓库无前端，交付**后端编辑面 API**，画布渲染与交互 UI 归 L3
- **对齐**：读 Spec（[story-06](../docs/PRD-v2-Agent/story-06-skill-studio.md)「工作流编排」「对话式生成」「画布连线校验」「空状态」）/ Plan（[03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) + §3.1、[01 §7](../docs/技术架构-v2/01-平台共享契约.md) / §2、[05-L3 §5](../docs/技术架构-v2/05-L3-对话主入口.md)）/ 前置实现（`l1/workflow` 全套 + `contracts.schema_check` + `registry_types`）后做 **④ 对齐**——向人复述「交付什么 / 验收怎么算过（GWT-1–5）/ 接口面 / 遗留面 / 技术路径」，并就 §4 与 05 §5 未定的**四处口径**四问，人**逐项采纳推荐项**：① 草稿结构**另携** `workflow_draft` 子对象（不污染 `parameter_draft`）② **形状**非法才拒、**语义**不合规照开画布 ③ 注册名由 ASCII `flow_name` 或展示名 slug 派生、重名即拒 ④ 变更留痕落 `execution_log/workflow-change/`。口径全部落 [D-021](决策日志.md)；假设 A1–A5 见任务文件
- **输入**：[03 §4 / §3.1](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) · [01 §7 / §2](../docs/技术架构-v2/01-平台共享契约.md) · [05-L3 §5](../docs/技术架构-v2/05-L3-对话主入口.md)；`T-L1-003.1` 的 `WorkflowDAG` / `checked_dag` / `validate_dag` / `dependency_graph` / `WorkflowStore.save`；`T-SC-001.4` 的 `ChangeRecord` / `SemVer` / `ChangeId`；`contracts.schema_check.check_link`（01 §2 唯一口径）；`T-L0-001` 的 `Store`
- **关键动作**：
  ① **⑦ 链（宪法 8：文档先改）**——[03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 新增「编辑面落地口径」表（11 行 + 收束句），[05-L3 §5](../docs/技术架构-v2/05-L3-对话主入口.md) 加一行指针（跨层交接面须在 L3 侧可见：`target=工作流` 时草稿另携 `workflow_draft`，身份字段**不在草稿中**）；**01 契约未动**——无新 ID 类型（复用既有 `flow_id` 与 `change_id`）、无新结构字段，`ChangeRecord` 原样复用
  ② **`find_cycle` 提升为公共判定件**——`workflow/validate.py` 把模块私有的 `_find_cycle` 暴露为公共 `find_cycle`（与 `dependency_graph` / `topological_order` 并列），`validate_dag` 内部改调它：保存期与编辑期共用**同一份**成环判定（01 §2 唯一口径），不另造第二套
  ③ **新增 `l1/studio/canvas.py`**（`CanvasEditor` + `EditResult`）——**全函数**：用户级编辑冲突一律 `applied=False` 返回、不抛异常；`connect` 只拦**本连线造成**的问题：`_cycle_closed_by` 判「**不含本边**的依赖图上 `to` 能否走回 `from`」→ 携环路径（故草稿**既有**的环不阻断后续连线），`_link_issue` 走 `check_link` → 携「这里需要一个 XX 类型的输入」式说明；每次写操作后自动跑一遍 `validate_dag` 并随结果返回
  ④ **删节点连带**（实现期口径，随本件登记）——`remove_node` 移除该节点的**全部引用**：入射 / 出射连线、指向它的 `ref` 绑定、分组内的成员位。依据是 §3.1 的依赖图口径「边 ∪ `ref` 绑定」（取并）；若只清边，其他节点会留下 `unknown_node` 发现，而 GWT-1 列出的编辑操作里**没有**参数编辑项 → 用户无 API 可修
  ⑤ **新增 `l1/studio/{errors,ids,draft}.py`**——`WorkflowDraft`（05 §5 四字段 + `workflow_draft` 子对象）、`DraftSession`、`DraftIntake`（`receive` / `tune` / `accept` / `reject`）；接收期做**显式字段白名单**（携带身份字段或无法识别字段即拒、不静默丢弃）；`receive` 以**临时身份** `wf_<注册名>_v0.0` 开画布，`accept` 换首版 `v1.0` 落 `config/workflow/` 并留痕 `execution_log/workflow-change/<change_id>.json`（对齐 §5.3 `mcp-hub-change/` 先例）
  ⑥ **验证期修正（值得复用）**——`add_node` / `connect` / `group` 首版按 `WorkflowValidationError` 捕获构造期非法，实测**被拒**：pydantic 会把模型校验器内抛的 `WorkflowValidationError`（`ValueError` 子类）**包成 `ValidationError`**，故改捕 `ValidationError`（非显然，已在代码内留一行注释说明）
  ⑦ **任务文件 A2 措辞修正**——「待补」二字撞上 `verify_docs.py` 检查 5 的占位符正则（`待补`），改写为「契约暂缺」；该正则是全局门禁，不改则任务置 `doing` 即报「假设节未实填」
- **变更文件**：docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md（§4 +13 行）· docs/技术架构-v2/05-L3-对话主入口.md（§5 +1 行）· src/st_agent/l1/workflow/validate.py（+`find_cycle`，`__all__` +1）· src/st_agent/l1/workflow/__init__.py（+1 导出）· src/st_agent/l1/studio/（新，5 文件：`__init__` / `canvas` / `draft` / `errors` / `ids`）· tests/l1/test_studio_canvas.py（新，34 用例）· 项目管理/tasks/T-L1-003.4（④ 三节实填 + `verify`）· 项目管理/{决策日志（[D-021](决策日志.md)）, 执行日志, 任务账本}.md
- **验证**：pytest **858 passed / 2 deselected**（既有 819 + 本批 34 用例 + 分层校验新增源文件 5 案）· **证伪对照双验（口径非纸面）**：① **成环拦截范围口径**——把 `_cycle_closed_by` 临时改成「候选图上有任何环就拦」（即备选「整图有环即拒」），`test_studio_canvas.py` **4 例精准失败**（含关键的「既有环不阻断其他连线」），还原后 34/34 绿；② **删节点连带口径**——去掉 `_strip_refs` 级联后 `test_gwt1_remove_node_cascades_all_references` **精准失败**（结构性断言未空转），还原后绿 · `verify_docs.py` 检查 1–8 全 0（`--strict` 见收口条）· **静态核对**：`git status --short` 仅 `docs/`（03 / 05 两份）+ `src/st_agent/l1/workflow/`（`find_cycle` 提升）+ `src/st_agent/l1/studio/`（新）+ `tests/l1/` + `项目管理/`——**不含**其他层代码；`tests/test_layering.py` 自动覆盖 5 个新源文件、无向上依赖
- **假设复核**：**A1 成立**（草稿态不落盘：`receive` 后 `config` 分区无 `workflow/` 文件，有用例断言）· **A2 成立**（连线即时校验复用 `check_link`——GWT-2 用例直接断言其原文措辞「这里需要一个 string 类型的输入」，编辑期与保存期是同一判定件的两次调用）· **A3 成立**（`WorkflowDraft` 接收契约由本任务定义、字段名对齐 05 §5；并已在 05 §5 加指针行使 L3 可见，`T-L3-003` 开工时按它核对）· **A4 成立**（接受复用 01 §7 变更留痕机制：`ChangeRecord` + `change_id` + `execution_log` 留痕，未另造）· **A5 成立**（拒绝原因仅随 `RejectResult.reason` 透传、不落盘；反馈池仍归 `T-L6-001`）
- **决策**：[D-021](决策日志.md)（五项落地口径：草稿载具 / 非法草稿边界 / 删节点连带 / 注册名派生 / 变更留痕分区；含 A–O 备选取舍与代价）
- **遗留**：① **L0 / L1 两册均无命中**（已核未闭区：L0 册 A1b / A3 / A4 / A5 / B1 / B2 / C1–C2 / D1–D3，L1 册 A1 / A2——归属均为「人」或其他任务）→ 本批**不销账** ② **`T-L1-003` 父任务仍余 `.5`（模板库与空状态）** ③ **就绪集 6 条**：`T-L1-003.5`（队首）· `T-L1-004` · `T-L1-005` · `T-L1-006` · `T-L2-001` · `T-L4-001` ④ **M0 收口尚需 `T-L1-003.5`、`T-L1-004`–`006` 与 `T-INT-001`** ⑤ 待走 PR 收口（分支 + commit + squash 合入）

## [sc] 2026-09-26T20:15+08:00 — T-L1-003.4 PR 合入收口 · done

- **目标**：把「编辑面（画布 mutation + 草稿落画布）」（`T-L1-003.4`，含 [03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 新增的「编辑面落地口径」表与 [05-L3 §5](../docs/技术架构-v2/05-L3-对话主入口.md) 跨层指针、`validate.find_cycle` 提升为公共判定件）走 PR 合入 main，完成全链路
- **对齐**：沿用既定流程（[Git协作规范](Git协作规范.md) §5.2 gate + auto-merge 合入即删分支），分支 `feat/T-L1-003.4-studio-canvas`（基于 main HEAD `69f7752`）。**本会话接手前 PR 已建、已合入**——本会话只做事后核对与收口留痕（下 ①–⑤ 为**回读实测**，非事后追述）
- **关键动作**：
  ① **回读 PR 状态** —— [PR #29](https://github.com/Dline6/ST-Agent/pull/29) `state: MERGED`（`mergedAt 2026-09-26T12:09:25Z`），head `feat/T-L1-003.4-studio-canvas` → base `main`
  ② **auto-merge 未污染提交信息** —— `autoMergeRequest.mergeMethod = SQUASH`、`enabledAt 12:07:08Z`，`commitHeadline` / `commitBody` 均为 `null`（即建 PR 时**未传** `--subject` / `--body`）；合入后 squash `88e6171` 的提交体经 `git log -1 --pretty=%B` 复核，含 `Task: T-L1-003.4` trailer 与 `Co-authored-by: Claude Haiku 4.5` 署名行——与 `[sc] 2026-09-26T19:03` 的教训一致
  ③ **CI 必检** —— 挂 auto-merge 时两次 `check`（CI workflow）分别为 `12:08:33Z` / `12:08:56Z` 绿，随后 `12:09:25Z` 自动合入（与 [D-012](决策日志.md) 的「真在等必检」特征一致）
  ④ **分支清理** —— 远端由 `delete_branch_on_merge` 删除（`git ls-remote --heads origin` 仅 `refs/heads/main`）；本地侧先 `git diff --quiet main feat/T-L1-003.4-studio-canvas` **证明树完全一致**再 `-D`，收尾 `git branch -a` 仅 `main` + `origin/main`
  ⑤ **补收口留痕** —— 任务 `verify` 由「待 PR 收口」改写为 PR 链接 + squash hash；执行日志补本条；`render` 刷新账本
- **变更文件**：项目管理/执行日志.md（+本条）· 项目管理/tasks/T-L1-003.4（verify 补 PR + squash hash）· 项目管理/任务账本.md（render 刷新）
- **验证**：PR #29 `state: MERGED`（`mergedAt 2026-09-26T12:09:25Z`）· squash `88e6171` 提交体含 `Task: T-L1-003.4` trailer 与 `Co-authored-by: Claude Haiku 4.5` 署名行（`git log -1 --pretty=%B` 复核）· 本地 `main` = `origin/main` = `88e6171` · `git ls-remote --heads origin` 仅 `refs/heads/main` · `git branch -a` 仅 `main` + `origin/main` · 工作区干净 · **main 上 pytest 858 passed / 2 deselected**（100% 通过，退出码 0）· `verify_docs.py --strict` PASS
- **假设复核**：无（纯流程收口，无新增假设）
- **决策**：无新选型（五项落地口径由 [D-021](决策日志.md) 覆盖）
- **遗留**：① **就绪集 6 条**：`T-L1-003.5`（队首）· `T-L1-004` · `T-L1-005` · `T-L1-006` · `T-L2-001` · `T-L4-001` ② **`T-L1-003` 余 `.5`（模板库与空状态）一个叶子**，M0 收口尚需 `.5`、`T-L1-004`–`006` 与 `T-INT-001` ③ **CI 监视**：PR #29 随合入释放，**不构成遗留** ④ **本地 `main` 落后 `origin/main` 一条的成因已排除**——本会话 `git merge --ff-only` 前本地为 `69f7752`，属前一会话收口后未再拉取，非分叉

## [T-L1-003.5] 2026-09-26T20:43+08:00 — 模板库与空状态 · done

- **目标**：落地 [03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 的**模板库**与**空状态**——L1 **内置**一批官方工作流模板（纯数据种子、随包发布、不落用户存储），提供**浏览 / 加载 / fork** 三面；并在用户尚无任何自建工作流时返回结构化的 Studio 空状态（模板库清单 + 一句话入口）。仓库无前端，交付**后端查询与 fork API**，渲染归 L3
- **对齐**：读 Spec（[story-06](../docs/PRD-v2-Agent/story-06-skill-studio.md)「模板库」「空状态」及其验收标准末条）/ Plan（[03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 的模板库 / 空状态两条 bullet + §3.2 依赖提示、[01 §1](../docs/技术架构-v2/01-平台共享契约.md) / §6）/ 前置实现（`l1/workflow` 全套 + `composite.missing_dependencies` + `skills/pack` + `studio.ids`）后做 **④ 对齐**——向人复述「交付什么 / 验收怎么算过（GWT-1–4）/ 接口面 / 遗留面 / 技术路径」，并就 §4 未定的**三处口径**三问，人**逐项采纳推荐项**：① `template_id` **不登记** 01 §1 契约 ID 表 ② fork **不落盘**（交编辑面，「接受」是唯一落盘点）③ fork 重名**加序号**而非重名即拒。口径全部落 [D-022](决策日志.md)；假设 A1–A7 见任务文件
- **输入**：[03 §4 / §3.2](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) · [01 §1 / §6](../docs/技术架构-v2/01-平台共享契约.md) · [story-06](../docs/PRD-v2-Agent/story-06-skill-studio.md)；`T-L1-003.1` 的 `WorkflowDAG` / `checked_dag` / `validate_dag` / `flow_id_for` / `base_of` / `parse_flow_id` / `WorkflowStore.{list_all,save,get}`；`T-L1-003.2` 的 `missing_dependencies` / `CompositeDependencyError` / `save_as_composite_skill`；`T-L1-003.4` 的 `studio.ids.{slugify,SLUG_PATTERN}` / `CanvasEditor`；`T-L1-001.1` 的 `SkillRegistry` / `ensure_official_pack`；`T-L0-001` 的 `Store`
- **关键动作**：
  ① **⑦ 链（宪法 8：文档先改）**——[03 §4](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 新增「模板库落地口径」表（8 行 + 收束句），[05-L3 §6](../docs/技术架构-v2/05-L3-对话主入口.md) 加一行指针（跨层交接面须在 L3 侧可见：`TemplateSummary` / `StudioHome` 是渲染数据源）；**01 契约未动**——无新 ID 类型、无新结构字段（`template_id` 有意**不进** 01 §1，见 A6）
  ② **新增 `l1/studio/templates.py`**——`OFFICIAL_TEMPLATES` 三条种子（每日 ST 简报 / 退市风险扫描 / 策略回测流水线，各 4 节点），形态与 [`skills/pack.py::OFFICIAL_PACK`](../src/st_agent/l1/skills/pack.py) 同构（字段 kwargs 式纯数据、**可增不可缺**、**不含身份字段** `flow_id` / `version`）+ 两个模板自有字段（`template_id` 进程内寻址键 / `flow_name` ASCII 注册名）；种子在**装载期**经 `checked_dag` + `validate_dag` 校验（与注册表无关的那部分：§6 命名 / 标识 / 连线 / 成环 / 分组 / `skill_id` 形态），有错即装载失败
  ③ **`load` 与 `fork` 分工**——`load` 取**只读预览**（临时身份 `wf_<注册名>_v0.0`，`0.0` 表未发布，与编辑面画布期身份同口径）；`fork` 取**用户自有副本**（base 由 `flow_name` 派生，与既有 base 冲突即加序号 `_2` / `_3` …，身份 `v1.0`）。**两者都不落盘**——fork 产物即「来自模板的草稿」
  ④ **依赖提示不另造**——三条路径（`list` 摘要的 `missing_skill_ids` / `load` 抛错 / `fork` 抛错）全部走 §3.2 的 `missing_dependencies` **同一判定件**，与复合 Skill 依赖缺失同口径；`list` 只标注不拦（浏览面），`load` / `fork` 抛 `CompositeDependencyError`（不静默通过）
  ⑤ **新增 `l1/studio/empty_state.py`**——`StudioHome`（`templates` + `one_line_entry` + `is_empty`）；判据取 `WorkflowStore.list_all()` 为空，模板清单**恒返回**（非空时亦然，供「新建」入口复用同一数据源）；入口文案逐字取 story-06「用一句话描述你想要的工作流」
  ⑥ **`errors.py` 加 `TemplateError`**（与 `Draft*` 三型并列；依赖缺失**不用**它——走 `CompositeDependencyError`）；`studio/__init__.py` 导出新面
  ⑦ **验证期发现（值得复用）**——A6 用例首版按「config 分区不得有文件名含 `template_id`」断言，**撞上同名子串**：官方 Skill 文件名 `sk_delisting_risk_scan_v1.0.json` 含模板 id `delisting_risk_scan`，断言与 A6 的实质（`template_id` 不作身份来源）**无关**。改为「`template_id` 与 `flow_name` 刻意取不同值 → 断言身份只由 `flow_name` 派生」，直指要证的那件事
- **变更文件**：docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md（§4 +15 行）· docs/技术架构-v2/05-L3-对话主入口.md（§6 +1 行）· src/st_agent/l1/studio/templates.py（新）· src/st_agent/l1/studio/empty_state.py（新）· src/st_agent/l1/studio/{errors,__init__}.py · tests/l1/test_studio_templates.py（新，20 用例）· 项目管理/tasks/T-L1-003.5（④ 三节实填 + `verify`）· 项目管理/{决策日志（[D-022](决策日志.md)）, 执行日志, 任务账本}.md
- **验证**：pytest **880 passed / 0 failed**（既有 858 + 本批 20 用例 + 分层校验新增源文件 2 案；`-m 'not live'` 排除 2 条 live）· **证伪对照双验（口径非纸面）**：① **深拷贝口径**——把 `_build_dag` 的 `deepcopy(body)` 改为 `body`，`test_gwt2_fork_copy_does_not_share_mutable_state_with_template` **精准失败**（就地改副本污染了后续 fork，实得 `{'scope': 'sh'}` 期望 `{'scope': 'all'}`），还原后 20/20 绿；② **fork 去重口径**——把 `_dedup_base` 的去重循环短路，`test_gwt2_fork_twice_does_not_clobber_the_first` **精准失败**（第二次落盘撞 `WorkflowExistsError`，正是「冲掉第一次」的等价事故），还原后绿 · `verify_docs.py` 检查 1–8 全 0（`--strict` 见收口条）· **静态核对**：`git status --short` 仅 `docs/`（03 / 05 两份）+ `src/st_agent/l1/studio/` + `tests/l1/` + `项目管理/`——**不含**其他层代码；`tests/test_layering.py` 自动覆盖 2 个新源文件、无向上依赖
- **假设复核**：**A1 成立**（种子为纯数据——用例递归断言无对象 / 无回调；`list` / `load` / `fork` 全程不碰网络）· **A2 成立**（官方 Pack 播种后三条模板 `validate_dag(dag, registry)` 全 `ok`、`missing_skill_ids` 为空；fork 产物可直接 `save_as_composite_skill` 物化为 `sk_daily_st_brief_v1.0`（`source=user-built`））· **A3 成立**（fork → 落盘 → 再 fork 得 `_2`、第三次 `_3`，首个副本逐字段未被冲掉）· **A4 成立**（空 store → `is_empty=True`；落一条后 `False`，且模板清单**仍返回**）· **A5 成立**（非中性命名种子在**装载期**即 `TemplateError`，用例覆盖）· **A6 成立**（浏览 / 加载 / fork 三面后 `config` 分区文件列表**逐项不变**；`template_id` 与 `flow_name` 取不同值时身份只由后者派生）· **A7 成立**（三类坏种子——非中性命名 / 无法识别字段 / 成环——装载期即失败；深拷贝有专项用例）
- **决策**：[D-022](决策日志.md)（三项落地口径：`template_id` 不登记 / fork 不落盘 / fork 重名加序号；含 A–F 备选取舍与「与编辑面重名即拒**有意不同**」的理由）
- **遗留**：① **L0 / L1 两册均无命中**（已逐条核未闭区：L0 册 `A1b` / `A3` / `A4` / `A5` / `B1` / `B2` / `C1`–`C2` / `D1`–`D3`，L1 册 `A1` / `A2`——归属均为「人」或其他任务）→ 本批**不销账** ② **`T-L1-003` 父任务 `done`**（`.1`–`.5` 全 `done`，状态由 render 派生）；M0 其余未了在 `T-L1-004`–`006` 与 `T-INT-001`——`archive` 需整个 M0 满员，**本批不跑** ③ **就绪集 6 条**：`T-L1-004`（队首）· `T-L1-005` · `T-L1-006` · `T-L2-001` · `T-L4-001` · `T-ECO-001`（父任务 `T-L1-003` 转 `done` 后被解锁） ④ **模板内容与官方 Skill 清单的对应关系**若在 `T-L1-004` 落地时变化，须回头复核本任务种子（任务文件「备注」已记） ⑤ 待走 PR 收口（分支 + commit + squash 合入）

## [T-L1-007] 2026-09-26T21:16+08:00 — stdio 审计边界校正 + MCP 派生 Skill 回收（L1 遗留 A1/A2 收口） · done

- **目标**：收口 L1 遗留册**仅剩的两条**（原归属「人」）——**A1** 把 02/03 关于 stdio MCP 审计的过度声明校正为真实的「已知边界 + 缓解项」；**A2** 补 MCP 派生 Skill 的回收能力（Server 显式移除即硬回收、tool 消失即软失效）
- **对齐**：已与人确认——① A1 走「校正文档边界 + 记决策」、**不**立项做进程级隔离；② A2 新立 `T-L1-007` 并**本轮实现**；③ 回收接线取「`remove_server` 加**可选**回调」（★ ④ 复述见任务文件 `## 接口面` / `## 可关闭的遗留` / `## 假设与前提` 三节；假设编号 A1–A5）
- **输入**：L1 册 `A1` / `A2`；[02 §1 / §6](../docs/技术架构-v2/02-L0-本地优先基座.md)；[03 §5.1 / §5.2 / §5.3](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md)；`McpServerRegistry.remove_server`（[registry.py:167](../src/st_agent/l1/mcp/registry.py)）；`McpSkillMapper.{mappings,available_skills}`（[mapping.py:365](../src/st_agent/l1/mcp/mapping.py)·[:388](../src/st_agent/l1/mcp/mapping.py)）；`SkillRegistry.{list_versions,list_all,_skill_path,_update_path}` + `base_of`（[registry.py:271](../src/st_agent/l1/skills/registry.py)·[:284](../src/st_agent/l1/skills/registry.py)·[ids.py:55](../src/st_agent/l1/skills/ids.py)）；[D-010](决策日志.md) / [D-013](决策日志.md) / [D-014](决策日志.md)
- **关键动作**：
  ① **⑦ 链（宪法 8：文档先改）**——[02 §1](../docs/技术架构-v2/02-L0-本地优先基座.md) 依赖规则 bullet 与 [§6](../docs/技术架构-v2/02-L0-本地优先基座.md) 开篇、[03 §5.3](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 三处由「其网络行为仍受 L0 审计 / **所有**对外网络请求必须经网关 / Server 的**全部**网络请求经网关登记」改为**已知边界 + 缓解项**（经网关的请求仍全部登记；stdio 子进程自身出网不在审计范围）；[03 §5.1](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 加「删除即回收」一行、[§5.2](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 加「tool 消失标失效」一行与**回收落地口径表**；**01 契约未动**（不是接口变更，不触发工作流第 ⑦ 步）
  ② **新增 `SkillRegistry.unregister(base)`**（[l1/skills/registry.py](../src/st_agent/l1/skills/registry.py)）——删该 base 的**全部版本** + `skill-update/<base>.json` 待检查标记（标记不同清即成孤儿）；只动 `config` 分区、**不动 `execution_log`**（append-only 审计，02 §6）；base 无版本抛 `SkillNotFoundError`（不静默 no-op）；非 `sk_` 前缀即 `SkillValidationError`
  ③ **`MappingStatus` 增 `vanished` + `_mark_vanished`**（[l1/mcp/mapping.py](../src/st_agent/l1/mcp/mapping.py)）——`sync_tools` 在取回 `tools/list` 后把「已有映射 ∖ 本次 tool 集」标 `vanished`（必带变化说明，与 `pending-remap` 同一校验口径）；`_sync_one` 既有的自愈分支（status ≠ `active` 且指纹回到基线 → 回 `active`）**照旧复用**，故 tool 回归即自愈
  ④ **可用列表滤除 `vanished`**——`_unavailable_skill_ids` 由「Server 不在可用集合」扩为「**或**映射已 `vanished`」，`available_skills()` 据此一并滤掉（GWT-4）
  ⑤ **新增 `McpSkillMapper.recycle_server(server_id)`**（[l1/mcp/mapping.py](../src/st_agent/l1/mcp/mapping.py)）——按映射记录的派生 base 去重后 `skills.unregister`，再删 `mcp-mapping/<sid>/*.json`；**不调用 `get_server`**（注册记录此时已不存在）；对已不在册的描述体跳过（不误抛），非 `mcp-mapped` 来源不受影响
  ⑥ **`McpServerRegistry.__init__` 加可选 `on_server_removed`**（[l1/mcp/registry.py](../src/st_agent/l1/mcp/registry.py)）——`remove_server` 删除成功后调用（先删记录、再 `permissions.forget`、最后回调）；**缺省 `None` 时行为逐字节不变**（沿用 [D-014](决策日志.md)「给已 done 模块加可选形参、缺省不变」的先例）；回调形态别名 `ServerRemovedHook` 随 `st_agent.l1.mcp` 导出
  ⑦ **改写一条旧边界用例**——`test_removed_tool_leaves_record_untouched` 由「A8：tool 消失不回收」改为「标 `vanished`、记录与描述体保留、可逆」，**保留用例名**（`T-L1-002.2` 的 `verify` 字段引用了它，不为改名去动已 `done` 的任务文件）
  ⑧ **补 T-INT-001 依赖**——`T-L1-007` 是 M0 任务，`verify_docs` 检查 7 要求 M0 集成关卡覆盖本里程碑全部任务，故 `T-INT-001.depends_on` 追加 `T-L1-007`
- **变更文件**：docs/技术架构-v2/02-L0-本地优先基座.md（§1 / §6）· docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md（§5.1 / §5.2 + 回收口径表 / §5.3）· src/st_agent/l1/skills/registry.py（+`unregister`）· src/st_agent/l1/mcp/mapping.py（`MappingStatus` / `_mark_vanished` / `_unavailable_skill_ids` / `recycle_server`）· src/st_agent/l1/mcp/registry.py（+`ServerRemovedHook` / `on_server_removed`）· src/st_agent/l1/mcp/__init__.py（导出）· tests/l1/test_registry.py（+4）· tests/l1/test_mcp_mapping.py（+11，含改写 1）· tests/l1/test_mcp_registry.py（+3）· 项目管理/tasks/T-L1-007-*.md（新）· 项目管理/tasks/T-INT-001-*.md（depends_on +T-L1-007）· 项目管理/{决策日志（[D-023](决策日志.md) / [D-024](决策日志.md)）, 执行日志, 遗留问题/L1-遗留问题.md, 任务账本.md}
- **验证**：pytest **898 passed / 0 failed**（本批 +18：反注册 4 · 失效与回收 11 · 回调接线 3；`-m 'not live'` 排除 2 条 live）· `verify_docs.py` 检查 1–8 全 0（`--strict` 见收口条）· **静态核对**：`git status --short` 仅 `docs/技术架构-v2/` 两份 + `src/st_agent/l1/{skills,mcp}/` + `tests/l1/` + `项目管理/`——**不含** `src/st_agent/l0/` 与 `src/st_agent/contracts/`（未触 L0、未触 01 契约）；`tests/test_layering.py` 通过
- **假设复核**：**A1 成立**（02/03 不再出现「stdio 子进程自身出网受审计」式声明；断链 0 + 分层校验通过）· **A2 成立**（`test_removed_tool_leaves_record_untouched` 断言 `vanished` 且描述体保留；`test_vanished_tool_returns_and_heals` 断言自愈回 `active`）· **A3 成立**（`test_unwired_remove_server_keeps_records` 证缺省不触发回收；`test_recycle_via_remove_server_hook` 证接线后即回收；包内仍 `mapping → registry` **单向**，未成环）· **A4 成立**（`test_removes_all_versions_and_marker` 断言三版本 + 标记同清；`test_other_bases_and_audit_untouched` 断言 `execution_log` 存活、其他 base 不受影响）· **A5 成立**（三项相邻孤儿本期未处理，已按册规 1 在 L1 册新登记）
- **决策**：[D-023](决策日志.md)（stdio 审计边界：校正文档为已知边界 + 缓解项、不立项做进程级隔离）· [D-024](决策日志.md)（回收口径：删除即硬回收 / tool 消失软失效 / 反注册按 base / 回调接线；含回收落地口径表与跨任务复用规则）
- **遗留**：① **L1 册 `A1` / `A2` 本批关闭**——从册内未闭区移除、册尾「已闭（备查）」各留一行（册规 3）；归属栏由「人」**修正为本任务** ② **新登记 3 条相邻边界**（`config/mcp-lifecycle/<server_id>.json` 孤儿 · `config/sandbox-disabled/<skill_id>.json` 陈旧旗标（硬删后同 id 重注册会被旧旗标误伤）· WorkflowDAG 对已回收 Skill 的悬空引用）——归属待人定 / 后续任务，已在 L1 册新登记段列明 ③ **`T-L1-006` 组合根开工时须按 `disable → cancel_inflight → remove_server → recycle_server` 顺序接线**（任务文件「备注」与 [D-024](决策日志.md) 均已写明），否则「删除即回收」不会发生 ④ 待走 PR 收口（分支 + commit + squash 合入）

## [T-L1-008] 2026-09-26T21:54+08:00 — 陈旧禁用旗标自愈 + 复合契约悬空引用显式化（L1 遗留 B2/B3 收口） · done

- **目标**：收口 L1 遗留册 `B2` / `B3`（原归属「人」）——**B2** 让新注册的 Skill 不继承旧 `sandbox-disabled` 旗标（含同进程内存态）；**B3** 让 `derive_io_contract` 对悬空引用显式化（抛 `CompositeDependencyError` 而非裸 `SkillNotFoundError`）
- **对齐**：已与人确认三项策略——① 旗标取「**注册侧自愈**」；② 悬空引用取「改抛 `CompositeDependencyError`」；③ `unknown_skill` **维持**不入 `FATAL_ISSUE_CODES`（★ ④ 复述见任务文件 `## 接口面` / `## 可关闭的遗留` / `## 假设与前提` 三节；假设编号 A1–A3）
- **输入**：L1 册 `B2` / `B3`；`SkillRegistry.register`（[registry.py:146](../src/st_agent/l1/skills/registry.py)）；`SkillSandbox.{is_disabled,disabled_skills,_disabled_path,disable,enable}`（[sandbox.py:155](../src/st_agent/l1/sandbox/sandbox.py)）+ `DISABLED_PREFIX`（[models.py:32](../src/st_agent/l1/sandbox/models.py)）；`derive_io_contract` / `missing_dependencies` / `CompositeDependencyError` / `DependencyGap`（[composite.py:145](../src/st_agent/l1/workflow/composite.py)·[:116](../src/st_agent/l1/workflow/composite.py)·[:77](../src/st_agent/l1/workflow/composite.py)）
- **关键动作**：
  ① **实测校正 B3 的描述（先核实后动手）**——复现脚本实测：`trial` 路径**不抛错**（`SkillRunner.run` [runner.py:262](../src/st_agent/l1/runner/runner.py) 与 `TrialRunner._resolve` [trial.py:518](../src/st_agent/l1/workflow/trial.py) 均已 catch → `validation_failed` 信封，实测 `status=validation_failed`）；`save_as_composite_skill` 有依赖门 → `CompositeDependencyError`；**唯一裸抛点**为 `derive_io_contract`。册内原文「`composite` / `trial` 的 `registry.get` 未 catch 会抛」据此修正
  ② **⑦ 链（宪法 8：文档先改）**——[03 §5.2](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) 回收口径表「范围边界」行改写：`sandbox-disabled` 旗标从「不在回收范围」→「不经回收动作清除、由注册侧自愈兜底」；[03 §1.5](../docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md) / §3.2 经核**无需改**；**01 契约未动**
  ③ **`SkillRegistry.register` 增自愈步**（[l1/skills/registry.py](../src/st_agent/l1/skills/registry.py)）——`list_files("config")` 合并为一次调用；`SkillExistsError` 判定**在先**（已注册 id 的禁用态不得被误清），其后才删 `sandbox-disabled/<skill_id>.json`（走到此处该 id 必未注册，旗标只能属已消失实体）
  ④ **`is_disabled` / `disabled_skills` 改为有 `Store` 时读盘**（[l1/sandbox/sandbox.py](../src/st_agent/l1/sandbox/sandbox.py)）——修第二半缺陷：盘上旗标清了但同进程内存副本仍误伤；无 `Store` 时仍走内存 `_disabled`（既有语义不变）
  ⑤ **`derive_io_contract` 改抛 `CompositeDependencyError`**（[l1/workflow/composite.py](../src/st_agent/l1/workflow/composite.py)）——复用 `missing_dependencies`「同一判定件」，携逐条 `gaps`；docstring 同步
  ⑥ **补 T-INT-001 依赖**——`T-L1-008` 是 M0 任务，`verify_docs` 检查 7 要求覆盖，故 `T-INT-001.depends_on` 追加 `T-L1-008`
- **变更文件**：docs/技术架构-v2/03-L1-能力底座-Skills与MCP.md（§5.2 范围边界）· src/st_agent/l1/skills/registry.py（register 自愈 + `DISABLED_PREFIX` import）· src/st_agent/l1/sandbox/sandbox.py（`is_disabled` / `disabled_skills` 读盘）· src/st_agent/l1/workflow/composite.py（`derive_io_contract` 显式化）· tests/l1/test_sandbox.py（+5）· tests/l1/test_workflow_composite.py（+4）· 项目管理/tasks/T-L1-008-*.md（新）· 项目管理/tasks/T-INT-001-*.md（depends_on +T-L1-008）· 项目管理/{决策日志（[D-025](决策日志.md) + [D-024](决策日志.md) 事实校正标注）, 执行日志, 遗留问题/L1-遗留问题.md, 任务账本.md}
- **验证**：pytest **907 passed / 0 failed**（本批 +9：旗标自愈与读盘 5 · 悬空引用显式化 4）· **红-绿已验**：暂存 `src/` 后新用例失败 5 条（sandbox 2 · composite 3）、恢复后全绿——证用例确实在测本次变更 · `verify_docs.py --strict` 检查 1–8 全 0 · **静态核对**：`git status --short` 仅 `docs/技术架构-v2/03-*` + `src/st_agent/l1/{skills,sandbox,workflow}/` + `tests/l1/` + `项目管理/`——**不含** `src/st_agent/l0/` 与 `src/st_agent/contracts/`（未触 L0、未触 01 契约）；`tests/test_layering.py` 通过
- **假设复核**：**A1 成立**（`is_disabled` 有 `Store` 读盘、无 `Store` 走内存，`test_memory_fallback_without_store` 证；既有 `test_sandbox.py` 全绿）· **A2 成立**（`derive_io_contract` 改抛类型，src 内无其他调用方；`pytest` 全绿）· **A3 成立**（`test_existing_skill_flag_survives_duplicate_register` 证 `SkillExistsError` 在先、不误清已注册态的旗标）
- **决策**：[D-025](决策日志.md)（旗标注册侧自愈 + 悬空引用显式化 + 试跑门禁维持）——同批在 [D-024](决策日志.md) 影响段加事实校正标注（不改其决策结论）
- **遗留**：① **L1 册 `B2` / `B3` 本批关闭**——从册内未闭区移除、册尾「已闭（备查）」各留一行（册规 3）；归属栏由「人」**修正为本任务**；**B3 条目描述同批修正**（原文对 trial 的断言与实测不符）② **`B1` 归属不变**——仍为 `T-L1-006` 组合根（`mcp-lifecycle` 孤儿）③ 待走 PR 收口（分支 + commit + squash 合入 + auto-merge）

## [T-L1-004.1] 2026-09-26T22:30+08:00 — 官方 Pack 装载接线与数据访问基件 · done

- **目标**：交付官方 Skill Pack 的**装载入口**与 12 个执行器共用的**取数基件**——一条 `install_official_pack(...)` 把「描述体播种 + 执行器注册」做完，装载后 12 个 Skill 全部可执行；取数一律经 L0 `MarketDb.query` 的单一入口，非 `ok` 信封原样透出
- **对齐**：人 2026-09-26 确认三项并指示「拆 3 个叶子后依次连续完成，全部完成后再集中全量测试与推送」——① 拆分粒度取 3 个叶子（`.1` 装载 + 基件 / `.2` 公共认知 6 / `.3` 主动服务 6），`.1` 不碰任何具体 Skill 语义；② 运行期输入取「声明的 `parameters` + `ctx.upstream`」两条既有通道、**不新增第三通道**（`SkillRunner` / `SkillContext` 零签名变化），种子 `input_schema` 维持连线契约角色、不增删既有参数（否则断 T-L1-003.5 模板绑定）；③ `strategy-design` 的未来函数检测件为**公开纯函数**（假设编号见任务 A1–A6）
- **输入**：`skills/pack.py::{OFFICIAL_PACK, ensure_official_pack}`（T-L1-001.1）· `skills/ids.py::skill_id_for` · `runner/runner.py::{SkillRunner.register_executor, SkillExecutor, SkillContext}`（T-L1-001.2）· `l0/market/db.py::MarketDb.query`（`ok` / `empty` / `unavailable` 三分支 + `last_updated_at`）与 `snapshot_id()`（T-L0-005）· `contracts/result_envelope.py::{ResultEnvelope, EvidenceRef}`（T-SC-001.1）
- **关键动作**：
  ① **新子包 `st_agent.l1.skills.official`**（`__init__` / `common` / `install` / `errors`）——描述体种子仍留在 `skills/pack.py`（T-L1-001.1 的面），本子包只补「可执行」这一半
  ② **取数单一入口**（`common.query_rows`）：`MarketQuerySource` 为**鸭子类型**协议（只要求 `query(sql, params=())`，实测 `MarketDb` 满足 `isinstance`）；`ok` → 摊平 `data["rows"]` 为 `RowsResult(rows, as_of)`，非 `ok` → **原样返回同一信封**（判定权在 L0，不二次判定、不编造）；取数源返回非法类型或 `ok` 缺 `rows` → `OfficialPackLoadError`（fail-fast，不静默当空结果）
  ③ **装载入口**（`install.install_official_pack`）：校验取数源形态 → 校验工厂可调用 → 委托 `ensure_official_pack` 播种 → 按 `skill_id_for(base, version)` 为工厂表覆盖的 base 注册执行器；两 Bundle 模块**延迟导入**（`BUNDLE_MODULES`），`uncovered_bases()` 给出「尚无执行器的 base」这一装载完整性口径（不静默）
  ④ **证据引用有界化**（`common.snapshot_ref`）：L0 水位组合串实测满表约 350 字符、非 `snap_<20 位>` 形态（`EvidenceRef.ref` 上限 128）→ 取 `snap_ + sha256(组合串)[:20]`（确定性、可复核）；取数源无 `snapshot_id` 接口 → `None`（不编造）。**该越界是 L0 侧既有口径问题**，按册规登记 [L0 册 `E3`](遗留问题/L0-遗留问题.md)（归属「待立项」），本任务只做有界化
  ⑤ **断言口径落成用例**：GWT-5 以 AST 扫描 `official/*.py` 的 import，断言不含 `sqlite3` 与 `st_agent.l0.*`——「执行面只能经基件取数」成为机器可查的事实，而非注释里的承诺；GWT-1/2 以探针工厂表证装载与幂等，GWT-3/4 以直通执行器证信封逐字段透传
- **变更文件**：src/st_agent/l1/skills/official/{__init__.py,common.py,install.py,errors.py}（新）· tests/l1/test_official_pack_install.py（新，18 用例）· 项目管理/tasks/{T-L1-004-官方SkillPack公共认知主动服务Bundl.md（父任务改写 + 拆分记录）, T-L1-004.1（新）, T-L1-004.2（新）, T-L1-004.3（新）, T-INT-001（depends_on 改挂三叶子）} · 项目管理/{决策日志（[D-026](决策日志.md)）, 执行日志, 遗留问题/L0-遗留问题.md（补 E3）, 任务账本.md}
- **验证**：pytest **929 passed / 2 deselected**（本批 +18 用例 + `test_layering` 因新增 4 个源文件 +4 参数化）· **红-绿已验**：临时把「注册执行器」改为 `pass`、把「非 `ok` 原样透出」改为改写为 `ok`，命中 4 条用例精准失败（GWT-1 载入可执行 1 · GWT-2 幂等 1 · GWT-3/4 透传 2），恢复后全绿——证用例确实在测本次变更 · `verify_docs.py --strict` 检查 1–8 全 0 · **静态核对**：新增仅 `src/st_agent/l1/skills/official/` 与 `tests/l1/`，**未触** `src/st_agent/l0/`、`contracts/`、`runner/`、`skills/` 既有文件（`git status --short` 可查）· **接口面实测**：`MarketDb` 满足 `isinstance(MarketDb(_Store()), MarketQuerySource)`；`SkillRunner` / `SkillContext` / `SkillRegistry` 无签名变化（装载只调既有 `register_executor` 与 `ensure_official_pack`）
- **假设复核**：**A1 成立**（空参 `runner.run` 即得非「未注册执行器」结果，`SkillContext` 字段表零变化）· **A2 成立**（二次装载前后 `list_all()` 逐字段 JSON 相等，种子未被改造）· **A3 成立**（离线 Fake 跑通 GWT-1/3/4；`test_layering` 通过）· **A4 成立**（`unavailable` 的 `reason` / `last_updated_at` 与来源逐字段相等，执行器零二次判定）· **A5 成立**（二次装载返回空清单、执行器行为不变）· **A6 成立**（同名 / 换名 / 长度 ≤128 / `kind` 与 `snap_` 形态四向断言通过；L0 越界已登记 `E3`）
- **决策**：[D-026](决策日志.md)（取数单入口 / 装载工厂表可注入 / 证据引用有界化）
- **遗留**：① **L0 / L1 两册命中 1 条**——新登记 **L0 册 `E3`**（`snapshot_id` 越界，归属「待立项」待人定）；`.1` **不销** `A3`（解封条件＝父任务 `T-L1-004` `done`，由 `.3` 收口时销）② **`T-L1-004` 余 `.2` / `.3`**；M0 收口尚需这三个叶子、`T-L1-005`、`T-L1-006` 与 `T-INT-001` ③ 按人指示**暂不推送**——三个叶子全部完成后再集中全量测试与 PR 收口

## [T-L1-004.2] 2026-09-26T22:58+08:00 — 公共认知 Bundle 执行器 · done

- **目标**：为 03 §2.1 公共认知 Bundle 的 6 个 Skill 交付可执行实现，逐个履行表列职责与关键输出；取数经 `.1` 基件进入 L0 数据源缓存，输出过各自 `output_schema`，措辞去拟人化
- **对齐**：沿用 ④ 已确认口径（参数 + `ctx.upstream` 两条通道；种子的 `input_schema` / `output_schema` 与既有参数不动；假设编号见任务 A1–A5），并**新增 [D-027](决策日志.md)** 记录 4 个评分 / 评级口径（原以「可解释规则常量」写在 A3，落码时须显式化才不致与 `.3` 漂移）
- **输入**：`T-L1-004.1` 的 `official.common`（`query_rows` / `ok_envelope` / `empty_envelope` / `snapshot_ref` / `MarketQuerySource`）与 `official.install`（`EXECUTORS` 登记表的消费约定）· `T-L0-005` 的单库表与视图（`security` / `k_line_daily` / `v_st_universe` / `v_k_line_latest` / `stock_industry` / `financial_quarter` / `performance_express` / `profit_forecast`）· `T-L1-001.2` 的 `SkillContext.upstream`（上游依赖输出）
- **关键动作**：
  ① **`official/cognition.py` 新模块**——常量区（阈值 / 权重 / 条数上限集中一处，D-027 的口径）+ 6 个执行器工厂 + `EXECUTORS` 登记表（按 base 回填装载入口）
  ② **取数失败即执行结果**：私有 `_Stop` 异常承载非 `ok` 信封，`@_factory` 在边界还原——执行器体内是线性取数逻辑，不出现「到处 if 信封」的分支噪音；`unavailable` / `empty` 原样成为结果（不编造）
  ③ **六个 Skill 的语义**：`st-list-sync` 取最近两个交易日的 `is_st=1` 集合做双向 diff（含 `exchange` 参数过滤、无变更 / 无数据两条 `empty`）；`delisting-risk-scan` 扫 ST 池四类信号加权打分（`threshold` 为判 high 的分位），触发项携原因 + 人可读说明 + `dataset_snapshot_id` 证据引用；`unhat-eligibility-check` 逐标的五项摘帽条件（连续 `fiscal_years` 个财年盈利 / 最近报告期净利润 / 净资产 / 营收 1 亿 / 资产负债率），无数据的条件显式标 `satisfied: None` 而非当作不满足；`sector-heatmap` 行业快照 × 最新日线按 ST 占比分档评级；`sentiment-flow-analysis` 以标的**自身窗口均值**为基准判高换手（≥1.5×）与高热度（≥2.0×）；`fundamental-screening` 归一化等权评分 + `margin` 作入选分位
  ④ **数据面用真实 `MarketDb`**：用例经 `Store` + `data_cache` + 内置 `schema.sql` 建库并灌离线样本行（8 只标的 × 3 个交易日 + 财务 / 快报 / 预告 / 行业快照），故 SQL 与 schema 的一致性被真实校验，不靠匹配 SQL 文本的 Fake
- **变更文件**：src/st_agent/l1/skills/official/cognition.py（新）· tests/l1/test_official_cognition.py（新，10 用例）· 项目管理/{决策日志（[D-027](决策日志.md)）, 执行日志, 任务账本.md}· 项目管理/tasks/T-L1-004.2-*.md（状态 / verify）
- **验证**：pytest **940 passed / 2 deselected**（本批 +10 用例 + `test_layering` 因新增 1 个源文件 +1）· **红-绿已验**：临时把 diff 两向对调、把风险扫描的 `evidence_refs` 置空，命中 2 条用例精准失败（GWT-2 双向 diff · GWT-3 可解释与证据），恢复后全绿 · `verify_docs.py --strict` 检查 1–8 全 0 · **实测抽样**（临时脚本打印 6 条载荷，用后即删）：`st-list-sync` 得 added `[sh.600001, sh.600004]` / removed `[sz.000003, sz.000005]` 与样本预期逐条一致；`delisting-risk-scan` 得 `risk_level=high`、`scanned=3`、`triggers` 2 条（首条 4 类信号齐全、score 1.0）；`unhat` 15 条条件（3 标的 × 5 项）、`score` 落在 0–1；`sector-heatmap` 7 个行业、整体 `rating=high`；`sentiment` 2 条预警（高换手 2.45× · 高热度 2.25×）；`fundamental-screening` 1 只入选（score 0.4889）· **静态核对**：新增仅 `official/cognition.py` 与 `tests/l1/`，未触 `l0/`、`contracts/`、`runner/` 与 `.1` 既有文件（`official/` 的 GWT-5 AST 断言同时覆盖本新文件——无 `sqlite3` / `st_agent.l0` import）
- **假设复核**：**A1 成立**（6 条空参 `{}` 执行全部 `ok`，标的范围＝本地缓存全量、参数只收窄）· **A2 成立**（diff 以本地相邻两交易日 `is_st` 标记对照，双向用例 + 无变更 `empty` 用例通过）· **A3 成立**（阈值取可解释规则常量并集中定义；D-027 显式化后 `threshold` 的语义为「判 high 的分位」）· **A4 成立**（6 条输出整体 JSON 过 `NeutralityGuard.check_output`，无第一人称 / 情感 / 对话体命中）· **A5 成立**（`unavailable` 与 `empty` 两条路径逐 Skill 断言原样透出，`data is None`）
- **决策**：[D-027](决策日志.md)（公共认知 Bundle 的评分 / 评级口径：规则化加权、阈值即分位、归一化等权）
- **遗留**：① **L0 / L1 两册无新增命中**（`A3` 仍待 `.3` 收口销账；`E3` 为 `.1` 新登记，本批未触及）② **`T-L1-004` 余 `.3`**；M0 收口尚需 `.3`、`T-L1-005`、`T-L1-006` 与 `T-INT-001` ③ 按人指示**暂不推送**——待 `.3` 完成后再集中全量测试与 PR 收口

## [T-L1-004.3] 2026-09-26T23:35+08:00 — 主动服务 Bundle 执行器 · done

- **目标**：为 03 §2.2 主动服务 Bundle 的 6 个 Skill 交付可执行实现；联动按已声明 `dependencies` 生效（上游失败即 `dependency_failed`），并兑现 `strategy-design` 未来函数检测的特殊契约；本任务为父任务 `T-L1-004` 最后一个叶子，同批销 [L0 册 `A3`](遗留问题/L0-遗留问题.md)
- **对齐**：沿用 ④ 已确认口径（假设编号见任务 A1–A5）；**开工查册命中 1 条**——[L0 册 `A3`](遗留问题/L0-遗留问题.md)（归属 `T-L1-004`，解封条件＝父任务 `done`）→ 本批关闭。**④ 后修正两处**（实现期事实推翻了原设想，已就地改写任务文件并留痕）：
  ① 原 A3 写「`portfolio-stress-test` 上游组合未提供即 `validation_failed`」——实测该 Skill 描述体 `dependencies` 为空、`ctx.upstream` **恒空**，照此实现会**恒失败**（GWT-1 的 `ok` 不可达）→ 改为「默认取本地缓存最新交易日的在市标的等权组合 + 载荷显式标注 `portfolio_source`」
  ② GWT-1 原写「六条均为 `ok`」——`data-aggregate` 的语义就是「聚合上游」，无上游时 `ok` 包空卡片即属**编造** → 改为「五条 `ok` + `data-aggregate` 无上游 `empty` / 上游就绪 `ok`」
  两处修正连同「对象类输入缺口」的处置一并记 [D-028](决策日志.md)
- **输入**：`.1` 的 `official.common`（`query_rows` / `ok_envelope` / `empty_envelope` / `evidence_of`）与 `official.install` · `.2` 的 `cognition.EXECUTORS` 与上游载荷形态（`triggers[].risk_level` / `pool[].score`，按 [D-027](决策日志.md) 的口径消费、不自行重算）· `T-L1-001.2` 的 `SkillContext.upstream`（由 `dependencies` 驱动）· L0 表与视图（`k_line_daily` / `v_k_line_latest` / `security` / `profit_forecast` / `performance_express` / `financial_quarter`）
- **关键动作**：
  ① **`official/ambient.py` 新模块**——常量区 + 6 个执行器工厂 + `EXECUTORS` 登记表
  ② **`official/signal_check.py` 新模块**——未来函数检测的**公开纯函数** `check_future_function`：输入「字段清单 + 逐字段对齐口径」，按 `FIELD_AVAILABILITY`（行情字段随当日日线可得 / 财务与公司报告字段须到披露日）与时点序 `ALIGN_RANK` 比对，**财务字段对齐到报告期 `stat_date` 即判未来函数**；缺失 `align`、未知字段、未知对齐口径逐条报错；未提供信号定义时 `passed=None`（不假装通过）（[D-028](决策日志.md)）
  ③ **支撑件上提**（[`common.py`](../src/st_agent/l1/skills/official/common.py)）——`ExecutorStop` / `stopping_factory` / `fetch_rows` / `evidence_of` / `round_of` 成为两 Bundle 共用件，`cognition.py` 的同名私有副本删除（行为逐字节不变，`.2` 的 10 条用例复跑全绿）——`.3` 与 `.2` 不各造一套取数与信封构造
  ④ **六个 Skill 的语义**：`stock-watch` 三项可评估条件（异动 / 换手率 / 财报日）逐标的留触发理由，**不可评估的「关键词」以 `unevaluated_conditions` 显式标注**（无公告域表，登记 `C1`）；`data-aggregate` 读 `ctx.upstream` 生成聚合卡片（`format` 决定 `table`/`trend`/`brief` 形态）；`risk-alert` **以上游风险清单的标的为范围**，逐标的数「连续低于面值的交易日数」与「连续成交额枯竭的交易日数」，按 `lookahead_days` 过滤（剩余天数 ≤ 前瞻窗口才预警），随数据变化强度
  ⑤ **对象类输入显式化**：`opportunity-mine` 以 `preference: "none"` 标注未使用偏好；`strategy-design` 的 `report.signal_check` 在有上游信号定义时给结论、无生产者时给 `passed: None` + 原因——**执行器不冒充用户输入**
  ⑥ **上提封装**：6 个执行器全部经 `stopping_factory` 与 `fetch_rows`，执行器体内是线性逻辑；`empty` / `unavailable` 越界即整条执行结果
- **变更文件**：src/st_agent/l1/skills/official/{ambient.py,signal_check.py}（新）· src/st_agent/l1/skills/official/common.py（+支撑件）· src/st_agent/l1/skills/official/cognition.py（改复用支撑件）· src/st_agent/l1/skills/official/__init__.py（导出）· tests/l1/test_official_ambient.py（新，30 用例）· 项目管理/tasks/{T-L1-004.3-*.md, T-L1-004-*.md（父任务由 children 派生 `done`）} · 项目管理/{决策日志（[D-028](决策日志.md)）, 执行日志, 遗留问题/L1-遗留问题.md（新 C 段 C1/C2）, 遗留问题/L0-遗留问题.md（销 A3）, 任务账本.md}
- **验证**：pytest **972 passed / 2 deselected**（本批 +30 用例 + `test_layering` 因新增 2 个源文件 +2）· **红-绿已验**：临时把未来函数判据的比较方向反转、把 `risk-alert` 的前瞻过滤去掉、把 `data-aggregate` 的无上游分支改成 `ok` 包空卡片，命中 5 条用例精准失败（GWT-1 1 · GWT-4 1 · GWT-3 2 · GWT-5 1），恢复后全绿 · `verify_docs.py --strict` 检查 1–8 全 0（含**检查 8 遗留销账**：A3 已从 L0 册未闭区移除）· **实测抽样**（临时脚本打印 8 条载荷，用后即删）：`stock-watch` 3 条触发（异动 +8.00% · 财报日 · 换手率 12.00%）+ `unevaluated_conditions=["关键词"]`；`risk-alert` 3 条预警（600001 倒计时剩余 0 · 000003 流动性剩余 15 · 600002 剩余 17），`lookahead_days=5` 时收敛为 1 条；`opportunity-mine` 按 score 降序取前 N（`max_results=1` → 辛电子 0.7222）；`portfolio-stress-test` 三档情景 + `portfolio_source` 标注 + `beta=2.0`（样本全为 ST）；`strategy-design` 四步链路 + 回测 -13.50%；`data-aggregate` 的 `brief`/`trend`/`table` 形态各异；未来函数检测对 `roe_avg@stat_date` 给 `passed=false` 并点名 `roe_avg` · **静态核对**：新增仅 `official/` 与该测试文件，未触 `l0/`、`contracts/`、`runner/`
- **假设复核**：**A1 成立**（信号规格按本任务定义落地，检测件为公开纯函数，执行期无信号定义时 `passed=None` 而非假装通过）· **A2 成立**（6 个 Skill 运行期输入仍只取参数 + `ctx.upstream`，未新增通道）· **A3 已修正并成立**（组合改取本地等权默认口径 + 来源显式标注；原「上游组合」通道不成立，理由与返工面见任务文件 A3）· **A4 成立**（`strategy-design` 的上游由 runner 按声明依赖自动递归，未绕开编排；上游失败 → 下游 `dependency_failed` 用例通过）· **A5 成立**（A3 在本任务销账，`verify_docs --strict` 检查 8 为 0）
- **决策**：[D-028](决策日志.md)（对象类输入缺口显式化 + 未来函数检测规格）
- **遗留**：① **L0 册 `A3` 本批关闭**（从册内未闭区移除、册尾「已闭（备查）」留一行）② **L1 册新登记 `C1` / `C2`**——「关键词」条件无公告域数据面（归属待立项）、对象类输入无通道（归属 L2 记忆图谱落地后的接入面）；两条均**不占用本任务验收**（已在载荷显式标注，非静默）③ **`T-L1-004` 父任务转 `done`**（三个叶子全 `done`，状态由 render 派生）④ **M0 收口尚需 `T-L1-005`（定时调度）、`T-L1-006`（组合根）与 `T-INT-001`**；`archive` 需整个 M0 满员，本批不跑 ⑤ 待走集中 PR 收口（本批三个叶子的分支 + commit + squash 合入）

## [sc] 2026-09-26T23:55+08:00 — L1 册 `C1` 补全（点名承接机制）+ 登记未决 `Q-002`

- **背景**：[T-L1-004.3] 收工时把「`stock-watch` 关键词条件无数据面」登记为 [L1 册 `C1`](遗留问题/L1-遗留问题.md)，归属写作「公告域数据面（待立项）」。负责人 2026-09-26 复核后指出：解决它需要**专门构建一套信息收集与管理机制**，要求补全条目并找实现时机
- **关键动作**：
  ① **补全 `C1`**——把「一个公告域表」改写为**完整链路**（按 [数据库设计 §多源扩展规约](../docs/数据库设计-BaoStock数据层/00-设计总览.md)：每个新源＝`data_source` 注册 + 按域前缀建表组 + 复用 `sync_state` 同步任务 + 在 [05 同步策略与新鲜度契约](../docs/数据库设计-BaoStock数据层/05-同步策略与新鲜度契约.md) 登记新鲜度契约），覆盖面点名为**公告 / 龙虎榜 / 股东变化 / 舆情**四域；归属改为「『多源信息面采集与管理机制』立项承接（待立项）」
  ② **登记未决 [`Q-002`](阻塞与未决.md)**——核查发现**采集通道归属在文档间未协调**：[00 §5 步骤 1](../docs/技术架构-v2/00-架构总览.md) 写「L0 数据源网关抓取公告」；[story-08](../docs/PRD-v2-Agent/story-08-mcp-hub.md) 写「官方 MCP Server 自带交易所公告」，且「MCP 崩溃 → 降级到官方数据源」（把 L0 当兜底）；[01 §1](../docs/技术架构-v2/01-平台共享契约.md) 又把 `announcement_id` 产生方登记为 L0。三条并列而**主备关系未定义**，实现方只能猜。给出三候选（全走 L0 / 全走官方 MCP / 分工）与影响任务
- **实现时机结论**（供负责人择时，本批不立项）：**前置已就绪**——L0 数据源缓存（`T-L0-005`）、出网网关（`T-L0-004`）、真实抓取器范式（`T-L0-005.1`）均 `done`，无硬技术阻塞；**需求侧最早在 M2**——L4 情绪视角的职责明列「舆情热度、龙虎榜资金」（[story-04](../docs/PRD-v2-Agent/story-04-multi-lens.md)），M3 的端到端触发流（[00 §5](../docs/技术架构-v2/00-架构总览.md)）以**公告**为样例，而 M1（首次可对话）不需要它。**建议** `milestone: M2` / `priority: P0`，排在 L4 情绪视角开工**之前**；若希望更早兑现 L1 官方 Pack 的关键词条件与公告类证据引用，可前移到 **M1 尾巴**（与 `T-L0-007` 全量长跑同窗口）。规模上按域拆为**父任务 + 4 个叶子**（四域获取方式与频率各异），通道归属待 `Q-002` 定案
- **变更文件**：项目管理/{遗留问题/L1-遗留问题.md, 阻塞与未决.md, 执行日志.md}
- **验证**：`verify_docs.py --strict` 检查 1–8 全 0（136 文件 / 1850 链接 / **0 断链**）· **检查 8 遗留销账仍 0**（`C1` 归属为「待立项」短语、不含已 `done` 任务 id，不触发误报）
- **遗留**：① **`Q-002` open**——待信息面采集通道的归属决议；**未定案前不建议立项**（否则表结构落点与同步任务归属会靠猜）② 该机制**尚未立项**——本批只补条目与时机建议，不建任务文件（立项须先过该任务的 ③④ 与 `Q-002`）

## [sc] 2026-09-26T23:59+08:00 — 信息面采集通道定案 A（`Q-002` → [D-029](决策日志.md)）+ 借鉴仓库核实 + 登记衍生未决 `Q-003`–`Q-005`

- **背景**：[`Q-002`](阻塞与未决.md) 卡住 [L1 册 `C1`](遗留问题/L1-遗留问题.md) 的承接机制（「多源信息面采集与管理机制」，未立项）。负责人 2026-09-26 提出以开源项目 [`simonlin1212/a-stock-data`](https://github.com/simonlin1212/a-stock-data) 借鉴承接，并要求评估可行性 → 定通道 → 记录。本批完成全链路，**不实现**（不立项、不改 PRD 措辞、不写代码）
- **关键动作**：
  ① **核实外部仓库**（`gh api` 实测，非二手描述）：Apache-2.0 · 10,349 star / 1,888 fork · 创建 2026-05-11 · 最后推送 **2026-09-25** · 单文件 `SKILL.md`（425 KB / 7,509 行，15 层 · 87 端点 · 34 源）· 根目录**无 `pyproject.toml` / `setup.py`**（**非 Python 库，不可 `pip` 安装**）· `.github/` 仅 `FUNDING.yml`（**无 CI**）。定性＝「**信源字典而非机制**」——**取知识、不取依赖**
  ② **三层分解**（写入 [L1 册 `C1` 补充](遗留问题/L1-遗留问题.md)）：A **信源知识**（端点 / 参数 / 防封 `em_get()` / 主备映射）~35–40% 可**高保真移植** · B **数据建模**（四域表组 / 主键 / `sync_state` / 新鲜度契约）~45% 被返回形态**加速**仍须自研 · C **机制**（通道 / 开关 / 降级 / 消费面）~15% 仓库**无**。四域返回形态可直接反推表组（公告 `{title,type,date,url}` · 龙虎榜上榜记录 + 席位 TOP5 · 股东户数 `{date,holder_num,change_num,change_ratio,avg_shares}` · 舆情互动易问答）
  ③ **范围口径修正**（负责人指示）：「互补的四域」→「**互补的部分（暂定四域）**」（后续可能再引入其他域，**不写死**）；收录判据补为**两条并列**——**供给侧不重复 ∧ 需求侧有明确消费方**。仅前者会把「BaoStock 补集」（按仓库端点计约 **60 个**）误当路线图
  ④ **去重口径**：以 [06-API映射契约](../docs/数据库设计-BaoStock数据层/06-API映射契约.md) + `indicator_dictionary` 为单一事实源逐列比对，**核出六处重叠**（行情 `k_line_*` / 分红送转 `dividend` / 财务 `financial_quarter` / 行业 `stock_industry`（部分，仓库给时点历史）/ 宏观 `macro_*` / 指数成分与日历 / 业绩预告 `profit_forecast`）——照抄即须按[多源扩展规约 §3](../docs/数据库设计-BaoStock数据层/00-设计总览.md)另建独立源表 + `v_` 聚合视图
  ⑤ **通道定案 A**（负责人裁定）＝**全部走 L0 网关直抓**。四条理由：**①** 00 §5 步骤 1 / 01 §1 `announcement_id` 产生方 / 02 §5「抓取请求全部经 §6 网关」**三条都已在 A 上**，只有 story-08 在 B 上 → A 的契约改动面最小（01 / 00 / 02 **均不动**）；**②** [02 §6](../docs/技术架构-v2/02-L0-本地优先基座.md) 明载「一切对外请求须经网关」并**登记了「本地 stdio MCP 子进程自身出网不在审计范围」的已知边界**（[D-023](决策日志.md) 已闭的 L1 册 `A1`），而 story-08 要求「默认只允许本地 stdio Server」→ B / C 会把官方采集放进**审计盲区**，冲击 [00 §5](../docs/技术架构-v2/00-架构总览.md) 的公告证据链；**③**「互补」蕴含**同一读取面**（L4 情绪视角需同时读行情与舆情），C 的「按数据形态分工」用错了切分维度（应按**谁出网**划分；公告与舆情同样需要缓存 / 快照 ID / 源级开关 / 新鲜度契约 / 离线可查五样，在 L0 之外无落点）；**④** 外部仓库是**现成直抓实现**。**补充口径**：官方 MCP Server 若保留，降为「**L0 缓存的只读暴露面**」（不出网 / 不采集 / 不写库），交付**可延后**
  ⑥ **文本类文件存储需求登记**（负责人提出：便于后续 RAG 与 AI 自主查阅）——核实**底座已就绪、存储层零改动**：[02 §2.1](../docs/技术架构-v2/02-L0-本地优先基座.md) 的 `data_cache` 分区内容**已明列**「公告 / 行情 / 龙虎榜 / 股东 / 舆情」（不需新分区）；[`Store`](../src/st_agent/l0/storage/store.py) 已是「分区 + 分区内相对路径 → bytes」的**通用 KV**（嵌套路径 / 逐文件 `digest`+`size` 清单 / 落盘加密 / 分区级校验 / 独立导出清空）；[T-L0-006](tasks/T-L0-006-备份恢复完全清空数据留存策略.md) 备份已支持 `data_cache` **可选**纳入
  ⑦ **登记衍生未决**：`Q-003`（各域内官方源与抓取源的主备口径——决定 `data_source` 每域登记几条）· `Q-004`（北交所 `920xxx`/`8xxxx` 等 `security` 未收录代码的主键映射——`security` 现仅登记 `sh./sz.`，仓库专门处理过此类代码）· `Q-005`（文本类信息的存储形态 · 查阅面归属（L1 沙箱拦截越界，Skill 不能直接 `open()`）· RAG 索引作为**派生数据**的落点 · 档案型文本是否升为默认备份）
- **变更文件**：项目管理/{阻塞与未决.md（`Q-002` 转 decided + 新增 `Q-003`–`Q-005`）, 决策日志.md（[D-029](决策日志.md)）, 执行日志.md, 遗留问题/L1-遗留问题.md（`C1` 行通道口径 + `C1` 补充：三层分解 / 互补范围口径 / 六处重叠 / 文本类存储）}
- **验证**：`verify_docs.py --strict` 检查 1–8 全 0（136 文件 / **0 断链**）· **检查 8 遗留销账仍 0**（`C1` 行的「归属 / 解封条件」两列仅含「待立项」短语与 `Q-*` / `D-029`，**非任务 id**（`ID_RE` 仅匹配 `T-<SC|L\d|ECO|INT>-\d{3}`），不触发误报）· **静态核对**：`git status --short` 仅 `项目管理/` 下 4 文件，**未触** `docs/` `src/` `tests/`——本批只定决策与登记未决，**不改 PRD 措辞、不写代码**
- **决策**：[D-029](决策日志.md)（信息面采集通道定为 L0 网关直抓；官方 MCP Server 降为只读暴露面、可延后）
- **遗留**：① **`Q-003`–`Q-005` open**——三项须在机制立项**前**定案（否则表结构落点、主键映射与文本存储形态会靠猜）② **机制仍未立项**——未建任务文件；立项须先过该任务的 ③④（[工作流](工作流.md)）③ **`story-08` 措辞待修订**——归属该机制立项的[铁律 8](工程宪法.md)文档链（其余 00 / 01 / 02 / 03 **无需改**），本批**不碰 PRD** ④ 待走 PR 收口
