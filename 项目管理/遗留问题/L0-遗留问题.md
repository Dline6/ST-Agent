---
title: L0 遗留问题
type: registry-layer
consumed_by: [编码 Agent, 项目负责人]
snapshot: 2026-09-26
---

# L0 · 本地优先基座 — 遗留问题

> **快照 2026-09-26**（`T-L1-002.3` 收工补 `E1` 段）。盘点口径：L0 全部 8 个任务（`T-L0-001` ~ `T-L0-007`，含 `005.1`）——7 个 `done`，`T-L0-007` 已拆为 `.1`（`done`）/ `.2`（`todo`）。来源＝各任务 `done` 时的执行日志「遗留」行 + 未决 `Q-001` / 决策 `D-005` + 任务文件悬空假设 + **下游任务实现期暴露的 L0 缺陷（E 段）**。
> 本册只收**未解决**项，逐条给归属与解封条件；关闭留痕见 [执行日志](../执行日志.md) 与 [阻塞与未决](../阻塞与未决.md)。规则见 [本册 README](README.md)。

## A · 待下游任务兑现（L0 侧交付面已完整，只等接入）

| # | 遗留内容 | 来源 | 归属 | 解封条件 |
|---|---|---|---|---|
| A1b | 官方 Skill 索引浏览的审计调用点未接入（`EgressGateway` 已就绪；`kind=index_browse`） | [T-L0-004] 遗留①（2026-09-26 由原 A1 拆出） | `T-ECO-002` | T-ECO-002 `done`（官方 Skill 索引浏览接入网关） |
| A3 | 官方 Skill Pack 的行情/财务查询面经 `MarketDb.query` 进入 | [T-L0-005] 遗留② | `T-L1-004` | T-L1-004 `done` |
| A4 | `export_all_safe`（`src/st_agent/l0/secrets/vault.py:217`）无调用方——备份路径改由「secrets 永不进归档」满足禁明文，本方法留给分享物导出 | [T-L0-002] 遗留① · [T-L0-006] A1 | `T-ECO-001` | T-ECO-001 `done`；若届时分享物仍不携带凭据安全视图，需再评估该方法存废 |
| A5 | 离线补发执行（L0 仅提供 `pending_reconnect` 查询口） | [T-L0-004] 遗留② | L5 投递编排与升级链（候选 `T-L5-002`） | L5 升级链 `done` |

> **A1 拆分留痕（2026-09-26，`T-L1-002.1` 开工查册）**：原 A1 一条含两种 `NetworkRequestKind`（`remote_mcp` + `index_browse`）。`T-L1-002` 拆为三个叶子后复核：远程 MCP 归 `T-L1-002`，而「官方 Skill 索引浏览」的载体是 [09-生态 §4](../../docs/技术架构-v2/09-生态与分享.md) 的官方 Skill 索引，属 `T-ECO-002` 面，`T-L1-002` 力不能及。按[册规](README.md) 2「就地修正归属」拆为 A1 / A1b 两行。

## B · 阻塞于架构缺口：运行时组合根（未决 `Q-001` → 决议 `D-005`）

| # | 遗留内容 | 来源 | 归属 | 解封条件 |
|---|---|---|---|---|
| B1 | `EndpointRegistry` / `CredentialVault` / `LlmClient` / `OutputRegistry` **无运行时装配**——`src/` 无 app / 组合根层，L0 与 L1 子系统建好了却没人把它们接起来并交给上层 | 执行日志 [T-L1-001.3] 遗留③ · [T-L1-001.4] 遗留① | `T-L1-006` | T-L1-006 `done`（组合根 + 复核 `provider_hosts` 是否迁回 L0） |
| B2 | 登录态会话密钥缓存未落地——`Store.open` 每次重跑 PBKDF2 600k（本机约 0.6s/次：主密钥 + secrets 各一次），`T-L0-001` 假设 A6 的「会话密钥缓存复用同一口径」验证至今悬空 | T-L0-001 假设 A6 与遗留② | 与 B1 同源（会话归属＝组合根职责） | T-L1-006 `done`；若届时仍不做，须显式记 `wontfix` 而非继续悬空 |

## C · 长跑运营（人择时执行）

| # | 遗留内容 | 来源 | 归属 | 解封条件 |
|---|---|---|---|---|
| C1 | 真实**全市场**首次全量同步 + 真实数据基线未跑。小样本真实冒烟**已通过**（2026-09-26：`setup`/日历/主档/全量快照/3 只日线全历史，网关审计 6 条，盘上明文 0 命中；见 [执行日志](../执行日志.md) `[T-L0-007.2]`） | T-L0-005.1 遗留①② · T-L0-005 GWT-1 | `T-L0-007.2`（`gate: skip`，现 `doing`） | 人择时执行全市场长跑；完成后父任务 `T-L0-007` 与 M1 方可 `archive` |
| C2 | **分批 + 退避能力缺失**——侦察实测全市场全量 ≈ **85 小时**（非任务备注所写「数小时」）；同批发现三处结构障碍：码分片会被**全局水位跳历史**、任务内中断**水位不推进**（重跑全码重拉）、[`live.py`](../../src/st_agent/l0/market/live.py) 抛错时**丢失 `error_code`** 致限流无法识别 | [执行日志](../执行日志.md) `[T-L0-007.2]` 遗留① | 建议新立 `T-L0-008`（**待立项**，待人定） | 立项并实现后，C1 的全量长跑方可安全执行 |

## D · 流程 / 待人决（非 L0 代码专属，列此备查）

| # | 遗留内容 | 来源 | 归属 | 解封条件 |
|---|---|---|---|---|
| D1 | 分支保护未开（`main` protection 404）——副作用须先想清：勾「Require status checks」+「Include administrators」会拦本仓库「收口日志直推 main」惯例，需配 bypass 名单或取消该勾选 | 执行日志 [sc] 2026-09-26T12:16 遗留② | 人 | 人定策略 |
| D2 | PAT 仍为 classic `repo` + `workflow`，建议换 fine-grained 最小权限 | 执行日志 [sc] 2026-09-25T17:00 遗留① 等 | 人 | 人换 token |
| D3 | 未加 `.gitattributes`——行尾一致性依赖各机 `core.autocrlf` | 执行日志 [sc] 2026-09-26T11:27 遗留① | 人 | 人决定是否 `* text=auto eol=lf` + 一次 renormalize |

## E · 实现期新发现（下游任务暴露的 L0 缺陷）

| # | 遗留内容 | 来源 | 归属 | 解封条件 |
|---|---|---|---|---|
| E1 | **`Store` 写并发不安全**——分区清单在内存缓存（`src/st_agent/l0/storage/store.py` 的 `self._manifests`），`put` / `delete` 是「读缓存 → 重建清单 → 写回」三步且全程无锁；两个线程同时写**同一分区**会丢条目（后写者的快照不含前写者的条目）。实证：`T-L1-002.3` 的 GWT-4 全链路用例（一个线程在途调用、另一线程禁用）中，后写的审计记录覆盖了同分区的生命周期留痕之外的条目，`gateway.query` 查不到该次出网；该任务为此把用例改为「并发窗口内单一写者」并把约定写进用例注释 | [T-L1-002.3] 实现期发现 | 人 | 人定策略（是否立项做 `Store` 写串行化：分区级锁 / 写前重读清单 / 单写者队列） |

## 已闭（备查）

- **`is_stale` 无消费方**（`src/st_agent/contracts/time_events.py:66`）→ **已由 T-L0-005 的设计关闭**（2026-09-26 定案）：L0 走 `sync_state` 自检 SQL（`MarketDb.freshness` + `BaoStockSync.freshness_verdict`），该通用超龄判定保留给「无 `sync_state` 可查的内存路径」，L1 复用层的 `age_based_verdict` 为其兜底对位实现。定案留痕见 [执行日志](../执行日志.md) `[sc] 2026-09-26T12:44`。
- **T-L0-001 遗留①**（T-L0-002 / 004 / 005 / 006 均依赖其分区容器）→ 四任务均已 `done`，容器依赖全部兑现。
- **未决 `Q-001`**（运行时组合根未定义）→ `decided (D-005)`；残余执行工作见上 B1。
- **A2**（Skill 调用 LLM 时复用 `LlmClient.invoke` + `pick` 口径）→ **已由 `T-L1-001` 关闭**（2026-09-26 收工）：`.5` 落地 LLM 出口形态（受限 `guard_llm` 经 `LlmClient.invoke`，见 [D-005](../决策日志.md)），`.6` 收工使父任务 `T-L1-001` 全叶子 `done`，解封条件兑现。关闭留痕见 [执行日志](../执行日志.md) `[T-L1-001.6]`。
- **A1**（远程 MCP 的审计调用点未接入，`kind=remote_mcp`）→ **已由 `T-L1-002` 关闭**（2026-09-26 收工）：`.1` 建好经网关的远程登记调用点（`HttpSseTransport` 以 `initiator=<server_id>` 调 `gateway.execute("remote_mcp", …)`），`.3` 接成完整闭环（`gateway.query(kind="remote_mcp", initiator=…)` 可查、`remove_server` 后历史审计保留），`.3` 为最后一个叶子使父任务全 `done`、解封条件兑现。关闭留痕见 [执行日志](../执行日志.md) `[T-L1-002.3]`。
